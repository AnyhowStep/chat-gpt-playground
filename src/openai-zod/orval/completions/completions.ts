/**
 * Generated by orval v7.7.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import {
  z as zod
} from 'zod';


/**
 * Creates a completion for the provided prompt and parameters.
 * @summary Create completion
 */
export const createCompletionBodyPromptDefaultOne = "";export const createCompletionBodyPromptItemDefault = "";export const createCompletionBodyBestOfDefault = 1;
export const createCompletionBodyBestOfMin = 0;

export const createCompletionBodyBestOfMax = 20;
export const createCompletionBodyEchoDefault = false;export const createCompletionBodyFrequencyPenaltyDefault = 0;
export const createCompletionBodyFrequencyPenaltyMin = -2;

export const createCompletionBodyFrequencyPenaltyMax = 2;
export const createCompletionBodyLogitBiasDefault = null;export const createCompletionBodyLogprobsDefault = null;
export const createCompletionBodyLogprobsMin = 0;

export const createCompletionBodyLogprobsMax = 5;
export const createCompletionBodyMaxTokensDefault = 16;
export const createCompletionBodyMaxTokensMin = 0;
export const createCompletionBodyNDefault = 1;
export const createCompletionBodyNMax = 128;
export const createCompletionBodyPresencePenaltyDefault = 0;
export const createCompletionBodyPresencePenaltyMin = -2;

export const createCompletionBodyPresencePenaltyMax = 2;
export const createCompletionBodyStopDefaultOne = "<|endoftext|>";export const createCompletionBodyStopMaxTwo = 4;
export const createCompletionBodyStreamDefault = false;export const createCompletionBodySuffixDefault = null;export const createCompletionBodyTemperatureDefault = 1;
export const createCompletionBodyTemperatureMin = 0;

export const createCompletionBodyTemperatureMax = 2;
export const createCompletionBodyTopPDefault = 1;
export const createCompletionBodyTopPMin = 0;

export const createCompletionBodyTopPMax = 1;


export const createCompletionBody = zod.object({
  "model": zod.string().or(zod.enum(['gpt-3.5-turbo-instruct', 'davinci-002', 'babbage-002'])).describe('ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.\n'),
  "prompt": zod.string().optional().or(zod.array(zod.string().optional())).or(zod.array(zod.number()).min(1)).or(zod.array(zod.array(zod.number()).min(1)).min(1)).nullable().describe('The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.\n\nNote that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document.\n'),
  "best_of": zod.number().min(createCompletionBodyBestOfMin).max(createCompletionBodyBestOfMax).optional().describe('Generates `best_of` completions server-side and returns the \"best\" (the one with the highest log probability per token). Results cannot be streamed.\n\nWhen used with `n`, `best_of` controls the number of candidate completions and `n` specifies how many to return – `best_of` must be greater than `n`.\n\n**Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.\n'),
  "echo": zod.boolean().nullish().describe('Echo back the prompt in addition to the completion\n'),
  "frequency_penalty": zod.number().min(createCompletionBodyFrequencyPenaltyMin).max(createCompletionBodyFrequencyPenaltyMax).nullish().describe('Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so far, decreasing the model\'s likelihood to repeat the same line verbatim.\n\n[See more information about frequency and presence penalties.](https://platform.openai.com/docs/guides/text-generation)\n'),
  "logit_bias": zod.record(zod.string(), zod.number()).nullish().describe('Modify the likelihood of specified tokens appearing in the completion.\n\nAccepts a JSON object that maps tokens (specified by their token ID in the GPT tokenizer) to an associated bias value from -100 to 100. You can use this [tokenizer tool](/tokenizer?view=bpe) to convert text to token IDs. Mathematically, the bias is added to the logits generated by the model prior to sampling. The exact effect will vary per model, but values between -1 and 1 should decrease or increase likelihood of selection; values like -100 or 100 should result in a ban or exclusive selection of the relevant token.\n\nAs an example, you can pass `{\"50256\": -100}` to prevent the <|endoftext|> token from being generated.\n'),
  "logprobs": zod.number().min(createCompletionBodyLogprobsMin).max(createCompletionBodyLogprobsMax).nullish().describe('Include the log probabilities on the `logprobs` most likely output tokens, as well the chosen tokens. For example, if `logprobs` is 5, the API will return a list of the 5 most likely tokens. The API will always return the `logprob` of the sampled token, so there may be up to `logprobs+1` elements in the response.\n\nThe maximum value for `logprobs` is 5.\n'),
  "max_tokens": zod.number().min(createCompletionBodyMaxTokensMin).optional().describe('The maximum number of [tokens](/tokenizer) that can be generated in the completion.\n\nThe token count of your prompt plus `max_tokens` cannot exceed the model\'s context length. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens.\n'),
  "n": zod.number().min(1).max(createCompletionBodyNMax).optional().describe('How many completions to generate for each prompt.\n\n**Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.\n'),
  "presence_penalty": zod.number().min(createCompletionBodyPresencePenaltyMin).max(createCompletionBodyPresencePenaltyMax).nullish().describe('Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far, increasing the model\'s likelihood to talk about new topics.\n\n[See more information about frequency and presence penalties.](https://platform.openai.com/docs/guides/text-generation)\n'),
  "seed": zod.number().nullish().describe('If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.\n\nDeterminism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.\n'),
  "stop": zod.string().optional().or(zod.array(zod.string()).min(1).max(createCompletionBodyStopMaxTwo)).nullish().describe('Not supported with latest reasoning models `o3` and `o4-mini`.\n\nUp to 4 sequences where the API will stop generating further tokens. The\nreturned text will not contain the stop sequence.\n'),
  "stream": zod.boolean().nullish().describe('Whether to stream back partial progress. If set, tokens will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available, with the stream terminated by a `data: [DONE]` message. [Example Python code](https://cookbook.openai.com/examples/how_to_stream_completions).\n'),
  "stream_options": zod.object({
  "include_usage": zod.boolean().optional().describe('If set, an additional chunk will be streamed before the `data: [DONE]`\nmessage. The `usage` field on this chunk shows the token usage statistics\nfor the entire request, and the `choices` field will always be an empty\narray.\n\nAll other chunks will also include a `usage` field, but with a null\nvalue. **NOTE:** If the stream is interrupted, you may not receive the\nfinal usage chunk which contains the total token usage for the request.\n'),
  "include_obfuscation": zod.boolean().optional().describe('When true, stream obfuscation will be enabled. Stream obfuscation adds\nrandom characters to an `obfuscation` field on streaming delta events to\nnormalize payload sizes as a mitigation to certain side-channel attacks.\nThese obfuscation fields are included by default, but add a small amount\nof overhead to the data stream. You can set `include_obfuscation` to\nfalse to optimize for bandwidth if you trust the network links between\nyour application and the OpenAI API.\n')
}).describe('Options for streaming response. Only set this when you set `stream: true`.\n').or(zod.null()).optional(),
  "suffix": zod.string().nullish().describe('The suffix that comes after a completion of inserted text.\n\nThis parameter is only supported for `gpt-3.5-turbo-instruct`.\n'),
  "temperature": zod.number().min(createCompletionBodyTemperatureMin).max(createCompletionBodyTemperatureMax).optional().describe('What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.\n\nWe generally recommend altering this or `top_p` but not both.\n'),
  "top_p": zod.number().min(createCompletionBodyTopPMin).max(createCompletionBodyTopPMax).optional().describe('An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.\n\nWe generally recommend altering this or `temperature` but not both.\n'),
  "user": zod.string().optional().describe('A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).\n')
})

export const createCompletionResponseUsageCompletionTokensDefault = 0;export const createCompletionResponseUsagePromptTokensDefault = 0;export const createCompletionResponseUsageTotalTokensDefault = 0;export const createCompletionResponseUsageCompletionTokensDetailsAcceptedPredictionTokensDefault = 0;export const createCompletionResponseUsageCompletionTokensDetailsAudioTokensDefault = 0;export const createCompletionResponseUsageCompletionTokensDetailsReasoningTokensDefault = 0;export const createCompletionResponseUsageCompletionTokensDetailsRejectedPredictionTokensDefault = 0;export const createCompletionResponseUsagePromptTokensDetailsAudioTokensDefault = 0;export const createCompletionResponseUsagePromptTokensDetailsCachedTokensDefault = 0;

export const createCompletionResponse = zod.object({
  "id": zod.string().describe('A unique identifier for the completion.'),
  "choices": zod.array(zod.object({
  "finish_reason": zod.enum(['stop', 'length', 'content_filter']).describe('The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,\n`length` if the maximum number of tokens specified in the request was reached,\nor `content_filter` if content was omitted due to a flag from our content filters.\n'),
  "index": zod.number(),
  "logprobs": zod.object({
  "text_offset": zod.array(zod.number()).optional(),
  "token_logprobs": zod.array(zod.number()).optional(),
  "tokens": zod.array(zod.string()).optional(),
  "top_logprobs": zod.array(zod.record(zod.string(), zod.number())).optional()
}).or(zod.null()),
  "text": zod.string()
})).describe('The list of completion choices the model generated for the input prompt.'),
  "created": zod.number().describe('The Unix timestamp (in seconds) of when the completion was created.'),
  "model": zod.string().describe('The model used for completion.'),
  "system_fingerprint": zod.string().optional().describe('This fingerprint represents the backend configuration that the model runs with.\n\nCan be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.\n'),
  "object": zod.enum(['text_completion']).describe('The object type, which is always \"text_completion\"'),
  "usage": zod.object({
  "completion_tokens": zod.number().optional().describe('Number of tokens in the generated completion.'),
  "prompt_tokens": zod.number().optional().describe('Number of tokens in the prompt.'),
  "total_tokens": zod.number().optional().describe('Total number of tokens used in the request (prompt + completion).'),
  "completion_tokens_details": zod.object({
  "accepted_prediction_tokens": zod.number().optional().describe('When using Predicted Outputs, the number of tokens in the\nprediction that appeared in the completion.\n'),
  "audio_tokens": zod.number().optional().describe('Audio input tokens generated by the model.'),
  "reasoning_tokens": zod.number().optional().describe('Tokens generated by the model for reasoning.'),
  "rejected_prediction_tokens": zod.number().optional().describe('When using Predicted Outputs, the number of tokens in the\nprediction that did not appear in the completion. However, like\nreasoning tokens, these tokens are still counted in the total\ncompletion tokens for purposes of billing, output, and context window\nlimits.\n')
}).optional().describe('Breakdown of tokens used in a completion.'),
  "prompt_tokens_details": zod.object({
  "audio_tokens": zod.number().optional().describe('Audio input tokens present in the prompt.'),
  "cached_tokens": zod.number().optional().describe('Cached tokens present in the prompt.')
}).optional().describe('Breakdown of tokens used in the prompt.')
}).optional().describe('Usage statistics for the completion request.')
}).describe('Represents a completion response from the API. Note: both the streamed and non-streamed response objects share the same shape (unlike the chat endpoint).\n')

