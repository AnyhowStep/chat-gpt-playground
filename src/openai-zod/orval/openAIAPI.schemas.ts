/**
 * Generated by orval v7.7.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
export interface AddUploadPartRequest {
  /** The chunk of bytes for this Part.
 */
  data: Blob;
}

export type AdminApiKeyLastUsedAt = number | null;

export type AdminApiKeyOwner = {
  /** Always `user` */
  type?: string;
  /** The object type, which is always organization.user */
  object?: string;
  /** The identifier, which can be referenced in API endpoints */
  id?: string;
  /** The name of the user */
  name?: string;
  /** The Unix timestamp (in seconds) of when the user was created */
  created_at?: number;
  /** Always `owner` */
  role?: string;
};

/**
 * Represents an individual Admin API key in an org.
 */
export interface AdminApiKey {
  /** The object type, which is always `organization.admin_api_key` */
  object: string;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the API key */
  name: string;
  /** The redacted value of the API key */
  redacted_value: string;
  /** The value of the API key. Only shown on create. */
  value?: string;
  /** The Unix timestamp (in seconds) of when the API key was created */
  created_at: number;
  last_used_at: AdminApiKeyLastUsedAt;
  owner: AdminApiKeyOwner;
}

export interface ApiKeyList {
  object?: string;
  data?: AdminApiKey[];
  has_more?: boolean;
  first_id?: string;
  last_id?: string;
}

/**
 * The object type, which is always `assistant`.
 */
export type AssistantObjectObject = typeof AssistantObjectObject[keyof typeof AssistantObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantObjectObject = {
  assistant: 'assistant',
} as const;

export type AssistantObjectName = string | null;

export type AssistantObjectDescription = string | null;

export type AssistantObjectInstructions = string | null;

export type AssistantObjectToolResourcesAnyOfCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter`` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type AssistantObjectToolResourcesAnyOfFileSearch = {
  /**
   * The ID of the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type AssistantObjectToolResourcesAnyOf = {
  code_interpreter?: AssistantObjectToolResourcesAnyOfCodeInterpreter;
  file_search?: AssistantObjectToolResourcesAnyOfFileSearch;
};

export type AssistantObjectToolResources = AssistantObjectToolResourcesAnyOf | null;

export type AssistantObjectTemperature = number | null;

export type AssistantObjectTopP = number | null;

export type AssistantObjectResponseFormat = AssistantsApiResponseFormatOption | null;

/**
 * Represents an `assistant` that can call the model and use tools.
 */
export interface AssistantObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `assistant`. */
  object: AssistantObjectObject;
  /** The Unix timestamp (in seconds) for when the assistant was created. */
  created_at: number;
  name: AssistantObjectName;
  description: AssistantObjectDescription;
  /** ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
 */
  model: string;
  instructions: AssistantObjectInstructions;
  /**
   * A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`.

   * @maxItems 128
   */
  tools: AssistantTool[];
  tool_resources?: AssistantObjectToolResources;
  metadata: Metadata;
  temperature?: AssistantObjectTemperature;
  top_p?: AssistantObjectTopP;
  response_format?: AssistantObjectResponseFormat;
}

/**
 * Represents an event emitted when streaming a Run.

Each event in a server-sent events stream has an `event` and `data` property:

```
event: thread.created
data: {"id": "thread_123", "object": "thread", ...}
```

We emit events whenever a new object is created, transitions to a new state, or is being
streamed in parts (deltas). For example, we emit `thread.run.created` when a new run
is created, `thread.run.completed` when a run completes, and so on. When an Assistant chooses
to create a message during a run, we emit a `thread.message.created event`, a
`thread.message.in_progress` event, many `thread.message.delta` events, and finally a
`thread.message.completed` event.

We may add additional events over time, so we recommend handling unknown events gracefully
in your code. See the [Assistants API quickstart](https://platform.openai.com/docs/assistants/overview) to learn how to
integrate the Assistants API with streaming.

 */
export type AssistantStreamEvent = ThreadStreamEvent | RunStreamEvent | RunStepStreamEvent | MessageStreamEvent | ErrorEvent;

export type AssistantSupportedModels = typeof AssistantSupportedModels[keyof typeof AssistantSupportedModels];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantSupportedModels = {
  'gpt-5': 'gpt-5',
  'gpt-5-mini': 'gpt-5-mini',
  'gpt-5-nano': 'gpt-5-nano',
  'gpt-5-2025-08-07': 'gpt-5-2025-08-07',
  'gpt-5-mini-2025-08-07': 'gpt-5-mini-2025-08-07',
  'gpt-5-nano-2025-08-07': 'gpt-5-nano-2025-08-07',
  'gpt-41': 'gpt-4.1',
  'gpt-41-mini': 'gpt-4.1-mini',
  'gpt-41-nano': 'gpt-4.1-nano',
  'gpt-41-2025-04-14': 'gpt-4.1-2025-04-14',
  'gpt-41-mini-2025-04-14': 'gpt-4.1-mini-2025-04-14',
  'gpt-41-nano-2025-04-14': 'gpt-4.1-nano-2025-04-14',
  'o3-mini': 'o3-mini',
  'o3-mini-2025-01-31': 'o3-mini-2025-01-31',
  o1: 'o1',
  'o1-2024-12-17': 'o1-2024-12-17',
  'gpt-4o': 'gpt-4o',
  'gpt-4o-2024-11-20': 'gpt-4o-2024-11-20',
  'gpt-4o-2024-08-06': 'gpt-4o-2024-08-06',
  'gpt-4o-2024-05-13': 'gpt-4o-2024-05-13',
  'gpt-4o-mini': 'gpt-4o-mini',
  'gpt-4o-mini-2024-07-18': 'gpt-4o-mini-2024-07-18',
  'gpt-45-preview': 'gpt-4.5-preview',
  'gpt-45-preview-2025-02-27': 'gpt-4.5-preview-2025-02-27',
  'gpt-4-turbo': 'gpt-4-turbo',
  'gpt-4-turbo-2024-04-09': 'gpt-4-turbo-2024-04-09',
  'gpt-4-0125-preview': 'gpt-4-0125-preview',
  'gpt-4-turbo-preview': 'gpt-4-turbo-preview',
  'gpt-4-1106-preview': 'gpt-4-1106-preview',
  'gpt-4-vision-preview': 'gpt-4-vision-preview',
  'gpt-4': 'gpt-4',
  'gpt-4-0314': 'gpt-4-0314',
  'gpt-4-0613': 'gpt-4-0613',
  'gpt-4-32k': 'gpt-4-32k',
  'gpt-4-32k-0314': 'gpt-4-32k-0314',
  'gpt-4-32k-0613': 'gpt-4-32k-0613',
  'gpt-35-turbo': 'gpt-3.5-turbo',
  'gpt-35-turbo-16k': 'gpt-3.5-turbo-16k',
  'gpt-35-turbo-0613': 'gpt-3.5-turbo-0613',
  'gpt-35-turbo-1106': 'gpt-3.5-turbo-1106',
  'gpt-35-turbo-0125': 'gpt-3.5-turbo-0125',
  'gpt-35-turbo-16k-0613': 'gpt-3.5-turbo-16k-0613',
} as const;

/**
 * The type of tool being defined: `code_interpreter`
 */
export type AssistantToolsCodeType = typeof AssistantToolsCodeType[keyof typeof AssistantToolsCodeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantToolsCodeType = {
  code_interpreter: 'code_interpreter',
} as const;

export interface AssistantToolsCode {
  /** The type of tool being defined: `code_interpreter` */
  type: AssistantToolsCodeType;
}

/**
 * The type of tool being defined: `file_search`
 */
export type AssistantToolsFileSearchType = typeof AssistantToolsFileSearchType[keyof typeof AssistantToolsFileSearchType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantToolsFileSearchType = {
  file_search: 'file_search',
} as const;

/**
 * Overrides for the file search tool.
 */
export type AssistantToolsFileSearchFileSearch = {
  /**
   * The maximum number of results the file search tool should output. The default is 20 for `gpt-4*` models and 5 for `gpt-3.5-turbo`. This number should be between 1 and 50 inclusive.

Note that the file search tool may output fewer than `max_num_results` results. See the [file search tool documentation](https://platform.openai.com/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.

   * @minimum 1
   * @maximum 50
   */
  max_num_results?: number;
  ranking_options?: FileSearchRankingOptions;
};

export interface AssistantToolsFileSearch {
  /** The type of tool being defined: `file_search` */
  type: AssistantToolsFileSearchType;
  /** Overrides for the file search tool. */
  file_search?: AssistantToolsFileSearchFileSearch;
}

/**
 * The type of tool being defined: `file_search`
 */
export type AssistantToolsFileSearchTypeOnlyType = typeof AssistantToolsFileSearchTypeOnlyType[keyof typeof AssistantToolsFileSearchTypeOnlyType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantToolsFileSearchTypeOnlyType = {
  file_search: 'file_search',
} as const;

export interface AssistantToolsFileSearchTypeOnly {
  /** The type of tool being defined: `file_search` */
  type: AssistantToolsFileSearchTypeOnlyType;
}

/**
 * The type of tool being defined: `function`
 */
export type AssistantToolsFunctionType = typeof AssistantToolsFunctionType[keyof typeof AssistantToolsFunctionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantToolsFunctionType = {
  function: 'function',
} as const;

export interface AssistantToolsFunction {
  /** The type of tool being defined: `function` */
  type: AssistantToolsFunctionType;
  function: FunctionObject;
}

/**
 * Specifies the format that the model must output. Compatible with [GPT-4o](https://platform.openai.com/docs/models#gpt-4o), [GPT-4 Turbo](https://platform.openai.com/docs/models#gpt-4-turbo-and-gpt-4), and all GPT-3.5 Turbo models since `gpt-3.5-turbo-1106`.

Setting to `{ "type": "json_schema", "json_schema": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).

Setting to `{ "type": "json_object" }` enables JSON mode, which ensures the message the model generates is valid JSON.

**Important:** when using JSON mode, you **must** also instruct the model to produce JSON yourself via a system or user message. Without this, the model may generate an unending stream of whitespace until the generation reaches the token limit, resulting in a long-running and seemingly "stuck" request. Also note that the message content may be partially cut off if `finish_reason="length"`, which indicates the generation exceeded `max_tokens` or the conversation exceeded the max context length.

 */
export type AssistantsApiResponseFormatOption = 'auto' | ResponseFormatText | ResponseFormatJsonObject | ResponseFormatJsonSchema;

/**
 * Controls which (if any) tool is called by the model.
`none` means the model will not call any tools and instead generates a message.
`auto` is the default value and means the model can pick between generating a message or calling one or more tools.
`required` means the model must call one or more tools before responding to the user.
Specifying a particular tool like `{"type": "file_search"}` or `{"type": "function", "function": {"name": "my_function"}}` forces the model to call that tool.

 */
export type AssistantsApiToolChoiceOption = 'none' | 'auto' | 'required' | AssistantsNamedToolChoice;

/**
 * The type of the tool. If type is `function`, the function name must be set
 */
export type AssistantsNamedToolChoiceType = typeof AssistantsNamedToolChoiceType[keyof typeof AssistantsNamedToolChoiceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantsNamedToolChoiceType = {
  function: 'function',
  code_interpreter: 'code_interpreter',
  file_search: 'file_search',
} as const;

export type AssistantsNamedToolChoiceFunction = {
  /** The name of the function to call. */
  name: string;
};

/**
 * Specifies a tool the model should use. Use to force the model to call a specific tool.
 */
export interface AssistantsNamedToolChoice {
  /** The type of the tool. If type is `function`, the function name must be set */
  type: AssistantsNamedToolChoiceType;
  function?: AssistantsNamedToolChoiceFunction;
}

/**
 * The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, `vtt`, or `diarized_json`. For `gpt-4o-transcribe` and `gpt-4o-mini-transcribe`, the only supported format is `json`. For `gpt-4o-transcribe-diarize`, the supported formats are `json`, `text`, and `diarized_json`, with `diarized_json` required to receive speaker annotations.

 */
export type AudioResponseFormat = typeof AudioResponseFormat[keyof typeof AudioResponseFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AudioResponseFormat = {
  json: 'json',
  text: 'text',
  srt: 'srt',
  verbose_json: 'verbose_json',
  vtt: 'vtt',
  diarized_json: 'diarized_json',
} as const;

/**
 * The model to use for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-transcribe`, and `gpt-4o-transcribe-diarize`. Use `gpt-4o-transcribe-diarize` when you need diarization with speaker labels.

 */
export type AudioTranscriptionModel = typeof AudioTranscriptionModel[keyof typeof AudioTranscriptionModel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AudioTranscriptionModel = {
  'whisper-1': 'whisper-1',
  'gpt-4o-mini-transcribe': 'gpt-4o-mini-transcribe',
  'gpt-4o-transcribe': 'gpt-4o-transcribe',
  'gpt-4o-transcribe-diarize': 'gpt-4o-transcribe-diarize',
} as const;

export interface AudioTranscription {
  /** The model to use for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-transcribe`, and `gpt-4o-transcribe-diarize`. Use `gpt-4o-transcribe-diarize` when you need diarization with speaker labels.
 */
  model?: AudioTranscriptionModel;
  /** The language of the input audio. Supplying the input language in
[ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) (e.g. `en`) format
will improve accuracy and latency.
 */
  language?: string;
  /** An optional text to guide the model's style or continue a previous audio
segment.
For `whisper-1`, the [prompt is a list of keywords](https://platform.openai.com/docs/guides/speech-to-text#prompting).
For `gpt-4o-transcribe` models (excluding `gpt-4o-transcribe-diarize`), the prompt is a free text string, for example "expect words related to technology".
 */
  prompt?: string;
}

/**
 * The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project.
 */
export type AuditLogProject = {
  /** The project ID. */
  id?: string;
  /** The project title. */
  name?: string;
};

/**
 * The payload used to create the API key.
 */
export type AuditLogApiKeyCreatedData = {
  /** A list of scopes allowed for the API key, e.g. `["api.model.request"]` */
  scopes?: string[];
};

/**
 * The details for events with this `type`.
 */
export type AuditLogApiKeyCreated = {
  /** The tracking ID of the API key. */
  id?: string;
  /** The payload used to create the API key. */
  data?: AuditLogApiKeyCreatedData;
};

/**
 * The payload used to update the API key.
 */
export type AuditLogApiKeyUpdatedChangesRequested = {
  /** A list of scopes allowed for the API key, e.g. `["api.model.request"]` */
  scopes?: string[];
};

/**
 * The details for events with this `type`.
 */
export type AuditLogApiKeyUpdated = {
  /** The tracking ID of the API key. */
  id?: string;
  /** The payload used to update the API key. */
  changes_requested?: AuditLogApiKeyUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogApiKeyDeleted = {
  /** The tracking ID of the API key. */
  id?: string;
};

/**
 * The payload used to create the checkpoint permission.
 */
export type AuditLogCheckpointPermissionCreatedData = {
  /** The ID of the project that the checkpoint permission was created for. */
  project_id?: string;
  /** The ID of the fine-tuned model checkpoint. */
  fine_tuned_model_checkpoint?: string;
};

/**
 * The project and fine-tuned model checkpoint that the checkpoint permission was created for.
 */
export type AuditLogCheckpointPermissionCreated = {
  /** The ID of the checkpoint permission. */
  id?: string;
  /** The payload used to create the checkpoint permission. */
  data?: AuditLogCheckpointPermissionCreatedData;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCheckpointPermissionDeleted = {
  /** The ID of the checkpoint permission. */
  id?: string;
};

/**
 * The configuration for the external key.
 */
export type AuditLogExternalKeyRegisteredData = { [key: string]: unknown };

/**
 * The details for events with this `type`.
 */
export type AuditLogExternalKeyRegistered = {
  /** The ID of the external key configuration. */
  id?: string;
  /** The configuration for the external key. */
  data?: AuditLogExternalKeyRegisteredData;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogExternalKeyRemoved = {
  /** The ID of the external key configuration. */
  id?: string;
};

/**
 * Information about the created group.
 */
export type AuditLogGroupCreatedData = {
  /** The group name. */
  group_name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogGroupCreated = {
  /** The ID of the group. */
  id?: string;
  /** Information about the created group. */
  data?: AuditLogGroupCreatedData;
};

/**
 * The payload used to update the group.
 */
export type AuditLogGroupUpdatedChangesRequested = {
  /** The updated group name. */
  group_name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogGroupUpdated = {
  /** The ID of the group. */
  id?: string;
  /** The payload used to update the group. */
  changes_requested?: AuditLogGroupUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogGroupDeleted = {
  /** The ID of the group. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogScimEnabled = {
  /** The ID of the SCIM was enabled for. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogScimDisabled = {
  /** The ID of the SCIM was disabled for. */
  id?: string;
};

/**
 * The payload used to create the invite.
 */
export type AuditLogInviteSentData = {
  /** The email invited to the organization. */
  email?: string;
  /** The role the email was invited to be. Is either `owner` or `member`. */
  role?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogInviteSent = {
  /** The ID of the invite. */
  id?: string;
  /** The payload used to create the invite. */
  data?: AuditLogInviteSentData;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogInviteAccepted = {
  /** The ID of the invite. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogInviteDeleted = {
  /** The ID of the invite. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogIpAllowlistCreated = {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
  /** The IP addresses or CIDR ranges included in the configuration. */
  allowed_ips?: string[];
};

/**
 * The details for events with this `type`.
 */
export type AuditLogIpAllowlistUpdated = {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The updated set of IP addresses or CIDR ranges in the configuration. */
  allowed_ips?: string[];
};

/**
 * The details for events with this `type`.
 */
export type AuditLogIpAllowlistDeleted = {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
  /** The IP addresses or CIDR ranges that were in the configuration. */
  allowed_ips?: string[];
};

export type AuditLogIpAllowlistConfigActivatedConfigsItem = {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogIpAllowlistConfigActivated = {
  /** The configurations that were activated. */
  configs?: AuditLogIpAllowlistConfigActivatedConfigsItem[];
};

export type AuditLogIpAllowlistConfigDeactivatedConfigsItem = {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogIpAllowlistConfigDeactivated = {
  /** The configurations that were deactivated. */
  configs?: AuditLogIpAllowlistConfigDeactivatedConfigsItem[];
};

/**
 * This event has no additional fields beyond the standard audit log attributes.
 */
export type AuditLogLoginSucceeded = { [key: string]: unknown };

/**
 * The details for events with this `type`.
 */
export type AuditLogLoginFailed = {
  /** The error code of the failure. */
  error_code?: string;
  /** The error message of the failure. */
  error_message?: string;
};

/**
 * This event has no additional fields beyond the standard audit log attributes.
 */
export type AuditLogLogoutSucceeded = { [key: string]: unknown };

/**
 * The details for events with this `type`.
 */
export type AuditLogLogoutFailed = {
  /** The error code of the failure. */
  error_code?: string;
  /** The error message of the failure. */
  error_message?: string;
};

/**
 * The payload used to update the organization settings.
 */
export type AuditLogOrganizationUpdatedChangesRequested = {
  /** The organization title. */
  title?: string;
  /** The organization description. */
  description?: string;
  /** The organization name. */
  name?: string;
  /** Visibility of the threads page which shows messages created with the Assistants API and Playground. One of `ANY_ROLE`, `OWNERS`, or `NONE`. */
  threads_ui_visibility?: string;
  /** Visibility of the usage dashboard which shows activity and costs for your organization. One of `ANY_ROLE` or `OWNERS`. */
  usage_dashboard_visibility?: string;
  /** How your organization logs data from supported API calls. One of `disabled`, `enabled_per_call`, `enabled_for_all_projects`, or `enabled_for_selected_projects` */
  api_call_logging?: string;
  /** The list of project ids if api_call_logging is set to `enabled_for_selected_projects` */
  api_call_logging_project_ids?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogOrganizationUpdated = {
  /** The organization ID. */
  id?: string;
  /** The payload used to update the organization settings. */
  changes_requested?: AuditLogOrganizationUpdatedChangesRequested;
};

/**
 * The payload used to create the project.
 */
export type AuditLogProjectCreatedData = {
  /** The project name. */
  name?: string;
  /** The title of the project as seen on the dashboard. */
  title?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogProjectCreated = {
  /** The project ID. */
  id?: string;
  /** The payload used to create the project. */
  data?: AuditLogProjectCreatedData;
};

/**
 * The payload used to update the project.
 */
export type AuditLogProjectUpdatedChangesRequested = {
  /** The title of the project as seen on the dashboard. */
  title?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogProjectUpdated = {
  /** The project ID. */
  id?: string;
  /** The payload used to update the project. */
  changes_requested?: AuditLogProjectUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogProjectArchived = {
  /** The project ID. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogProjectDeleted = {
  /** The project ID. */
  id?: string;
};

/**
 * The payload used to update the rate limits.
 */
export type AuditLogRateLimitUpdatedChangesRequested = {
  /** The maximum requests per minute. */
  max_requests_per_1_minute?: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute?: number;
  /** The maximum images per minute. Only relevant for certain models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only relevant for certain models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only relevant for certain models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only relevant for certain models. */
  batch_1_day_max_input_tokens?: number;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRateLimitUpdated = {
  /** The rate limit ID */
  id?: string;
  /** The payload used to update the rate limits. */
  changes_requested?: AuditLogRateLimitUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRateLimitDeleted = {
  /** The rate limit ID */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRoleCreated = {
  /** The role ID. */
  id?: string;
  /** The name of the role. */
  role_name?: string;
  /** The permissions granted by the role. */
  permissions?: string[];
  /** The type of resource the role belongs to. */
  resource_type?: string;
  /** The resource the role is scoped to. */
  resource_id?: string;
};

/**
 * Additional metadata stored on the role.
 */
export type AuditLogRoleUpdatedChangesRequestedMetadata = { [key: string]: unknown };

/**
 * The payload used to update the role.
 */
export type AuditLogRoleUpdatedChangesRequested = {
  /** The updated role name, when provided. */
  role_name?: string;
  /** The resource the role is scoped to. */
  resource_id?: string;
  /** The type of resource the role belongs to. */
  resource_type?: string;
  /** The permissions added to the role. */
  permissions_added?: string[];
  /** The permissions removed from the role. */
  permissions_removed?: string[];
  /** The updated role description, when provided. */
  description?: string;
  /** Additional metadata stored on the role. */
  metadata?: AuditLogRoleUpdatedChangesRequestedMetadata;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRoleUpdated = {
  /** The role ID. */
  id?: string;
  /** The payload used to update the role. */
  changes_requested?: AuditLogRoleUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRoleDeleted = {
  /** The role ID. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRoleAssignmentCreated = {
  /** The identifier of the role assignment. */
  id?: string;
  /** The principal (user or group) that received the role. */
  principal_id?: string;
  /** The type of principal (user or group) that received the role. */
  principal_type?: string;
  /** The resource the role assignment is scoped to. */
  resource_id?: string;
  /** The type of resource the role assignment is scoped to. */
  resource_type?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogRoleAssignmentDeleted = {
  /** The identifier of the role assignment. */
  id?: string;
  /** The principal (user or group) that had the role removed. */
  principal_id?: string;
  /** The type of principal (user or group) that had the role removed. */
  principal_type?: string;
  /** The resource the role assignment was scoped to. */
  resource_id?: string;
  /** The type of resource the role assignment was scoped to. */
  resource_type?: string;
};

/**
 * The payload used to create the service account.
 */
export type AuditLogServiceAccountCreatedData = {
  /** The role of the service account. Is either `owner` or `member`. */
  role?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogServiceAccountCreated = {
  /** The service account ID. */
  id?: string;
  /** The payload used to create the service account. */
  data?: AuditLogServiceAccountCreatedData;
};

/**
 * The payload used to updated the service account.
 */
export type AuditLogServiceAccountUpdatedChangesRequested = {
  /** The role of the service account. Is either `owner` or `member`. */
  role?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogServiceAccountUpdated = {
  /** The service account ID. */
  id?: string;
  /** The payload used to updated the service account. */
  changes_requested?: AuditLogServiceAccountUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogServiceAccountDeleted = {
  /** The service account ID. */
  id?: string;
};

/**
 * The payload used to add the user to the project.
 */
export type AuditLogUserAddedData = {
  /** The role of the user. Is either `owner` or `member`. */
  role?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogUserAdded = {
  /** The user ID. */
  id?: string;
  /** The payload used to add the user to the project. */
  data?: AuditLogUserAddedData;
};

/**
 * The payload used to update the user.
 */
export type AuditLogUserUpdatedChangesRequested = {
  /** The role of the user. Is either `owner` or `member`. */
  role?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogUserUpdated = {
  /** The project ID. */
  id?: string;
  /** The payload used to update the user. */
  changes_requested?: AuditLogUserUpdatedChangesRequested;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogUserDeleted = {
  /** The user ID. */
  id?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCertificateCreated = {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCertificateUpdated = {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCertificateDeleted = {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
  /** The certificate content in PEM format. */
  certificate?: string;
};

export type AuditLogCertificatesActivatedCertificatesItem = {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCertificatesActivated = {
  certificates?: AuditLogCertificatesActivatedCertificatesItem[];
};

export type AuditLogCertificatesDeactivatedCertificatesItem = {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
};

/**
 * The details for events with this `type`.
 */
export type AuditLogCertificatesDeactivated = {
  certificates?: AuditLogCertificatesDeactivatedCertificatesItem[];
};

/**
 * A log of a user action or configuration change within this organization.
 */
export interface AuditLog {
  /** The ID of this log. */
  id: string;
  type: AuditLogEventType;
  /** The Unix timestamp (in seconds) of the event. */
  effective_at: number;
  /** The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project. */
  project?: AuditLogProject;
  actor: AuditLogActor;
  /** The details for events with this `type`. */
  'api_key.created'?: AuditLogApiKeyCreated;
  /** The details for events with this `type`. */
  'api_key.updated'?: AuditLogApiKeyUpdated;
  /** The details for events with this `type`. */
  'api_key.deleted'?: AuditLogApiKeyDeleted;
  /** The project and fine-tuned model checkpoint that the checkpoint permission was created for. */
  'checkpoint.permission.created'?: AuditLogCheckpointPermissionCreated;
  /** The details for events with this `type`. */
  'checkpoint.permission.deleted'?: AuditLogCheckpointPermissionDeleted;
  /** The details for events with this `type`. */
  'external_key.registered'?: AuditLogExternalKeyRegistered;
  /** The details for events with this `type`. */
  'external_key.removed'?: AuditLogExternalKeyRemoved;
  /** The details for events with this `type`. */
  'group.created'?: AuditLogGroupCreated;
  /** The details for events with this `type`. */
  'group.updated'?: AuditLogGroupUpdated;
  /** The details for events with this `type`. */
  'group.deleted'?: AuditLogGroupDeleted;
  /** The details for events with this `type`. */
  'scim.enabled'?: AuditLogScimEnabled;
  /** The details for events with this `type`. */
  'scim.disabled'?: AuditLogScimDisabled;
  /** The details for events with this `type`. */
  'invite.sent'?: AuditLogInviteSent;
  /** The details for events with this `type`. */
  'invite.accepted'?: AuditLogInviteAccepted;
  /** The details for events with this `type`. */
  'invite.deleted'?: AuditLogInviteDeleted;
  /** The details for events with this `type`. */
  'ip_allowlist.created'?: AuditLogIpAllowlistCreated;
  /** The details for events with this `type`. */
  'ip_allowlist.updated'?: AuditLogIpAllowlistUpdated;
  /** The details for events with this `type`. */
  'ip_allowlist.deleted'?: AuditLogIpAllowlistDeleted;
  /** The details for events with this `type`. */
  'ip_allowlist.config.activated'?: AuditLogIpAllowlistConfigActivated;
  /** The details for events with this `type`. */
  'ip_allowlist.config.deactivated'?: AuditLogIpAllowlistConfigDeactivated;
  /** This event has no additional fields beyond the standard audit log attributes. */
  'login.succeeded'?: AuditLogLoginSucceeded;
  /** The details for events with this `type`. */
  'login.failed'?: AuditLogLoginFailed;
  /** This event has no additional fields beyond the standard audit log attributes. */
  'logout.succeeded'?: AuditLogLogoutSucceeded;
  /** The details for events with this `type`. */
  'logout.failed'?: AuditLogLogoutFailed;
  /** The details for events with this `type`. */
  'organization.updated'?: AuditLogOrganizationUpdated;
  /** The details for events with this `type`. */
  'project.created'?: AuditLogProjectCreated;
  /** The details for events with this `type`. */
  'project.updated'?: AuditLogProjectUpdated;
  /** The details for events with this `type`. */
  'project.archived'?: AuditLogProjectArchived;
  /** The details for events with this `type`. */
  'project.deleted'?: AuditLogProjectDeleted;
  /** The details for events with this `type`. */
  'rate_limit.updated'?: AuditLogRateLimitUpdated;
  /** The details for events with this `type`. */
  'rate_limit.deleted'?: AuditLogRateLimitDeleted;
  /** The details for events with this `type`. */
  'role.created'?: AuditLogRoleCreated;
  /** The details for events with this `type`. */
  'role.updated'?: AuditLogRoleUpdated;
  /** The details for events with this `type`. */
  'role.deleted'?: AuditLogRoleDeleted;
  /** The details for events with this `type`. */
  'role.assignment.created'?: AuditLogRoleAssignmentCreated;
  /** The details for events with this `type`. */
  'role.assignment.deleted'?: AuditLogRoleAssignmentDeleted;
  /** The details for events with this `type`. */
  'service_account.created'?: AuditLogServiceAccountCreated;
  /** The details for events with this `type`. */
  'service_account.updated'?: AuditLogServiceAccountUpdated;
  /** The details for events with this `type`. */
  'service_account.deleted'?: AuditLogServiceAccountDeleted;
  /** The details for events with this `type`. */
  'user.added'?: AuditLogUserAdded;
  /** The details for events with this `type`. */
  'user.updated'?: AuditLogUserUpdated;
  /** The details for events with this `type`. */
  'user.deleted'?: AuditLogUserDeleted;
  /** The details for events with this `type`. */
  'certificate.created'?: AuditLogCertificateCreated;
  /** The details for events with this `type`. */
  'certificate.updated'?: AuditLogCertificateUpdated;
  /** The details for events with this `type`. */
  'certificate.deleted'?: AuditLogCertificateDeleted;
  /** The details for events with this `type`. */
  'certificates.activated'?: AuditLogCertificatesActivated;
  /** The details for events with this `type`. */
  'certificates.deactivated'?: AuditLogCertificatesDeactivated;
}

/**
 * The type of actor. Is either `session` or `api_key`.
 */
export type AuditLogActorType = typeof AuditLogActorType[keyof typeof AuditLogActorType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuditLogActorType = {
  session: 'session',
  api_key: 'api_key',
} as const;

/**
 * The actor who performed the audit logged action.
 */
export interface AuditLogActor {
  /** The type of actor. Is either `session` or `api_key`. */
  type?: AuditLogActorType;
  session?: AuditLogActorSession;
  api_key?: AuditLogActorApiKey;
}

/**
 * The type of API key. Can be either `user` or `service_account`.
 */
export type AuditLogActorApiKeyType = typeof AuditLogActorApiKeyType[keyof typeof AuditLogActorApiKeyType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuditLogActorApiKeyType = {
  user: 'user',
  service_account: 'service_account',
} as const;

/**
 * The API Key used to perform the audit logged action.
 */
export interface AuditLogActorApiKey {
  /** The tracking id of the API key. */
  id?: string;
  /** The type of API key. Can be either `user` or `service_account`. */
  type?: AuditLogActorApiKeyType;
  user?: AuditLogActorUser;
  service_account?: AuditLogActorServiceAccount;
}

/**
 * The service account that performed the audit logged action.
 */
export interface AuditLogActorServiceAccount {
  /** The service account id. */
  id?: string;
}

/**
 * The session in which the audit logged action was performed.
 */
export interface AuditLogActorSession {
  user?: AuditLogActorUser;
  /** The IP address from which the action was performed. */
  ip_address?: string;
}

/**
 * The user who performed the audit logged action.
 */
export interface AuditLogActorUser {
  /** The user id. */
  id?: string;
  /** The user email. */
  email?: string;
}

/**
 * The event type.
 */
export type AuditLogEventType = typeof AuditLogEventType[keyof typeof AuditLogEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuditLogEventType = {
  api_keycreated: 'api_key.created',
  api_keyupdated: 'api_key.updated',
  api_keydeleted: 'api_key.deleted',
  certificatecreated: 'certificate.created',
  certificateupdated: 'certificate.updated',
  certificatedeleted: 'certificate.deleted',
  certificatesactivated: 'certificates.activated',
  certificatesdeactivated: 'certificates.deactivated',
  checkpointpermissioncreated: 'checkpoint.permission.created',
  checkpointpermissiondeleted: 'checkpoint.permission.deleted',
  external_keyregistered: 'external_key.registered',
  external_keyremoved: 'external_key.removed',
  groupcreated: 'group.created',
  groupupdated: 'group.updated',
  groupdeleted: 'group.deleted',
  invitesent: 'invite.sent',
  inviteaccepted: 'invite.accepted',
  invitedeleted: 'invite.deleted',
  ip_allowlistcreated: 'ip_allowlist.created',
  ip_allowlistupdated: 'ip_allowlist.updated',
  ip_allowlistdeleted: 'ip_allowlist.deleted',
  ip_allowlistconfigactivated: 'ip_allowlist.config.activated',
  ip_allowlistconfigdeactivated: 'ip_allowlist.config.deactivated',
  loginsucceeded: 'login.succeeded',
  loginfailed: 'login.failed',
  logoutsucceeded: 'logout.succeeded',
  logoutfailed: 'logout.failed',
  organizationupdated: 'organization.updated',
  projectcreated: 'project.created',
  projectupdated: 'project.updated',
  projectarchived: 'project.archived',
  projectdeleted: 'project.deleted',
  rate_limitupdated: 'rate_limit.updated',
  rate_limitdeleted: 'rate_limit.deleted',
  resourcedeleted: 'resource.deleted',
  rolecreated: 'role.created',
  roleupdated: 'role.updated',
  roledeleted: 'role.deleted',
  roleassignmentcreated: 'role.assignment.created',
  roleassignmentdeleted: 'role.assignment.deleted',
  scimenabled: 'scim.enabled',
  scimdisabled: 'scim.disabled',
  service_accountcreated: 'service_account.created',
  service_accountupdated: 'service_account.updated',
  service_accountdeleted: 'service_account.deleted',
  useradded: 'user.added',
  userupdated: 'user.updated',
  userdeleted: 'user.deleted',
} as const;

/**
 * Always `auto`.
 */
export type AutoChunkingStrategyRequestParamType = typeof AutoChunkingStrategyRequestParamType[keyof typeof AutoChunkingStrategyRequestParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AutoChunkingStrategyRequestParamType = {
  auto: 'auto',
} as const;

/**
 * The default strategy. This strategy currently uses a `max_chunk_size_tokens` of `800` and `chunk_overlap_tokens` of `400`.
 */
export interface AutoChunkingStrategyRequestParam {
  /** Always `auto`. */
  type: AutoChunkingStrategyRequestParamType;
}

/**
 * The object type, which is always `batch`.
 */
export type BatchObject = typeof BatchObject[keyof typeof BatchObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BatchObject = {
  batch: 'batch',
} as const;

export type BatchErrors = {
  /** The object type, which is always `list`. */
  object?: string;
  data?: BatchError[];
};

/**
 * The current status of the batch.
 */
export type BatchStatus = typeof BatchStatus[keyof typeof BatchStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BatchStatus = {
  validating: 'validating',
  failed: 'failed',
  in_progress: 'in_progress',
  finalizing: 'finalizing',
  completed: 'completed',
  expired: 'expired',
  cancelling: 'cancelling',
  cancelled: 'cancelled',
} as const;

/**
 * A detailed breakdown of the input tokens.
 */
export type BatchUsageInputTokensDetails = {
  /** The number of tokens that were retrieved from the cache. [More on
prompt caching](https://platform.openai.com/docs/guides/prompt-caching).
 */
  cached_tokens: number;
};

/**
 * A detailed breakdown of the output tokens.
 */
export type BatchUsageOutputTokensDetails = {
  /** The number of reasoning tokens. */
  reasoning_tokens: number;
};

/**
 * Represents token usage details including input tokens, output tokens, a
breakdown of output tokens, and the total tokens used. Only populated on
batches created after September 7, 2025.

 */
export type BatchUsage = {
  /** The number of input tokens. */
  input_tokens: number;
  /** A detailed breakdown of the input tokens. */
  input_tokens_details: BatchUsageInputTokensDetails;
  /** The number of output tokens. */
  output_tokens: number;
  /** A detailed breakdown of the output tokens. */
  output_tokens_details: BatchUsageOutputTokensDetails;
  /** The total number of tokens used. */
  total_tokens: number;
};

export interface Batch {
  id: string;
  /** The object type, which is always `batch`. */
  object: BatchObject;
  /** The OpenAI API endpoint used by the batch. */
  endpoint: string;
  /** Model ID used to process the batch, like `gpt-5-2025-08-07`. OpenAI
offers a wide range of models with different capabilities, performance
characteristics, and price points. Refer to the [model
guide](https://platform.openai.com/docs/models) to browse and compare available models.
 */
  model?: string;
  errors?: BatchErrors;
  /** The ID of the input file for the batch. */
  input_file_id: string;
  /** The time frame within which the batch should be processed. */
  completion_window: string;
  /** The current status of the batch. */
  status: BatchStatus;
  /** The ID of the file containing the outputs of successfully executed requests. */
  output_file_id?: string;
  /** The ID of the file containing the outputs of requests with errors. */
  error_file_id?: string;
  /** The Unix timestamp (in seconds) for when the batch was created. */
  created_at: number;
  /** The Unix timestamp (in seconds) for when the batch started processing. */
  in_progress_at?: number;
  /** The Unix timestamp (in seconds) for when the batch will expire. */
  expires_at?: number;
  /** The Unix timestamp (in seconds) for when the batch started finalizing. */
  finalizing_at?: number;
  /** The Unix timestamp (in seconds) for when the batch was completed. */
  completed_at?: number;
  /** The Unix timestamp (in seconds) for when the batch failed. */
  failed_at?: number;
  /** The Unix timestamp (in seconds) for when the batch expired. */
  expired_at?: number;
  /** The Unix timestamp (in seconds) for when the batch started cancelling. */
  cancelling_at?: number;
  /** The Unix timestamp (in seconds) for when the batch was cancelled. */
  cancelled_at?: number;
  request_counts?: BatchRequestCounts;
  /** Represents token usage details including input tokens, output tokens, a
breakdown of output tokens, and the total tokens used. Only populated on
batches created after September 7, 2025.
 */
  usage?: BatchUsage;
  metadata?: Metadata;
}

/**
 * Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`. Note that the anchor is the file creation time, not the time the batch is created.
 */
export type BatchFileExpirationAfterAnchor = typeof BatchFileExpirationAfterAnchor[keyof typeof BatchFileExpirationAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BatchFileExpirationAfterAnchor = {
  created_at: 'created_at',
} as const;

/**
 * The expiration policy for the output and/or error file that are generated for a batch.
 */
export interface BatchFileExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`. Note that the anchor is the file creation time, not the time the batch is created. */
  anchor: BatchFileExpirationAfterAnchor;
  /**
   * The number of seconds after the anchor time that the file will expire. Must be between 3600 (1 hour) and 2592000 (30 days).
   * @minimum 3600
   * @maximum 2592000
   */
  seconds: number;
}

/**
 * The HTTP method to be used for the request. Currently only `POST` is supported.
 */
export type BatchRequestInputMethod = typeof BatchRequestInputMethod[keyof typeof BatchRequestInputMethod];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BatchRequestInputMethod = {
  POST: 'POST',
} as const;

/**
 * The per-line object of the batch input file
 */
export interface BatchRequestInput {
  /** A developer-provided per-request id that will be used to match outputs to inputs. Must be unique for each request in a batch. */
  custom_id?: string;
  /** The HTTP method to be used for the request. Currently only `POST` is supported. */
  method?: BatchRequestInputMethod;
  /** The OpenAI API relative URL to be used for the request. Currently `/v1/chat/completions`, `/v1/embeddings`, and `/v1/completions` are supported. */
  url?: string;
}

/**
 * The JSON body of the response
 */
export type BatchRequestOutputResponseAnyOfBody = { [key: string]: unknown };

export type BatchRequestOutputResponseAnyOf = {
  /** The HTTP status code of the response */
  status_code?: number;
  /** An unique identifier for the OpenAI API request. Please include this request ID when contacting support. */
  request_id?: string;
  /** The JSON body of the response */
  body?: BatchRequestOutputResponseAnyOfBody;
};

export type BatchRequestOutputResponse = BatchRequestOutputResponseAnyOf | null;

/**
 * For requests that failed with a non-HTTP error, this will contain more information on the cause of the failure.
 */
export type BatchRequestOutputErrorAnyOf = {
  /** A machine-readable error code.

Possible values:
- `batch_expired`: The request could not be executed before the
  completion window ended.
- `batch_cancelled`: The batch was cancelled before this request
  executed.
- `request_timeout`: The underlying call to the model timed out.
 */
  code?: string;
  /** A human-readable error message. */
  message?: string;
};

export type BatchRequestOutputError = BatchRequestOutputErrorAnyOf | null;

/**
 * The per-line object of the batch output and error files
 */
export interface BatchRequestOutput {
  id?: string;
  /** A developer-provided per-request id that will be used to match outputs to inputs. */
  custom_id?: string;
  response?: BatchRequestOutputResponse;
  error?: BatchRequestOutputError;
}

/**
 * The object type.

- If creating, updating, or getting a specific certificate, the object type is `certificate`.
- If listing, activating, or deactivating certificates for the organization, the object type is `organization.certificate`.
- If listing, activating, or deactivating certificates for a project, the object type is `organization.project.certificate`.

 */
export type CertificateObject = typeof CertificateObject[keyof typeof CertificateObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CertificateObject = {
  certificate: 'certificate',
  organizationcertificate: 'organization.certificate',
  organizationprojectcertificate: 'organization.project.certificate',
} as const;

export type CertificateCertificateDetails = {
  /** The Unix timestamp (in seconds) of when the certificate becomes valid. */
  valid_at?: number;
  /** The Unix timestamp (in seconds) of when the certificate expires. */
  expires_at?: number;
  /** The content of the certificate in PEM format. */
  content?: string;
};

/**
 * Represents an individual `certificate` uploaded to the organization.
 */
export interface Certificate {
  /** The object type.

- If creating, updating, or getting a specific certificate, the object type is `certificate`.
- If listing, activating, or deactivating certificates for the organization, the object type is `organization.certificate`.
- If listing, activating, or deactivating certificates for a project, the object type is `organization.project.certificate`.
 */
  object: CertificateObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the certificate. */
  name: string;
  /** The Unix timestamp (in seconds) of when the certificate was uploaded. */
  created_at: number;
  certificate_details: CertificateCertificateDetails;
  /** Whether the certificate is currently active at the specified scope. Not returned when getting details for a specific certificate. */
  active?: boolean;
}

/**
 * Constrains the tools available to the model to a pre-defined set.

`auto` allows the model to pick from among the allowed tools and generate a
message.

`required` requires the model to call one or more of the allowed tools.

 */
export type ChatCompletionAllowedToolsMode = typeof ChatCompletionAllowedToolsMode[keyof typeof ChatCompletionAllowedToolsMode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionAllowedToolsMode = {
  auto: 'auto',
  required: 'required',
} as const;

/**
 * A tool definition that the model should be allowed to call.

 */
export type ChatCompletionAllowedToolsToolsItem = { [key: string]: unknown };

/**
 * Constrains the tools available to the model to a pre-defined set.

 */
export interface ChatCompletionAllowedTools {
  /** Constrains the tools available to the model to a pre-defined set.

`auto` allows the model to pick from among the allowed tools and generate a
message.

`required` requires the model to call one or more of the allowed tools.
 */
  mode: ChatCompletionAllowedToolsMode;
  /** A list of tool definitions that the model should be allowed to call.

For the Chat Completions API, the list of tool definitions might look like:
```json
[
  { "type": "function", "function": { "name": "get_weather" } },
  { "type": "function", "function": { "name": "get_time" } }
]
```
 */
  tools: ChatCompletionAllowedToolsToolsItem[];
}

/**
 * Allowed tool configuration type. Always `allowed_tools`.
 */
export type ChatCompletionAllowedToolsChoiceType = typeof ChatCompletionAllowedToolsChoiceType[keyof typeof ChatCompletionAllowedToolsChoiceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionAllowedToolsChoiceType = {
  allowed_tools: 'allowed_tools',
} as const;

/**
 * Constrains the tools available to the model to a pre-defined set.

 */
export interface ChatCompletionAllowedToolsChoice {
  /** Allowed tool configuration type. Always `allowed_tools`. */
  type: ChatCompletionAllowedToolsChoiceType;
  allowed_tools: ChatCompletionAllowedTools;
}

/**
 * The type of object being deleted.
 */
export type ChatCompletionDeletedObject = typeof ChatCompletionDeletedObject[keyof typeof ChatCompletionDeletedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionDeletedObject = {
  chatcompletiondeleted: 'chat.completion.deleted',
} as const;

export interface ChatCompletionDeleted {
  /** The type of object being deleted. */
  object: ChatCompletionDeletedObject;
  /** The ID of the chat completion that was deleted. */
  id: string;
  /** Whether the chat completion was deleted. */
  deleted: boolean;
}

/**
 * Specifying a particular function via `{"name": "my_function"}` forces the model to call that function.

 */
export interface ChatCompletionFunctionCallOption {
  /** The name of the function to call. */
  name: string;
}

/**
 * @deprecated
 */
export interface ChatCompletionFunctions {
  /** A description of what the function does, used by the model to choose when and how to call the function. */
  description?: string;
  /** The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64. */
  name: string;
  parameters?: FunctionParameters;
}

/**
 * The type of this object. It is always set to "list".

 */
export type ChatCompletionListObject = typeof ChatCompletionListObject[keyof typeof ChatCompletionListObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionListObject = {
  list: 'list',
} as const;

/**
 * An object representing a list of Chat Completions.

 */
export interface ChatCompletionList {
  /** The type of this object. It is always set to "list".
 */
  object: ChatCompletionListObject;
  /** An array of chat completion objects.
 */
  data: CreateChatCompletionResponse[];
  /** The identifier of the first chat completion in the data array. */
  first_id: string;
  /** The identifier of the last chat completion in the data array. */
  last_id: string;
  /** Indicates whether there are more Chat Completions available. */
  has_more: boolean;
}

/**
 * The type of the tool. Always `custom`.
 */
export type ChatCompletionMessageCustomToolCallType = typeof ChatCompletionMessageCustomToolCallType[keyof typeof ChatCompletionMessageCustomToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionMessageCustomToolCallType = {
  custom: 'custom',
} as const;

/**
 * The custom tool that the model called.
 */
export type ChatCompletionMessageCustomToolCallCustom = {
  /** The name of the custom tool to call. */
  name: string;
  /** The input for the custom tool call generated by the model. */
  input: string;
};

/**
 * A call to a custom tool created by the model.

 */
export interface ChatCompletionMessageCustomToolCall {
  /** The ID of the tool call. */
  id: string;
  /** The type of the tool. Always `custom`. */
  type: ChatCompletionMessageCustomToolCallType;
  /** The custom tool that the model called. */
  custom: ChatCompletionMessageCustomToolCallCustom;
}

/**
 * The type of this object. It is always set to "list".

 */
export type ChatCompletionMessageListObject = typeof ChatCompletionMessageListObject[keyof typeof ChatCompletionMessageListObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionMessageListObject = {
  list: 'list',
} as const;

export type ChatCompletionMessageListDataItemAllOfContentPartsAnyOfItem = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartImage;

export type ChatCompletionMessageListDataItemAllOfContentParts = ChatCompletionMessageListDataItemAllOfContentPartsAnyOfItem[] | null;

export type ChatCompletionMessageListDataItemAllOf = {
  /** The identifier of the chat message. */
  id: string;
  content_parts?: ChatCompletionMessageListDataItemAllOfContentParts;
};

export type ChatCompletionMessageListDataItem = ChatCompletionResponseMessage & ChatCompletionMessageListDataItemAllOf;

/**
 * An object representing a list of chat completion messages.

 */
export interface ChatCompletionMessageList {
  /** The type of this object. It is always set to "list".
 */
  object: ChatCompletionMessageListObject;
  /** An array of chat completion message objects.
 */
  data: ChatCompletionMessageListDataItem[];
  /** The identifier of the first chat message in the data array. */
  first_id: string;
  /** The identifier of the last chat message in the data array. */
  last_id: string;
  /** Indicates whether there are more chat messages available. */
  has_more: boolean;
}

/**
 * The type of the tool. Currently, only `function` is supported.
 */
export type ChatCompletionMessageToolCallType = typeof ChatCompletionMessageToolCallType[keyof typeof ChatCompletionMessageToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionMessageToolCallType = {
  function: 'function',
} as const;

/**
 * The function that the model called.
 */
export type ChatCompletionMessageToolCallFunction = {
  /** The name of the function to call. */
  name: string;
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
};

/**
 * A call to a function tool created by the model.

 */
export interface ChatCompletionMessageToolCall {
  /** The ID of the tool call. */
  id: string;
  /** The type of the tool. Currently, only `function` is supported. */
  type: ChatCompletionMessageToolCallType;
  /** The function that the model called. */
  function: ChatCompletionMessageToolCallFunction;
}

/**
 * The type of the tool. Currently, only `function` is supported.
 */
export type ChatCompletionMessageToolCallChunkType = typeof ChatCompletionMessageToolCallChunkType[keyof typeof ChatCompletionMessageToolCallChunkType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionMessageToolCallChunkType = {
  function: 'function',
} as const;

export type ChatCompletionMessageToolCallChunkFunction = {
  /** The name of the function to call. */
  name?: string;
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments?: string;
};

export interface ChatCompletionMessageToolCallChunk {
  index: number;
  /** The ID of the tool call. */
  id?: string;
  /** The type of the tool. Currently, only `function` is supported. */
  type?: ChatCompletionMessageToolCallChunkType;
  function?: ChatCompletionMessageToolCallChunkFunction;
}

export type ChatCompletionMessageToolCallsItem = ChatCompletionMessageToolCall | ChatCompletionMessageCustomToolCall;

/**
 * The tool calls generated by the model, such as function calls.
 */
export type ChatCompletionMessageToolCalls = ChatCompletionMessageToolCallsItem[];

export type ChatCompletionModalitiesAnyOfItem = typeof ChatCompletionModalitiesAnyOfItem[keyof typeof ChatCompletionModalitiesAnyOfItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionModalitiesAnyOfItem = {
  text: 'text',
  audio: 'audio',
} as const;

export type ChatCompletionModalities = ChatCompletionModalitiesAnyOfItem[] | null;

/**
 * For function calling, the type is always `function`.
 */
export type ChatCompletionNamedToolChoiceType = typeof ChatCompletionNamedToolChoiceType[keyof typeof ChatCompletionNamedToolChoiceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionNamedToolChoiceType = {
  function: 'function',
} as const;

export type ChatCompletionNamedToolChoiceFunction = {
  /** The name of the function to call. */
  name: string;
};

/**
 * Specifies a tool the model should use. Use to force the model to call a specific function.
 */
export interface ChatCompletionNamedToolChoice {
  /** For function calling, the type is always `function`. */
  type: ChatCompletionNamedToolChoiceType;
  function: ChatCompletionNamedToolChoiceFunction;
}

/**
 * For custom tool calling, the type is always `custom`.
 */
export type ChatCompletionNamedToolChoiceCustomType = typeof ChatCompletionNamedToolChoiceCustomType[keyof typeof ChatCompletionNamedToolChoiceCustomType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionNamedToolChoiceCustomType = {
  custom: 'custom',
} as const;

export type ChatCompletionNamedToolChoiceCustomCustom = {
  /** The name of the custom tool to call. */
  name: string;
};

/**
 * Specifies a tool the model should use. Use to force the model to call a specific custom tool.
 */
export interface ChatCompletionNamedToolChoiceCustom {
  /** For custom tool calling, the type is always `custom`. */
  type: ChatCompletionNamedToolChoiceCustomType;
  custom: ChatCompletionNamedToolChoiceCustomCustom;
}

/**
 * The contents of the assistant message. Required unless `tool_calls` or `function_call` is specified.

 */
export type ChatCompletionRequestAssistantMessageContentAnyOf = string | ChatCompletionRequestAssistantMessageContentPart[];

export type ChatCompletionRequestAssistantMessageContent = ChatCompletionRequestAssistantMessageContentAnyOf | null;

export type ChatCompletionRequestAssistantMessageRefusal = string | null;

/**
 * The role of the messages author, in this case `assistant`.
 */
export type ChatCompletionRequestAssistantMessageRole = typeof ChatCompletionRequestAssistantMessageRole[keyof typeof ChatCompletionRequestAssistantMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestAssistantMessageRole = {
  assistant: 'assistant',
} as const;

/**
 * Data about a previous audio response from the model.
[Learn more](https://platform.openai.com/docs/guides/audio).

 */
export type ChatCompletionRequestAssistantMessageAudioAnyOf = {
  /** Unique identifier for a previous audio response from the model.
 */
  id: string;
};

export type ChatCompletionRequestAssistantMessageAudio = ChatCompletionRequestAssistantMessageAudioAnyOf | null;

/**
 * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
 * @deprecated
 */
export type ChatCompletionRequestAssistantMessageFunctionCallAnyOf = {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
  /** The name of the function to call. */
  name: string;
};

export type ChatCompletionRequestAssistantMessageFunctionCall = ChatCompletionRequestAssistantMessageFunctionCallAnyOf | null;

/**
 * Messages sent by the model in response to user messages.

 */
export interface ChatCompletionRequestAssistantMessage {
  content?: ChatCompletionRequestAssistantMessageContent;
  refusal?: ChatCompletionRequestAssistantMessageRefusal;
  /** The role of the messages author, in this case `assistant`. */
  role: ChatCompletionRequestAssistantMessageRole;
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
  audio?: ChatCompletionRequestAssistantMessageAudio;
  tool_calls?: ChatCompletionMessageToolCalls;
  function_call?: ChatCompletionRequestAssistantMessageFunctionCall;
}

export type ChatCompletionRequestAssistantMessageContentPart = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartRefusal;

/**
 * The contents of the developer message.
 */
export type ChatCompletionRequestDeveloperMessageContent = string | ChatCompletionRequestMessageContentPartText[];

/**
 * The role of the messages author, in this case `developer`.
 */
export type ChatCompletionRequestDeveloperMessageRole = typeof ChatCompletionRequestDeveloperMessageRole[keyof typeof ChatCompletionRequestDeveloperMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestDeveloperMessageRole = {
  developer: 'developer',
} as const;

/**
 * Developer-provided instructions that the model should follow, regardless of
messages sent by the user. With o1 models and newer, `developer` messages
replace the previous `system` messages.

 */
export interface ChatCompletionRequestDeveloperMessage {
  /** The contents of the developer message. */
  content: ChatCompletionRequestDeveloperMessageContent;
  /** The role of the messages author, in this case `developer`. */
  role: ChatCompletionRequestDeveloperMessageRole;
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

/**
 * The role of the messages author, in this case `function`.
 */
export type ChatCompletionRequestFunctionMessageRole = typeof ChatCompletionRequestFunctionMessageRole[keyof typeof ChatCompletionRequestFunctionMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestFunctionMessageRole = {
  function: 'function',
} as const;

export type ChatCompletionRequestFunctionMessageContent = string | null;

/**
 * @deprecated
 */
export interface ChatCompletionRequestFunctionMessage {
  /** The role of the messages author, in this case `function`. */
  role: ChatCompletionRequestFunctionMessageRole;
  content: ChatCompletionRequestFunctionMessageContent;
  /** The name of the function to call. */
  name: string;
}

export type ChatCompletionRequestMessage = ChatCompletionRequestDeveloperMessage | ChatCompletionRequestSystemMessage | ChatCompletionRequestUserMessage | ChatCompletionRequestAssistantMessage | ChatCompletionRequestToolMessage | ChatCompletionRequestFunctionMessage;

/**
 * The type of the content part. Always `input_audio`.
 */
export type ChatCompletionRequestMessageContentPartAudioType = typeof ChatCompletionRequestMessageContentPartAudioType[keyof typeof ChatCompletionRequestMessageContentPartAudioType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartAudioType = {
  input_audio: 'input_audio',
} as const;

/**
 * The format of the encoded audio data. Currently supports "wav" and "mp3".

 */
export type ChatCompletionRequestMessageContentPartAudioInputAudioFormat = typeof ChatCompletionRequestMessageContentPartAudioInputAudioFormat[keyof typeof ChatCompletionRequestMessageContentPartAudioInputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartAudioInputAudioFormat = {
  wav: 'wav',
  mp3: 'mp3',
} as const;

export type ChatCompletionRequestMessageContentPartAudioInputAudio = {
  /** Base64 encoded audio data. */
  data: string;
  /** The format of the encoded audio data. Currently supports "wav" and "mp3".
 */
  format: ChatCompletionRequestMessageContentPartAudioInputAudioFormat;
};

/**
 * Learn about [audio inputs](https://platform.openai.com/docs/guides/audio).

 */
export interface ChatCompletionRequestMessageContentPartAudio {
  /** The type of the content part. Always `input_audio`. */
  type: ChatCompletionRequestMessageContentPartAudioType;
  input_audio: ChatCompletionRequestMessageContentPartAudioInputAudio;
}

/**
 * The type of the content part. Always `file`.
 */
export type ChatCompletionRequestMessageContentPartFileType = typeof ChatCompletionRequestMessageContentPartFileType[keyof typeof ChatCompletionRequestMessageContentPartFileType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartFileType = {
  file: 'file',
} as const;

export type ChatCompletionRequestMessageContentPartFileFile = {
  /** The name of the file, used when passing the file to the model as a
string.
 */
  filename?: string;
  /** The base64 encoded file data, used when passing the file to the model
as a string.
 */
  file_data?: string;
  /** The ID of an uploaded file to use as input.
 */
  file_id?: string;
};

/**
 * Learn about [file inputs](https://platform.openai.com/docs/guides/text) for text generation.

 */
export interface ChatCompletionRequestMessageContentPartFile {
  /** The type of the content part. Always `file`. */
  type: ChatCompletionRequestMessageContentPartFileType;
  file: ChatCompletionRequestMessageContentPartFileFile;
}

/**
 * The type of the content part.
 */
export type ChatCompletionRequestMessageContentPartImageType = typeof ChatCompletionRequestMessageContentPartImageType[keyof typeof ChatCompletionRequestMessageContentPartImageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartImageType = {
  image_url: 'image_url',
} as const;

/**
 * Specifies the detail level of the image. Learn more in the [Vision guide](https://platform.openai.com/docs/guides/vision#low-or-high-fidelity-image-understanding).
 */
export type ChatCompletionRequestMessageContentPartImageImageUrlDetail = typeof ChatCompletionRequestMessageContentPartImageImageUrlDetail[keyof typeof ChatCompletionRequestMessageContentPartImageImageUrlDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartImageImageUrlDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type ChatCompletionRequestMessageContentPartImageImageUrl = {
  /** Either a URL of the image or the base64 encoded image data. */
  url: string;
  /** Specifies the detail level of the image. Learn more in the [Vision guide](https://platform.openai.com/docs/guides/vision#low-or-high-fidelity-image-understanding). */
  detail?: ChatCompletionRequestMessageContentPartImageImageUrlDetail;
};

/**
 * Learn about [image inputs](https://platform.openai.com/docs/guides/vision).

 */
export interface ChatCompletionRequestMessageContentPartImage {
  /** The type of the content part. */
  type: ChatCompletionRequestMessageContentPartImageType;
  image_url: ChatCompletionRequestMessageContentPartImageImageUrl;
}

/**
 * The type of the content part.
 */
export type ChatCompletionRequestMessageContentPartRefusalType = typeof ChatCompletionRequestMessageContentPartRefusalType[keyof typeof ChatCompletionRequestMessageContentPartRefusalType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartRefusalType = {
  refusal: 'refusal',
} as const;

export interface ChatCompletionRequestMessageContentPartRefusal {
  /** The type of the content part. */
  type: ChatCompletionRequestMessageContentPartRefusalType;
  /** The refusal message generated by the model. */
  refusal: string;
}

/**
 * The type of the content part.
 */
export type ChatCompletionRequestMessageContentPartTextType = typeof ChatCompletionRequestMessageContentPartTextType[keyof typeof ChatCompletionRequestMessageContentPartTextType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestMessageContentPartTextType = {
  text: 'text',
} as const;

/**
 * Learn about [text inputs](https://platform.openai.com/docs/guides/text-generation).

 */
export interface ChatCompletionRequestMessageContentPartText {
  /** The type of the content part. */
  type: ChatCompletionRequestMessageContentPartTextType;
  /** The text content. */
  text: string;
}

/**
 * The contents of the system message.
 */
export type ChatCompletionRequestSystemMessageContent = string | ChatCompletionRequestSystemMessageContentPart[];

/**
 * The role of the messages author, in this case `system`.
 */
export type ChatCompletionRequestSystemMessageRole = typeof ChatCompletionRequestSystemMessageRole[keyof typeof ChatCompletionRequestSystemMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestSystemMessageRole = {
  system: 'system',
} as const;

/**
 * Developer-provided instructions that the model should follow, regardless of
messages sent by the user. With o1 models and newer, use `developer` messages
for this purpose instead.

 */
export interface ChatCompletionRequestSystemMessage {
  /** The contents of the system message. */
  content: ChatCompletionRequestSystemMessageContent;
  /** The role of the messages author, in this case `system`. */
  role: ChatCompletionRequestSystemMessageRole;
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

export type ChatCompletionRequestSystemMessageContentPart = ChatCompletionRequestMessageContentPartText;

/**
 * The role of the messages author, in this case `tool`.
 */
export type ChatCompletionRequestToolMessageRole = typeof ChatCompletionRequestToolMessageRole[keyof typeof ChatCompletionRequestToolMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestToolMessageRole = {
  tool: 'tool',
} as const;

/**
 * The contents of the tool message.
 */
export type ChatCompletionRequestToolMessageContent = string | ChatCompletionRequestToolMessageContentPart[];

export interface ChatCompletionRequestToolMessage {
  /** The role of the messages author, in this case `tool`. */
  role: ChatCompletionRequestToolMessageRole;
  /** The contents of the tool message. */
  content: ChatCompletionRequestToolMessageContent;
  /** Tool call that this message is responding to. */
  tool_call_id: string;
}

export type ChatCompletionRequestToolMessageContentPart = ChatCompletionRequestMessageContentPartText;

/**
 * The contents of the user message.

 */
export type ChatCompletionRequestUserMessageContent = string | ChatCompletionRequestUserMessageContentPart[];

/**
 * The role of the messages author, in this case `user`.
 */
export type ChatCompletionRequestUserMessageRole = typeof ChatCompletionRequestUserMessageRole[keyof typeof ChatCompletionRequestUserMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRequestUserMessageRole = {
  user: 'user',
} as const;

/**
 * Messages sent by an end user, containing prompts or additional context
information.

 */
export interface ChatCompletionRequestUserMessage {
  /** The contents of the user message.
 */
  content: ChatCompletionRequestUserMessageContent;
  /** The role of the messages author, in this case `user`. */
  role: ChatCompletionRequestUserMessageRole;
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

export type ChatCompletionRequestUserMessageContentPart = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartImage | ChatCompletionRequestMessageContentPartAudio | ChatCompletionRequestMessageContentPartFile;

export type ChatCompletionResponseMessageContent = string | null;

export type ChatCompletionResponseMessageRefusal = string | null;

/**
 * The type of the URL citation. Always `url_citation`.
 */
export type ChatCompletionResponseMessageAnnotationsItemType = typeof ChatCompletionResponseMessageAnnotationsItemType[keyof typeof ChatCompletionResponseMessageAnnotationsItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionResponseMessageAnnotationsItemType = {
  url_citation: 'url_citation',
} as const;

/**
 * A URL citation when using web search.
 */
export type ChatCompletionResponseMessageAnnotationsItemUrlCitation = {
  /** The index of the last character of the URL citation in the message. */
  end_index: number;
  /** The index of the first character of the URL citation in the message. */
  start_index: number;
  /** The URL of the web resource. */
  url: string;
  /** The title of the web resource. */
  title: string;
};

/**
 * A URL citation when using web search.

 */
export type ChatCompletionResponseMessageAnnotationsItem = {
  /** The type of the URL citation. Always `url_citation`. */
  type: ChatCompletionResponseMessageAnnotationsItemType;
  /** A URL citation when using web search. */
  url_citation: ChatCompletionResponseMessageAnnotationsItemUrlCitation;
};

/**
 * The role of the author of this message.
 */
export type ChatCompletionResponseMessageRole = typeof ChatCompletionResponseMessageRole[keyof typeof ChatCompletionResponseMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionResponseMessageRole = {
  assistant: 'assistant',
} as const;

/**
 * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
 * @deprecated
 */
export type ChatCompletionResponseMessageFunctionCall = {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
  /** The name of the function to call. */
  name: string;
};

/**
 * If the audio output modality is requested, this object contains data
about the audio response from the model. [Learn more](https://platform.openai.com/docs/guides/audio).

 */
export type ChatCompletionResponseMessageAudioAnyOf = {
  /** Unique identifier for this audio response. */
  id: string;
  /** The Unix timestamp (in seconds) for when this audio response will
no longer be accessible on the server for use in multi-turn
conversations.
 */
  expires_at: number;
  /** Base64 encoded audio bytes generated by the model, in the format
specified in the request.
 */
  data: string;
  /** Transcript of the audio generated by the model. */
  transcript: string;
};

export type ChatCompletionResponseMessageAudio = ChatCompletionResponseMessageAudioAnyOf | null;

/**
 * A chat completion message generated by the model.
 */
export interface ChatCompletionResponseMessage {
  content: ChatCompletionResponseMessageContent;
  refusal: ChatCompletionResponseMessageRefusal;
  tool_calls?: ChatCompletionMessageToolCalls;
  /** Annotations for the message, when applicable, as when using the
[web search tool](https://platform.openai.com/docs/guides/tools-web-search?api-mode=chat).
 */
  annotations?: ChatCompletionResponseMessageAnnotationsItem[];
  /** The role of the author of this message. */
  role: ChatCompletionResponseMessageRole;
  /**
   * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
   * @deprecated
   */
  function_call?: ChatCompletionResponseMessageFunctionCall;
  audio?: ChatCompletionResponseMessageAudio;
}

/**
 * The role of the author of a message
 */
export type ChatCompletionRole = typeof ChatCompletionRole[keyof typeof ChatCompletionRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionRole = {
  developer: 'developer',
  system: 'system',
  user: 'user',
  assistant: 'assistant',
  tool: 'tool',
  function: 'function',
} as const;

/**
 * Options for streaming response. Only set this when you set `stream: true`.

 */
export type ChatCompletionStreamOptionsAnyOf = {
  /** If set, an additional chunk will be streamed before the `data: [DONE]`
message. The `usage` field on this chunk shows the token usage statistics
for the entire request, and the `choices` field will always be an empty
array.

All other chunks will also include a `usage` field, but with a null
value. **NOTE:** If the stream is interrupted, you may not receive the
final usage chunk which contains the total token usage for the request.
 */
  include_usage?: boolean;
  /** When true, stream obfuscation will be enabled. Stream obfuscation adds
random characters to an `obfuscation` field on streaming delta events to
normalize payload sizes as a mitigation to certain side-channel attacks.
These obfuscation fields are included by default, but add a small amount
of overhead to the data stream. You can set `include_obfuscation` to
false to optimize for bandwidth if you trust the network links between
your application and the OpenAI API.
 */
  include_obfuscation?: boolean;
};

export type ChatCompletionStreamOptions = ChatCompletionStreamOptionsAnyOf | null;

export type ChatCompletionStreamResponseDeltaContent = string | null;

/**
 * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
 * @deprecated
 */
export type ChatCompletionStreamResponseDeltaFunctionCall = {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments?: string;
  /** The name of the function to call. */
  name?: string;
};

/**
 * The role of the author of this message.
 */
export type ChatCompletionStreamResponseDeltaRole = typeof ChatCompletionStreamResponseDeltaRole[keyof typeof ChatCompletionStreamResponseDeltaRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionStreamResponseDeltaRole = {
  developer: 'developer',
  system: 'system',
  user: 'user',
  assistant: 'assistant',
  tool: 'tool',
} as const;

export type ChatCompletionStreamResponseDeltaRefusal = string | null;

/**
 * A chat completion delta generated by streamed model responses.
 */
export interface ChatCompletionStreamResponseDelta {
  content?: ChatCompletionStreamResponseDeltaContent;
  /**
   * Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
   * @deprecated
   */
  function_call?: ChatCompletionStreamResponseDeltaFunctionCall;
  tool_calls?: ChatCompletionMessageToolCallChunk[];
  /** The role of the author of this message. */
  role?: ChatCompletionStreamResponseDeltaRole;
  refusal?: ChatCompletionStreamResponseDeltaRefusal;
}

export type ChatCompletionTokenLogprobBytes = number[] | null;

export type ChatCompletionTokenLogprobTopLogprobsItemBytes = number[] | null;

export type ChatCompletionTokenLogprobTopLogprobsItem = {
  /** The token. */
  token: string;
  /** The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely. */
  logprob: number;
  bytes: ChatCompletionTokenLogprobTopLogprobsItemBytes;
};

export interface ChatCompletionTokenLogprob {
  /** The token. */
  token: string;
  /** The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely. */
  logprob: number;
  bytes: ChatCompletionTokenLogprobBytes;
  /** List of the most likely tokens and their log probability, at this token position. In rare cases, there may be fewer than the number of requested `top_logprobs` returned. */
  top_logprobs: ChatCompletionTokenLogprobTopLogprobsItem[];
}

/**
 * The type of the tool. Currently, only `function` is supported.
 */
export type ChatCompletionToolType = typeof ChatCompletionToolType[keyof typeof ChatCompletionToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatCompletionToolType = {
  function: 'function',
} as const;

/**
 * A function tool that can be used to generate a response.

 */
export interface ChatCompletionTool {
  /** The type of the tool. Currently, only `function` is supported. */
  type: ChatCompletionToolType;
  function: FunctionObject;
}

/**
 * Controls which (if any) tool is called by the model.
`none` means the model will not call any tool and instead generates a message.
`auto` means the model can pick between generating a message or calling one or more tools.
`required` means the model must call one or more tools.
Specifying a particular tool via `{"type": "function", "function": {"name": "my_function"}}` forces the model to call that tool.

`none` is the default when no tools are present. `auto` is the default if tools are present.

 */
export type ChatCompletionToolChoiceOption = 'none' | 'auto' | 'required' | ChatCompletionAllowedToolsChoice | ChatCompletionNamedToolChoice | ChatCompletionNamedToolChoiceCustom;

/**
 * The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. Only applicable if `file_ids` is non-empty.
 */
export type ChunkingStrategyRequestParam = AutoChunkingStrategyRequestParam | StaticChunkingStrategyRequestParam;

/**
 * The type of the code interpreter file output. Always `files`.

 */
export type CodeInterpreterFileOutputType = typeof CodeInterpreterFileOutputType[keyof typeof CodeInterpreterFileOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterFileOutputType = {
  files: 'files',
} as const;

export type CodeInterpreterFileOutputFilesItem = {
  /** The MIME type of the file.
 */
  mime_type: string;
  /** The ID of the file.
 */
  file_id: string;
};

/**
 * The output of a code interpreter tool call that is a file.

 */
export interface CodeInterpreterFileOutput {
  /** The type of the code interpreter file output. Always `files`.
 */
  type: CodeInterpreterFileOutputType;
  files: CodeInterpreterFileOutputFilesItem[];
}

/**
 * The type of the code interpreter text output. Always `logs`.

 */
export type CodeInterpreterTextOutputType = typeof CodeInterpreterTextOutputType[keyof typeof CodeInterpreterTextOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterTextOutputType = {
  logs: 'logs',
} as const;

/**
 * The output of a code interpreter tool call that is text.

 */
export interface CodeInterpreterTextOutput {
  /** The type of the code interpreter text output. Always `logs`.
 */
  type: CodeInterpreterTextOutputType;
  /** The logs of the code interpreter tool call.
 */
  logs: string;
}

/**
 * The type of the code interpreter tool. Always `code_interpreter`.

 */
export type CodeInterpreterToolType = typeof CodeInterpreterToolType[keyof typeof CodeInterpreterToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterToolType = {
  code_interpreter: 'code_interpreter',
} as const;

/**
 * The code interpreter container. Can be a container ID or an object that
specifies uploaded file IDs to make available to your code.

 */
export type CodeInterpreterToolContainer = string | CodeInterpreterContainerAuto;

/**
 * A tool that runs Python code to help generate a response to a prompt.

 */
export interface CodeInterpreterTool {
  /** The type of the code interpreter tool. Always `code_interpreter`.
 */
  type: CodeInterpreterToolType;
  /** The code interpreter container. Can be a container ID or an object that
specifies uploaded file IDs to make available to your code.
 */
  container: CodeInterpreterToolContainer;
}

/**
 * The type of the code interpreter tool call. Always `code_interpreter_call`.

 */
export type CodeInterpreterToolCallType = typeof CodeInterpreterToolCallType[keyof typeof CodeInterpreterToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterToolCallType = {
  code_interpreter_call: 'code_interpreter_call',
} as const;

/**
 * The status of the code interpreter tool call. Valid values are `in_progress`, `completed`, `incomplete`, `interpreting`, and `failed`.

 */
export type CodeInterpreterToolCallStatus = typeof CodeInterpreterToolCallStatus[keyof typeof CodeInterpreterToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
  interpreting: 'interpreting',
  failed: 'failed',
} as const;

export type CodeInterpreterToolCallCode = string | null;

export type CodeInterpreterToolCallOutputsAnyOfItem = CodeInterpreterOutputLogs | CodeInterpreterOutputImage;

export type CodeInterpreterToolCallOutputs = CodeInterpreterToolCallOutputsAnyOfItem[] | null;

/**
 * A tool call to run code.

 */
export interface CodeInterpreterToolCall {
  /** The type of the code interpreter tool call. Always `code_interpreter_call`.
 */
  type: CodeInterpreterToolCallType;
  /** The unique ID of the code interpreter tool call.
 */
  id: string;
  /** The status of the code interpreter tool call. Valid values are `in_progress`, `completed`, `incomplete`, `interpreting`, and `failed`.
 */
  status: CodeInterpreterToolCallStatus;
  /** The ID of the container used to run the code.
 */
  container_id: string;
  code: CodeInterpreterToolCallCode;
  outputs: CodeInterpreterToolCallOutputs;
}

/**
 * Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
- `eq`: equals
- `ne`: not equal
- `gt`: greater than
- `gte`: greater than or equal
- `lt`: less than
- `lte`: less than or equal
- `in`: in
- `nin`: not in

 */
export type ComparisonFilterType = typeof ComparisonFilterType[keyof typeof ComparisonFilterType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComparisonFilterType = {
  eq: 'eq',
  ne: 'ne',
  gt: 'gt',
  gte: 'gte',
  lt: 'lt',
  lte: 'lte',
} as const;

/**
 * The value to compare against the attribute key; supports string, number, or boolean types.
 */
export type ComparisonFilterValue = string | number | boolean | ComparisonFilterValueItems[];

/**
 * A filter used to compare a specified attribute key to a given value using a defined comparison operation.

 */
export interface ComparisonFilter {
  /** Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
- `eq`: equals
- `ne`: not equal
- `gt`: greater than
- `gte`: greater than or equal
- `lt`: less than
- `lte`: less than or equal
- `in`: in
- `nin`: not in
 */
  type: ComparisonFilterType;
  /** The key to compare against the value. */
  key: string;
  /** The value to compare against the attribute key; supports string, number, or boolean types. */
  value: ComparisonFilterValue;
}

export interface CompleteUploadRequest {
  /** The ordered list of Part IDs.
 */
  part_ids: string[];
  /** The optional md5 checksum for the file contents to verify if the bytes uploaded matches what you expect.
 */
  md5?: string;
}

/**
 * Breakdown of tokens used in a completion.
 */
export type CompletionUsageCompletionTokensDetails = {
  /** When using Predicted Outputs, the number of tokens in the
prediction that appeared in the completion.
 */
  accepted_prediction_tokens?: number;
  /** Audio input tokens generated by the model. */
  audio_tokens?: number;
  /** Tokens generated by the model for reasoning. */
  reasoning_tokens?: number;
  /** When using Predicted Outputs, the number of tokens in the
prediction that did not appear in the completion. However, like
reasoning tokens, these tokens are still counted in the total
completion tokens for purposes of billing, output, and context window
limits.
 */
  rejected_prediction_tokens?: number;
};

/**
 * Breakdown of tokens used in the prompt.
 */
export type CompletionUsagePromptTokensDetails = {
  /** Audio input tokens present in the prompt. */
  audio_tokens?: number;
  /** Cached tokens present in the prompt. */
  cached_tokens?: number;
};

/**
 * Usage statistics for the completion request.
 */
export interface CompletionUsage {
  /** Number of tokens in the generated completion. */
  completion_tokens: number;
  /** Number of tokens in the prompt. */
  prompt_tokens: number;
  /** Total number of tokens used in the request (prompt + completion). */
  total_tokens: number;
  /** Breakdown of tokens used in a completion. */
  completion_tokens_details?: CompletionUsageCompletionTokensDetails;
  /** Breakdown of tokens used in the prompt. */
  prompt_tokens_details?: CompletionUsagePromptTokensDetails;
}

/**
 * Type of operation: `and` or `or`.
 */
export type CompoundFilterType = typeof CompoundFilterType[keyof typeof CompoundFilterType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CompoundFilterType = {
  and: 'and',
  or: 'or',
} as const;

export type CompoundFilterFiltersItem = ComparisonFilter | unknown;

/**
 * Combine multiple filters using `and` or `or`.
 */
export interface CompoundFilter {
  /** Type of operation: `and` or `or`. */
  type: CompoundFilterType;
  /** Array of filters to combine. Items can be `ComparisonFilter` or `CompoundFilter`. */
  filters: CompoundFilterFiltersItem[];
}

export type ComputerAction = ClickParam | DoubleClickAction | Drag | KeyPressAction | Move | Screenshot | Scroll | Type | Wait;

/**
 * Specifies the event type. For a computer screenshot, this property is
always set to `computer_screenshot`.

 */
export type ComputerScreenshotImageType = typeof ComputerScreenshotImageType[keyof typeof ComputerScreenshotImageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerScreenshotImageType = {
  computer_screenshot: 'computer_screenshot',
} as const;

/**
 * A computer screenshot image used with the computer use tool.

 */
export interface ComputerScreenshotImage {
  /** Specifies the event type. For a computer screenshot, this property is
always set to `computer_screenshot`.
 */
  type: ComputerScreenshotImageType;
  /** The URL of the screenshot image. */
  image_url?: string;
  /** The identifier of an uploaded file that contains the screenshot. */
  file_id?: string;
}

/**
 * The type of the computer call. Always `computer_call`.
 */
export type ComputerToolCallType = typeof ComputerToolCallType[keyof typeof ComputerToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerToolCallType = {
  computer_call: 'computer_call',
} as const;

/**
 * The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.

 */
export type ComputerToolCallStatus = typeof ComputerToolCallStatus[keyof typeof ComputerToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * A tool call to a computer use tool. See the
[computer use guide](https://platform.openai.com/docs/guides/tools-computer-use) for more information.

 */
export interface ComputerToolCall {
  /** The type of the computer call. Always `computer_call`. */
  type: ComputerToolCallType;
  /** The unique ID of the computer call. */
  id: string;
  /** An identifier used when responding to the tool call with output.
 */
  call_id: string;
  action: ComputerAction;
  /** The pending safety checks for the computer call.
 */
  pending_safety_checks: ComputerCallSafetyCheckParam[];
  /** The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
 */
  status: ComputerToolCallStatus;
}

/**
 * The type of the computer tool call output. Always `computer_call_output`.

 */
export type ComputerToolCallOutputType = typeof ComputerToolCallOutputType[keyof typeof ComputerToolCallOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerToolCallOutputType = {
  computer_call_output: 'computer_call_output',
} as const;

/**
 * The status of the message input. One of `in_progress`, `completed`, or
`incomplete`. Populated when input items are returned via API.

 */
export type ComputerToolCallOutputStatus = typeof ComputerToolCallOutputStatus[keyof typeof ComputerToolCallOutputStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerToolCallOutputStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * The output of a computer tool call.

 */
export interface ComputerToolCallOutput {
  /** The type of the computer tool call output. Always `computer_call_output`.
 */
  type: ComputerToolCallOutputType;
  /** The ID of the computer tool call output.
 */
  id?: string;
  /** The ID of the computer tool call that produced the output.
 */
  call_id: string;
  /** The safety checks reported by the API that have been acknowledged by the
developer.
 */
  acknowledged_safety_checks?: ComputerCallSafetyCheckParam[];
  output: ComputerScreenshotImage;
  /** The status of the message input. One of `in_progress`, `completed`, or
`incomplete`. Populated when input items are returned via API.
 */
  status?: ComputerToolCallOutputStatus;
}

export type ComputerToolCallOutputResourceAllOf = {
  /** The unique ID of the computer call tool output.
 */
  id: string;
};

export type ComputerToolCallOutputResource = ComputerToolCallOutput & ComputerToolCallOutputResourceAllOf;

export interface ContainerFileListResource {
  /** The type of object returned, must be 'list'. */
  object: 'list';
  /** A list of container files. */
  data: ContainerFileResource[];
  /** The ID of the first file in the list. */
  first_id: string;
  /** The ID of the last file in the list. */
  last_id: string;
  /** Whether there are more files available. */
  has_more: boolean;
}

export interface ContainerFileResource {
  /** Unique identifier for the file. */
  id: string;
  /** The type of this object (`container.file`). */
  object: 'container.file';
  /** The container this file belongs to. */
  container_id: string;
  /** Unix timestamp (in seconds) when the file was created. */
  created_at: number;
  /** Size of the file in bytes. */
  bytes: number;
  /** Path of the file in the container. */
  path: string;
  /** Source of the file (e.g., `user`, `assistant`). */
  source: string;
}

export interface ContainerListResource {
  /** The type of object returned, must be 'list'. */
  object: 'list';
  /** A list of containers. */
  data: ContainerResource[];
  /** The ID of the first container in the list. */
  first_id: string;
  /** The ID of the last container in the list. */
  last_id: string;
  /** Whether there are more containers available. */
  has_more: boolean;
}

/**
 * The reference point for the expiration.
 */
export type ContainerResourceExpiresAfterAnchor = typeof ContainerResourceExpiresAfterAnchor[keyof typeof ContainerResourceExpiresAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ContainerResourceExpiresAfterAnchor = {
  last_active_at: 'last_active_at',
} as const;

/**
 * The container will expire after this time period.
The anchor is the reference point for the expiration.
The minutes is the number of minutes after the anchor before the container expires.

 */
export type ContainerResourceExpiresAfter = {
  /** The reference point for the expiration. */
  anchor?: ContainerResourceExpiresAfterAnchor;
  /** The number of minutes after the anchor before the container expires. */
  minutes?: number;
};

export interface ContainerResource {
  /** Unique identifier for the container. */
  id: string;
  /** The type of this object. */
  object: string;
  /** Name of the container. */
  name: string;
  /** Unix timestamp (in seconds) when the container was created. */
  created_at: number;
  /** Status of the container (e.g., active, deleted). */
  status: string;
  /** The container will expire after this time period.
The anchor is the reference point for the expiration.
The minutes is the number of minutes after the anchor before the container expires.
 */
  expires_after?: ContainerResourceExpiresAfter;
}

/**
 * Multi-modal input and output contents.

 */
export type Content = InputContent | OutputContent;

export type Conversation = ConversationResource;

/**
 * A single item within a conversation. The set of possible types are the same as the `output` type of a [Response object](https://platform.openai.com/docs/api-reference/responses/object#responses/object-output).
 */
export type ConversationItem = Message | FunctionToolCallResource | FunctionToolCallOutputResource | FileSearchToolCall | WebSearchToolCall | ImageGenToolCall | ComputerToolCall | ComputerToolCallOutputResource | ReasoningItem | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | MCPListTools | MCPApprovalRequest | MCPApprovalResponseResource | MCPToolCall | CustomToolCall | CustomToolCallOutput;

/**
 * A list of Conversation items.
 */
export interface ConversationItemList {
  /** The type of object returned, must be `list`. */
  object: 'list';
  /** A list of conversation items. */
  data: ConversationItem[];
  /** Whether there are more items available. */
  has_more: boolean;
  /** The ID of the first item in the list. */
  first_id: string;
  /** The ID of the last item in the list. */
  last_id: string;
}

/**
 * The conversation that this response belongs to. Items from this conversation are prepended to `input_items` for this response request.
Input items and output items from this response are automatically added to this conversation after this response completes.

 */
export type ConversationParam = string | ConversationParam2;

export type CostsResultObject = typeof CostsResultObject[keyof typeof CostsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CostsResultObject = {
  organizationcostsresult: 'organization.costs.result',
} as const;

/**
 * The monetary value in its associated currency.
 */
export type CostsResultAmount = {
  /** The numeric value of the cost. */
  value?: number;
  /** Lowercase ISO-4217 currency e.g. "usd" */
  currency?: string;
};

export type CostsResultLineItem = string | null;

export type CostsResultProjectId = string | null;

/**
 * The aggregated costs details of the specific time bucket.
 */
export interface CostsResult {
  object: CostsResultObject;
  /** The monetary value in its associated currency. */
  amount?: CostsResultAmount;
  line_item?: CostsResultLineItem;
  project_id?: CostsResultProjectId;
}

/**
 * ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.

 */
export type CreateAssistantRequestModel = string | AssistantSupportedModels;

export type CreateAssistantRequestName = string | null;

export type CreateAssistantRequestDescription = string | null;

export type CreateAssistantRequestInstructions = string | null;

export type CreateAssistantRequestToolResourcesAnyOfCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

/**
 * Always `auto`.
 */
export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType = typeof CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType[keyof typeof CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType = {
  auto: 'auto',
} as const;

/**
 * The default strategy. This strategy currently uses a `max_chunk_size_tokens` of `800` and `chunk_overlap_tokens` of `400`.
 */
export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOf = {
  /** Always `auto`. */
  type: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType;
};

/**
 * Always `static`.
 */
export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType = typeof CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType[keyof typeof CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType = {
  static: 'static',
} as const;

export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeStatic = {
  /**
   * The maximum number of tokens in each chunk. The default value is `800`. The minimum value is `100` and the maximum value is `4096`.
   * @minimum 100
   * @maximum 4096
   */
  max_chunk_size_tokens: number;
  /** The number of tokens that overlap between chunks. The default value is `400`.

Note that the overlap must not exceed half of `max_chunk_size_tokens`.
 */
  chunk_overlap_tokens: number;
};

export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThree = {
  /** Always `static`. */
  type: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType;
  static: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeStatic;
};

/**
 * The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy.
 */
export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategy = CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOf | CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThree;

export type CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItem = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs to add to the vector store. There can be a maximum of 10000 files in a vector store.

   * @maxItems 10000
   */
  file_ids?: string[];
  /** The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. */
  chunking_strategy?: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategy;
  metadata?: Metadata;
};

export type CreateAssistantRequestToolResourcesAnyOfFileSearch = (unknown & {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
  /**
   * A helper to create a [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) with file_ids and attach it to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_stores?: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItem[];
}) | (unknown & {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
  /**
   * A helper to create a [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) with file_ids and attach it to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_stores?: CreateAssistantRequestToolResourcesAnyOfFileSearchVectorStoresItem[];
});

/**
 * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type CreateAssistantRequestToolResourcesAnyOf = {
  code_interpreter?: CreateAssistantRequestToolResourcesAnyOfCodeInterpreter;
  file_search?: CreateAssistantRequestToolResourcesAnyOfFileSearch;
};

export type CreateAssistantRequestToolResources = CreateAssistantRequestToolResourcesAnyOf | null;

export type CreateAssistantRequestTemperature = number | null;

export type CreateAssistantRequestTopP = number | null;

export type CreateAssistantRequestResponseFormat = AssistantsApiResponseFormatOption | null;

export interface CreateAssistantRequest {
  /** ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
 */
  model: CreateAssistantRequestModel;
  name?: CreateAssistantRequestName;
  description?: CreateAssistantRequestDescription;
  instructions?: CreateAssistantRequestInstructions;
  reasoning_effort?: ReasoningEffort;
  /**
   * A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`.

   * @maxItems 128
   */
  tools?: AssistantTool[];
  tool_resources?: CreateAssistantRequestToolResources;
  metadata?: Metadata;
  temperature?: CreateAssistantRequestTemperature;
  top_p?: CreateAssistantRequestTopP;
  response_format?: CreateAssistantRequestResponseFormat;
}

/**
 * The type of location approximation. Always `approximate`.

 */
export type CreateChatCompletionRequestAllOfWebSearchOptionsUserLocationType = typeof CreateChatCompletionRequestAllOfWebSearchOptionsUserLocationType[keyof typeof CreateChatCompletionRequestAllOfWebSearchOptionsUserLocationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionRequestAllOfWebSearchOptionsUserLocationType = {
  approximate: 'approximate',
} as const;

/**
 * Approximate location parameters for the search.

 * @nullable
 */
export type CreateChatCompletionRequestAllOfWebSearchOptionsUserLocation = {
  /** The type of location approximation. Always `approximate`.
 */
  type: CreateChatCompletionRequestAllOfWebSearchOptionsUserLocationType;
  approximate: WebSearchLocation;
} | null;

/**
 * This tool searches the web for relevant results to use in a response.
Learn more about the [web search tool](https://platform.openai.com/docs/guides/tools-web-search?api-mode=chat).

 */
export type CreateChatCompletionRequestAllOfWebSearchOptions = {
  /**
   * Approximate location parameters for the search.

   * @nullable
   */
  user_location?: CreateChatCompletionRequestAllOfWebSearchOptionsUserLocation;
  search_context_size?: WebSearchContextSize;
};

/**
 * An object specifying the format that the model must output.

Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
Structured Outputs which ensures the model will match your supplied JSON
schema. Learn more in the [Structured Outputs
guide](https://platform.openai.com/docs/guides/structured-outputs).

Setting to `{ "type": "json_object" }` enables the older JSON mode, which
ensures the message the model generates is valid JSON. Using `json_schema`
is preferred for models that support it.

 */
export type CreateChatCompletionRequestAllOfResponseFormat = ResponseFormatText | ResponseFormatJsonSchema | ResponseFormatJsonObject;

/**
 * Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`,
`opus`, or `pcm16`.

 */
export type CreateChatCompletionRequestAllOfAudioFormat = typeof CreateChatCompletionRequestAllOfAudioFormat[keyof typeof CreateChatCompletionRequestAllOfAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionRequestAllOfAudioFormat = {
  wav: 'wav',
  aac: 'aac',
  mp3: 'mp3',
  flac: 'flac',
  opus: 'opus',
  pcm16: 'pcm16',
} as const;

/**
 * Parameters for audio output. Required when audio output is requested with
`modalities: ["audio"]`. [Learn more](https://platform.openai.com/docs/guides/audio).

 * @nullable
 */
export type CreateChatCompletionRequestAllOfAudio = {
  /** The voice the model uses to respond. Supported voices are
`alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `nova`, `onyx`, `sage`, and `shimmer`.
 */
  voice: VoiceIdsShared;
  /** Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`,
`opus`, or `pcm16`.
 */
  format: CreateChatCompletionRequestAllOfAudioFormat;
} | null;

/**
 * Modify the likelihood of specified tokens appearing in the completion.

Accepts a JSON object that maps tokens (specified by their token ID in the
tokenizer) to an associated bias value from -100 to 100. Mathematically,
the bias is added to the logits generated by the model prior to sampling.
The exact effect will vary per model, but values between -1 and 1 should
decrease or increase likelihood of selection; values like -100 or 100
should result in a ban or exclusive selection of the relevant token.

 * @nullable
 */
export type CreateChatCompletionRequestAllOfLogitBias = {[key: string]: number} | null;

/**
 * Configuration for a [Predicted Output](https://platform.openai.com/docs/guides/predicted-outputs),
which can greatly improve response times when large parts of the model
response are known ahead of time. This is most common when you are
regenerating a file with only minor changes to most of the content.

 * @nullable
 */
export type CreateChatCompletionRequestAllOfPrediction = PredictionContent | null;

export type CreateChatCompletionRequestAllOfToolsItem = ChatCompletionTool | CustomToolChatCompletions;

/**
 * Deprecated in favor of `tool_choice`.

Controls which (if any) function is called by the model.

`none` means the model will not call a function and instead generates a
message.

`auto` means the model can pick between generating a message or calling a
function.

Specifying a particular function via `{"name": "my_function"}` forces the
model to call that function.

`none` is the default when no functions are present. `auto` is the default
if functions are present.

 * @deprecated
 */
export type CreateChatCompletionRequestAllOfFunctionCall = 'none' | 'auto' | ChatCompletionFunctionCallOption;

export type CreateChatCompletionRequestAllOf = {
  /**
   * A list of messages comprising the conversation so far. Depending on the
[model](https://platform.openai.com/docs/models) you use, different message types (modalities) are
supported, like [text](https://platform.openai.com/docs/guides/text-generation),
[images](https://platform.openai.com/docs/guides/vision), and [audio](https://platform.openai.com/docs/guides/audio).

   * @minItems 1
   */
  messages: ChatCompletionRequestMessage[];
  /** Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI
offers a wide range of models with different capabilities, performance
characteristics, and price points. Refer to the [model guide](https://platform.openai.com/docs/models)
to browse and compare available models.
 */
  model: ModelIdsShared;
  modalities?: ResponseModalities;
  verbosity?: Verbosity;
  reasoning_effort?: ReasoningEffort;
  /**
   * An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and [reasoning tokens](https://platform.openai.com/docs/guides/reasoning).

   * @nullable
   */
  max_completion_tokens?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on
their existing frequency in the text so far, decreasing the model's
likelihood to repeat the same line verbatim.

   * @minimum -2
   * @maximum 2
   * @nullable
   */
  frequency_penalty?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on
whether they appear in the text so far, increasing the model's likelihood
to talk about new topics.

   * @minimum -2
   * @maximum 2
   * @nullable
   */
  presence_penalty?: number | null;
  /** This tool searches the web for relevant results to use in a response.
Learn more about the [web search tool](https://platform.openai.com/docs/guides/tools-web-search?api-mode=chat).
 */
  web_search_options?: CreateChatCompletionRequestAllOfWebSearchOptions;
  /**
   * An integer between 0 and 20 specifying the number of most likely tokens to
return at each token position, each with an associated log probability.
`logprobs` must be set to `true` if this parameter is used.

   * @minimum 0
   * @maximum 20
   * @nullable
   */
  top_logprobs?: number | null;
  /** An object specifying the format that the model must output.

Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
Structured Outputs which ensures the model will match your supplied JSON
schema. Learn more in the [Structured Outputs
guide](https://platform.openai.com/docs/guides/structured-outputs).

Setting to `{ "type": "json_object" }` enables the older JSON mode, which
ensures the message the model generates is valid JSON. Using `json_schema`
is preferred for models that support it.
 */
  response_format?: CreateChatCompletionRequestAllOfResponseFormat;
  /**
   * Parameters for audio output. Required when audio output is requested with
`modalities: ["audio"]`. [Learn more](https://platform.openai.com/docs/guides/audio).

   * @nullable
   */
  audio?: CreateChatCompletionRequestAllOfAudio;
  /**
   * Whether or not to store the output of this chat completion request for
use in our [model distillation](https://platform.openai.com/docs/guides/distillation) or
[evals](https://platform.openai.com/docs/guides/evals) products.

Supports text and image inputs. Note: image inputs over 8MB will be dropped.

   * @nullable
   */
  store?: boolean | null;
  /**
   * If set to true, the model response data will be streamed to the client
as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).
See the [Streaming section below](https://platform.openai.com/docs/api-reference/chat/streaming)
for more information, along with the [streaming responses](https://platform.openai.com/docs/guides/streaming-responses)
guide for more information on how to handle the streaming events.

   * @nullable
   */
  stream?: boolean | null;
  stop?: StopConfiguration;
  /**
   * Modify the likelihood of specified tokens appearing in the completion.

Accepts a JSON object that maps tokens (specified by their token ID in the
tokenizer) to an associated bias value from -100 to 100. Mathematically,
the bias is added to the logits generated by the model prior to sampling.
The exact effect will vary per model, but values between -1 and 1 should
decrease or increase likelihood of selection; values like -100 or 100
should result in a ban or exclusive selection of the relevant token.

   * @nullable
   */
  logit_bias?: CreateChatCompletionRequestAllOfLogitBias;
  /**
   * Whether to return log probabilities of the output tokens or not. If true,
returns the log probabilities of each output token returned in the
`content` of `message`.

   * @nullable
   */
  logprobs?: boolean | null;
  /**
   * The maximum number of [tokens](/tokenizer) that can be generated in the
chat completion. This value can be used to control
[costs](https://openai.com/api/pricing/) for text generated via API.

This value is now deprecated in favor of `max_completion_tokens`, and is
not compatible with [o-series models](https://platform.openai.com/docs/guides/reasoning).

   * @deprecated
   * @nullable
   */
  max_tokens?: number | null;
  /**
   * How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.
   * @minimum 1
   * @maximum 128
   * @nullable
   */
  n?: number | null;
  /**
   * Configuration for a [Predicted Output](https://platform.openai.com/docs/guides/predicted-outputs),
which can greatly improve response times when large parts of the model
response are known ahead of time. This is most common when you are
regenerating a file with only minor changes to most of the content.

   * @nullable
   */
  prediction?: CreateChatCompletionRequestAllOfPrediction;
  /**
   * This feature is in Beta.
If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.

   * @deprecated
   * @minimum -9223372036854776000
   * @maximum 9223372036854776000
   * @nullable
   */
  seed?: number | null;
  stream_options?: ChatCompletionStreamOptions;
  /** A list of tools the model may call. You can provide either
[custom tools](https://platform.openai.com/docs/guides/function-calling#custom-tools) or
[function tools](https://platform.openai.com/docs/guides/function-calling).
 */
  tools?: CreateChatCompletionRequestAllOfToolsItem[];
  tool_choice?: ChatCompletionToolChoiceOption;
  parallel_tool_calls?: ParallelToolCalls;
  /**
   * Deprecated in favor of `tool_choice`.

Controls which (if any) function is called by the model.

`none` means the model will not call a function and instead generates a
message.

`auto` means the model can pick between generating a message or calling a
function.

Specifying a particular function via `{"name": "my_function"}` forces the
model to call that function.

`none` is the default when no functions are present. `auto` is the default
if functions are present.

   * @deprecated
   */
  function_call?: CreateChatCompletionRequestAllOfFunctionCall;
  /**
   * Deprecated in favor of `tools`.

A list of functions the model may generate JSON inputs for.

   * @deprecated
   * @minItems 1
   * @maxItems 128
   */
  functions?: ChatCompletionFunctions[];
};

export type CreateChatCompletionRequest = CreateModelResponseProperties & CreateChatCompletionRequestAllOf;

/**
 * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
`content_filter` if content was omitted due to a flag from our content filters,
`tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.

 */
export type CreateChatCompletionResponseChoicesItemFinishReason = typeof CreateChatCompletionResponseChoicesItemFinishReason[keyof typeof CreateChatCompletionResponseChoicesItemFinishReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionResponseChoicesItemFinishReason = {
  stop: 'stop',
  length: 'length',
  tool_calls: 'tool_calls',
  content_filter: 'content_filter',
  function_call: 'function_call',
} as const;

export type CreateChatCompletionResponseChoicesItemLogprobsAnyOfContent = ChatCompletionTokenLogprob[] | null;

export type CreateChatCompletionResponseChoicesItemLogprobsAnyOfRefusal = ChatCompletionTokenLogprob[] | null;

/**
 * Log probability information for the choice.
 */
export type CreateChatCompletionResponseChoicesItemLogprobsAnyOf = {
  content: CreateChatCompletionResponseChoicesItemLogprobsAnyOfContent;
  refusal: CreateChatCompletionResponseChoicesItemLogprobsAnyOfRefusal;
};

export type CreateChatCompletionResponseChoicesItemLogprobs = CreateChatCompletionResponseChoicesItemLogprobsAnyOf | null;

export type CreateChatCompletionResponseChoicesItem = {
  /** The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
`content_filter` if content was omitted due to a flag from our content filters,
`tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.
 */
  finish_reason: CreateChatCompletionResponseChoicesItemFinishReason;
  /** The index of the choice in the list of choices. */
  index: number;
  message: ChatCompletionResponseMessage;
  logprobs: CreateChatCompletionResponseChoicesItemLogprobs;
};

/**
 * The object type, which is always `chat.completion`.
 */
export type CreateChatCompletionResponseObject = typeof CreateChatCompletionResponseObject[keyof typeof CreateChatCompletionResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionResponseObject = {
  chatcompletion: 'chat.completion',
} as const;

/**
 * Represents a chat completion response returned by model, based on the provided input.
 */
export interface CreateChatCompletionResponse {
  /** A unique identifier for the chat completion. */
  id: string;
  /** A list of chat completion choices. Can be more than one if `n` is greater than 1. */
  choices: CreateChatCompletionResponseChoicesItem[];
  /** The Unix timestamp (in seconds) of when the chat completion was created. */
  created: number;
  /** The model used for the chat completion. */
  model: string;
  service_tier?: ServiceTier;
  /**
   * This fingerprint represents the backend configuration that the model runs with.

Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.

   * @deprecated
   */
  system_fingerprint?: string;
  /** The object type, which is always `chat.completion`. */
  object: CreateChatCompletionResponseObject;
  usage?: CompletionUsage;
}

/**
 * Log probability information for the choice.
 * @nullable
 */
export type CreateChatCompletionStreamResponseChoicesItemLogprobs = {
  /**
   * A list of message content tokens with log probability information.
   * @nullable
   */
  content: ChatCompletionTokenLogprob[] | null;
  /**
   * A list of message refusal tokens with log probability information.
   * @nullable
   */
  refusal: ChatCompletionTokenLogprob[] | null;
} | null;

/**
 * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
`content_filter` if content was omitted due to a flag from our content filters,
`tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.

 * @nullable
 */
export type CreateChatCompletionStreamResponseChoicesItemFinishReason = typeof CreateChatCompletionStreamResponseChoicesItemFinishReason[keyof typeof CreateChatCompletionStreamResponseChoicesItemFinishReason] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionStreamResponseChoicesItemFinishReason = {
  stop: 'stop',
  length: 'length',
  tool_calls: 'tool_calls',
  content_filter: 'content_filter',
  function_call: 'function_call',
} as const;

export type CreateChatCompletionStreamResponseChoicesItem = {
  delta: ChatCompletionStreamResponseDelta;
  /**
   * Log probability information for the choice.
   * @nullable
   */
  logprobs?: CreateChatCompletionStreamResponseChoicesItemLogprobs;
  /**
   * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
`content_filter` if content was omitted due to a flag from our content filters,
`tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.

   * @nullable
   */
  finish_reason: CreateChatCompletionStreamResponseChoicesItemFinishReason;
  /** The index of the choice in the list of choices. */
  index: number;
};

/**
 * The object type, which is always `chat.completion.chunk`.
 */
export type CreateChatCompletionStreamResponseObject = typeof CreateChatCompletionStreamResponseObject[keyof typeof CreateChatCompletionStreamResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateChatCompletionStreamResponseObject = {
  chatcompletionchunk: 'chat.completion.chunk',
} as const;

/**
 * Represents a streamed chunk of a chat completion response returned
by the model, based on the provided input.
[Learn more](https://platform.openai.com/docs/guides/streaming-responses).

 */
export interface CreateChatCompletionStreamResponse {
  /** A unique identifier for the chat completion. Each chunk has the same ID. */
  id: string;
  /** A list of chat completion choices. Can contain more than one elements if `n` is greater than 1. Can also be empty for the
last chunk if you set `stream_options: {"include_usage": true}`.
 */
  choices: CreateChatCompletionStreamResponseChoicesItem[];
  /** The Unix timestamp (in seconds) of when the chat completion was created. Each chunk has the same timestamp. */
  created: number;
  /** The model to generate the completion. */
  model: string;
  service_tier?: ServiceTier;
  /**
   * This fingerprint represents the backend configuration that the model runs with.
Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.

   * @deprecated
   */
  system_fingerprint?: string;
  /** The object type, which is always `chat.completion.chunk`. */
  object: CreateChatCompletionStreamResponseObject;
  /**
   * An optional field that will only be present when you set
`stream_options: {"include_usage": true}` in your request. When present, it
contains a null value **except for the last chunk** which contains the
token usage statistics for the entire request.

**NOTE:** If the stream is interrupted or cancelled, you may not
receive the final usage chunk which contains the total token usage for
the request.

   * @nullable
   */
  usage?: CompletionUsage;
}

/**
 * ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.

 */
export type CreateCompletionRequestModel = string | 'gpt-3.5-turbo-instruct' | 'davinci-002' | 'babbage-002';

/**
 * The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.

Note that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document.

 * @nullable
 */
export type CreateCompletionRequestPrompt = string | string[] | number[] | number[][] | null;

/**
 * Modify the likelihood of specified tokens appearing in the completion.

Accepts a JSON object that maps tokens (specified by their token ID in the GPT tokenizer) to an associated bias value from -100 to 100. You can use this [tokenizer tool](/tokenizer?view=bpe) to convert text to token IDs. Mathematically, the bias is added to the logits generated by the model prior to sampling. The exact effect will vary per model, but values between -1 and 1 should decrease or increase likelihood of selection; values like -100 or 100 should result in a ban or exclusive selection of the relevant token.

As an example, you can pass `{"50256": -100}` to prevent the <|endoftext|> token from being generated.

 * @nullable
 */
export type CreateCompletionRequestLogitBias = {[key: string]: number} | null;

export interface CreateCompletionRequest {
  /** ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
 */
  model: CreateCompletionRequestModel;
  /**
   * The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.

Note that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document.

   * @nullable
   */
  prompt: CreateCompletionRequestPrompt;
  /**
   * Generates `best_of` completions server-side and returns the "best" (the one with the highest log probability per token). Results cannot be streamed.

When used with `n`, `best_of` controls the number of candidate completions and `n` specifies how many to return – `best_of` must be greater than `n`.

**Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.

   * @minimum 0
   * @maximum 20
   * @nullable
   */
  best_of?: number | null;
  /**
   * Echo back the prompt in addition to the completion

   * @nullable
   */
  echo?: boolean | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so far, decreasing the model's likelihood to repeat the same line verbatim.

[See more information about frequency and presence penalties.](https://platform.openai.com/docs/guides/text-generation)

   * @minimum -2
   * @maximum 2
   * @nullable
   */
  frequency_penalty?: number | null;
  /**
   * Modify the likelihood of specified tokens appearing in the completion.

Accepts a JSON object that maps tokens (specified by their token ID in the GPT tokenizer) to an associated bias value from -100 to 100. You can use this [tokenizer tool](/tokenizer?view=bpe) to convert text to token IDs. Mathematically, the bias is added to the logits generated by the model prior to sampling. The exact effect will vary per model, but values between -1 and 1 should decrease or increase likelihood of selection; values like -100 or 100 should result in a ban or exclusive selection of the relevant token.

As an example, you can pass `{"50256": -100}` to prevent the <|endoftext|> token from being generated.

   * @nullable
   */
  logit_bias?: CreateCompletionRequestLogitBias;
  /**
   * Include the log probabilities on the `logprobs` most likely output tokens, as well the chosen tokens. For example, if `logprobs` is 5, the API will return a list of the 5 most likely tokens. The API will always return the `logprob` of the sampled token, so there may be up to `logprobs+1` elements in the response.

The maximum value for `logprobs` is 5.

   * @minimum 0
   * @maximum 5
   * @nullable
   */
  logprobs?: number | null;
  /**
   * The maximum number of [tokens](/tokenizer) that can be generated in the completion.

The token count of your prompt plus `max_tokens` cannot exceed the model's context length. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens.

   * @minimum 0
   * @nullable
   */
  max_tokens?: number | null;
  /**
   * How many completions to generate for each prompt.

**Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.

   * @minimum 1
   * @maximum 128
   * @nullable
   */
  n?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far, increasing the model's likelihood to talk about new topics.

[See more information about frequency and presence penalties.](https://platform.openai.com/docs/guides/text-generation)

   * @minimum -2
   * @maximum 2
   * @nullable
   */
  presence_penalty?: number | null;
  /**
   * If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.

Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.

   * @nullable
   */
  seed?: number | null;
  stop?: StopConfiguration;
  /**
   * Whether to stream back partial progress. If set, tokens will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available, with the stream terminated by a `data: [DONE]` message. [Example Python code](https://cookbook.openai.com/examples/how_to_stream_completions).

   * @nullable
   */
  stream?: boolean | null;
  stream_options?: ChatCompletionStreamOptions;
  /**
   * The suffix that comes after a completion of inserted text.

This parameter is only supported for `gpt-3.5-turbo-instruct`.

   * @nullable
   */
  suffix?: string | null;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

We generally recommend altering this or `top_p` but not both.

   * @minimum 0
   * @maximum 2
   * @nullable
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or `temperature` but not both.

   * @minimum 0
   * @maximum 1
   * @nullable
   */
  top_p?: number | null;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
 */
  user?: string;
}

/**
 * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
or `content_filter` if content was omitted due to a flag from our content filters.

 */
export type CreateCompletionResponseChoicesItemFinishReason = typeof CreateCompletionResponseChoicesItemFinishReason[keyof typeof CreateCompletionResponseChoicesItemFinishReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateCompletionResponseChoicesItemFinishReason = {
  stop: 'stop',
  length: 'length',
  content_filter: 'content_filter',
} as const;

export type CreateCompletionResponseChoicesItemLogprobsAnyOfTopLogprobsItem = {[key: string]: number};

export type CreateCompletionResponseChoicesItemLogprobsAnyOf = {
  text_offset?: number[];
  token_logprobs?: number[];
  tokens?: string[];
  top_logprobs?: CreateCompletionResponseChoicesItemLogprobsAnyOfTopLogprobsItem[];
};

export type CreateCompletionResponseChoicesItemLogprobs = CreateCompletionResponseChoicesItemLogprobsAnyOf | null;

export type CreateCompletionResponseChoicesItem = {
  /** The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
`length` if the maximum number of tokens specified in the request was reached,
or `content_filter` if content was omitted due to a flag from our content filters.
 */
  finish_reason: CreateCompletionResponseChoicesItemFinishReason;
  index: number;
  logprobs: CreateCompletionResponseChoicesItemLogprobs;
  text: string;
};

/**
 * The object type, which is always "text_completion"
 */
export type CreateCompletionResponseObject = typeof CreateCompletionResponseObject[keyof typeof CreateCompletionResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateCompletionResponseObject = {
  text_completion: 'text_completion',
} as const;

/**
 * Represents a completion response from the API. Note: both the streamed and non-streamed response objects share the same shape (unlike the chat endpoint).

 */
export interface CreateCompletionResponse {
  /** A unique identifier for the completion. */
  id: string;
  /** The list of completion choices the model generated for the input prompt. */
  choices: CreateCompletionResponseChoicesItem[];
  /** The Unix timestamp (in seconds) of when the completion was created. */
  created: number;
  /** The model used for completion. */
  model: string;
  /** This fingerprint represents the backend configuration that the model runs with.

Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
 */
  system_fingerprint?: string;
  /** The object type, which is always "text_completion" */
  object: CreateCompletionResponseObject;
  usage?: CompletionUsage;
}

/**
 * Time anchor for the expiration time. Currently only 'last_active_at' is supported.
 */
export type CreateContainerBodyExpiresAfterAnchor = typeof CreateContainerBodyExpiresAfterAnchor[keyof typeof CreateContainerBodyExpiresAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateContainerBodyExpiresAfterAnchor = {
  last_active_at: 'last_active_at',
} as const;

/**
 * Container expiration time in seconds relative to the 'anchor' time.
 */
export type CreateContainerBodyExpiresAfter = {
  /** Time anchor for the expiration time. Currently only 'last_active_at' is supported. */
  anchor: CreateContainerBodyExpiresAfterAnchor;
  minutes: number;
};

export interface CreateContainerBody {
  /** Name of the container to create. */
  name: string;
  /** IDs of files to copy to the container. */
  file_ids?: string[];
  /** Container expiration time in seconds relative to the 'anchor' time. */
  expires_after?: CreateContainerBodyExpiresAfter;
}

export interface CreateContainerFileBody {
  /** Name of the file to create. */
  file_id?: string;
  /** The File object (not file name) to be uploaded.
 */
  file?: Blob;
}

/**
 * Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request.

 */
export type CreateEmbeddingRequestInput = string | string[] | number[] | number[][];

/**
 * ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.

 */
export type CreateEmbeddingRequestModel = string | 'text-embedding-ada-002' | 'text-embedding-3-small' | 'text-embedding-3-large';

/**
 * The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).
 */
export type CreateEmbeddingRequestEncodingFormat = typeof CreateEmbeddingRequestEncodingFormat[keyof typeof CreateEmbeddingRequestEncodingFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEmbeddingRequestEncodingFormat = {
  float: 'float',
  base64: 'base64',
} as const;

export interface CreateEmbeddingRequest {
  /** Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request.
 */
  input: CreateEmbeddingRequestInput;
  /** ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
 */
  model: CreateEmbeddingRequestModel;
  /** The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/). */
  encoding_format?: CreateEmbeddingRequestEncodingFormat;
  /**
   * The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models.

   * @minimum 1
   */
  dimensions?: number;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
 */
  user?: string;
}

/**
 * The object type, which is always "list".
 */
export type CreateEmbeddingResponseObject = typeof CreateEmbeddingResponseObject[keyof typeof CreateEmbeddingResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEmbeddingResponseObject = {
  list: 'list',
} as const;

/**
 * The usage information for the request.
 */
export type CreateEmbeddingResponseUsage = {
  /** The number of tokens used by the prompt. */
  prompt_tokens: number;
  /** The total number of tokens used by the request. */
  total_tokens: number;
};

export interface CreateEmbeddingResponse {
  /** The list of embeddings generated by the model. */
  data: Embedding[];
  /** The name of the model used to generate the embedding. */
  model: string;
  /** The object type, which is always "list". */
  object: CreateEmbeddingResponseObject;
  /** The usage information for the request. */
  usage: CreateEmbeddingResponseUsage;
}

/**
 * The type of run data source. Always `completions`.
 */
export type CreateEvalCompletionsRunDataSourceType = typeof CreateEvalCompletionsRunDataSourceType[keyof typeof CreateEvalCompletionsRunDataSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalCompletionsRunDataSourceType = {
  completions: 'completions',
} as const;

/**
 * The type of input messages. Always `template`.
 */
export type CreateEvalCompletionsRunDataSourceInputMessagesAnyOfType = typeof CreateEvalCompletionsRunDataSourceInputMessagesAnyOfType[keyof typeof CreateEvalCompletionsRunDataSourceInputMessagesAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalCompletionsRunDataSourceInputMessagesAnyOfType = {
  template: 'template',
} as const;

export type CreateEvalCompletionsRunDataSourceInputMessagesAnyOfTemplateItem = EasyInputMessage | EvalItem;

export type CreateEvalCompletionsRunDataSourceInputMessagesAnyOf = {
  /** The type of input messages. Always `template`. */
  type: CreateEvalCompletionsRunDataSourceInputMessagesAnyOfType;
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  template: CreateEvalCompletionsRunDataSourceInputMessagesAnyOfTemplateItem[];
};

/**
 * The type of input messages. Always `item_reference`.
 */
export type CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFourType = typeof CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFourType[keyof typeof CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFourType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFourType = {
  item_reference: 'item_reference',
} as const;

export type CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFour = {
  /** The type of input messages. Always `item_reference`. */
  type: CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFourType;
  /** A reference to a variable in the `item` namespace. Ie, "item.input_trajectory" */
  item_reference: string;
};

/**
 * Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace.
 */
export type CreateEvalCompletionsRunDataSourceInputMessages = CreateEvalCompletionsRunDataSourceInputMessagesAnyOf | CreateEvalCompletionsRunDataSourceInputMessagesAnyOfFour;

/**
 * An object specifying the format that the model must output.

Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
Structured Outputs which ensures the model will match your supplied JSON
schema. Learn more in the [Structured Outputs
guide](https://platform.openai.com/docs/guides/structured-outputs).

Setting to `{ "type": "json_object" }` enables the older JSON mode, which
ensures the message the model generates is valid JSON. Using `json_schema`
is preferred for models that support it.

 */
export type CreateEvalCompletionsRunDataSourceSamplingParamsResponseFormat = ResponseFormatText | ResponseFormatJsonSchema | ResponseFormatJsonObject;

export type CreateEvalCompletionsRunDataSourceSamplingParams = {
  reasoning_effort?: ReasoningEffort;
  /** A higher temperature increases randomness in the outputs. */
  temperature?: number;
  /** The maximum number of tokens in the generated output. */
  max_completion_tokens?: number;
  /** An alternative to temperature for nucleus sampling; 1.0 includes all tokens. */
  top_p?: number;
  /** A seed value to initialize the randomness, during sampling. */
  seed?: number;
  /** An object specifying the format that the model must output.

Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
Structured Outputs which ensures the model will match your supplied JSON
schema. Learn more in the [Structured Outputs
guide](https://platform.openai.com/docs/guides/structured-outputs).

Setting to `{ "type": "json_object" }` enables the older JSON mode, which
ensures the message the model generates is valid JSON. Using `json_schema`
is preferred for models that support it.
 */
  response_format?: CreateEvalCompletionsRunDataSourceSamplingParamsResponseFormat;
  /** A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported.
 */
  tools?: ChatCompletionTool[];
};

/**
 * Determines what populates the `item` namespace in this run's data source.
 */
export type CreateEvalCompletionsRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource | EvalStoredCompletionsSource;

/**
 * A CompletionsRunDataSource object describing a model sampling configuration.

 */
export interface CreateEvalCompletionsRunDataSource {
  /** The type of run data source. Always `completions`. */
  type: CreateEvalCompletionsRunDataSourceType;
  /** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
  input_messages?: CreateEvalCompletionsRunDataSourceInputMessages;
  sampling_params?: CreateEvalCompletionsRunDataSourceSamplingParams;
  /** The name of the model to use for generating completions (e.g. "o3-mini"). */
  model?: string;
  /** Determines what populates the `item` namespace in this run's data source. */
  source: CreateEvalCompletionsRunDataSourceSource;
}

/**
 * The type of data source. Always `custom`.
 */
export type CreateEvalCustomDataSourceConfigType = typeof CreateEvalCustomDataSourceConfigType[keyof typeof CreateEvalCustomDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalCustomDataSourceConfigType = {
  custom: 'custom',
} as const;

/**
 * The json schema for each row in the data source.
 */
export type CreateEvalCustomDataSourceConfigItemSchema = { [key: string]: unknown };

/**
 * A CustomDataSourceConfig object that defines the schema for the data source used for the evaluation runs.
This schema is used to define the shape of the data that will be:
- Used to define your testing criteria and
- What data is required when creating a run

 */
export interface CreateEvalCustomDataSourceConfig {
  /** The type of data source. Always `custom`. */
  type: CreateEvalCustomDataSourceConfigType;
  /** The json schema for each row in the data source. */
  item_schema: CreateEvalCustomDataSourceConfigItemSchema;
  /** Whether the eval should expect you to populate the sample namespace (ie, by generating responses off of your data source) */
  include_sample_schema?: boolean;
}

export type CreateEvalItemAnyOf = {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role: string;
  /** The content of the message. */
  content: string;
};

/**
 * A chat message that makes up the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}.
 */
export type CreateEvalItem = CreateEvalItemAnyOf | EvalItem;

/**
 * The type of data source. Always `jsonl`.
 */
export type CreateEvalJsonlRunDataSourceType = typeof CreateEvalJsonlRunDataSourceType[keyof typeof CreateEvalJsonlRunDataSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalJsonlRunDataSourceType = {
  jsonl: 'jsonl',
} as const;

/**
 * Determines what populates the `item` namespace in the data source.
 */
export type CreateEvalJsonlRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource;

/**
 * A JsonlRunDataSource object with that specifies a JSONL file that matches the eval

 */
export interface CreateEvalJsonlRunDataSource {
  /** The type of data source. Always `jsonl`. */
  type: CreateEvalJsonlRunDataSourceType;
  /** Determines what populates the `item` namespace in the data source. */
  source: CreateEvalJsonlRunDataSourceSource;
}

/**
 * The object type, which is always `label_model`.
 */
export type CreateEvalLabelModelGraderType = typeof CreateEvalLabelModelGraderType[keyof typeof CreateEvalLabelModelGraderType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalLabelModelGraderType = {
  label_model: 'label_model',
} as const;

/**
 * A LabelModelGrader object which uses a model to assign labels to each item
in the evaluation.

 */
export interface CreateEvalLabelModelGrader {
  /** The object type, which is always `label_model`. */
  type: CreateEvalLabelModelGraderType;
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. Must support structured outputs. */
  model: string;
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  input: CreateEvalItem[];
  /** The labels to classify to each item in the evaluation. */
  labels: string[];
  /** The labels that indicate a passing result. Must be a subset of labels. */
  passing_labels: string[];
}

/**
 * The type of data source. Always `logs`.
 */
export type CreateEvalLogsDataSourceConfigType = typeof CreateEvalLogsDataSourceConfigType[keyof typeof CreateEvalLogsDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalLogsDataSourceConfigType = {
  logs: 'logs',
} as const;

/**
 * Metadata filters for the logs data source.
 */
export type CreateEvalLogsDataSourceConfigMetadata = { [key: string]: unknown };

/**
 * A data source config which specifies the metadata property of your logs query.
This is usually metadata like `usecase=chatbot` or `prompt-version=v2`, etc.

 */
export interface CreateEvalLogsDataSourceConfig {
  /** The type of data source. Always `logs`. */
  type: CreateEvalLogsDataSourceConfigType;
  /** Metadata filters for the logs data source. */
  metadata?: CreateEvalLogsDataSourceConfigMetadata;
}

/**
 * The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation.
 */
export type CreateEvalRequestDataSourceConfig = CreateEvalCustomDataSourceConfig | CreateEvalLogsDataSourceConfig | CreateEvalStoredCompletionsDataSourceConfig;

export type CreateEvalRequestTestingCriteriaItem = CreateEvalLabelModelGrader | EvalGraderStringCheck | EvalGraderTextSimilarity | EvalGraderPython | EvalGraderScoreModel;

export interface CreateEvalRequest {
  /** The name of the evaluation. */
  name?: string;
  metadata?: Metadata;
  /** The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation. */
  data_source_config: CreateEvalRequestDataSourceConfig;
  /** A list of graders for all eval runs in this group. Graders can reference variables in the data source using double curly braces notation, like `{{item.variable_name}}`. To reference the model's output, use the `sample` namespace (ie, `{{sample.output_text}}`). */
  testing_criteria: CreateEvalRequestTestingCriteriaItem[];
}

/**
 * The type of run data source. Always `responses`.
 */
export type CreateEvalResponsesRunDataSourceType = typeof CreateEvalResponsesRunDataSourceType[keyof typeof CreateEvalResponsesRunDataSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalResponsesRunDataSourceType = {
  responses: 'responses',
} as const;

/**
 * The type of input messages. Always `template`.
 */
export type CreateEvalResponsesRunDataSourceInputMessagesAnyOfType = typeof CreateEvalResponsesRunDataSourceInputMessagesAnyOfType[keyof typeof CreateEvalResponsesRunDataSourceInputMessagesAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalResponsesRunDataSourceInputMessagesAnyOfType = {
  template: 'template',
} as const;

export type CreateEvalResponsesRunDataSourceInputMessagesAnyOfTemplateItemAnyOf = {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role: string;
  /** The content of the message. */
  content: string;
};

export type CreateEvalResponsesRunDataSourceInputMessagesAnyOfTemplateItem = CreateEvalResponsesRunDataSourceInputMessagesAnyOfTemplateItemAnyOf | EvalItem;

export type CreateEvalResponsesRunDataSourceInputMessagesAnyOf = {
  /** The type of input messages. Always `template`. */
  type: CreateEvalResponsesRunDataSourceInputMessagesAnyOfType;
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  template: CreateEvalResponsesRunDataSourceInputMessagesAnyOfTemplateItem[];
};

/**
 * The type of input messages. Always `item_reference`.
 */
export type CreateEvalResponsesRunDataSourceInputMessagesAnyOfFiveType = typeof CreateEvalResponsesRunDataSourceInputMessagesAnyOfFiveType[keyof typeof CreateEvalResponsesRunDataSourceInputMessagesAnyOfFiveType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalResponsesRunDataSourceInputMessagesAnyOfFiveType = {
  item_reference: 'item_reference',
} as const;

export type CreateEvalResponsesRunDataSourceInputMessagesAnyOfFive = {
  /** The type of input messages. Always `item_reference`. */
  type: CreateEvalResponsesRunDataSourceInputMessagesAnyOfFiveType;
  /** A reference to a variable in the `item` namespace. Ie, "item.name" */
  item_reference: string;
};

/**
 * Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace.
 */
export type CreateEvalResponsesRunDataSourceInputMessages = CreateEvalResponsesRunDataSourceInputMessagesAnyOf | CreateEvalResponsesRunDataSourceInputMessagesAnyOfFive;

/**
 * Configuration options for a text response from the model. Can be plain
text or structured JSON data. Learn more:
- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
- [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)

 */
export type CreateEvalResponsesRunDataSourceSamplingParamsText = {
  format?: TextResponseFormatConfiguration;
};

export type CreateEvalResponsesRunDataSourceSamplingParams = {
  reasoning_effort?: ReasoningEffort;
  /** A higher temperature increases randomness in the outputs. */
  temperature?: number;
  /** The maximum number of tokens in the generated output. */
  max_completion_tokens?: number;
  /** An alternative to temperature for nucleus sampling; 1.0 includes all tokens. */
  top_p?: number;
  /** A seed value to initialize the randomness, during sampling. */
  seed?: number;
  /** An array of tools the model may call while generating a response. You
can specify which tool to use by setting the `tool_choice` parameter.

The two categories of tools you can provide the model are:

- **Built-in tools**: Tools that are provided by OpenAI that extend the
  model's capabilities, like [web search](https://platform.openai.com/docs/guides/tools-web-search)
  or [file search](https://platform.openai.com/docs/guides/tools-file-search). Learn more about
  [built-in tools](https://platform.openai.com/docs/guides/tools).
- **Function calls (custom tools)**: Functions that are defined by you,
  enabling the model to call your own code. Learn more about
  [function calling](https://platform.openai.com/docs/guides/function-calling).
 */
  tools?: Tool[];
  /** Configuration options for a text response from the model. Can be plain
text or structured JSON data. Learn more:
- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
- [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)
 */
  text?: CreateEvalResponsesRunDataSourceSamplingParamsText;
};

/**
 * Determines what populates the `item` namespace in this run's data source.
 */
export type CreateEvalResponsesRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource | EvalResponsesSource;

/**
 * A ResponsesRunDataSource object describing a model sampling configuration.

 */
export interface CreateEvalResponsesRunDataSource {
  /** The type of run data source. Always `responses`. */
  type: CreateEvalResponsesRunDataSourceType;
  /** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
  input_messages?: CreateEvalResponsesRunDataSourceInputMessages;
  sampling_params?: CreateEvalResponsesRunDataSourceSamplingParams;
  /** The name of the model to use for generating completions (e.g. "o3-mini"). */
  model?: string;
  /** Determines what populates the `item` namespace in this run's data source. */
  source: CreateEvalResponsesRunDataSourceSource;
}

/**
 * Details about the run's data source.
 */
export type CreateEvalRunRequestDataSource = CreateEvalJsonlRunDataSource | CreateEvalCompletionsRunDataSource | CreateEvalResponsesRunDataSource;

export interface CreateEvalRunRequest {
  /** The name of the run. */
  name?: string;
  metadata?: Metadata;
  /** Details about the run's data source. */
  data_source: CreateEvalRunRequestDataSource;
}

/**
 * The type of data source. Always `stored_completions`.
 */
export type CreateEvalStoredCompletionsDataSourceConfigType = typeof CreateEvalStoredCompletionsDataSourceConfigType[keyof typeof CreateEvalStoredCompletionsDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateEvalStoredCompletionsDataSourceConfigType = {
  stored_completions: 'stored_completions',
} as const;

/**
 * Metadata filters for the stored completions data source.
 */
export type CreateEvalStoredCompletionsDataSourceConfigMetadata = { [key: string]: unknown };

/**
 * Deprecated in favor of LogsDataSourceConfig.

 * @deprecated
 */
export interface CreateEvalStoredCompletionsDataSourceConfig {
  /** The type of data source. Always `stored_completions`. */
  type: CreateEvalStoredCompletionsDataSourceConfigType;
  /** Metadata filters for the stored completions data source. */
  metadata?: CreateEvalStoredCompletionsDataSourceConfigMetadata;
}

export interface CreateFileRequest {
  /** The File object (not file name) to be uploaded.
 */
  file: Blob;
  purpose: FilePurpose;
  expires_after?: FileExpirationAfter;
}

export interface CreateFineTuningCheckpointPermissionRequest {
  /** The project identifiers to grant access to. */
  project_ids: string[];
}

/**
 * The name of the model to fine-tune. You can select one of the
[supported models](https://platform.openai.com/docs/guides/fine-tuning#which-models-can-be-fine-tuned).

 */
export type CreateFineTuningJobRequestModel = string | 'babbage-002' | 'davinci-002' | 'gpt-3.5-turbo' | 'gpt-4o-mini';

/**
 * Number of examples in each batch. A larger batch size means that model parameters
are updated less frequently, but with lower variance.

 */
export type CreateFineTuningJobRequestHyperparametersBatchSize = 'auto' | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
overfitting.

 */
export type CreateFineTuningJobRequestHyperparametersLearningRateMultiplier = 'auto' | number;

/**
 * The number of epochs to train the model for. An epoch refers to one full cycle
through the training dataset.

 */
export type CreateFineTuningJobRequestHyperparametersNEpochs = 'auto' | number;

/**
 * The hyperparameters used for the fine-tuning job.
This value is now deprecated in favor of `method`, and should be passed in under the `method` parameter.

 * @deprecated
 */
export type CreateFineTuningJobRequestHyperparameters = {
  /** Number of examples in each batch. A larger batch size means that model parameters
are updated less frequently, but with lower variance.
 */
  batch_size?: CreateFineTuningJobRequestHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
overfitting.
 */
  learning_rate_multiplier?: CreateFineTuningJobRequestHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle
through the training dataset.
 */
  n_epochs?: CreateFineTuningJobRequestHyperparametersNEpochs;
};

/**
 * The settings for your integration with Weights and Biases. This payload specifies the project that
metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
to your run, and set a default entity (team, username, etc) to be associated with your run.

 */
export type CreateFineTuningJobRequestIntegrationsItemWandb = {
  /** The name of the project that the new run will be created under.
 */
  project: string;
  /**
   * A display name to set for the run. If not set, we will use the Job ID as the name.

   * @nullable
   */
  name?: string | null;
  /**
   * The entity to use for the run. This allows you to set the team or username of the WandB user that you would
like associated with the run. If not set, the default entity for the registered WandB API key is used.

   * @nullable
   */
  entity?: string | null;
  /** A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some
default tags are generated by OpenAI: "openai/finetune", "openai/{base-model}", "openai/{ftjob-abcdef}".
 */
  tags?: string[];
};

export type CreateFineTuningJobRequestIntegrationsItem = {
  /** The type of integration to enable. Currently, only "wandb" (Weights and Biases) is supported.
 */
  type: 'wandb';
  /** The settings for your integration with Weights and Biases. This payload specifies the project that
metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
to your run, and set a default entity (team, username, etc) to be associated with your run.
 */
  wandb: CreateFineTuningJobRequestIntegrationsItemWandb;
};

export interface CreateFineTuningJobRequest {
  /** The name of the model to fine-tune. You can select one of the
[supported models](https://platform.openai.com/docs/guides/fine-tuning#which-models-can-be-fine-tuned).
 */
  model: CreateFineTuningJobRequestModel;
  /** The ID of an uploaded file that contains training data.

See [upload file](https://platform.openai.com/docs/api-reference/files/create) for how to upload a file.

Your dataset must be formatted as a JSONL file. Additionally, you must upload your file with the purpose `fine-tune`.

The contents of the file should differ depending on if the model uses the [chat](https://platform.openai.com/docs/api-reference/fine-tuning/chat-input), [completions](https://platform.openai.com/docs/api-reference/fine-tuning/completions-input) format, or if the fine-tuning method uses the [preference](https://platform.openai.com/docs/api-reference/fine-tuning/preference-input) format.

See the [fine-tuning guide](https://platform.openai.com/docs/guides/model-optimization) for more details.
 */
  training_file: string;
  /**
   * The hyperparameters used for the fine-tuning job.
This value is now deprecated in favor of `method`, and should be passed in under the `method` parameter.

   * @deprecated
   */
  hyperparameters?: CreateFineTuningJobRequestHyperparameters;
  /**
   * A string of up to 64 characters that will be added to your fine-tuned model name.

For example, a `suffix` of "custom-model-name" would produce a model name like `ft:gpt-4o-mini:openai:custom-model-name:7p4lURel`.

   * @minLength 1
   * @maxLength 64
   * @nullable
   */
  suffix?: string | null;
  /**
   * The ID of an uploaded file that contains validation data.

If you provide this file, the data is used to generate validation
metrics periodically during fine-tuning. These metrics can be viewed in
the fine-tuning results file.
The same data should not be present in both train and validation files.

Your dataset must be formatted as a JSONL file. You must upload your file with the purpose `fine-tune`.

See the [fine-tuning guide](https://platform.openai.com/docs/guides/model-optimization) for more details.

   * @nullable
   */
  validation_file?: string | null;
  /**
   * A list of integrations to enable for your fine-tuning job.
   * @nullable
   */
  integrations?: CreateFineTuningJobRequestIntegrationsItem[] | null;
  /**
   * The seed controls the reproducibility of the job. Passing in the same seed and job parameters should produce the same results, but may differ in rare cases.
If a seed is not specified, one will be generated for you.

   * @minimum 0
   * @maximum 2147483647
   * @nullable
   */
  seed?: number | null;
  method?: FineTuneMethod;
  metadata?: Metadata;
}

/**
 * The image(s) to edit. Must be a supported image file or an array of images.

For `gpt-image-1`, each image should be a `png`, `webp`, or `jpg` file less
than 50MB. You can provide up to 16 images.

For `dall-e-2`, you can only provide one image, and it should be a square
`png` file less than 4MB.

 */
export type CreateImageEditRequestImage = Blob | Blob[];

/**
 * Allows to set transparency for the background of the generated image(s).
This parameter is only supported for `gpt-image-1`. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

 * @nullable
 */
export type CreateImageEditRequestBackground = typeof CreateImageEditRequestBackground[keyof typeof CreateImageEditRequestBackground] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageEditRequestBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The model to use for image generation. Only `dall-e-2` and `gpt-image-1` are supported. Defaults to `dall-e-2` unless a parameter specific to `gpt-image-1` is used.
 * @nullable
 */
export type CreateImageEditRequestModel = string | 'dall-e-2' | 'gpt-image-1' | 'gpt-image-1-mini' | null;

/**
 * The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for `gpt-image-1`, and one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`.
 * @nullable
 */
export type CreateImageEditRequestSize = typeof CreateImageEditRequestSize[keyof typeof CreateImageEditRequestSize] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageEditRequestSize = {
  '256x256': '256x256',
  '512x512': '512x512',
  '1024x1024': '1024x1024',
  '1536x1024': '1536x1024',
  '1024x1536': '1024x1536',
  auto: 'auto',
} as const;

/**
 * The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter is only supported for `dall-e-2`, as `gpt-image-1` will always return base64-encoded images.
 * @nullable
 */
export type CreateImageEditRequestResponseFormat = typeof CreateImageEditRequestResponseFormat[keyof typeof CreateImageEditRequestResponseFormat] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageEditRequestResponseFormat = {
  url: 'url',
  b64_json: 'b64_json',
} as const;

/**
 * The format in which the generated images are returned. This parameter is
only supported for `gpt-image-1`. Must be one of `png`, `jpeg`, or `webp`.
The default value is `png`.

 * @nullable
 */
export type CreateImageEditRequestOutputFormat = typeof CreateImageEditRequestOutputFormat[keyof typeof CreateImageEditRequestOutputFormat] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageEditRequestOutputFormat = {
  png: 'png',
  jpeg: 'jpeg',
  webp: 'webp',
} as const;

export type CreateImageEditRequestInputFidelity = InputFidelity | null;

/**
 * The quality of the image that will be generated. `high`, `medium` and `low` are only supported for `gpt-image-1`. `dall-e-2` only supports `standard` quality. Defaults to `auto`.

 * @nullable
 */
export type CreateImageEditRequestQuality = typeof CreateImageEditRequestQuality[keyof typeof CreateImageEditRequestQuality] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageEditRequestQuality = {
  standard: 'standard',
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

export interface CreateImageEditRequest {
  /** The image(s) to edit. Must be a supported image file or an array of images.

For `gpt-image-1`, each image should be a `png`, `webp`, or `jpg` file less
than 50MB. You can provide up to 16 images.

For `dall-e-2`, you can only provide one image, and it should be a square
`png` file less than 4MB.
 */
  image: CreateImageEditRequestImage;
  /** A text description of the desired image(s). The maximum length is 1000 characters for `dall-e-2`, and 32000 characters for `gpt-image-1`. */
  prompt: string;
  /** An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where `image` should be edited. If there are multiple images provided, the mask will be applied on the first image. Must be a valid PNG file, less than 4MB, and have the same dimensions as `image`. */
  mask?: Blob;
  /**
   * Allows to set transparency for the background of the generated image(s).
This parameter is only supported for `gpt-image-1`. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

   * @nullable
   */
  background?: CreateImageEditRequestBackground;
  /**
   * The model to use for image generation. Only `dall-e-2` and `gpt-image-1` are supported. Defaults to `dall-e-2` unless a parameter specific to `gpt-image-1` is used.
   * @nullable
   */
  model?: CreateImageEditRequestModel;
  /**
   * The number of images to generate. Must be between 1 and 10.
   * @minimum 1
   * @maximum 10
   * @nullable
   */
  n?: number | null;
  /**
   * The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for `gpt-image-1`, and one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`.
   * @nullable
   */
  size?: CreateImageEditRequestSize;
  /**
   * The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter is only supported for `dall-e-2`, as `gpt-image-1` will always return base64-encoded images.
   * @nullable
   */
  response_format?: CreateImageEditRequestResponseFormat;
  /**
   * The format in which the generated images are returned. This parameter is
only supported for `gpt-image-1`. Must be one of `png`, `jpeg`, or `webp`.
The default value is `png`.

   * @nullable
   */
  output_format?: CreateImageEditRequestOutputFormat;
  /**
   * The compression level (0-100%) for the generated images. This parameter
is only supported for `gpt-image-1` with the `webp` or `jpeg` output
formats, and defaults to 100.

   * @nullable
   */
  output_compression?: number | null;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
 */
  user?: string;
  input_fidelity?: CreateImageEditRequestInputFidelity;
  /**
   * Edit the image in streaming mode. Defaults to `false`. See the
[Image generation guide](https://platform.openai.com/docs/guides/image-generation) for more information.

   * @nullable
   */
  stream?: boolean | null;
  partial_images?: PartialImages;
  /**
   * The quality of the image that will be generated. `high`, `medium` and `low` are only supported for `gpt-image-1`. `dall-e-2` only supports `standard` quality. Defaults to `auto`.

   * @nullable
   */
  quality?: CreateImageEditRequestQuality;
}

/**
 * The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or `gpt-image-1`. Defaults to `dall-e-2` unless a parameter specific to `gpt-image-1` is used.
 * @nullable
 */
export type CreateImageRequestModel = string | 'dall-e-2' | 'dall-e-3' | 'gpt-image-1' | 'gpt-image-1-mini' | null;

/**
 * The quality of the image that will be generated.

- `auto` (default value) will automatically select the best quality for the given model.
- `high`, `medium` and `low` are supported for `gpt-image-1`.
- `hd` and `standard` are supported for `dall-e-3`.
- `standard` is the only option for `dall-e-2`.

 * @nullable
 */
export type CreateImageRequestQuality = typeof CreateImageRequestQuality[keyof typeof CreateImageRequestQuality] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestQuality = {
  standard: 'standard',
  hd: 'hd',
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for `gpt-image-1` which will always return base64-encoded images.
 * @nullable
 */
export type CreateImageRequestResponseFormat = typeof CreateImageRequestResponseFormat[keyof typeof CreateImageRequestResponseFormat] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestResponseFormat = {
  url: 'url',
  b64_json: 'b64_json',
} as const;

/**
 * The format in which the generated images are returned. This parameter is only supported for `gpt-image-1`. Must be one of `png`, `jpeg`, or `webp`.
 * @nullable
 */
export type CreateImageRequestOutputFormat = typeof CreateImageRequestOutputFormat[keyof typeof CreateImageRequestOutputFormat] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestOutputFormat = {
  png: 'png',
  jpeg: 'jpeg',
  webp: 'webp',
} as const;

/**
 * The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for `gpt-image-1`, one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`, and one of `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`.
 * @nullable
 */
export type CreateImageRequestSize = typeof CreateImageRequestSize[keyof typeof CreateImageRequestSize] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestSize = {
  auto: 'auto',
  '1024x1024': '1024x1024',
  '1536x1024': '1536x1024',
  '1024x1536': '1024x1536',
  '256x256': '256x256',
  '512x512': '512x512',
  '1792x1024': '1792x1024',
  '1024x1792': '1024x1792',
} as const;

/**
 * Control the content-moderation level for images generated by `gpt-image-1`. Must be either `low` for less restrictive filtering or `auto` (default value).
 * @nullable
 */
export type CreateImageRequestModeration = typeof CreateImageRequestModeration[keyof typeof CreateImageRequestModeration] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestModeration = {
  low: 'low',
  auto: 'auto',
} as const;

/**
 * Allows to set transparency for the background of the generated image(s).
This parameter is only supported for `gpt-image-1`. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

 * @nullable
 */
export type CreateImageRequestBackground = typeof CreateImageRequestBackground[keyof typeof CreateImageRequestBackground] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images.
 * @nullable
 */
export type CreateImageRequestStyle = typeof CreateImageRequestStyle[keyof typeof CreateImageRequestStyle] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageRequestStyle = {
  vivid: 'vivid',
  natural: 'natural',
} as const;

export interface CreateImageRequest {
  /** A text description of the desired image(s). The maximum length is 32000 characters for `gpt-image-1`, 1000 characters for `dall-e-2` and 4000 characters for `dall-e-3`. */
  prompt: string;
  /**
   * The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or `gpt-image-1`. Defaults to `dall-e-2` unless a parameter specific to `gpt-image-1` is used.
   * @nullable
   */
  model?: CreateImageRequestModel;
  /**
   * The number of images to generate. Must be between 1 and 10. For `dall-e-3`, only `n=1` is supported.
   * @minimum 1
   * @maximum 10
   * @nullable
   */
  n?: number | null;
  /**
   * The quality of the image that will be generated.

- `auto` (default value) will automatically select the best quality for the given model.
- `high`, `medium` and `low` are supported for `gpt-image-1`.
- `hd` and `standard` are supported for `dall-e-3`.
- `standard` is the only option for `dall-e-2`.

   * @nullable
   */
  quality?: CreateImageRequestQuality;
  /**
   * The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for `gpt-image-1` which will always return base64-encoded images.
   * @nullable
   */
  response_format?: CreateImageRequestResponseFormat;
  /**
   * The format in which the generated images are returned. This parameter is only supported for `gpt-image-1`. Must be one of `png`, `jpeg`, or `webp`.
   * @nullable
   */
  output_format?: CreateImageRequestOutputFormat;
  /**
   * The compression level (0-100%) for the generated images. This parameter is only supported for `gpt-image-1` with the `webp` or `jpeg` output formats, and defaults to 100.
   * @nullable
   */
  output_compression?: number | null;
  /**
   * Generate the image in streaming mode. Defaults to `false`. See the
[Image generation guide](https://platform.openai.com/docs/guides/image-generation) for more information.
This parameter is only supported for `gpt-image-1`.

   * @nullable
   */
  stream?: boolean | null;
  partial_images?: PartialImages;
  /**
   * The size of the generated images. Must be one of `1024x1024`, `1536x1024` (landscape), `1024x1536` (portrait), or `auto` (default value) for `gpt-image-1`, one of `256x256`, `512x512`, or `1024x1024` for `dall-e-2`, and one of `1024x1024`, `1792x1024`, or `1024x1792` for `dall-e-3`.
   * @nullable
   */
  size?: CreateImageRequestSize;
  /**
   * Control the content-moderation level for images generated by `gpt-image-1`. Must be either `low` for less restrictive filtering or `auto` (default value).
   * @nullable
   */
  moderation?: CreateImageRequestModeration;
  /**
   * Allows to set transparency for the background of the generated image(s).
This parameter is only supported for `gpt-image-1`. Must be one of
`transparent`, `opaque` or `auto` (default value). When `auto` is used, the
model will automatically determine the best background for the image.

If `transparent`, the output format needs to support transparency, so it
should be set to either `png` (default value) or `webp`.

   * @nullable
   */
  background?: CreateImageRequestBackground;
  /**
   * The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images.
   * @nullable
   */
  style?: CreateImageRequestStyle;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
 */
  user?: string;
}

/**
 * The model to use for image generation. Only `dall-e-2` is supported at this time.
 * @nullable
 */
export type CreateImageVariationRequestModel = string | 'dall-e-2' | null;

/**
 * The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated.
 * @nullable
 */
export type CreateImageVariationRequestResponseFormat = typeof CreateImageVariationRequestResponseFormat[keyof typeof CreateImageVariationRequestResponseFormat] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageVariationRequestResponseFormat = {
  url: 'url',
  b64_json: 'b64_json',
} as const;

/**
 * The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`.
 * @nullable
 */
export type CreateImageVariationRequestSize = typeof CreateImageVariationRequestSize[keyof typeof CreateImageVariationRequestSize] | null;


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateImageVariationRequestSize = {
  '256x256': '256x256',
  '512x512': '512x512',
  '1024x1024': '1024x1024',
} as const;

export interface CreateImageVariationRequest {
  /** The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square. */
  image: Blob;
  /**
   * The model to use for image generation. Only `dall-e-2` is supported at this time.
   * @nullable
   */
  model?: CreateImageVariationRequestModel;
  /**
   * The number of images to generate. Must be between 1 and 10.
   * @minimum 1
   * @maximum 10
   * @nullable
   */
  n?: number | null;
  /**
   * The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated.
   * @nullable
   */
  response_format?: CreateImageVariationRequestResponseFormat;
  /**
   * The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`.
   * @nullable
   */
  size?: CreateImageVariationRequestSize;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).
 */
  user?: string;
}

/**
 * The role of the entity that is creating the message. Allowed values include:
- `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
- `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.

 */
export type CreateMessageRequestRole = typeof CreateMessageRequestRole[keyof typeof CreateMessageRequestRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateMessageRequestRole = {
  user: 'user',
  assistant: 'assistant',
} as const;

export type CreateMessageRequestContentAnyOfItem = MessageContentImageFileObject | MessageContentImageUrlObject | MessageRequestContentTextObject;

export type CreateMessageRequestContent = string | CreateMessageRequestContentAnyOfItem[];

export type CreateMessageRequestAttachmentsAnyOfItemToolsItem = AssistantToolsCode | AssistantToolsFileSearchTypeOnly;

export type CreateMessageRequestAttachmentsAnyOfItem = {
  /** The ID of the file to attach to the message. */
  file_id?: string;
  /** The tools to add this file to. */
  tools?: CreateMessageRequestAttachmentsAnyOfItemToolsItem[];
};

export type CreateMessageRequestAttachments = CreateMessageRequestAttachmentsAnyOfItem[] | null;

export interface CreateMessageRequest {
  /** The role of the entity that is creating the message. Allowed values include:
- `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
- `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.
 */
  role: CreateMessageRequestRole;
  content: CreateMessageRequestContent;
  attachments?: CreateMessageRequestAttachments;
  metadata?: Metadata;
}

export type CreateModelResponsePropertiesAllOf = {
  /**
   * An integer between 0 and 20 specifying the number of most likely tokens to
return at each token position, each with an associated log probability.

   * @minimum 0
   * @maximum 20
   */
  top_logprobs?: number;
};

export type CreateModelResponseProperties = ModelResponseProperties & CreateModelResponsePropertiesAllOf;

export type CreateModerationRequestInputAnyOfItem = ModerationImageURLInput | ModerationTextInput;

/**
 * Input (or inputs) to classify. Can be a single string, an array of strings, or
an array of multi-modal input objects similar to other models.

 */
export type CreateModerationRequestInput = string | string[] | CreateModerationRequestInputAnyOfItem[];

/**
 * The content moderation model you would like to use. Learn more in
[the moderation guide](https://platform.openai.com/docs/guides/moderation), and learn about
available models [here](https://platform.openai.com/docs/models#moderation).

 */
export type CreateModerationRequestModel = string | 'omni-moderation-latest' | 'omni-moderation-2024-09-26' | 'text-moderation-latest' | 'text-moderation-stable';

export interface CreateModerationRequest {
  /** Input (or inputs) to classify. Can be a single string, an array of strings, or
an array of multi-modal input objects similar to other models.
 */
  input: CreateModerationRequestInput;
  /**
   * The content moderation model you would like to use. Learn more in
[the moderation guide](https://platform.openai.com/docs/guides/moderation), and learn about
available models [here](https://platform.openai.com/docs/models#moderation).

   */
  model?: CreateModerationRequestModel;
}

export type CreateModerationResponseResultsItemCategoriesIllicit = boolean | null;

export type CreateModerationResponseResultsItemCategoriesIllicitViolent = boolean | null;

/**
 * A list of the categories, and whether they are flagged or not.
 */
export type CreateModerationResponseResultsItemCategories = {
  /** Content that expresses, incites, or promotes hate based on race, gender, ethnicity, religion, nationality, sexual orientation, disability status, or caste. Hateful content aimed at non-protected groups (e.g., chess players) is harassment. */
  hate: boolean;
  /** Hateful content that also includes violence or serious harm towards the targeted group based on race, gender, ethnicity, religion, nationality, sexual orientation, disability status, or caste. */
  'hate/threatening': boolean;
  /** Content that expresses, incites, or promotes harassing language towards any target. */
  harassment: boolean;
  /** Harassment content that also includes violence or serious harm towards any target. */
  'harassment/threatening': boolean;
  illicit: CreateModerationResponseResultsItemCategoriesIllicit;
  'illicit/violent': CreateModerationResponseResultsItemCategoriesIllicitViolent;
  /** Content that promotes, encourages, or depicts acts of self-harm, such as suicide, cutting, and eating disorders. */
  'self-harm': boolean;
  /** Content where the speaker expresses that they are engaging or intend to engage in acts of self-harm, such as suicide, cutting, and eating disorders. */
  'self-harm/intent': boolean;
  /** Content that encourages performing acts of self-harm, such as suicide, cutting, and eating disorders, or that gives instructions or advice on how to commit such acts. */
  'self-harm/instructions': boolean;
  /** Content meant to arouse sexual excitement, such as the description of sexual activity, or that promotes sexual services (excluding sex education and wellness). */
  sexual: boolean;
  /** Sexual content that includes an individual who is under 18 years old. */
  'sexual/minors': boolean;
  /** Content that depicts death, violence, or physical injury. */
  violence: boolean;
  /** Content that depicts death, violence, or physical injury in graphic detail. */
  'violence/graphic': boolean;
};

/**
 * A list of the categories along with their scores as predicted by model.
 */
export type CreateModerationResponseResultsItemCategoryScores = {
  /** The score for the category 'hate'. */
  hate: number;
  /** The score for the category 'hate/threatening'. */
  'hate/threatening': number;
  /** The score for the category 'harassment'. */
  harassment: number;
  /** The score for the category 'harassment/threatening'. */
  'harassment/threatening': number;
  /** The score for the category 'illicit'. */
  illicit: number;
  /** The score for the category 'illicit/violent'. */
  'illicit/violent': number;
  /** The score for the category 'self-harm'. */
  'self-harm': number;
  /** The score for the category 'self-harm/intent'. */
  'self-harm/intent': number;
  /** The score for the category 'self-harm/instructions'. */
  'self-harm/instructions': number;
  /** The score for the category 'sexual'. */
  sexual: number;
  /** The score for the category 'sexual/minors'. */
  'sexual/minors': number;
  /** The score for the category 'violence'. */
  violence: number;
  /** The score for the category 'violence/graphic'. */
  'violence/graphic': number;
};

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesHateItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHateItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHateItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesHateItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesHateThreateningItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHateThreateningItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHateThreateningItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesHateThreateningItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentThreateningItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentThreateningItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentThreateningItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentThreateningItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitViolentItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitViolentItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitViolentItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitViolentItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem = {
  text: 'text',
  image: 'image',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmIntentItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmIntentItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmIntentItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmIntentItem = {
  text: 'text',
  image: 'image',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmInstructionsItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmInstructionsItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmInstructionsItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmInstructionsItem = {
  text: 'text',
  image: 'image',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualItem = {
  text: 'text',
  image: 'image',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualMinorsItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualMinorsItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualMinorsItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualMinorsItem = {
  text: 'text',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceItem = {
  text: 'text',
  image: 'image',
} as const;

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceGraphicItem = typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceGraphicItem[keyof typeof CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceGraphicItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceGraphicItem = {
  text: 'text',
  image: 'image',
} as const;

/**
 * A list of the categories along with the input type(s) that the score applies to.
 */
export type CreateModerationResponseResultsItemCategoryAppliedInputTypes = {
  /** The applied input type(s) for the category 'hate'. */
  hate: CreateModerationResponseResultsItemCategoryAppliedInputTypesHateItem[];
  /** The applied input type(s) for the category 'hate/threatening'. */
  'hate/threatening': CreateModerationResponseResultsItemCategoryAppliedInputTypesHateThreateningItem[];
  /** The applied input type(s) for the category 'harassment'. */
  harassment: CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentItem[];
  /** The applied input type(s) for the category 'harassment/threatening'. */
  'harassment/threatening': CreateModerationResponseResultsItemCategoryAppliedInputTypesHarassmentThreateningItem[];
  /** The applied input type(s) for the category 'illicit'. */
  illicit: CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitItem[];
  /** The applied input type(s) for the category 'illicit/violent'. */
  'illicit/violent': CreateModerationResponseResultsItemCategoryAppliedInputTypesIllicitViolentItem[];
  /** The applied input type(s) for the category 'self-harm'. */
  'self-harm': CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'self-harm/intent'. */
  'self-harm/intent': CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmIntentItem[];
  /** The applied input type(s) for the category 'self-harm/instructions'. */
  'self-harm/instructions': CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmInstructionsItem[];
  /** The applied input type(s) for the category 'sexual'. */
  sexual: CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualItem[];
  /** The applied input type(s) for the category 'sexual/minors'. */
  'sexual/minors': CreateModerationResponseResultsItemCategoryAppliedInputTypesSexualMinorsItem[];
  /** The applied input type(s) for the category 'violence'. */
  violence: CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceItem[];
  /** The applied input type(s) for the category 'violence/graphic'. */
  'violence/graphic': CreateModerationResponseResultsItemCategoryAppliedInputTypesViolenceGraphicItem[];
};

export type CreateModerationResponseResultsItem = {
  /** Whether any of the below categories are flagged. */
  flagged: boolean;
  /** A list of the categories, and whether they are flagged or not. */
  categories: CreateModerationResponseResultsItemCategories;
  /** A list of the categories along with their scores as predicted by model. */
  category_scores: CreateModerationResponseResultsItemCategoryScores;
  /** A list of the categories along with the input type(s) that the score applies to. */
  category_applied_input_types: CreateModerationResponseResultsItemCategoryAppliedInputTypes;
};

/**
 * Represents if a given text input is potentially harmful.
 */
export interface CreateModerationResponse {
  /** The unique identifier for the moderation request. */
  id: string;
  /** The model used to generate the moderation results. */
  model: string;
  /** A list of moderation objects. */
  results: CreateModerationResponseResultsItem[];
}

export type CreateResponseAllOfInclude = IncludeEnum[] | null;

export type CreateResponseAllOfParallelToolCalls = boolean | null;

export type CreateResponseAllOfStore = boolean | null;

export type CreateResponseAllOfInstructions = string | null;

export type CreateResponseAllOfStream = boolean | null;

export type CreateResponseAllOfConversation = ConversationParam | null;

export type CreateResponseAllOf = {
  input?: InputParam;
  include?: CreateResponseAllOfInclude;
  parallel_tool_calls?: CreateResponseAllOfParallelToolCalls;
  store?: CreateResponseAllOfStore;
  instructions?: CreateResponseAllOfInstructions;
  stream?: CreateResponseAllOfStream;
  stream_options?: ResponseStreamOptions;
  conversation?: CreateResponseAllOfConversation;
};

export type CreateResponse = CreateModelResponseProperties & ResponseProperties & CreateResponseAllOf;

/**
 * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
 * @nullable
 */
export type CreateRunRequestModel = string | AssistantSupportedModels | null;

/**
 * @nullable
 */
export type CreateRunRequestTruncationStrategyAllOf = unknown | null;

export type CreateRunRequestTruncationStrategy = TruncationObject & CreateRunRequestTruncationStrategyAllOf;

/**
 * @nullable
 */
export type CreateRunRequestToolChoiceAllOf = unknown | null;

export type CreateRunRequestToolChoice = AssistantsApiToolChoiceOption & CreateRunRequestToolChoiceAllOf;

export interface CreateRunRequest {
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  /**
   * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
   * @nullable
   */
  model?: CreateRunRequestModel;
  reasoning_effort?: ReasoningEffort;
  /**
   * Overrides the [instructions](https://platform.openai.com/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for modifying the behavior on a per-run basis.
   * @nullable
   */
  instructions?: string | null;
  /**
   * Appends additional instructions at the end of the instructions for the run. This is useful for modifying the behavior on a per-run basis without overriding other instructions.
   * @nullable
   */
  additional_instructions?: string | null;
  /**
   * Adds additional messages to the thread before creating the run.
   * @nullable
   */
  additional_messages?: CreateMessageRequest[] | null;
  /**
   * Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
   * @maxItems 20
   * @nullable
   */
  tools?: AssistantTool[] | null;
  metadata?: Metadata;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

   * @minimum 0
   * @maximum 2
   * @nullable
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or temperature but not both.

   * @minimum 0
   * @maximum 1
   * @nullable
   */
  top_p?: number | null;
  /**
   * If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.

   * @nullable
   */
  stream?: boolean | null;
  /**
   * The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_prompt_tokens?: number | null;
  /**
   * The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_completion_tokens?: number | null;
  truncation_strategy?: CreateRunRequestTruncationStrategy;
  tool_choice?: CreateRunRequestToolChoice;
  parallel_tool_calls?: ParallelToolCalls;
  /** @nullable */
  response_format?: AssistantsApiResponseFormatOption;
}

/**
 * One of the available [TTS models](https://platform.openai.com/docs/models#tts): `tts-1`, `tts-1-hd` or `gpt-4o-mini-tts`.

 */
export type CreateSpeechRequestModel = string | 'tts-1' | 'tts-1-hd' | 'gpt-4o-mini-tts';

/**
 * The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`.
 */
export type CreateSpeechRequestResponseFormat = typeof CreateSpeechRequestResponseFormat[keyof typeof CreateSpeechRequestResponseFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpeechRequestResponseFormat = {
  mp3: 'mp3',
  opus: 'opus',
  aac: 'aac',
  flac: 'flac',
  wav: 'wav',
  pcm: 'pcm',
} as const;

/**
 * The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`.
 */
export type CreateSpeechRequestStreamFormat = typeof CreateSpeechRequestStreamFormat[keyof typeof CreateSpeechRequestStreamFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpeechRequestStreamFormat = {
  sse: 'sse',
  audio: 'audio',
} as const;

export interface CreateSpeechRequest {
  /** One of the available [TTS models](https://platform.openai.com/docs/models#tts): `tts-1`, `tts-1-hd` or `gpt-4o-mini-tts`.
 */
  model: CreateSpeechRequestModel;
  /**
   * The text to generate audio for. The maximum length is 4096 characters.
   * @maxLength 4096
   */
  input: string;
  /**
   * Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`.
   * @maxLength 4096
   */
  instructions?: string;
  /** The voice to use when generating the audio. Supported voices are `alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `onyx`, `nova`, `sage`, `shimmer`, and `verse`. Previews of the voices are available in the [Text to speech guide](https://platform.openai.com/docs/guides/text-to-speech#voice-options). */
  voice: VoiceIdsShared;
  /** The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`. */
  response_format?: CreateSpeechRequestResponseFormat;
  /**
   * The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default.
   * @minimum 0.25
   * @maximum 4
   */
  speed?: number;
  /** The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`. */
  stream_format?: CreateSpeechRequestStreamFormat;
}

export type CreateSpeechResponseStreamEvent = SpeechAudioDeltaEvent | SpeechAudioDoneEvent;

/**
 * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
 * @nullable
 */
export type CreateThreadAndRunRequestModel = string | 'gpt-5' | 'gpt-5-mini' | 'gpt-5-nano' | 'gpt-5-2025-08-07' | 'gpt-5-mini-2025-08-07' | 'gpt-5-nano-2025-08-07' | 'gpt-4.1' | 'gpt-4.1-mini' | 'gpt-4.1-nano' | 'gpt-4.1-2025-04-14' | 'gpt-4.1-mini-2025-04-14' | 'gpt-4.1-nano-2025-04-14' | 'gpt-4o' | 'gpt-4o-2024-11-20' | 'gpt-4o-2024-08-06' | 'gpt-4o-2024-05-13' | 'gpt-4o-mini' | 'gpt-4o-mini-2024-07-18' | 'gpt-4.5-preview' | 'gpt-4.5-preview-2025-02-27' | 'gpt-4-turbo' | 'gpt-4-turbo-2024-04-09' | 'gpt-4-0125-preview' | 'gpt-4-turbo-preview' | 'gpt-4-1106-preview' | 'gpt-4-vision-preview' | 'gpt-4' | 'gpt-4-0314' | 'gpt-4-0613' | 'gpt-4-32k' | 'gpt-4-32k-0314' | 'gpt-4-32k-0613' | 'gpt-3.5-turbo' | 'gpt-3.5-turbo-16k' | 'gpt-3.5-turbo-0613' | 'gpt-3.5-turbo-1106' | 'gpt-3.5-turbo-0125' | 'gpt-3.5-turbo-16k-0613' | null;

export type CreateThreadAndRunRequestToolResourcesCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type CreateThreadAndRunRequestToolResourcesFileSearch = {
  /**
   * The ID of the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 * @nullable
 */
export type CreateThreadAndRunRequestToolResources = {
  code_interpreter?: CreateThreadAndRunRequestToolResourcesCodeInterpreter;
  file_search?: CreateThreadAndRunRequestToolResourcesFileSearch;
} | null;

/**
 * @nullable
 */
export type CreateThreadAndRunRequestTruncationStrategyAllOf = unknown | null;

export type CreateThreadAndRunRequestTruncationStrategy = TruncationObject & CreateThreadAndRunRequestTruncationStrategyAllOf;

/**
 * @nullable
 */
export type CreateThreadAndRunRequestToolChoiceAllOf = unknown | null;

export type CreateThreadAndRunRequestToolChoice = AssistantsApiToolChoiceOption & CreateThreadAndRunRequestToolChoiceAllOf;

export interface CreateThreadAndRunRequest {
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  thread?: CreateThreadRequest;
  /**
   * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
   * @nullable
   */
  model?: CreateThreadAndRunRequestModel;
  /**
   * Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis.
   * @nullable
   */
  instructions?: string | null;
  /**
   * Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
   * @maxItems 20
   * @nullable
   */
  tools?: AssistantTool[] | null;
  /**
   * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

   * @nullable
   */
  tool_resources?: CreateThreadAndRunRequestToolResources;
  metadata?: Metadata;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

   * @minimum 0
   * @maximum 2
   * @nullable
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or temperature but not both.

   * @minimum 0
   * @maximum 1
   * @nullable
   */
  top_p?: number | null;
  /**
   * If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.

   * @nullable
   */
  stream?: boolean | null;
  /**
   * The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_prompt_tokens?: number | null;
  /**
   * The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_completion_tokens?: number | null;
  truncation_strategy?: CreateThreadAndRunRequestTruncationStrategy;
  tool_choice?: CreateThreadAndRunRequestToolChoice;
  parallel_tool_calls?: ParallelToolCalls;
  /** @nullable */
  response_format?: AssistantsApiResponseFormatOption;
}

export type CreateThreadRequestToolResourcesAnyOfCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

/**
 * Always `auto`.
 */
export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType = typeof CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType[keyof typeof CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType = {
  auto: 'auto',
} as const;

/**
 * The default strategy. This strategy currently uses a `max_chunk_size_tokens` of `800` and `chunk_overlap_tokens` of `400`.
 */
export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOf = {
  /** Always `auto`. */
  type: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfType;
};

/**
 * Always `static`.
 */
export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType = typeof CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType[keyof typeof CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType = {
  static: 'static',
} as const;

export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeStatic = {
  /**
   * The maximum number of tokens in each chunk. The default value is `800`. The minimum value is `100` and the maximum value is `4096`.
   * @minimum 100
   * @maximum 4096
   */
  max_chunk_size_tokens: number;
  /** The number of tokens that overlap between chunks. The default value is `400`.

Note that the overlap must not exceed half of `max_chunk_size_tokens`.
 */
  chunk_overlap_tokens: number;
};

export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThree = {
  /** Always `static`. */
  type: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeType;
  static: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThreeStatic;
};

/**
 * The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy.
 */
export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategy = CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOf | CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategyAnyOfThree;

export type CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItem = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs to add to the vector store. There can be a maximum of 10000 files in a vector store.

   * @maxItems 10000
   */
  file_ids?: string[];
  /** The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. */
  chunking_strategy?: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItemChunkingStrategy;
  metadata?: Metadata;
};

export type CreateThreadRequestToolResourcesAnyOfFileSearch = (unknown & {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_store_ids?: string[];
  /**
   * A helper to create a [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) with file_ids and attach it to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_stores?: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItem[];
}) | (unknown & {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_store_ids?: string[];
  /**
   * A helper to create a [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) with file_ids and attach it to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_stores?: CreateThreadRequestToolResourcesAnyOfFileSearchVectorStoresItem[];
});

/**
 * A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type CreateThreadRequestToolResourcesAnyOf = {
  code_interpreter?: CreateThreadRequestToolResourcesAnyOfCodeInterpreter;
  file_search?: CreateThreadRequestToolResourcesAnyOfFileSearch;
};

export type CreateThreadRequestToolResources = CreateThreadRequestToolResourcesAnyOf | null;

/**
 * Options to create a new thread. If no thread is provided when running a
request, an empty thread will be created.

 */
export interface CreateThreadRequest {
  /** A list of [messages](https://platform.openai.com/docs/api-reference/messages) to start the thread with. */
  messages?: CreateMessageRequest[];
  tool_resources?: CreateThreadRequestToolResources;
  metadata?: Metadata;
}

/**
 * ID of the model to use. The options are `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, `whisper-1` (which is powered by our open source Whisper V2 model), and `gpt-4o-transcribe-diarize`.

 */
export type CreateTranscriptionRequestModel = string | 'whisper-1' | 'gpt-4o-transcribe' | 'gpt-4o-mini-transcribe' | 'gpt-4o-transcribe-diarize';

export type CreateTranscriptionRequestTimestampGranularitiesItem = typeof CreateTranscriptionRequestTimestampGranularitiesItem[keyof typeof CreateTranscriptionRequestTimestampGranularitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateTranscriptionRequestTimestampGranularitiesItem = {
  word: 'word',
  segment: 'segment',
} as const;

export type CreateTranscriptionRequestStream = boolean | null;

export interface CreateTranscriptionRequest {
  /** The audio file object (not file name) to transcribe, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, or webm.
 */
  file: Blob;
  /** ID of the model to use. The options are `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, `whisper-1` (which is powered by our open source Whisper V2 model), and `gpt-4o-transcribe-diarize`.
 */
  model: CreateTranscriptionRequestModel;
  /** The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) (e.g. `en`) format will improve accuracy and latency.
 */
  language?: string;
  /** An optional text to guide the model's style or continue a previous audio segment. The [prompt](https://platform.openai.com/docs/guides/speech-to-text#prompting) should match the audio language. This field is not supported when using `gpt-4o-transcribe-diarize`.
 */
  prompt?: string;
  response_format?: AudioResponseFormat;
  /** The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.
 */
  temperature?: number;
  /** Additional information to include in the transcription response.
`logprobs` will return the log probabilities of the tokens in the
response to understand the model's confidence in the transcription.
`logprobs` only works with response_format set to `json` and only with
the models `gpt-4o-transcribe` and `gpt-4o-mini-transcribe`. This field is not supported when using `gpt-4o-transcribe-diarize`.
 */
  include?: TranscriptionInclude[];
  /** The timestamp granularities to populate for this transcription. `response_format` must be set `verbose_json` to use timestamp granularities. Either or both of these options are supported: `word`, or `segment`. Note: There is no additional latency for segment timestamps, but generating word timestamps incurs additional latency.
This option is not available for `gpt-4o-transcribe-diarize`.
 */
  timestamp_granularities?: CreateTranscriptionRequestTimestampGranularitiesItem[];
  stream?: CreateTranscriptionRequestStream;
  chunking_strategy?: TranscriptionChunkingStrategy;
  /**
   * Optional list of speaker names that correspond to the audio samples provided in `known_speaker_references[]`. Each entry should be a short identifier (for example `customer` or `agent`). Up to 4 speakers are supported.

   * @maxItems 4
   */
  known_speaker_names?: string[];
  /**
   * Optional list of audio samples (as [data URLs](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_URLs)) that contain known speaker references matching `known_speaker_names[]`. Each sample must be between 2 and 10 seconds, and can use any of the same input audio formats supported by `file`.

   * @maxItems 4
   */
  known_speaker_references?: string[];
}

/**
 * The type of task that was run. Always `transcribe`.
 */
export type CreateTranscriptionResponseDiarizedJsonTask = typeof CreateTranscriptionResponseDiarizedJsonTask[keyof typeof CreateTranscriptionResponseDiarizedJsonTask];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateTranscriptionResponseDiarizedJsonTask = {
  transcribe: 'transcribe',
} as const;

/**
 * Token or duration usage statistics for the request.
 */
export type CreateTranscriptionResponseDiarizedJsonUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/**
 * Represents a diarized transcription response returned by the model, including the combined transcript and speaker-segment annotations.

 */
export interface CreateTranscriptionResponseDiarizedJson {
  /** The type of task that was run. Always `transcribe`. */
  task: CreateTranscriptionResponseDiarizedJsonTask;
  /** Duration of the input audio in seconds. */
  duration: number;
  /** The concatenated transcript text for the entire audio input. */
  text: string;
  /** Segments of the transcript annotated with timestamps and speaker labels. */
  segments: TranscriptionDiarizedSegment[];
  /** Token or duration usage statistics for the request. */
  usage?: CreateTranscriptionResponseDiarizedJsonUsage;
}

export type CreateTranscriptionResponseJsonLogprobsItem = {
  /** The token in the transcription. */
  token?: string;
  /** The log probability of the token. */
  logprob?: number;
  /** The bytes of the token. */
  bytes?: number[];
};

/**
 * Token usage statistics for the request.
 */
export type CreateTranscriptionResponseJsonUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/**
 * Represents a transcription response returned by model, based on the provided input.
 */
export interface CreateTranscriptionResponseJson {
  /** The transcribed text. */
  text: string;
  /** The log probabilities of the tokens in the transcription. Only returned with the models `gpt-4o-transcribe` and `gpt-4o-mini-transcribe` if `logprobs` is added to the `include` array.
 */
  logprobs?: CreateTranscriptionResponseJsonLogprobsItem[];
  /** Token usage statistics for the request. */
  usage?: CreateTranscriptionResponseJsonUsage;
}

export type CreateTranscriptionResponseStreamEvent = TranscriptTextSegmentEvent | TranscriptTextDeltaEvent | TranscriptTextDoneEvent;

/**
 * Represents a verbose json transcription response returned by model, based on the provided input.
 */
export interface CreateTranscriptionResponseVerboseJson {
  /** The language of the input audio. */
  language: string;
  /** The duration of the input audio. */
  duration: number;
  /** The transcribed text. */
  text: string;
  /** Extracted words and their corresponding timestamps. */
  words?: TranscriptionWord[];
  /** Segments of the transcribed text and their corresponding details. */
  segments?: TranscriptionSegment[];
  usage?: TranscriptTextUsageDuration;
}

/**
 * ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available.

 */
export type CreateTranslationRequestModel = string | 'whisper-1';

/**
 * The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`.

 */
export type CreateTranslationRequestResponseFormat = typeof CreateTranslationRequestResponseFormat[keyof typeof CreateTranslationRequestResponseFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateTranslationRequestResponseFormat = {
  json: 'json',
  text: 'text',
  srt: 'srt',
  verbose_json: 'verbose_json',
  vtt: 'vtt',
} as const;

export interface CreateTranslationRequest {
  /** The audio file object (not file name) translate, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, or webm.
 */
  file: Blob;
  /** ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available.
 */
  model: CreateTranslationRequestModel;
  /** An optional text to guide the model's style or continue a previous audio segment. The [prompt](https://platform.openai.com/docs/guides/speech-to-text#prompting) should be in English.
 */
  prompt?: string;
  /** The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`.
 */
  response_format?: CreateTranslationRequestResponseFormat;
  /** The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.
 */
  temperature?: number;
}

export interface CreateTranslationResponseJson {
  text: string;
}

export interface CreateTranslationResponseVerboseJson {
  /** The language of the output translation (always `english`). */
  language: string;
  /** The duration of the input audio. */
  duration: number;
  /** The translated text. */
  text: string;
  /** Segments of the translated text and their corresponding details. */
  segments?: TranscriptionSegment[];
}

/**
 * The intended purpose of the uploaded file.

See the [documentation on File purposes](https://platform.openai.com/docs/api-reference/files/create#files-create-purpose).

 */
export type CreateUploadRequestPurpose = typeof CreateUploadRequestPurpose[keyof typeof CreateUploadRequestPurpose];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateUploadRequestPurpose = {
  assistants: 'assistants',
  batch: 'batch',
  'fine-tune': 'fine-tune',
  vision: 'vision',
} as const;

export interface CreateUploadRequest {
  /** The name of the file to upload.
 */
  filename: string;
  /** The intended purpose of the uploaded file.

See the [documentation on File purposes](https://platform.openai.com/docs/api-reference/files/create#files-create-purpose).
 */
  purpose: CreateUploadRequestPurpose;
  /** The number of bytes in the file you are uploading.
 */
  bytes: number;
  /** The MIME type of the file.

This must fall within the supported MIME types for your file purpose. See the supported MIME types for assistants and vision.
 */
  mime_type: string;
  expires_after?: FileExpirationAfter;
}

export interface CreateVectorStoreFileBatchRequest {
  /**
   * A list of [File](https://platform.openai.com/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.
   * @minItems 1
   * @maxItems 500
   */
  file_ids: string[];
  chunking_strategy?: ChunkingStrategyRequestParam;
  attributes?: VectorStoreFileAttributes;
}

export interface CreateVectorStoreFileRequest {
  /** A [File](https://platform.openai.com/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that can access files. */
  file_id: string;
  chunking_strategy?: ChunkingStrategyRequestParam;
  attributes?: VectorStoreFileAttributes;
}

export interface CreateVectorStoreRequest {
  /**
   * A list of [File](https://platform.openai.com/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.
   * @maxItems 500
   */
  file_ids?: string[];
  /** The name of the vector store. */
  name?: string;
  /** A description for the vector store. Can be used to describe the vector store's purpose. */
  description?: string;
  expires_after?: VectorStoreExpirationAfter;
  chunking_strategy?: ChunkingStrategyRequestParam;
  metadata?: Metadata;
}

/**
 * The type of the custom tool call. Always `custom_tool_call`.

 */
export type CustomToolCallType = typeof CustomToolCallType[keyof typeof CustomToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolCallType = {
  custom_tool_call: 'custom_tool_call',
} as const;

/**
 * A call to a custom tool created by the model.

 */
export interface CustomToolCall {
  /** The type of the custom tool call. Always `custom_tool_call`.
 */
  type: CustomToolCallType;
  /** The unique ID of the custom tool call in the OpenAI platform.
 */
  id?: string;
  /** An identifier used to map this custom tool call to a tool call output.
 */
  call_id: string;
  /** The name of the custom tool being called.
 */
  name: string;
  /** The input for the custom tool call generated by the model.
 */
  input: string;
}

/**
 * The type of the custom tool call output. Always `custom_tool_call_output`.

 */
export type CustomToolCallOutputType = typeof CustomToolCallOutputType[keyof typeof CustomToolCallOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolCallOutputType = {
  custom_tool_call_output: 'custom_tool_call_output',
} as const;

/**
 * The output from the custom tool call generated by your code.
Can be a string or an list of output content.

 */
export type CustomToolCallOutputOutput = string | FunctionAndCustomToolCallOutput[];

/**
 * The output of a custom tool call from your code, being sent back to the model.

 */
export interface CustomToolCallOutput {
  /** The type of the custom tool call output. Always `custom_tool_call_output`.
 */
  type: CustomToolCallOutputType;
  /** The unique ID of the custom tool call output in the OpenAI platform.
 */
  id?: string;
  /** The call ID, used to map this custom tool call output to a custom tool call.
 */
  call_id: string;
  /** The output from the custom tool call generated by your code.
Can be a string or an list of output content.
 */
  output: CustomToolCallOutputOutput;
}

/**
 * The type of the custom tool. Always `custom`.
 */
export type CustomToolChatCompletionsType = typeof CustomToolChatCompletionsType[keyof typeof CustomToolChatCompletionsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolChatCompletionsType = {
  custom: 'custom',
} as const;

/**
 * Unconstrained text format. Always `text`.
 */
export type CustomToolChatCompletionsCustomFormatAnyOfType = typeof CustomToolChatCompletionsCustomFormatAnyOfType[keyof typeof CustomToolChatCompletionsCustomFormatAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolChatCompletionsCustomFormatAnyOfType = {
  text: 'text',
} as const;

/**
 * Unconstrained free-form text.
 */
export type CustomToolChatCompletionsCustomFormatAnyOf = {
  /** Unconstrained text format. Always `text`. */
  type: CustomToolChatCompletionsCustomFormatAnyOfType;
};

/**
 * Grammar format. Always `grammar`.
 */
export type CustomToolChatCompletionsCustomFormatAnyOfThreeType = typeof CustomToolChatCompletionsCustomFormatAnyOfThreeType[keyof typeof CustomToolChatCompletionsCustomFormatAnyOfThreeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolChatCompletionsCustomFormatAnyOfThreeType = {
  grammar: 'grammar',
} as const;

/**
 * The syntax of the grammar definition. One of `lark` or `regex`.
 */
export type CustomToolChatCompletionsCustomFormatAnyOfThreeGrammarSyntax = typeof CustomToolChatCompletionsCustomFormatAnyOfThreeGrammarSyntax[keyof typeof CustomToolChatCompletionsCustomFormatAnyOfThreeGrammarSyntax];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolChatCompletionsCustomFormatAnyOfThreeGrammarSyntax = {
  lark: 'lark',
  regex: 'regex',
} as const;

/**
 * Your chosen grammar.
 */
export type CustomToolChatCompletionsCustomFormatAnyOfThreeGrammar = {
  /** The grammar definition. */
  definition: string;
  /** The syntax of the grammar definition. One of `lark` or `regex`. */
  syntax: CustomToolChatCompletionsCustomFormatAnyOfThreeGrammarSyntax;
};

/**
 * A grammar defined by the user.
 */
export type CustomToolChatCompletionsCustomFormatAnyOfThree = {
  /** Grammar format. Always `grammar`. */
  type: CustomToolChatCompletionsCustomFormatAnyOfThreeType;
  /** Your chosen grammar. */
  grammar: CustomToolChatCompletionsCustomFormatAnyOfThreeGrammar;
};

/**
 * The input format for the custom tool. Default is unconstrained text.

 */
export type CustomToolChatCompletionsCustomFormat = CustomToolChatCompletionsCustomFormatAnyOf | CustomToolChatCompletionsCustomFormatAnyOfThree;

/**
 * Properties of the custom tool.

 */
export type CustomToolChatCompletionsCustom = {
  /** The name of the custom tool, used to identify it in tool calls. */
  name: string;
  /** Optional description of the custom tool, used to provide more context.
 */
  description?: string;
  /** The input format for the custom tool. Default is unconstrained text.
 */
  format?: CustomToolChatCompletionsCustomFormat;
};

/**
 * A custom tool that processes input using a specified format.

 */
export interface CustomToolChatCompletions {
  /** The type of the custom tool. Always `custom`. */
  type: CustomToolChatCompletionsType;
  /** Properties of the custom tool.
 */
  custom: CustomToolChatCompletionsCustom;
}

export type DeleteAssistantResponseObject = typeof DeleteAssistantResponseObject[keyof typeof DeleteAssistantResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteAssistantResponseObject = {
  assistantdeleted: 'assistant.deleted',
} as const;

export interface DeleteAssistantResponse {
  id: string;
  deleted: boolean;
  object: DeleteAssistantResponseObject;
}

export interface DeleteCertificateResponse {
  /** The object type, must be `certificate.deleted`. */
  object: 'certificate.deleted';
  /** The ID of the certificate that was deleted. */
  id: string;
}

export type DeleteFileResponseObject = typeof DeleteFileResponseObject[keyof typeof DeleteFileResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteFileResponseObject = {
  file: 'file',
} as const;

export interface DeleteFileResponse {
  id: string;
  object: DeleteFileResponseObject;
  deleted: boolean;
}

/**
 * The object type, which is always "checkpoint.permission".
 */
export type DeleteFineTuningCheckpointPermissionResponseObject = typeof DeleteFineTuningCheckpointPermissionResponseObject[keyof typeof DeleteFineTuningCheckpointPermissionResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteFineTuningCheckpointPermissionResponseObject = {
  checkpointpermission: 'checkpoint.permission',
} as const;

export interface DeleteFineTuningCheckpointPermissionResponse {
  /** The ID of the fine-tuned model checkpoint permission that was deleted. */
  id: string;
  /** The object type, which is always "checkpoint.permission". */
  object: DeleteFineTuningCheckpointPermissionResponseObject;
  /** Whether the fine-tuned model checkpoint permission was successfully deleted. */
  deleted: boolean;
}

export type DeleteMessageResponseObject = typeof DeleteMessageResponseObject[keyof typeof DeleteMessageResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteMessageResponseObject = {
  threadmessagedeleted: 'thread.message.deleted',
} as const;

export interface DeleteMessageResponse {
  id: string;
  deleted: boolean;
  object: DeleteMessageResponseObject;
}

export interface DeleteModelResponse {
  id: string;
  deleted: boolean;
  object: string;
}

export type DeleteThreadResponseObject = typeof DeleteThreadResponseObject[keyof typeof DeleteThreadResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteThreadResponseObject = {
  threaddeleted: 'thread.deleted',
} as const;

export interface DeleteThreadResponse {
  id: string;
  deleted: boolean;
  object: DeleteThreadResponseObject;
}

export type DeleteVectorStoreFileResponseObject = typeof DeleteVectorStoreFileResponseObject[keyof typeof DeleteVectorStoreFileResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteVectorStoreFileResponseObject = {
  vector_storefiledeleted: 'vector_store.file.deleted',
} as const;

export interface DeleteVectorStoreFileResponse {
  id: string;
  deleted: boolean;
  object: DeleteVectorStoreFileResponseObject;
}

export type DeleteVectorStoreResponseObject = typeof DeleteVectorStoreResponseObject[keyof typeof DeleteVectorStoreResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteVectorStoreResponseObject = {
  vector_storedeleted: 'vector_store.deleted',
} as const;

export interface DeleteVectorStoreResponse {
  id: string;
  deleted: boolean;
  object: DeleteVectorStoreResponseObject;
}

export type DeletedConversation = DeletedConversationResource;

export type DoneEventEvent = typeof DoneEventEvent[keyof typeof DoneEventEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DoneEventEvent = {
  done: 'done',
} as const;

export type DoneEventData = typeof DoneEventData[keyof typeof DoneEventData];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DoneEventData = {
  '[DONE]': '[DONE]',
} as const;

/**
 * Occurs when a stream ends.
 */
export interface DoneEvent {
  event: DoneEventEvent;
  data: DoneEventData;
}

/**
 * Specifies the event type. For a drag action, this property is
always set to `drag`.

 */
export type DragType = typeof DragType[keyof typeof DragType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DragType = {
  drag: 'drag',
} as const;

/**
 * A drag action.

 */
export interface Drag {
  /** Specifies the event type. For a drag action, this property is
always set to `drag`.
 */
  type: DragType;
  /** An array of coordinates representing the path of the drag action. Coordinates will appear as an array
of objects, eg
```
[
  { x: 100, y: 200 },
  { x: 200, y: 300 }
]
```
 */
  path: DragPoint[];
}

/**
 * The role of the message input. One of `user`, `assistant`, `system`, or
`developer`.

 */
export type EasyInputMessageRole = typeof EasyInputMessageRole[keyof typeof EasyInputMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EasyInputMessageRole = {
  user: 'user',
  assistant: 'assistant',
  system: 'system',
  developer: 'developer',
} as const;

/**
 * Text, image, or audio input to the model, used to generate a response.
Can also contain previous assistant responses.

 */
export type EasyInputMessageContent = string | InputMessageContentList;

/**
 * The type of the message input. Always `message`.

 */
export type EasyInputMessageType = typeof EasyInputMessageType[keyof typeof EasyInputMessageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EasyInputMessageType = {
  message: 'message',
} as const;

/**
 * A message input to the model with a role indicating instruction following
hierarchy. Instructions given with the `developer` or `system` role take
precedence over instructions given with the `user` role. Messages with the
`assistant` role are presumed to have been generated by the model in previous
interactions.

 */
export interface EasyInputMessage {
  /** The role of the message input. One of `user`, `assistant`, `system`, or
`developer`.
 */
  role: EasyInputMessageRole;
  /** Text, image, or audio input to the model, used to generate a response.
Can also contain previous assistant responses.
 */
  content: EasyInputMessageContent;
  /** The type of the message input. Always `message`.
 */
  type?: EasyInputMessageType;
}

/**
 * The object type, which is always "embedding".
 */
export type EmbeddingObject = typeof EmbeddingObject[keyof typeof EmbeddingObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EmbeddingObject = {
  embedding: 'embedding',
} as const;

/**
 * Represents an embedding vector returned by embedding endpoint.

 */
export interface Embedding {
  /** The index of the embedding in the list of embeddings. */
  index: number;
  /** The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](https://platform.openai.com/docs/guides/embeddings).
 */
  embedding: number[];
  /** The object type, which is always "embedding". */
  object: EmbeddingObject;
}

export type ErrorCode = string | null;

export type ErrorParam = string | null;

export interface Error {
  code: ErrorCode;
  message: string;
  param: ErrorParam;
  type: string;
}

export type ErrorEventEvent = typeof ErrorEventEvent[keyof typeof ErrorEventEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ErrorEventEvent = {
  error: 'error',
} as const;

/**
 * Occurs when an [error](https://platform.openai.com/docs/guides/error-codes#api-errors) occurs. This can happen due to an internal server error or a timeout.
 */
export interface ErrorEvent {
  event: ErrorEventEvent;
  data: Error;
}

export interface ErrorResponse {
  error: Error;
}

/**
 * The object type.
 */
export type EvalObject = typeof EvalObject[keyof typeof EvalObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalObject = {
  eval: 'eval',
} as const;

/**
 * Configuration of data sources used in runs of the evaluation.
 */
export type EvalDataSourceConfig = EvalCustomDataSourceConfig | EvalLogsDataSourceConfig | EvalStoredCompletionsDataSourceConfig;

export type EvalTestingCriteriaItem = EvalGraderLabelModel | EvalGraderStringCheck | EvalGraderTextSimilarity | EvalGraderPython | EvalGraderScoreModel;

/**
 * An Eval object with a data source config and testing criteria.
An Eval represents a task to be done for your LLM integration.
Like:
 - Improve the quality of my chatbot
 - See how well my chatbot handles customer support
 - Check if o4-mini is better at my usecase than gpt-4o

 */
export interface Eval {
  /** The object type. */
  object: EvalObject;
  /** Unique identifier for the evaluation. */
  id: string;
  /** The name of the evaluation. */
  name: string;
  /** Configuration of data sources used in runs of the evaluation. */
  data_source_config: EvalDataSourceConfig;
  /** A list of testing criteria. */
  testing_criteria: EvalTestingCriteriaItem[];
  /** The Unix timestamp (in seconds) for when the eval was created. */
  created_at: number;
  metadata: Metadata;
}

/**
 * An object representing an error response from the Eval API.

 */
export interface EvalApiError {
  /** The error code. */
  code: string;
  /** The error message. */
  message: string;
}

/**
 * The type of data source. Always `custom`.
 */
export type EvalCustomDataSourceConfigType = typeof EvalCustomDataSourceConfigType[keyof typeof EvalCustomDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalCustomDataSourceConfigType = {
  custom: 'custom',
} as const;

/**
 * The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).

 */
export type EvalCustomDataSourceConfigSchema = { [key: string]: unknown };

/**
 * A CustomDataSourceConfig which specifies the schema of your `item` and optionally `sample` namespaces.
The response schema defines the shape of the data that will be:
- Used to define your testing criteria and
- What data is required when creating a run

 */
export interface EvalCustomDataSourceConfig {
  /** The type of data source. Always `custom`. */
  type: EvalCustomDataSourceConfigType;
  /** The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).
 */
  schema: EvalCustomDataSourceConfigSchema;
}

export type EvalGraderLabelModel = GraderLabelModel;

export type EvalGraderPythonAllOf = {
  /** The threshold for the score. */
  pass_threshold?: number;
};

export type EvalGraderPython = GraderPython & EvalGraderPythonAllOf;

export type EvalGraderScoreModelAllOf = {
  /** The threshold for the score. */
  pass_threshold?: number;
};

export type EvalGraderScoreModel = GraderScoreModel & EvalGraderScoreModelAllOf;

export type EvalGraderStringCheck = GraderStringCheck;

export type EvalGraderTextSimilarityAllOf = {
  /** The threshold for the score. */
  pass_threshold: number;
};

export type EvalGraderTextSimilarity = GraderTextSimilarity & EvalGraderTextSimilarityAllOf;

/**
 * The role of the message input. One of `user`, `assistant`, `system`, or
`developer`.

 */
export type EvalItemRole = typeof EvalItemRole[keyof typeof EvalItemRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalItemRole = {
  user: 'user',
  assistant: 'assistant',
  system: 'system',
  developer: 'developer',
} as const;

/**
 * The type of the output text. Always `output_text`.

 */
export type EvalItemContentAnyOfType = typeof EvalItemContentAnyOfType[keyof typeof EvalItemContentAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalItemContentAnyOfType = {
  output_text: 'output_text',
} as const;

/**
 * A text output from the model.

 */
export type EvalItemContentAnyOf = {
  /** The type of the output text. Always `output_text`.
 */
  type: EvalItemContentAnyOfType;
  /** The text output from the model.
 */
  text: string;
};

/**
 * The type of the image input. Always `input_image`.

 */
export type EvalItemContentAnyOfThreeType = typeof EvalItemContentAnyOfThreeType[keyof typeof EvalItemContentAnyOfThreeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalItemContentAnyOfThreeType = {
  input_image: 'input_image',
} as const;

/**
 * An image input to the model.

 */
export type EvalItemContentAnyOfThree = {
  /** The type of the image input. Always `input_image`.
 */
  type: EvalItemContentAnyOfThreeType;
  /** The URL of the image input.
 */
  image_url: string;
  /** The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`.
 */
  detail?: string;
};

/**
 * Inputs to the model - can contain template strings.

 */
export type EvalItemContent = string | InputTextContent | EvalItemContentAnyOf | EvalItemContentAnyOfThree | InputAudio | unknown[];

/**
 * The type of the message input. Always `message`.

 */
export type EvalItemType = typeof EvalItemType[keyof typeof EvalItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalItemType = {
  message: 'message',
} as const;

/**
 * A message input to the model with a role indicating instruction following
hierarchy. Instructions given with the `developer` or `system` role take
precedence over instructions given with the `user` role. Messages with the
`assistant` role are presumed to have been generated by the model in previous
interactions.

 */
export interface EvalItem {
  /** The role of the message input. One of `user`, `assistant`, `system`, or
`developer`.
 */
  role: EvalItemRole;
  /** Inputs to the model - can contain template strings.
 */
  content: EvalItemContent;
  /** The type of the message input. Always `message`.
 */
  type?: EvalItemType;
}

/**
 * The type of jsonl source. Always `file_content`.
 */
export type EvalJsonlFileContentSourceType = typeof EvalJsonlFileContentSourceType[keyof typeof EvalJsonlFileContentSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalJsonlFileContentSourceType = {
  file_content: 'file_content',
} as const;

export type EvalJsonlFileContentSourceContentItemItem = { [key: string]: unknown };

export type EvalJsonlFileContentSourceContentItemSample = { [key: string]: unknown };

export type EvalJsonlFileContentSourceContentItem = {
  item: EvalJsonlFileContentSourceContentItemItem;
  sample?: EvalJsonlFileContentSourceContentItemSample;
};

export interface EvalJsonlFileContentSource {
  /** The type of jsonl source. Always `file_content`. */
  type: EvalJsonlFileContentSourceType;
  /** The content of the jsonl file. */
  content: EvalJsonlFileContentSourceContentItem[];
}

/**
 * The type of jsonl source. Always `file_id`.
 */
export type EvalJsonlFileIdSourceType = typeof EvalJsonlFileIdSourceType[keyof typeof EvalJsonlFileIdSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalJsonlFileIdSourceType = {
  file_id: 'file_id',
} as const;

export interface EvalJsonlFileIdSource {
  /** The type of jsonl source. Always `file_id`. */
  type: EvalJsonlFileIdSourceType;
  /** The identifier of the file. */
  id: string;
}

/**
 * The type of this object. It is always set to "list".

 */
export type EvalListObject = typeof EvalListObject[keyof typeof EvalListObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalListObject = {
  list: 'list',
} as const;

/**
 * An object representing a list of evals.

 */
export interface EvalList {
  /** The type of this object. It is always set to "list".
 */
  object: EvalListObject;
  /** An array of eval objects.
 */
  data: Eval[];
  /** The identifier of the first eval in the data array. */
  first_id: string;
  /** The identifier of the last eval in the data array. */
  last_id: string;
  /** Indicates whether there are more evals available. */
  has_more: boolean;
}

/**
 * The type of data source. Always `logs`.
 */
export type EvalLogsDataSourceConfigType = typeof EvalLogsDataSourceConfigType[keyof typeof EvalLogsDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalLogsDataSourceConfigType = {
  logs: 'logs',
} as const;

/**
 * The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).

 */
export type EvalLogsDataSourceConfigSchema = { [key: string]: unknown };

/**
 * A LogsDataSourceConfig which specifies the metadata property of your logs query.
This is usually metadata like `usecase=chatbot` or `prompt-version=v2`, etc.
The schema returned by this data source config is used to defined what variables are available in your evals.
`item` and `sample` are both defined when using this data source config.

 */
export interface EvalLogsDataSourceConfig {
  /** The type of data source. Always `logs`. */
  type: EvalLogsDataSourceConfigType;
  metadata?: Metadata;
  /** The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).
 */
  schema: EvalLogsDataSourceConfigSchema;
}

/**
 * The type of run data source. Always `responses`.
 */
export type EvalResponsesSourceType = typeof EvalResponsesSourceType[keyof typeof EvalResponsesSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalResponsesSourceType = {
  responses: 'responses',
} as const;

/**
 * Metadata filter for the responses. This is a query parameter used to select responses.
 */
export type EvalResponsesSourceMetadataAnyOf = { [key: string]: unknown };

export type EvalResponsesSourceMetadata = EvalResponsesSourceMetadataAnyOf | null;

export type EvalResponsesSourceModel = string | null;

export type EvalResponsesSourceInstructionsSearch = string | null;

export type EvalResponsesSourceCreatedAfter = number | null;

export type EvalResponsesSourceCreatedBefore = number | null;

export type EvalResponsesSourceReasoningEffort = ReasoningEffort | null;

export type EvalResponsesSourceTemperature = number | null;

export type EvalResponsesSourceTopP = number | null;

export type EvalResponsesSourceUsers = string[] | null;

export type EvalResponsesSourceTools = string[] | null;

/**
 * A EvalResponsesSource object describing a run data source configuration.

 */
export interface EvalResponsesSource {
  /** The type of run data source. Always `responses`. */
  type: EvalResponsesSourceType;
  metadata?: EvalResponsesSourceMetadata;
  model?: EvalResponsesSourceModel;
  instructions_search?: EvalResponsesSourceInstructionsSearch;
  created_after?: EvalResponsesSourceCreatedAfter;
  created_before?: EvalResponsesSourceCreatedBefore;
  reasoning_effort?: EvalResponsesSourceReasoningEffort;
  temperature?: EvalResponsesSourceTemperature;
  top_p?: EvalResponsesSourceTopP;
  users?: EvalResponsesSourceUsers;
  tools?: EvalResponsesSourceTools;
}

/**
 * The type of the object. Always "eval.run".
 */
export type EvalRunObject = typeof EvalRunObject[keyof typeof EvalRunObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalRunObject = {
  evalrun: 'eval.run',
} as const;

/**
 * Counters summarizing the outcomes of the evaluation run.
 */
export type EvalRunResultCounts = {
  /** Total number of executed output items. */
  total: number;
  /** Number of output items that resulted in an error. */
  errored: number;
  /** Number of output items that failed to pass the evaluation. */
  failed: number;
  /** Number of output items that passed the evaluation. */
  passed: number;
};

export type EvalRunPerModelUsageItem = {
  /** The name of the model. */
  model_name: string;
  /** The number of invocations. */
  invocation_count: number;
  /** The number of prompt tokens used. */
  prompt_tokens: number;
  /** The number of completion tokens generated. */
  completion_tokens: number;
  /** The total number of tokens used. */
  total_tokens: number;
  /** The number of tokens retrieved from cache. */
  cached_tokens: number;
};

export type EvalRunPerTestingCriteriaResultsItem = {
  /** A description of the testing criteria. */
  testing_criteria: string;
  /** Number of tests passed for this criteria. */
  passed: number;
  /** Number of tests failed for this criteria. */
  failed: number;
};

/**
 * Information about the run's data source.
 */
export type EvalRunDataSource = CreateEvalJsonlRunDataSource | CreateEvalCompletionsRunDataSource | CreateEvalResponsesRunDataSource;

/**
 * A schema representing an evaluation run.

 */
export interface EvalRun {
  /** The type of the object. Always "eval.run". */
  object: EvalRunObject;
  /** Unique identifier for the evaluation run. */
  id: string;
  /** The identifier of the associated evaluation. */
  eval_id: string;
  /** The status of the evaluation run. */
  status: string;
  /** The model that is evaluated, if applicable. */
  model: string;
  /** The name of the evaluation run. */
  name: string;
  /** Unix timestamp (in seconds) when the evaluation run was created. */
  created_at: number;
  /** The URL to the rendered evaluation run report on the UI dashboard. */
  report_url: string;
  /** Counters summarizing the outcomes of the evaluation run. */
  result_counts: EvalRunResultCounts;
  /** Usage statistics for each model during the evaluation run. */
  per_model_usage: EvalRunPerModelUsageItem[];
  /** Results per testing criteria applied during the evaluation run. */
  per_testing_criteria_results: EvalRunPerTestingCriteriaResultsItem[];
  /** Information about the run's data source. */
  data_source: EvalRunDataSource;
  metadata: Metadata;
  error: EvalApiError;
}

/**
 * The type of this object. It is always set to "list".

 */
export type EvalRunListObject = typeof EvalRunListObject[keyof typeof EvalRunListObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalRunListObject = {
  list: 'list',
} as const;

/**
 * An object representing a list of runs for an evaluation.

 */
export interface EvalRunList {
  /** The type of this object. It is always set to "list".
 */
  object: EvalRunListObject;
  /** An array of eval run objects.
 */
  data: EvalRun[];
  /** The identifier of the first eval run in the data array. */
  first_id: string;
  /** The identifier of the last eval run in the data array. */
  last_id: string;
  /** Indicates whether there are more evals available. */
  has_more: boolean;
}

/**
 * The type of the object. Always "eval.run.output_item".
 */
export type EvalRunOutputItemObject = typeof EvalRunOutputItemObject[keyof typeof EvalRunOutputItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalRunOutputItemObject = {
  evalrunoutput_item: 'eval.run.output_item',
} as const;

/**
 * Details of the input data source item.
 */
export type EvalRunOutputItemDatasourceItem = { [key: string]: unknown };

/**
 * An input message.
 */
export type EvalRunOutputItemSampleInputItem = {
  /** The role of the message sender (e.g., system, user, developer). */
  role: string;
  /** The content of the message. */
  content: string;
};

export type EvalRunOutputItemSampleOutputItem = {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role?: string;
  /** The content of the message. */
  content?: string;
};

/**
 * Token usage details for the sample.
 */
export type EvalRunOutputItemSampleUsage = {
  /** The total number of tokens used. */
  total_tokens: number;
  /** The number of completion tokens generated. */
  completion_tokens: number;
  /** The number of prompt tokens used. */
  prompt_tokens: number;
  /** The number of tokens retrieved from cache. */
  cached_tokens: number;
};

/**
 * A sample containing the input and output of the evaluation run.
 */
export type EvalRunOutputItemSample = {
  /** An array of input messages. */
  input: EvalRunOutputItemSampleInputItem[];
  /** An array of output messages. */
  output: EvalRunOutputItemSampleOutputItem[];
  /** The reason why the sample generation was finished. */
  finish_reason: string;
  /** The model used for generating the sample. */
  model: string;
  /** Token usage details for the sample. */
  usage: EvalRunOutputItemSampleUsage;
  error: EvalApiError;
  /** The sampling temperature used. */
  temperature: number;
  /** The maximum number of tokens allowed for completion. */
  max_completion_tokens: number;
  /** The top_p value used for sampling. */
  top_p: number;
  /** The seed used for generating the sample. */
  seed: number;
};

/**
 * A schema representing an evaluation run output item.

 */
export interface EvalRunOutputItem {
  /** The type of the object. Always "eval.run.output_item". */
  object: EvalRunOutputItemObject;
  /** Unique identifier for the evaluation run output item. */
  id: string;
  /** The identifier of the evaluation run associated with this output item. */
  run_id: string;
  /** The identifier of the evaluation group. */
  eval_id: string;
  /** Unix timestamp (in seconds) when the evaluation run was created. */
  created_at: number;
  /** The status of the evaluation run. */
  status: string;
  /** The identifier for the data source item. */
  datasource_item_id: number;
  /** Details of the input data source item. */
  datasource_item: EvalRunOutputItemDatasourceItem;
  /** A list of grader results for this output item. */
  results: EvalRunOutputItemResult[];
  /** A sample containing the input and output of the evaluation run. */
  sample: EvalRunOutputItemSample;
}

/**
 * The type of this object. It is always set to "list".

 */
export type EvalRunOutputItemListObject = typeof EvalRunOutputItemListObject[keyof typeof EvalRunOutputItemListObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalRunOutputItemListObject = {
  list: 'list',
} as const;

/**
 * An object representing a list of output items for an evaluation run.

 */
export interface EvalRunOutputItemList {
  /** The type of this object. It is always set to "list".
 */
  object: EvalRunOutputItemListObject;
  /** An array of eval run output item objects.
 */
  data: EvalRunOutputItem[];
  /** The identifier of the first eval run output item in the data array. */
  first_id: string;
  /** The identifier of the last eval run output item in the data array. */
  last_id: string;
  /** Indicates whether there are more eval run output items available. */
  has_more: boolean;
}

export type EvalRunOutputItemResultSampleAnyOf = { [key: string]: unknown };

/**
 * Optional sample or intermediate data produced by the grader.
 */
export type EvalRunOutputItemResultSample = EvalRunOutputItemResultSampleAnyOf | null;

/**
 * A single grader result for an evaluation run output item.

 */
export interface EvalRunOutputItemResult {
  /** The name of the grader. */
  name: string;
  /** The grader type (for example, "string-check-grader"). */
  type?: string;
  /** The numeric score produced by the grader. */
  score: number;
  /** Whether the grader considered the output a pass. */
  passed: boolean;
  /** Optional sample or intermediate data produced by the grader. */
  sample?: EvalRunOutputItemResultSample;
  [key: string]: unknown;
 }

/**
 * The type of data source. Always `stored_completions`.
 */
export type EvalStoredCompletionsDataSourceConfigType = typeof EvalStoredCompletionsDataSourceConfigType[keyof typeof EvalStoredCompletionsDataSourceConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalStoredCompletionsDataSourceConfigType = {
  stored_completions: 'stored_completions',
} as const;

/**
 * The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).

 */
export type EvalStoredCompletionsDataSourceConfigSchema = { [key: string]: unknown };

/**
 * Deprecated in favor of LogsDataSourceConfig.

 * @deprecated
 */
export interface EvalStoredCompletionsDataSourceConfig {
  /** The type of data source. Always `stored_completions`. */
  type: EvalStoredCompletionsDataSourceConfigType;
  metadata?: Metadata;
  /** The json schema for the run data source items.
Learn how to build JSON schemas [here](https://json-schema.org/).
 */
  schema: EvalStoredCompletionsDataSourceConfigSchema;
}

/**
 * The type of source. Always `stored_completions`.
 */
export type EvalStoredCompletionsSourceType = typeof EvalStoredCompletionsSourceType[keyof typeof EvalStoredCompletionsSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const EvalStoredCompletionsSourceType = {
  stored_completions: 'stored_completions',
} as const;

export type EvalStoredCompletionsSourceModel = string | null;

export type EvalStoredCompletionsSourceCreatedAfter = number | null;

export type EvalStoredCompletionsSourceCreatedBefore = number | null;

export type EvalStoredCompletionsSourceLimit = number | null;

/**
 * A StoredCompletionsRunDataSource configuration describing a set of filters

 */
export interface EvalStoredCompletionsSource {
  /** The type of source. Always `stored_completions`. */
  type: EvalStoredCompletionsSourceType;
  metadata?: Metadata;
  model?: EvalStoredCompletionsSourceModel;
  created_after?: EvalStoredCompletionsSourceCreatedAfter;
  created_before?: EvalStoredCompletionsSourceCreatedBefore;
  limit?: EvalStoredCompletionsSourceLimit;
}

/**
 * Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`.
 */
export type FileExpirationAfterAnchor = typeof FileExpirationAfterAnchor[keyof typeof FileExpirationAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileExpirationAfterAnchor = {
  created_at: 'created_at',
} as const;

/**
 * The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files are persisted until they are manually deleted.
 */
export interface FileExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`. */
  anchor: FileExpirationAfterAnchor;
  /**
   * The number of seconds after the anchor time that the file will expire. Must be between 3600 (1 hour) and 2592000 (30 days).
   * @minimum 3600
   * @maximum 2592000
   */
  seconds: number;
}

/**
 * The type of the file path. Always `file_path`.

 */
export type FilePathType = typeof FilePathType[keyof typeof FilePathType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FilePathType = {
  file_path: 'file_path',
} as const;

/**
 * A path to a file.

 */
export interface FilePath {
  /** The type of the file path. Always `file_path`.
 */
  type: FilePathType;
  /** The ID of the file.
 */
  file_id: string;
  /** The index of the file in the list of files.
 */
  index: number;
}

/**
 * The ranker to use for the file search. If not specified will use the `auto` ranker.
 */
export type FileSearchRanker = typeof FileSearchRanker[keyof typeof FileSearchRanker];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileSearchRanker = {
  auto: 'auto',
  default_2024_08_21: 'default_2024_08_21',
} as const;

/**
 * The ranking options for the file search. If not specified, the file search tool will use the `auto` ranker and a score_threshold of 0.

See the [file search tool documentation](https://platform.openai.com/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.

 */
export interface FileSearchRankingOptions {
  ranker?: FileSearchRanker;
  /**
   * The score threshold for the file search. All values must be a floating point number between 0 and 1.
   * @minimum 0
   * @maximum 1
   */
  score_threshold: number;
}

/**
 * The type of the file search tool call. Always `file_search_call`.

 */
export type FileSearchToolCallType = typeof FileSearchToolCallType[keyof typeof FileSearchToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileSearchToolCallType = {
  file_search_call: 'file_search_call',
} as const;

/**
 * The status of the file search tool call. One of `in_progress`,
`searching`, `incomplete` or `failed`,

 */
export type FileSearchToolCallStatus = typeof FileSearchToolCallStatus[keyof typeof FileSearchToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileSearchToolCallStatus = {
  in_progress: 'in_progress',
  searching: 'searching',
  completed: 'completed',
  incomplete: 'incomplete',
  failed: 'failed',
} as const;

export type FileSearchToolCallResultsAnyOfItem = {
  /** The unique ID of the file.
 */
  file_id?: string;
  /** The text that was retrieved from the file.
 */
  text?: string;
  /** The name of the file.
 */
  filename?: string;
  attributes?: VectorStoreFileAttributes;
  /** The relevance score of the file - a value between 0 and 1.
 */
  score?: number;
};

export type FileSearchToolCallResults = FileSearchToolCallResultsAnyOfItem[] | null;

/**
 * The results of a file search tool call. See the
[file search guide](https://platform.openai.com/docs/guides/tools-file-search) for more information.

 */
export interface FileSearchToolCall {
  /** The unique ID of the file search tool call.
 */
  id: string;
  /** The type of the file search tool call. Always `file_search_call`.
 */
  type: FileSearchToolCallType;
  /** The status of the file search tool call. One of `in_progress`,
`searching`, `incomplete` or `failed`,
 */
  status: FileSearchToolCallStatus;
  /** The queries used to search for files.
 */
  queries: string[];
  results?: FileSearchToolCallResults;
}

/**
 * Controls whether the assistant message is trained against (0 or 1)
 */
export type FineTuneChatCompletionRequestAssistantMessageAllOfWeight = typeof FineTuneChatCompletionRequestAssistantMessageAllOfWeight[keyof typeof FineTuneChatCompletionRequestAssistantMessageAllOfWeight];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuneChatCompletionRequestAssistantMessageAllOfWeight = {
  NUMBER_0: 0,
  NUMBER_1: 1,
} as const;

export type FineTuneChatCompletionRequestAssistantMessageAllOf = {
  /** Controls whether the assistant message is trained against (0 or 1) */
  weight?: FineTuneChatCompletionRequestAssistantMessageAllOfWeight;
};

export type FineTuneChatCompletionRequestAssistantMessage = FineTuneChatCompletionRequestAssistantMessageAllOf & ChatCompletionRequestAssistantMessage;

export type FineTuneChatRequestInputMessagesItem = ChatCompletionRequestSystemMessage | ChatCompletionRequestUserMessage | FineTuneChatCompletionRequestAssistantMessage | ChatCompletionRequestToolMessage | ChatCompletionRequestFunctionMessage;

/**
 * The per-line training example of a fine-tuning input file for chat models using the supervised method.
Input messages may contain text or image content only. Audio and file input messages
are not currently supported for fine-tuning.

 */
export interface FineTuneChatRequestInput {
  /** @minItems 1 */
  messages?: FineTuneChatRequestInputMessagesItem[];
  /** A list of tools the model may generate JSON inputs for. */
  tools?: ChatCompletionTool[];
  parallel_tool_calls?: ParallelToolCalls;
  /**
   * A list of functions the model may generate JSON inputs for.
   * @deprecated
   * @minItems 1
   * @maxItems 128
   */
  functions?: ChatCompletionFunctions[];
}

/**
 * The beta value for the DPO method. A higher beta value will increase the weight of the penalty between the policy and reference model.

 */
export type FineTuneDPOHyperparametersBeta = 'auto' | number;

/**
 * Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.

 */
export type FineTuneDPOHyperparametersBatchSize = 'auto' | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.

 */
export type FineTuneDPOHyperparametersLearningRateMultiplier = 'auto' | number;

/**
 * The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.

 */
export type FineTuneDPOHyperparametersNEpochs = 'auto' | number;

/**
 * The hyperparameters used for the DPO fine-tuning job.
 */
export interface FineTuneDPOHyperparameters {
  /** The beta value for the DPO method. A higher beta value will increase the weight of the penalty between the policy and reference model.
 */
  beta?: FineTuneDPOHyperparametersBeta;
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.
 */
  batch_size?: FineTuneDPOHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.
 */
  learning_rate_multiplier?: FineTuneDPOHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.
 */
  n_epochs?: FineTuneDPOHyperparametersNEpochs;
}

/**
 * Configuration for the DPO fine-tuning method.
 */
export interface FineTuneDPOMethod {
  hyperparameters?: FineTuneDPOHyperparameters;
}

/**
 * The type of method. Is either `supervised`, `dpo`, or `reinforcement`.
 */
export type FineTuneMethodType = typeof FineTuneMethodType[keyof typeof FineTuneMethodType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuneMethodType = {
  supervised: 'supervised',
  dpo: 'dpo',
  reinforcement: 'reinforcement',
} as const;

/**
 * The method used for fine-tuning.
 */
export interface FineTuneMethod {
  /** The type of method. Is either `supervised`, `dpo`, or `reinforcement`. */
  type: FineTuneMethodType;
  supervised?: FineTuneSupervisedMethod;
  dpo?: FineTuneDPOMethod;
  reinforcement?: FineTuneReinforcementMethod;
}

export type FineTunePreferenceRequestInputInputMessagesItem = ChatCompletionRequestSystemMessage | ChatCompletionRequestUserMessage | FineTuneChatCompletionRequestAssistantMessage | ChatCompletionRequestToolMessage | ChatCompletionRequestFunctionMessage;

export type FineTunePreferenceRequestInputInput = {
  /** @minItems 1 */
  messages?: FineTunePreferenceRequestInputInputMessagesItem[];
  /** A list of tools the model may generate JSON inputs for. */
  tools?: ChatCompletionTool[];
  parallel_tool_calls?: ParallelToolCalls;
};

/**
 * The per-line training example of a fine-tuning input file for chat models using the dpo method.
Input messages may contain text or image content only. Audio and file input messages
are not currently supported for fine-tuning.

 */
export interface FineTunePreferenceRequestInput {
  input?: FineTunePreferenceRequestInputInput;
  /**
   * The preferred completion message for the output.
   * @maxItems 1
   */
  preferred_output?: ChatCompletionRequestAssistantMessage[];
  /**
   * The non-preferred completion message for the output.
   * @maxItems 1
   */
  non_preferred_output?: ChatCompletionRequestAssistantMessage[];
}

/**
 * Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.

 */
export type FineTuneReinforcementHyperparametersBatchSize = 'auto' | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.

 */
export type FineTuneReinforcementHyperparametersLearningRateMultiplier = 'auto' | number;

/**
 * The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.

 */
export type FineTuneReinforcementHyperparametersNEpochs = 'auto' | number;

/**
 * Level of reasoning effort.

 */
export type FineTuneReinforcementHyperparametersReasoningEffort = typeof FineTuneReinforcementHyperparametersReasoningEffort[keyof typeof FineTuneReinforcementHyperparametersReasoningEffort];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuneReinforcementHyperparametersReasoningEffort = {
  default: 'default',
  low: 'low',
  medium: 'medium',
  high: 'high',
} as const;

/**
 * Multiplier on amount of compute used for exploring search space during training.

 */
export type FineTuneReinforcementHyperparametersComputeMultiplier = 'auto' | number;

/**
 * The number of training steps between evaluation runs.

 */
export type FineTuneReinforcementHyperparametersEvalInterval = 'auto' | number;

/**
 * Number of evaluation samples to generate per training step.

 */
export type FineTuneReinforcementHyperparametersEvalSamples = 'auto' | number;

/**
 * The hyperparameters used for the reinforcement fine-tuning job.
 */
export interface FineTuneReinforcementHyperparameters {
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.
 */
  batch_size?: FineTuneReinforcementHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.
 */
  learning_rate_multiplier?: FineTuneReinforcementHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.
 */
  n_epochs?: FineTuneReinforcementHyperparametersNEpochs;
  /** Level of reasoning effort.
 */
  reasoning_effort?: FineTuneReinforcementHyperparametersReasoningEffort;
  /** Multiplier on amount of compute used for exploring search space during training.
 */
  compute_multiplier?: FineTuneReinforcementHyperparametersComputeMultiplier;
  /** The number of training steps between evaluation runs.
 */
  eval_interval?: FineTuneReinforcementHyperparametersEvalInterval;
  /** Number of evaluation samples to generate per training step.
 */
  eval_samples?: FineTuneReinforcementHyperparametersEvalSamples;
}

/**
 * The grader used for the fine-tuning job.
 */
export type FineTuneReinforcementMethodGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

/**
 * Configuration for the reinforcement fine-tuning method.
 */
export interface FineTuneReinforcementMethod {
  /** The grader used for the fine-tuning job. */
  grader: FineTuneReinforcementMethodGrader;
  hyperparameters?: FineTuneReinforcementHyperparameters;
}

export type FineTuneReinforcementRequestInputMessagesItem = ChatCompletionRequestDeveloperMessage | ChatCompletionRequestUserMessage | FineTuneChatCompletionRequestAssistantMessage | ChatCompletionRequestToolMessage;

/**
 * Per-line training example for reinforcement fine-tuning. Note that `messages` and `tools` are the only reserved keywords.
Any other arbitrary key-value data can be included on training datapoints and will be available to reference during grading under the `{{ item.XXX }}` template variable.
Input messages may contain text or image content only. Audio and file input messages
are not currently supported for fine-tuning.

 */
export interface FineTuneReinforcementRequestInput {
  /** @minItems 1 */
  messages: FineTuneReinforcementRequestInputMessagesItem[];
  /** A list of tools the model may generate JSON inputs for. */
  tools?: ChatCompletionTool[];
}

/**
 * Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.

 */
export type FineTuneSupervisedHyperparametersBatchSize = 'auto' | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.

 */
export type FineTuneSupervisedHyperparametersLearningRateMultiplier = 'auto' | number;

/**
 * The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.

 */
export type FineTuneSupervisedHyperparametersNEpochs = 'auto' | number;

/**
 * The hyperparameters used for the fine-tuning job.
 */
export interface FineTuneSupervisedHyperparameters {
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance.
 */
  batch_size?: FineTuneSupervisedHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting.
 */
  learning_rate_multiplier?: FineTuneSupervisedHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset.
 */
  n_epochs?: FineTuneSupervisedHyperparametersNEpochs;
}

/**
 * Configuration for the supervised fine-tuning method.
 */
export interface FineTuneSupervisedMethod {
  hyperparameters?: FineTuneSupervisedHyperparameters;
}

/**
 * The object type, which is always "checkpoint.permission".
 */
export type FineTuningCheckpointPermissionObject = typeof FineTuningCheckpointPermissionObject[keyof typeof FineTuningCheckpointPermissionObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningCheckpointPermissionObject = {
  checkpointpermission: 'checkpoint.permission',
} as const;

/**
 * The `checkpoint.permission` object represents a permission for a fine-tuned model checkpoint.

 */
export interface FineTuningCheckpointPermission {
  /** The permission identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the permission was created. */
  created_at: number;
  /** The project identifier that the permission is for. */
  project_id: string;
  /** The object type, which is always "checkpoint.permission". */
  object: FineTuningCheckpointPermissionObject;
}

/**
 * The type of the integration being enabled for the fine-tuning job
 */
export type FineTuningIntegrationType = typeof FineTuningIntegrationType[keyof typeof FineTuningIntegrationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningIntegrationType = {
  wandb: 'wandb',
} as const;

export type FineTuningIntegrationWandbName = string | null;

export type FineTuningIntegrationWandbEntity = string | null;

/**
 * The settings for your integration with Weights and Biases. This payload specifies the project that
metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
to your run, and set a default entity (team, username, etc) to be associated with your run.

 */
export type FineTuningIntegrationWandb = {
  /** The name of the project that the new run will be created under.
 */
  project: string;
  name?: FineTuningIntegrationWandbName;
  entity?: FineTuningIntegrationWandbEntity;
  /** A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some
default tags are generated by OpenAI: "openai/finetune", "openai/{base-model}", "openai/{ftjob-abcdef}".
 */
  tags?: string[];
};

export interface FineTuningIntegration {
  /** The type of the integration being enabled for the fine-tuning job */
  type: FineTuningIntegrationType;
  /** The settings for your integration with Weights and Biases. This payload specifies the project that
metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
to your run, and set a default entity (team, username, etc) to be associated with your run.
 */
  wandb: FineTuningIntegrationWandb;
}

export type FineTuningJobErrorAnyOfParam = string | null;

/**
 * For fine-tuning jobs that have `failed`, this will contain more information on the cause of the failure.
 */
export type FineTuningJobErrorAnyOf = {
  /** A machine-readable error code. */
  code: string;
  /** A human-readable error message. */
  message: string;
  param: FineTuningJobErrorAnyOfParam;
};

export type FineTuningJobError = FineTuningJobErrorAnyOf | null;

export type FineTuningJobFineTunedModel = string | null;

export type FineTuningJobFinishedAt = number | null;

/**
 * Number of examples in each batch. A larger batch size means that model parameters
are updated less frequently, but with lower variance.

 */
export type FineTuningJobHyperparametersBatchSizeAnyOf = 'auto' | number;

export type FineTuningJobHyperparametersBatchSize = FineTuningJobHyperparametersBatchSizeAnyOf | null;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
overfitting.

 */
export type FineTuningJobHyperparametersLearningRateMultiplier = 'auto' | number;

/**
 * The number of epochs to train the model for. An epoch refers to one full cycle
through the training dataset.

 */
export type FineTuningJobHyperparametersNEpochs = 'auto' | number;

/**
 * The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs.
 */
export type FineTuningJobHyperparameters = {
  batch_size?: FineTuningJobHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
overfitting.
 */
  learning_rate_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle
through the training dataset.
 */
  n_epochs?: FineTuningJobHyperparametersNEpochs;
};

/**
 * The object type, which is always "fine_tuning.job".
 */
export type FineTuningJobObject = typeof FineTuningJobObject[keyof typeof FineTuningJobObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobObject = {
  fine_tuningjob: 'fine_tuning.job',
} as const;

/**
 * The current status of the fine-tuning job, which can be either `validating_files`, `queued`, `running`, `succeeded`, `failed`, or `cancelled`.
 */
export type FineTuningJobStatus = typeof FineTuningJobStatus[keyof typeof FineTuningJobStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobStatus = {
  validating_files: 'validating_files',
  queued: 'queued',
  running: 'running',
  succeeded: 'succeeded',
  failed: 'failed',
  cancelled: 'cancelled',
} as const;

export type FineTuningJobTrainedTokens = number | null;

export type FineTuningJobValidationFile = string | null;

export type FineTuningJobIntegrations = FineTuningIntegration[] | null;

export type FineTuningJobEstimatedFinish = number | null;

/**
 * The `fine_tuning.job` object represents a fine-tuning job that has been created through the API.

 */
export interface FineTuningJob {
  /** The object identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the fine-tuning job was created. */
  created_at: number;
  error: FineTuningJobError;
  fine_tuned_model: FineTuningJobFineTunedModel;
  finished_at: FineTuningJobFinishedAt;
  /** The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs. */
  hyperparameters: FineTuningJobHyperparameters;
  /** The base model that is being fine-tuned. */
  model: string;
  /** The object type, which is always "fine_tuning.job". */
  object: FineTuningJobObject;
  /** The organization that owns the fine-tuning job. */
  organization_id: string;
  /** The compiled results file ID(s) for the fine-tuning job. You can retrieve the results with the [Files API](https://platform.openai.com/docs/api-reference/files/retrieve-contents). */
  result_files: string[];
  /** The current status of the fine-tuning job, which can be either `validating_files`, `queued`, `running`, `succeeded`, `failed`, or `cancelled`. */
  status: FineTuningJobStatus;
  trained_tokens: FineTuningJobTrainedTokens;
  /** The file ID used for training. You can retrieve the training data with the [Files API](https://platform.openai.com/docs/api-reference/files/retrieve-contents). */
  training_file: string;
  validation_file: FineTuningJobValidationFile;
  integrations?: FineTuningJobIntegrations;
  /** The seed used for the fine-tuning job. */
  seed: number;
  estimated_finish?: FineTuningJobEstimatedFinish;
  method?: FineTuneMethod;
  metadata?: Metadata;
}

/**
 * Metrics at the step number during the fine-tuning job.
 */
export type FineTuningJobCheckpointMetrics = {
  step?: number;
  train_loss?: number;
  train_mean_token_accuracy?: number;
  valid_loss?: number;
  valid_mean_token_accuracy?: number;
  full_valid_loss?: number;
  full_valid_mean_token_accuracy?: number;
};

/**
 * The object type, which is always "fine_tuning.job.checkpoint".
 */
export type FineTuningJobCheckpointObject = typeof FineTuningJobCheckpointObject[keyof typeof FineTuningJobCheckpointObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobCheckpointObject = {
  fine_tuningjobcheckpoint: 'fine_tuning.job.checkpoint',
} as const;

/**
 * The `fine_tuning.job.checkpoint` object represents a model checkpoint for a fine-tuning job that is ready to use.

 */
export interface FineTuningJobCheckpoint {
  /** The checkpoint identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the checkpoint was created. */
  created_at: number;
  /** The name of the fine-tuned checkpoint model that is created. */
  fine_tuned_model_checkpoint: string;
  /** The step number that the checkpoint was created at. */
  step_number: number;
  /** Metrics at the step number during the fine-tuning job. */
  metrics: FineTuningJobCheckpointMetrics;
  /** The name of the fine-tuning job that this checkpoint was created from. */
  fine_tuning_job_id: string;
  /** The object type, which is always "fine_tuning.job.checkpoint". */
  object: FineTuningJobCheckpointObject;
}

/**
 * The object type, which is always "fine_tuning.job.event".
 */
export type FineTuningJobEventObject = typeof FineTuningJobEventObject[keyof typeof FineTuningJobEventObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobEventObject = {
  fine_tuningjobevent: 'fine_tuning.job.event',
} as const;

/**
 * The log level of the event.
 */
export type FineTuningJobEventLevel = typeof FineTuningJobEventLevel[keyof typeof FineTuningJobEventLevel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobEventLevel = {
  info: 'info',
  warn: 'warn',
  error: 'error',
} as const;

/**
 * The type of event.
 */
export type FineTuningJobEventType = typeof FineTuningJobEventType[keyof typeof FineTuningJobEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FineTuningJobEventType = {
  message: 'message',
  metrics: 'metrics',
} as const;

/**
 * The data associated with the event.
 */
export type FineTuningJobEventData = { [key: string]: unknown };

/**
 * Fine-tuning job event object
 */
export interface FineTuningJobEvent {
  /** The object type, which is always "fine_tuning.job.event". */
  object: FineTuningJobEventObject;
  /** The object identifier. */
  id: string;
  /** The Unix timestamp (in seconds) for when the fine-tuning job was created. */
  created_at: number;
  /** The log level of the event. */
  level: FineTuningJobEventLevel;
  /** The message of the event. */
  message: string;
  /** The type of event. */
  type?: FineTuningJobEventType;
  /** The data associated with the event. */
  data?: FineTuningJobEventData;
}

export type FunctionAndCustomToolCallOutput = InputTextContent | InputImageContent | InputFileContent;

export type FunctionObjectStrict = boolean | null;

export interface FunctionObject {
  /** A description of what the function does, used by the model to choose when and how to call the function. */
  description?: string;
  /** The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64. */
  name: string;
  parameters?: FunctionParameters;
  strict?: FunctionObjectStrict;
}

/**
 * The parameters the functions accepts, described as a JSON Schema object. See the [guide](https://platform.openai.com/docs/guides/function-calling) for examples, and the [JSON Schema reference](https://json-schema.org/understanding-json-schema/) for documentation about the format.

Omitting `parameters` defines a function with an empty parameter list.
 */
export interface FunctionParameters { [key: string]: unknown }

/**
 * The type of the function tool call. Always `function_call`.

 */
export type FunctionToolCallType = typeof FunctionToolCallType[keyof typeof FunctionToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionToolCallType = {
  function_call: 'function_call',
} as const;

/**
 * The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.

 */
export type FunctionToolCallStatus = typeof FunctionToolCallStatus[keyof typeof FunctionToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * A tool call to run a function. See the
[function calling guide](https://platform.openai.com/docs/guides/function-calling) for more information.

 */
export interface FunctionToolCall {
  /** The unique ID of the function tool call.
 */
  id?: string;
  /** The type of the function tool call. Always `function_call`.
 */
  type: FunctionToolCallType;
  /** The unique ID of the function tool call generated by the model.
 */
  call_id: string;
  /** The name of the function to run.
 */
  name: string;
  /** A JSON string of the arguments to pass to the function.
 */
  arguments: string;
  /** The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
 */
  status?: FunctionToolCallStatus;
}

/**
 * The type of the function tool call output. Always `function_call_output`.

 */
export type FunctionToolCallOutputType = typeof FunctionToolCallOutputType[keyof typeof FunctionToolCallOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionToolCallOutputType = {
  function_call_output: 'function_call_output',
} as const;

/**
 * The output from the function call generated by your code.
Can be a string or an list of output content.

 */
export type FunctionToolCallOutputOutput = string | FunctionAndCustomToolCallOutput[];

/**
 * The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.

 */
export type FunctionToolCallOutputStatus = typeof FunctionToolCallOutputStatus[keyof typeof FunctionToolCallOutputStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionToolCallOutputStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * The output of a function tool call.

 */
export interface FunctionToolCallOutput {
  /** The unique ID of the function tool call output. Populated when this item
is returned via API.
 */
  id?: string;
  /** The type of the function tool call output. Always `function_call_output`.
 */
  type: FunctionToolCallOutputType;
  /** The unique ID of the function tool call generated by the model.
 */
  call_id: string;
  /** The output from the function call generated by your code.
Can be a string or an list of output content.
 */
  output: FunctionToolCallOutputOutput;
  /** The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
 */
  status?: FunctionToolCallOutputStatus;
}

export type FunctionToolCallOutputResourceAllOf = {
  /** The unique ID of the function call tool output.
 */
  id: string;
};

export type FunctionToolCallOutputResource = FunctionToolCallOutput & FunctionToolCallOutputResourceAllOf;

export type FunctionToolCallResourceAllOf = {
  /** The unique ID of the function tool call.
 */
  id: string;
};

export type FunctionToolCallResource = FunctionToolCall & FunctionToolCallResourceAllOf;

/**
 * The object type, which is always `label_model`.
 */
export type GraderLabelModelType = typeof GraderLabelModelType[keyof typeof GraderLabelModelType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderLabelModelType = {
  label_model: 'label_model',
} as const;

/**
 * A LabelModelGrader object which uses a model to assign labels to each item
in the evaluation.

 */
export interface GraderLabelModel {
  /** The object type, which is always `label_model`. */
  type: GraderLabelModelType;
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. Must support structured outputs. */
  model: string;
  input: EvalItem[];
  /** The labels to assign to each item in the evaluation. */
  labels: string[];
  /** The labels that indicate a passing result. Must be a subset of labels. */
  passing_labels: string[];
}

/**
 * The object type, which is always `multi`.
 */
export type GraderMultiType = typeof GraderMultiType[keyof typeof GraderMultiType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderMultiType = {
  multi: 'multi',
} as const;

export type GraderMultiGraders = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderLabelModel;

/**
 * A MultiGrader object combines the output of multiple graders to produce a single score.
 */
export interface GraderMulti {
  /** The object type, which is always `multi`. */
  type: GraderMultiType;
  /** The name of the grader. */
  name: string;
  graders: GraderMultiGraders;
  /** A formula to calculate the output based on grader results. */
  calculate_output: string;
}

/**
 * The object type, which is always `python`.
 */
export type GraderPythonType = typeof GraderPythonType[keyof typeof GraderPythonType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderPythonType = {
  python: 'python',
} as const;

/**
 * A PythonGrader object that runs a python script on the input.

 */
export interface GraderPython {
  /** The object type, which is always `python`. */
  type: GraderPythonType;
  /** The name of the grader. */
  name: string;
  /** The source code of the python script. */
  source: string;
  /** The image tag to use for the python script. */
  image_tag?: string;
}

/**
 * The object type, which is always `score_model`.
 */
export type GraderScoreModelType = typeof GraderScoreModelType[keyof typeof GraderScoreModelType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderScoreModelType = {
  score_model: 'score_model',
} as const;

export type GraderScoreModelSamplingParamsSeed = number | null;

export type GraderScoreModelSamplingParamsTopP = number | null;

export type GraderScoreModelSamplingParamsTemperature = number | null;

export type GraderScoreModelSamplingParamsMaxCompletionsTokens = number | null;

/**
 * The sampling parameters for the model.
 */
export type GraderScoreModelSamplingParams = {
  seed?: GraderScoreModelSamplingParamsSeed;
  top_p?: GraderScoreModelSamplingParamsTopP;
  temperature?: GraderScoreModelSamplingParamsTemperature;
  max_completions_tokens?: GraderScoreModelSamplingParamsMaxCompletionsTokens;
  reasoning_effort?: ReasoningEffort;
};

/**
 * A ScoreModelGrader object that uses a model to assign a score to the input.

 */
export interface GraderScoreModel {
  /** The object type, which is always `score_model`. */
  type: GraderScoreModelType;
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. */
  model: string;
  /** The sampling parameters for the model. */
  sampling_params?: GraderScoreModelSamplingParams;
  /** The input text. This may include template strings. */
  input: EvalItem[];
  /** The range of the score. Defaults to `[0, 1]`. */
  range?: number[];
}

/**
 * The object type, which is always `string_check`.
 */
export type GraderStringCheckType = typeof GraderStringCheckType[keyof typeof GraderStringCheckType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderStringCheckType = {
  string_check: 'string_check',
} as const;

/**
 * The string check operation to perform. One of `eq`, `ne`, `like`, or `ilike`.
 */
export type GraderStringCheckOperation = typeof GraderStringCheckOperation[keyof typeof GraderStringCheckOperation];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderStringCheckOperation = {
  eq: 'eq',
  ne: 'ne',
  like: 'like',
  ilike: 'ilike',
} as const;

/**
 * A StringCheckGrader object that performs a string comparison between input and reference using a specified operation.

 */
export interface GraderStringCheck {
  /** The object type, which is always `string_check`. */
  type: GraderStringCheckType;
  /** The name of the grader. */
  name: string;
  /** The input text. This may include template strings. */
  input: string;
  /** The reference text. This may include template strings. */
  reference: string;
  /** The string check operation to perform. One of `eq`, `ne`, `like`, or `ilike`. */
  operation: GraderStringCheckOperation;
}

/**
 * The type of grader.
 */
export type GraderTextSimilarityType = typeof GraderTextSimilarityType[keyof typeof GraderTextSimilarityType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderTextSimilarityType = {
  text_similarity: 'text_similarity',
} as const;

/**
 * The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
`gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
or `rouge_l`.

 */
export type GraderTextSimilarityEvaluationMetric = typeof GraderTextSimilarityEvaluationMetric[keyof typeof GraderTextSimilarityEvaluationMetric];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GraderTextSimilarityEvaluationMetric = {
  cosine: 'cosine',
  fuzzy_match: 'fuzzy_match',
  bleu: 'bleu',
  gleu: 'gleu',
  meteor: 'meteor',
  rouge_1: 'rouge_1',
  rouge_2: 'rouge_2',
  rouge_3: 'rouge_3',
  rouge_4: 'rouge_4',
  rouge_5: 'rouge_5',
  rouge_l: 'rouge_l',
} as const;

/**
 * A TextSimilarityGrader object which grades text based on similarity metrics.

 */
export interface GraderTextSimilarity {
  /** The type of grader. */
  type: GraderTextSimilarityType;
  /** The name of the grader. */
  name: string;
  /** The text being graded. */
  input: string;
  /** The text being graded against. */
  reference: string;
  /** The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
`gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
or `rouge_l`.
 */
  evaluation_metric: GraderTextSimilarityEvaluationMetric;
}

/**
 * Represents the content or the URL of an image generated by the OpenAI API.
 */
export interface Image {
  /** The base64-encoded JSON of the generated image. Default value for `gpt-image-1`, and only present if `response_format` is set to `b64_json` for `dall-e-2` and `dall-e-3`. */
  b64_json?: string;
  /** When using `dall-e-2` or `dall-e-3`, the URL of the generated image if `response_format` is set to `url` (default value). Unsupported for `gpt-image-1`. */
  url?: string;
  /** For `dall-e-3` only, the revised prompt that was used to generate the image. */
  revised_prompt?: string;
}

/**
 * The type of the event. Always `image_edit.completed`.

 */
export type ImageEditCompletedEventType = typeof ImageEditCompletedEventType[keyof typeof ImageEditCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditCompletedEventType = {
  image_editcompleted: 'image_edit.completed',
} as const;

/**
 * The size of the edited image.

 */
export type ImageEditCompletedEventSize = typeof ImageEditCompletedEventSize[keyof typeof ImageEditCompletedEventSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditCompletedEventSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
  auto: 'auto',
} as const;

/**
 * The quality setting for the edited image.

 */
export type ImageEditCompletedEventQuality = typeof ImageEditCompletedEventQuality[keyof typeof ImageEditCompletedEventQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditCompletedEventQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The background setting for the edited image.

 */
export type ImageEditCompletedEventBackground = typeof ImageEditCompletedEventBackground[keyof typeof ImageEditCompletedEventBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditCompletedEventBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The output format for the edited image.

 */
export type ImageEditCompletedEventOutputFormat = typeof ImageEditCompletedEventOutputFormat[keyof typeof ImageEditCompletedEventOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditCompletedEventOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * Emitted when image editing has completed and the final image is available.

 */
export interface ImageEditCompletedEvent {
  /** The type of the event. Always `image_edit.completed`.
 */
  type: ImageEditCompletedEventType;
  /** Base64-encoded final edited image data, suitable for rendering as an image.
 */
  b64_json: string;
  /** The Unix timestamp when the event was created.
 */
  created_at: number;
  /** The size of the edited image.
 */
  size: ImageEditCompletedEventSize;
  /** The quality setting for the edited image.
 */
  quality: ImageEditCompletedEventQuality;
  /** The background setting for the edited image.
 */
  background: ImageEditCompletedEventBackground;
  /** The output format for the edited image.
 */
  output_format: ImageEditCompletedEventOutputFormat;
  usage: ImagesUsage;
}

/**
 * The type of the event. Always `image_edit.partial_image`.

 */
export type ImageEditPartialImageEventType = typeof ImageEditPartialImageEventType[keyof typeof ImageEditPartialImageEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditPartialImageEventType = {
  image_editpartial_image: 'image_edit.partial_image',
} as const;

/**
 * The size of the requested edited image.

 */
export type ImageEditPartialImageEventSize = typeof ImageEditPartialImageEventSize[keyof typeof ImageEditPartialImageEventSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditPartialImageEventSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
  auto: 'auto',
} as const;

/**
 * The quality setting for the requested edited image.

 */
export type ImageEditPartialImageEventQuality = typeof ImageEditPartialImageEventQuality[keyof typeof ImageEditPartialImageEventQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditPartialImageEventQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The background setting for the requested edited image.

 */
export type ImageEditPartialImageEventBackground = typeof ImageEditPartialImageEventBackground[keyof typeof ImageEditPartialImageEventBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditPartialImageEventBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The output format for the requested edited image.

 */
export type ImageEditPartialImageEventOutputFormat = typeof ImageEditPartialImageEventOutputFormat[keyof typeof ImageEditPartialImageEventOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageEditPartialImageEventOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * Emitted when a partial image is available during image editing streaming.

 */
export interface ImageEditPartialImageEvent {
  /** The type of the event. Always `image_edit.partial_image`.
 */
  type: ImageEditPartialImageEventType;
  /** Base64-encoded partial image data, suitable for rendering as an image.
 */
  b64_json: string;
  /** The Unix timestamp when the event was created.
 */
  created_at: number;
  /** The size of the requested edited image.
 */
  size: ImageEditPartialImageEventSize;
  /** The quality setting for the requested edited image.
 */
  quality: ImageEditPartialImageEventQuality;
  /** The background setting for the requested edited image.
 */
  background: ImageEditPartialImageEventBackground;
  /** The output format for the requested edited image.
 */
  output_format: ImageEditPartialImageEventOutputFormat;
  /** 0-based index for the partial image (streaming).
 */
  partial_image_index: number;
}

export type ImageEditStreamEvent = ImageEditPartialImageEvent | ImageEditCompletedEvent;

/**
 * The type of the event. Always `image_generation.completed`.

 */
export type ImageGenCompletedEventType = typeof ImageGenCompletedEventType[keyof typeof ImageGenCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenCompletedEventType = {
  image_generationcompleted: 'image_generation.completed',
} as const;

/**
 * The size of the generated image.

 */
export type ImageGenCompletedEventSize = typeof ImageGenCompletedEventSize[keyof typeof ImageGenCompletedEventSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenCompletedEventSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
  auto: 'auto',
} as const;

/**
 * The quality setting for the generated image.

 */
export type ImageGenCompletedEventQuality = typeof ImageGenCompletedEventQuality[keyof typeof ImageGenCompletedEventQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenCompletedEventQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The background setting for the generated image.

 */
export type ImageGenCompletedEventBackground = typeof ImageGenCompletedEventBackground[keyof typeof ImageGenCompletedEventBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenCompletedEventBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The output format for the generated image.

 */
export type ImageGenCompletedEventOutputFormat = typeof ImageGenCompletedEventOutputFormat[keyof typeof ImageGenCompletedEventOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenCompletedEventOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * Emitted when image generation has completed and the final image is available.

 */
export interface ImageGenCompletedEvent {
  /** The type of the event. Always `image_generation.completed`.
 */
  type: ImageGenCompletedEventType;
  /** Base64-encoded image data, suitable for rendering as an image.
 */
  b64_json: string;
  /** The Unix timestamp when the event was created.
 */
  created_at: number;
  /** The size of the generated image.
 */
  size: ImageGenCompletedEventSize;
  /** The quality setting for the generated image.
 */
  quality: ImageGenCompletedEventQuality;
  /** The background setting for the generated image.
 */
  background: ImageGenCompletedEventBackground;
  /** The output format for the generated image.
 */
  output_format: ImageGenCompletedEventOutputFormat;
  usage: ImagesUsage;
}

/**
 * The type of the event. Always `image_generation.partial_image`.

 */
export type ImageGenPartialImageEventType = typeof ImageGenPartialImageEventType[keyof typeof ImageGenPartialImageEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenPartialImageEventType = {
  image_generationpartial_image: 'image_generation.partial_image',
} as const;

/**
 * The size of the requested image.

 */
export type ImageGenPartialImageEventSize = typeof ImageGenPartialImageEventSize[keyof typeof ImageGenPartialImageEventSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenPartialImageEventSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
  auto: 'auto',
} as const;

/**
 * The quality setting for the requested image.

 */
export type ImageGenPartialImageEventQuality = typeof ImageGenPartialImageEventQuality[keyof typeof ImageGenPartialImageEventQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenPartialImageEventQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The background setting for the requested image.

 */
export type ImageGenPartialImageEventBackground = typeof ImageGenPartialImageEventBackground[keyof typeof ImageGenPartialImageEventBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenPartialImageEventBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

/**
 * The output format for the requested image.

 */
export type ImageGenPartialImageEventOutputFormat = typeof ImageGenPartialImageEventOutputFormat[keyof typeof ImageGenPartialImageEventOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenPartialImageEventOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * Emitted when a partial image is available during image generation streaming.

 */
export interface ImageGenPartialImageEvent {
  /** The type of the event. Always `image_generation.partial_image`.
 */
  type: ImageGenPartialImageEventType;
  /** Base64-encoded partial image data, suitable for rendering as an image.
 */
  b64_json: string;
  /** The Unix timestamp when the event was created.
 */
  created_at: number;
  /** The size of the requested image.
 */
  size: ImageGenPartialImageEventSize;
  /** The quality setting for the requested image.
 */
  quality: ImageGenPartialImageEventQuality;
  /** The background setting for the requested image.
 */
  background: ImageGenPartialImageEventBackground;
  /** The output format for the requested image.
 */
  output_format: ImageGenPartialImageEventOutputFormat;
  /** 0-based index for the partial image (streaming).
 */
  partial_image_index: number;
}

export type ImageGenStreamEvent = ImageGenPartialImageEvent | ImageGenCompletedEvent;

/**
 * The type of the image generation tool. Always `image_generation`.

 */
export type ImageGenToolType = typeof ImageGenToolType[keyof typeof ImageGenToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolType = {
  image_generation: 'image_generation',
} as const;

/**
 * The image generation model to use. Default: `gpt-image-1`.

 */
export type ImageGenToolModel = typeof ImageGenToolModel[keyof typeof ImageGenToolModel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolModel = {
  'gpt-image-1': 'gpt-image-1',
  'gpt-image-1-mini': 'gpt-image-1-mini',
} as const;

/**
 * The quality of the generated image. One of `low`, `medium`, `high`,
or `auto`. Default: `auto`.

 */
export type ImageGenToolQuality = typeof ImageGenToolQuality[keyof typeof ImageGenToolQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The size of the generated image. One of `1024x1024`, `1024x1536`,
`1536x1024`, or `auto`. Default: `auto`.

 */
export type ImageGenToolSize = typeof ImageGenToolSize[keyof typeof ImageGenToolSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
  auto: 'auto',
} as const;

/**
 * The output format of the generated image. One of `png`, `webp`, or
`jpeg`. Default: `png`.

 */
export type ImageGenToolOutputFormat = typeof ImageGenToolOutputFormat[keyof typeof ImageGenToolOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * Moderation level for the generated image. Default: `auto`.

 */
export type ImageGenToolModeration = typeof ImageGenToolModeration[keyof typeof ImageGenToolModeration];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolModeration = {
  auto: 'auto',
  low: 'low',
} as const;

/**
 * Background type for the generated image. One of `transparent`,
`opaque`, or `auto`. Default: `auto`.

 */
export type ImageGenToolBackground = typeof ImageGenToolBackground[keyof typeof ImageGenToolBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
  auto: 'auto',
} as const;

export type ImageGenToolInputFidelity = InputFidelity | null;

/**
 * Optional mask for inpainting. Contains `image_url`
(string, optional) and `file_id` (string, optional).

 */
export type ImageGenToolInputImageMask = {
  /** Base64-encoded mask image.
 */
  image_url?: string;
  /** File ID for the mask image.
 */
  file_id?: string;
};

/**
 * A tool that generates images using a model like `gpt-image-1`.

 */
export interface ImageGenTool {
  /** The type of the image generation tool. Always `image_generation`.
 */
  type: ImageGenToolType;
  /** The image generation model to use. Default: `gpt-image-1`.
 */
  model?: ImageGenToolModel;
  /** The quality of the generated image. One of `low`, `medium`, `high`,
or `auto`. Default: `auto`.
 */
  quality?: ImageGenToolQuality;
  /** The size of the generated image. One of `1024x1024`, `1024x1536`,
`1536x1024`, or `auto`. Default: `auto`.
 */
  size?: ImageGenToolSize;
  /** The output format of the generated image. One of `png`, `webp`, or
`jpeg`. Default: `png`.
 */
  output_format?: ImageGenToolOutputFormat;
  /**
   * Compression level for the output image. Default: 100.

   * @minimum 0
   * @maximum 100
   */
  output_compression?: number;
  /** Moderation level for the generated image. Default: `auto`.
 */
  moderation?: ImageGenToolModeration;
  /** Background type for the generated image. One of `transparent`,
`opaque`, or `auto`. Default: `auto`.
 */
  background?: ImageGenToolBackground;
  input_fidelity?: ImageGenToolInputFidelity;
  /** Optional mask for inpainting. Contains `image_url`
(string, optional) and `file_id` (string, optional).
 */
  input_image_mask?: ImageGenToolInputImageMask;
  /**
   * Number of partial images to generate in streaming mode, from 0 (default value) to 3.

   * @minimum 0
   * @maximum 3
   */
  partial_images?: number;
}

/**
 * The type of the image generation call. Always `image_generation_call`.

 */
export type ImageGenToolCallType = typeof ImageGenToolCallType[keyof typeof ImageGenToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolCallType = {
  image_generation_call: 'image_generation_call',
} as const;

/**
 * The status of the image generation call.

 */
export type ImageGenToolCallStatus = typeof ImageGenToolCallStatus[keyof typeof ImageGenToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageGenToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  generating: 'generating',
  failed: 'failed',
} as const;

export type ImageGenToolCallResult = string | null;

/**
 * An image generation request made by the model.

 */
export interface ImageGenToolCall {
  /** The type of the image generation call. Always `image_generation_call`.
 */
  type: ImageGenToolCallType;
  /** The unique ID of the image generation call.
 */
  id: string;
  /** The status of the image generation call.
 */
  status: ImageGenToolCallStatus;
  result: ImageGenToolCallResult;
}

/**
 * The background parameter used for the image generation. Either `transparent` or `opaque`.
 */
export type ImagesResponseBackground = typeof ImagesResponseBackground[keyof typeof ImagesResponseBackground];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImagesResponseBackground = {
  transparent: 'transparent',
  opaque: 'opaque',
} as const;

/**
 * The output format of the image generation. Either `png`, `webp`, or `jpeg`.
 */
export type ImagesResponseOutputFormat = typeof ImagesResponseOutputFormat[keyof typeof ImagesResponseOutputFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImagesResponseOutputFormat = {
  png: 'png',
  webp: 'webp',
  jpeg: 'jpeg',
} as const;

/**
 * The size of the image generated. Either `1024x1024`, `1024x1536`, or `1536x1024`.
 */
export type ImagesResponseSize = typeof ImagesResponseSize[keyof typeof ImagesResponseSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImagesResponseSize = {
  '1024x1024': '1024x1024',
  '1024x1536': '1024x1536',
  '1536x1024': '1536x1024',
} as const;

/**
 * The quality of the image generated. Either `low`, `medium`, or `high`.
 */
export type ImagesResponseQuality = typeof ImagesResponseQuality[keyof typeof ImagesResponseQuality];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImagesResponseQuality = {
  low: 'low',
  medium: 'medium',
  high: 'high',
} as const;

/**
 * The response from the image generation endpoint.
 */
export interface ImagesResponse {
  /** The Unix timestamp (in seconds) of when the image was created. */
  created: number;
  /** The list of generated images. */
  data?: Image[];
  /** The background parameter used for the image generation. Either `transparent` or `opaque`. */
  background?: ImagesResponseBackground;
  /** The output format of the image generation. Either `png`, `webp`, or `jpeg`. */
  output_format?: ImagesResponseOutputFormat;
  /** The size of the image generated. Either `1024x1024`, `1024x1536`, or `1536x1024`. */
  size?: ImagesResponseSize;
  /** The quality of the image generated. Either `low`, `medium`, or `high`. */
  quality?: ImagesResponseQuality;
  usage?: ImageGenUsage;
}

/**
 * The input tokens detailed information for the image generation.
 */
export type ImagesUsageInputTokensDetails = {
  /** The number of text tokens in the input prompt. */
  text_tokens: number;
  /** The number of image tokens in the input prompt. */
  image_tokens: number;
};

/**
 * For `gpt-image-1` only, the token usage information for the image generation.

 */
export interface ImagesUsage {
  /** The total number of tokens (images and text) used for the image generation.
 */
  total_tokens: number;
  /** The number of tokens (images and text) in the input prompt. */
  input_tokens: number;
  /** The number of image tokens in the output image. */
  output_tokens: number;
  /** The input tokens detailed information for the image generation. */
  input_tokens_details: ImagesUsageInputTokensDetails;
}

/**
 * The type of the input item. Always `input_audio`.

 */
export type InputAudioType = typeof InputAudioType[keyof typeof InputAudioType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputAudioType = {
  input_audio: 'input_audio',
} as const;

/**
 * The format of the audio data. Currently supported formats are `mp3` and
`wav`.

 */
export type InputAudioInputAudioFormat = typeof InputAudioInputAudioFormat[keyof typeof InputAudioInputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputAudioInputAudioFormat = {
  mp3: 'mp3',
  wav: 'wav',
} as const;

export type InputAudioInputAudio = {
  /** Base64-encoded audio data.
 */
  data: string;
  /** The format of the audio data. Currently supported formats are `mp3` and
`wav`.
 */
  format: InputAudioInputAudioFormat;
};

/**
 * An audio input to the model.

 */
export interface InputAudio {
  /** The type of the input item. Always `input_audio`.
 */
  type: InputAudioType;
  input_audio: InputAudioInputAudio;
}

export type InputContent = InputTextContent | InputImageContent | InputFileContent | InputAudio;

export type InputItem = EasyInputMessage | Item | ItemReferenceParam;

/**
 * The type of the message input. Always set to `message`.

 */
export type InputMessageType = typeof InputMessageType[keyof typeof InputMessageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputMessageType = {
  message: 'message',
} as const;

/**
 * The role of the message input. One of `user`, `system`, or `developer`.

 */
export type InputMessageRole = typeof InputMessageRole[keyof typeof InputMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputMessageRole = {
  user: 'user',
  system: 'system',
  developer: 'developer',
} as const;

/**
 * The status of item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.

 */
export type InputMessageStatus = typeof InputMessageStatus[keyof typeof InputMessageStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputMessageStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * A message input to the model with a role indicating instruction following
hierarchy. Instructions given with the `developer` or `system` role take
precedence over instructions given with the `user` role.

 */
export interface InputMessage {
  /** The type of the message input. Always set to `message`.
 */
  type?: InputMessageType;
  /** The role of the message input. One of `user`, `system`, or `developer`.
 */
  role: InputMessageRole;
  /** The status of item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
 */
  status?: InputMessageStatus;
  content: InputMessageContentList;
}

/**
 * A list of one or many input items to the model, containing different content
types.

 */
export type InputMessageContentList = InputContent[];

export type InputMessageResourceAllOf = {
  /** The unique ID of the message input.
 */
  id: string;
};

export type InputMessageResource = InputMessage & InputMessageResourceAllOf;

/**
 * Text, image, or file inputs to the model, used to generate a response.

Learn more:
- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
- [Image inputs](https://platform.openai.com/docs/guides/images)
- [File inputs](https://platform.openai.com/docs/guides/pdf-files)
- [Conversation state](https://platform.openai.com/docs/guides/conversation-state)
- [Function calling](https://platform.openai.com/docs/guides/function-calling)

 */
export type InputParam = string | InputItem[];

/**
 * The object type, which is always `organization.invite`
 */
export type InviteObject = typeof InviteObject[keyof typeof InviteObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteObject = {
  organizationinvite: 'organization.invite',
} as const;

/**
 * `owner` or `reader`
 */
export type InviteRole = typeof InviteRole[keyof typeof InviteRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteRole = {
  owner: 'owner',
  reader: 'reader',
} as const;

/**
 * `accepted`,`expired`, or `pending`
 */
export type InviteStatus = typeof InviteStatus[keyof typeof InviteStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteStatus = {
  accepted: 'accepted',
  expired: 'expired',
  pending: 'pending',
} as const;

/**
 * Project membership role
 */
export type InviteProjectsItemRole = typeof InviteProjectsItemRole[keyof typeof InviteProjectsItemRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteProjectsItemRole = {
  member: 'member',
  owner: 'owner',
} as const;

export type InviteProjectsItem = {
  /** Project's public ID */
  id?: string;
  /** Project membership role */
  role?: InviteProjectsItemRole;
};

/**
 * Represents an individual `invite` to the organization.
 */
export interface Invite {
  /** The object type, which is always `organization.invite` */
  object: InviteObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The email address of the individual to whom the invite was sent */
  email: string;
  /** `owner` or `reader` */
  role: InviteRole;
  /** `accepted`,`expired`, or `pending` */
  status: InviteStatus;
  /** The Unix timestamp (in seconds) of when the invite was sent. */
  invited_at: number;
  /** The Unix timestamp (in seconds) of when the invite expires. */
  expires_at: number;
  /** The Unix timestamp (in seconds) of when the invite was accepted. */
  accepted_at?: number;
  /** The projects that were granted membership upon acceptance of the invite. */
  projects?: InviteProjectsItem[];
}

/**
 * The object type, which is always `organization.invite.deleted`
 */
export type InviteDeleteResponseObject = typeof InviteDeleteResponseObject[keyof typeof InviteDeleteResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteDeleteResponseObject = {
  organizationinvitedeleted: 'organization.invite.deleted',
} as const;

export interface InviteDeleteResponse {
  /** The object type, which is always `organization.invite.deleted` */
  object: InviteDeleteResponseObject;
  id: string;
  deleted: boolean;
}

/**
 * The object type, which is always `list`
 */
export type InviteListResponseObject = typeof InviteListResponseObject[keyof typeof InviteListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteListResponseObject = {
  list: 'list',
} as const;

export interface InviteListResponse {
  /** The object type, which is always `list` */
  object: InviteListResponseObject;
  data: Invite[];
  /** The first `invite_id` in the retrieved `list` */
  first_id?: string;
  /** The last `invite_id` in the retrieved `list` */
  last_id?: string;
  /** The `has_more` property is used for pagination to indicate there are additional results. */
  has_more?: boolean;
}

/**
 * `owner` or `reader`
 */
export type InviteRequestRole = typeof InviteRequestRole[keyof typeof InviteRequestRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteRequestRole = {
  reader: 'reader',
  owner: 'owner',
} as const;

/**
 * Project membership role
 */
export type InviteRequestProjectsItemRole = typeof InviteRequestProjectsItemRole[keyof typeof InviteRequestProjectsItemRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InviteRequestProjectsItemRole = {
  member: 'member',
  owner: 'owner',
} as const;

export type InviteRequestProjectsItem = {
  /** Project's public ID */
  id: string;
  /** Project membership role */
  role: InviteRequestProjectsItemRole;
};

export interface InviteRequest {
  /** Send an email to this address */
  email: string;
  /** `owner` or `reader` */
  role: InviteRequestRole;
  /** An array of projects to which membership is granted at the same time the org invite is accepted. If omitted, the user will be invited to the default project for compatibility with legacy behavior. */
  projects?: InviteRequestProjectsItem[];
}

/**
 * Content item used to generate a response.

 */
export type Item = InputMessage | OutputMessage | FileSearchToolCall | ComputerToolCall | ComputerCallOutputItemParam | WebSearchToolCall | FunctionToolCall | FunctionCallOutputItemParam | ReasoningItem | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | MCPListTools | MCPApprovalRequest | MCPApprovalResponse | MCPToolCall | CustomToolCallOutput | CustomToolCall;

/**
 * Content item used to generate a response.

 */
export type ItemResource = InputMessageResource | OutputMessage | FileSearchToolCall | ComputerToolCall | ComputerToolCallOutputResource | WebSearchToolCall | FunctionToolCallResource | FunctionToolCallOutputResource | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | MCPListTools | MCPApprovalRequest | MCPApprovalResponseResource | MCPToolCall;

export interface ListAssistantsResponse {
  object: string;
  data: AssistantObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type ListAuditLogsResponseObject = typeof ListAuditLogsResponseObject[keyof typeof ListAuditLogsResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListAuditLogsResponseObject = {
  list: 'list',
} as const;

export interface ListAuditLogsResponse {
  object: ListAuditLogsResponseObject;
  data: AuditLog[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type ListBatchesResponseObject = typeof ListBatchesResponseObject[keyof typeof ListBatchesResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListBatchesResponseObject = {
  list: 'list',
} as const;

export interface ListBatchesResponse {
  data: Batch[];
  first_id?: string;
  last_id?: string;
  has_more: boolean;
  object: ListBatchesResponseObject;
}

export type ListCertificatesResponseObject = typeof ListCertificatesResponseObject[keyof typeof ListCertificatesResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListCertificatesResponseObject = {
  list: 'list',
} as const;

export interface ListCertificatesResponse {
  data: Certificate[];
  first_id?: string;
  last_id?: string;
  has_more: boolean;
  object: ListCertificatesResponseObject;
}

export interface ListFilesResponse {
  object: string;
  data: OpenAIFile[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type ListFineTuningCheckpointPermissionResponseObject = typeof ListFineTuningCheckpointPermissionResponseObject[keyof typeof ListFineTuningCheckpointPermissionResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFineTuningCheckpointPermissionResponseObject = {
  list: 'list',
} as const;

export type ListFineTuningCheckpointPermissionResponseFirstId = string | null;

export type ListFineTuningCheckpointPermissionResponseLastId = string | null;

export interface ListFineTuningCheckpointPermissionResponse {
  data: FineTuningCheckpointPermission[];
  object: ListFineTuningCheckpointPermissionResponseObject;
  first_id?: ListFineTuningCheckpointPermissionResponseFirstId;
  last_id?: ListFineTuningCheckpointPermissionResponseLastId;
  has_more: boolean;
}

export type ListFineTuningJobCheckpointsResponseObject = typeof ListFineTuningJobCheckpointsResponseObject[keyof typeof ListFineTuningJobCheckpointsResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFineTuningJobCheckpointsResponseObject = {
  list: 'list',
} as const;

export type ListFineTuningJobCheckpointsResponseFirstId = string | null;

export type ListFineTuningJobCheckpointsResponseLastId = string | null;

export interface ListFineTuningJobCheckpointsResponse {
  data: FineTuningJobCheckpoint[];
  object: ListFineTuningJobCheckpointsResponseObject;
  first_id?: ListFineTuningJobCheckpointsResponseFirstId;
  last_id?: ListFineTuningJobCheckpointsResponseLastId;
  has_more: boolean;
}

export type ListFineTuningJobEventsResponseObject = typeof ListFineTuningJobEventsResponseObject[keyof typeof ListFineTuningJobEventsResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFineTuningJobEventsResponseObject = {
  list: 'list',
} as const;

export interface ListFineTuningJobEventsResponse {
  data: FineTuningJobEvent[];
  object: ListFineTuningJobEventsResponseObject;
  has_more: boolean;
}

export interface ListMessagesResponse {
  object: string;
  data: MessageObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type ListModelsResponseObject = typeof ListModelsResponseObject[keyof typeof ListModelsResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListModelsResponseObject = {
  list: 'list',
} as const;

export interface ListModelsResponse {
  object: ListModelsResponseObject;
  data: Model[];
}

export type ListPaginatedFineTuningJobsResponseObject = typeof ListPaginatedFineTuningJobsResponseObject[keyof typeof ListPaginatedFineTuningJobsResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListPaginatedFineTuningJobsResponseObject = {
  list: 'list',
} as const;

export interface ListPaginatedFineTuningJobsResponse {
  data: FineTuningJob[];
  has_more: boolean;
  object: ListPaginatedFineTuningJobsResponseObject;
}

export interface ListRunStepsResponse {
  object: string;
  data: RunStepObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface ListRunsResponse {
  object: string;
  data: RunObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface ListVectorStoreFilesResponse {
  object: string;
  data: VectorStoreFileObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface ListVectorStoresResponse {
  object: string;
  data: VectorStoreObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/**
 * The type of the local shell call. Always `local_shell_call`.

 */
export type LocalShellToolCallType = typeof LocalShellToolCallType[keyof typeof LocalShellToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LocalShellToolCallType = {
  local_shell_call: 'local_shell_call',
} as const;

/**
 * The status of the local shell call.

 */
export type LocalShellToolCallStatus = typeof LocalShellToolCallStatus[keyof typeof LocalShellToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LocalShellToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * A tool call to run a command on the local shell.

 */
export interface LocalShellToolCall {
  /** The type of the local shell call. Always `local_shell_call`.
 */
  type: LocalShellToolCallType;
  /** The unique ID of the local shell call.
 */
  id: string;
  /** The unique ID of the local shell tool call generated by the model.
 */
  call_id: string;
  action: LocalShellExecAction;
  /** The status of the local shell call.
 */
  status: LocalShellToolCallStatus;
}

/**
 * The type of the local shell tool call output. Always `local_shell_call_output`.

 */
export type LocalShellToolCallOutputType = typeof LocalShellToolCallOutputType[keyof typeof LocalShellToolCallOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LocalShellToolCallOutputType = {
  local_shell_call_output: 'local_shell_call_output',
} as const;

export type LocalShellToolCallOutputStatus = 'in_progress' | 'completed' | 'incomplete' | null;

/**
 * The output of a local shell tool call.

 */
export interface LocalShellToolCallOutput {
  /** The type of the local shell tool call output. Always `local_shell_call_output`.
 */
  type: LocalShellToolCallOutputType;
  /** The unique ID of the local shell tool call generated by the model.
 */
  id: string;
  /** A JSON string of the output of the local shell tool call.
 */
  output: string;
  status?: LocalShellToolCallOutputStatus;
}

/**
 * A log probability object.

 */
export interface LogProbProperties {
  /** The token that was used to generate the log probability.
 */
  token: string;
  /** The log probability of the token.
 */
  logprob: number;
  /** The bytes that were used to generate the log probability.
 */
  bytes: number[];
}

/**
 * The type of the item. Always `mcp_approval_request`.

 */
export type MCPApprovalRequestType = typeof MCPApprovalRequestType[keyof typeof MCPApprovalRequestType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPApprovalRequestType = {
  mcp_approval_request: 'mcp_approval_request',
} as const;

/**
 * A request for human approval of a tool invocation.

 */
export interface MCPApprovalRequest {
  /** The type of the item. Always `mcp_approval_request`.
 */
  type: MCPApprovalRequestType;
  /** The unique ID of the approval request.
 */
  id: string;
  /** The label of the MCP server making the request.
 */
  server_label: string;
  /** The name of the tool to run.
 */
  name: string;
  /** A JSON string of arguments for the tool.
 */
  arguments: string;
}

/**
 * The type of the item. Always `mcp_approval_response`.

 */
export type MCPApprovalResponseType = typeof MCPApprovalResponseType[keyof typeof MCPApprovalResponseType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPApprovalResponseType = {
  mcp_approval_response: 'mcp_approval_response',
} as const;

export type MCPApprovalResponseId = string | null;

export type MCPApprovalResponseReason = string | null;

/**
 * A response to an MCP approval request.

 */
export interface MCPApprovalResponse {
  /** The type of the item. Always `mcp_approval_response`.
 */
  type: MCPApprovalResponseType;
  id?: MCPApprovalResponseId;
  /** The ID of the approval request being answered.
 */
  approval_request_id: string;
  /** Whether the request was approved.
 */
  approve: boolean;
  reason?: MCPApprovalResponseReason;
}

/**
 * The type of the item. Always `mcp_approval_response`.

 */
export type MCPApprovalResponseResourceType = typeof MCPApprovalResponseResourceType[keyof typeof MCPApprovalResponseResourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPApprovalResponseResourceType = {
  mcp_approval_response: 'mcp_approval_response',
} as const;

export type MCPApprovalResponseResourceReason = string | null;

/**
 * A response to an MCP approval request.

 */
export interface MCPApprovalResponseResource {
  /** The type of the item. Always `mcp_approval_response`.
 */
  type: MCPApprovalResponseResourceType;
  /** The unique ID of the approval response
 */
  id: string;
  /** The ID of the approval request being answered.
 */
  approval_request_id: string;
  /** Whether the request was approved.
 */
  approve: boolean;
  reason?: MCPApprovalResponseResourceReason;
}

/**
 * The type of the item. Always `mcp_list_tools`.

 */
export type MCPListToolsType = typeof MCPListToolsType[keyof typeof MCPListToolsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPListToolsType = {
  mcp_list_tools: 'mcp_list_tools',
} as const;

export type MCPListToolsError = string | null;

/**
 * A list of tools available on an MCP server.

 */
export interface MCPListTools {
  /** The type of the item. Always `mcp_list_tools`.
 */
  type: MCPListToolsType;
  /** The unique ID of the list.
 */
  id: string;
  /** The label of the MCP server.
 */
  server_label: string;
  /** The tools available on the server.
 */
  tools: MCPListToolsTool[];
  error?: MCPListToolsError;
}

export type MCPListToolsToolDescription = string | null;

/**
 * The JSON schema describing the tool's input.

 */
export type MCPListToolsToolInputSchema = { [key: string]: unknown };

/**
 * Additional annotations about the tool.

 */
export type MCPListToolsToolAnnotationsAnyOf = { [key: string]: unknown };

export type MCPListToolsToolAnnotations = MCPListToolsToolAnnotationsAnyOf | null;

/**
 * A tool available on an MCP server.

 */
export interface MCPListToolsTool {
  /** The name of the tool.
 */
  name: string;
  description?: MCPListToolsToolDescription;
  /** The JSON schema describing the tool's input.
 */
  input_schema: MCPListToolsToolInputSchema;
  annotations?: MCPListToolsToolAnnotations;
}

/**
 * The type of the MCP tool. Always `mcp`.
 */
export type MCPToolType = typeof MCPToolType[keyof typeof MCPToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPToolType = {
  mcp: 'mcp',
} as const;

/**
 * Identifier for service connectors, like those available in ChatGPT. One of
`server_url` or `connector_id` must be provided. Learn more about service
connectors [here](https://platform.openai.com/docs/guides/tools-remote-mcp#connectors).

Currently supported `connector_id` values are:

- Dropbox: `connector_dropbox`
- Gmail: `connector_gmail`
- Google Calendar: `connector_googlecalendar`
- Google Drive: `connector_googledrive`
- Microsoft Teams: `connector_microsoftteams`
- Outlook Calendar: `connector_outlookcalendar`
- Outlook Email: `connector_outlookemail`
- SharePoint: `connector_sharepoint`

 */
export type MCPToolConnectorId = typeof MCPToolConnectorId[keyof typeof MCPToolConnectorId];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPToolConnectorId = {
  connector_dropbox: 'connector_dropbox',
  connector_gmail: 'connector_gmail',
  connector_googlecalendar: 'connector_googlecalendar',
  connector_googledrive: 'connector_googledrive',
  connector_microsoftteams: 'connector_microsoftteams',
  connector_outlookcalendar: 'connector_outlookcalendar',
  connector_outlookemail: 'connector_outlookemail',
  connector_sharepoint: 'connector_sharepoint',
} as const;

/**
 * Optional HTTP headers to send to the MCP server. Use for authentication
or other purposes.

 */
export type MCPToolHeadersAnyOf = {[key: string]: string};

export type MCPToolHeaders = MCPToolHeadersAnyOf | null;

/**
 * List of allowed tool names or a filter object.

 */
export type MCPToolAllowedToolsAnyOf = string[] | MCPToolFilter;

export type MCPToolAllowedTools = MCPToolAllowedToolsAnyOf | null;

/**
 * Specify which of the MCP server's tools require approval. Can be
`always`, `never`, or a filter object associated with tools
that require approval.

 */
export type MCPToolRequireApprovalAnyOfAnyOf = {
  always?: MCPToolFilter;
  never?: MCPToolFilter;
};

/**
 * Specify which of the MCP server's tools require approval.
 */
export type MCPToolRequireApprovalAnyOf = MCPToolRequireApprovalAnyOfAnyOf | 'always' | 'never';

export type MCPToolRequireApproval = MCPToolRequireApprovalAnyOf | null;

/**
 * Give the model access to additional tools via remote Model Context Protocol
(MCP) servers. [Learn more about MCP](https://platform.openai.com/docs/guides/tools-remote-mcp).

 */
export interface MCPTool {
  /** The type of the MCP tool. Always `mcp`. */
  type: MCPToolType;
  /** A label for this MCP server, used to identify it in tool calls.
 */
  server_label: string;
  /** The URL for the MCP server. One of `server_url` or `connector_id` must be
provided.
 */
  server_url?: string;
  /** Identifier for service connectors, like those available in ChatGPT. One of
`server_url` or `connector_id` must be provided. Learn more about service
connectors [here](https://platform.openai.com/docs/guides/tools-remote-mcp#connectors).

Currently supported `connector_id` values are:

- Dropbox: `connector_dropbox`
- Gmail: `connector_gmail`
- Google Calendar: `connector_googlecalendar`
- Google Drive: `connector_googledrive`
- Microsoft Teams: `connector_microsoftteams`
- Outlook Calendar: `connector_outlookcalendar`
- Outlook Email: `connector_outlookemail`
- SharePoint: `connector_sharepoint`
 */
  connector_id?: MCPToolConnectorId;
  /** An OAuth access token that can be used with a remote MCP server, either
with a custom MCP server URL or a service connector. Your application
must handle the OAuth authorization flow and provide the token here.
 */
  authorization?: string;
  /** Optional description of the MCP server, used to provide more context.
 */
  server_description?: string;
  headers?: MCPToolHeaders;
  allowed_tools?: MCPToolAllowedTools;
  require_approval?: MCPToolRequireApproval;
}

/**
 * The type of the item. Always `mcp_call`.

 */
export type MCPToolCallType = typeof MCPToolCallType[keyof typeof MCPToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPToolCallType = {
  mcp_call: 'mcp_call',
} as const;

export type MCPToolCallOutput = string | null;

export type MCPToolCallError = string | null;

export type MCPToolCallApprovalRequestId = string | null;

/**
 * An invocation of a tool on an MCP server.

 */
export interface MCPToolCall {
  /** The type of the item. Always `mcp_call`.
 */
  type: MCPToolCallType;
  /** The unique ID of the tool call.
 */
  id: string;
  /** The label of the MCP server running the tool.
 */
  server_label: string;
  /** The name of the tool that was run.
 */
  name: string;
  /** A JSON string of the arguments passed to the tool.
 */
  arguments: string;
  output?: MCPToolCallOutput;
  error?: MCPToolCallError;
  /** The status of the tool call. One of `in_progress`, `completed`, `incomplete`, `calling`, or `failed`.
 */
  status?: MCPToolCallStatus;
  approval_request_id?: MCPToolCallApprovalRequestId;
}

/**
 * A filter object to specify which tools are allowed.

 */
export interface MCPToolFilter {
  /** List of allowed tool names. */
  tool_names?: string[];
  /** Indicates whether or not a tool modifies data or is read-only. If an
MCP server is [annotated with `readOnlyHint`](https://modelcontextprotocol.io/specification/2025-06-18/schema#toolannotations-readonlyhint),
it will match this filter.
 */
  read_only?: boolean;
}

/**
 * Always `image_file`.
 */
export type MessageContentImageFileObjectType = typeof MessageContentImageFileObjectType[keyof typeof MessageContentImageFileObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentImageFileObjectType = {
  image_file: 'image_file',
} as const;

/**
 * Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`.
 */
export type MessageContentImageFileObjectImageFileDetail = typeof MessageContentImageFileObjectImageFileDetail[keyof typeof MessageContentImageFileObjectImageFileDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentImageFileObjectImageFileDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type MessageContentImageFileObjectImageFile = {
  /** The [File](https://platform.openai.com/docs/api-reference/files) ID of the image in the message content. Set `purpose="vision"` when uploading the File if you need to later display the file content. */
  file_id: string;
  /** Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`. */
  detail?: MessageContentImageFileObjectImageFileDetail;
};

/**
 * References an image [File](https://platform.openai.com/docs/api-reference/files) in the content of a message.
 */
export interface MessageContentImageFileObject {
  /** Always `image_file`. */
  type: MessageContentImageFileObjectType;
  image_file: MessageContentImageFileObjectImageFile;
}

/**
 * The type of the content part.
 */
export type MessageContentImageUrlObjectType = typeof MessageContentImageUrlObjectType[keyof typeof MessageContentImageUrlObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentImageUrlObjectType = {
  image_url: 'image_url',
} as const;

/**
 * Specifies the detail level of the image. `low` uses fewer tokens, you can opt in to high resolution using `high`. Default value is `auto`
 */
export type MessageContentImageUrlObjectImageUrlDetail = typeof MessageContentImageUrlObjectImageUrlDetail[keyof typeof MessageContentImageUrlObjectImageUrlDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentImageUrlObjectImageUrlDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type MessageContentImageUrlObjectImageUrl = {
  /** The external URL of the image, must be a supported image types: jpeg, jpg, png, gif, webp. */
  url: string;
  /** Specifies the detail level of the image. `low` uses fewer tokens, you can opt in to high resolution using `high`. Default value is `auto` */
  detail?: MessageContentImageUrlObjectImageUrlDetail;
};

/**
 * References an image URL in the content of a message.
 */
export interface MessageContentImageUrlObject {
  /** The type of the content part. */
  type: MessageContentImageUrlObjectType;
  image_url: MessageContentImageUrlObjectImageUrl;
}

/**
 * Always `refusal`.
 */
export type MessageContentRefusalObjectType = typeof MessageContentRefusalObjectType[keyof typeof MessageContentRefusalObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentRefusalObjectType = {
  refusal: 'refusal',
} as const;

/**
 * The refusal content generated by the assistant.
 */
export interface MessageContentRefusalObject {
  /** Always `refusal`. */
  type: MessageContentRefusalObjectType;
  refusal: string;
}

/**
 * Always `file_citation`.
 */
export type MessageContentTextAnnotationsFileCitationObjectType = typeof MessageContentTextAnnotationsFileCitationObjectType[keyof typeof MessageContentTextAnnotationsFileCitationObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentTextAnnotationsFileCitationObjectType = {
  file_citation: 'file_citation',
} as const;

export type MessageContentTextAnnotationsFileCitationObjectFileCitation = {
  /** The ID of the specific File the citation is from. */
  file_id: string;
};

/**
 * A citation within the message that points to a specific quote from a specific File associated with the assistant or the message. Generated when the assistant uses the "file_search" tool to search files.
 */
export interface MessageContentTextAnnotationsFileCitationObject {
  /** Always `file_citation`. */
  type: MessageContentTextAnnotationsFileCitationObjectType;
  /** The text in the message content that needs to be replaced. */
  text: string;
  file_citation: MessageContentTextAnnotationsFileCitationObjectFileCitation;
  /** @minimum 0 */
  start_index: number;
  /** @minimum 0 */
  end_index: number;
}

/**
 * Always `file_path`.
 */
export type MessageContentTextAnnotationsFilePathObjectType = typeof MessageContentTextAnnotationsFilePathObjectType[keyof typeof MessageContentTextAnnotationsFilePathObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentTextAnnotationsFilePathObjectType = {
  file_path: 'file_path',
} as const;

export type MessageContentTextAnnotationsFilePathObjectFilePath = {
  /** The ID of the file that was generated. */
  file_id: string;
};

/**
 * A URL for the file that's generated when the assistant used the `code_interpreter` tool to generate a file.
 */
export interface MessageContentTextAnnotationsFilePathObject {
  /** Always `file_path`. */
  type: MessageContentTextAnnotationsFilePathObjectType;
  /** The text in the message content that needs to be replaced. */
  text: string;
  file_path: MessageContentTextAnnotationsFilePathObjectFilePath;
  /** @minimum 0 */
  start_index: number;
  /** @minimum 0 */
  end_index: number;
}

/**
 * Always `text`.
 */
export type MessageContentTextObjectType = typeof MessageContentTextObjectType[keyof typeof MessageContentTextObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageContentTextObjectType = {
  text: 'text',
} as const;

export type MessageContentTextObjectText = {
  /** The data that makes up the text. */
  value: string;
  annotations: TextAnnotation[];
};

/**
 * The text content that is part of a message.
 */
export interface MessageContentTextObject {
  /** Always `text`. */
  type: MessageContentTextObjectType;
  text: MessageContentTextObjectText;
}

/**
 * Always `image_file`.
 */
export type MessageDeltaContentImageFileObjectType = typeof MessageDeltaContentImageFileObjectType[keyof typeof MessageDeltaContentImageFileObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentImageFileObjectType = {
  image_file: 'image_file',
} as const;

/**
 * Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`.
 */
export type MessageDeltaContentImageFileObjectImageFileDetail = typeof MessageDeltaContentImageFileObjectImageFileDetail[keyof typeof MessageDeltaContentImageFileObjectImageFileDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentImageFileObjectImageFileDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type MessageDeltaContentImageFileObjectImageFile = {
  /** The [File](https://platform.openai.com/docs/api-reference/files) ID of the image in the message content. Set `purpose="vision"` when uploading the File if you need to later display the file content. */
  file_id?: string;
  /** Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`. */
  detail?: MessageDeltaContentImageFileObjectImageFileDetail;
};

/**
 * References an image [File](https://platform.openai.com/docs/api-reference/files) in the content of a message.
 */
export interface MessageDeltaContentImageFileObject {
  /** The index of the content part in the message. */
  index: number;
  /** Always `image_file`. */
  type: MessageDeltaContentImageFileObjectType;
  image_file?: MessageDeltaContentImageFileObjectImageFile;
}

/**
 * Always `image_url`.
 */
export type MessageDeltaContentImageUrlObjectType = typeof MessageDeltaContentImageUrlObjectType[keyof typeof MessageDeltaContentImageUrlObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentImageUrlObjectType = {
  image_url: 'image_url',
} as const;

/**
 * Specifies the detail level of the image. `low` uses fewer tokens, you can opt in to high resolution using `high`.
 */
export type MessageDeltaContentImageUrlObjectImageUrlDetail = typeof MessageDeltaContentImageUrlObjectImageUrlDetail[keyof typeof MessageDeltaContentImageUrlObjectImageUrlDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentImageUrlObjectImageUrlDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type MessageDeltaContentImageUrlObjectImageUrl = {
  /** The URL of the image, must be a supported image types: jpeg, jpg, png, gif, webp. */
  url?: string;
  /** Specifies the detail level of the image. `low` uses fewer tokens, you can opt in to high resolution using `high`. */
  detail?: MessageDeltaContentImageUrlObjectImageUrlDetail;
};

/**
 * References an image URL in the content of a message.
 */
export interface MessageDeltaContentImageUrlObject {
  /** The index of the content part in the message. */
  index: number;
  /** Always `image_url`. */
  type: MessageDeltaContentImageUrlObjectType;
  image_url?: MessageDeltaContentImageUrlObjectImageUrl;
}

/**
 * Always `refusal`.
 */
export type MessageDeltaContentRefusalObjectType = typeof MessageDeltaContentRefusalObjectType[keyof typeof MessageDeltaContentRefusalObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentRefusalObjectType = {
  refusal: 'refusal',
} as const;

/**
 * The refusal content that is part of a message.
 */
export interface MessageDeltaContentRefusalObject {
  /** The index of the refusal part in the message. */
  index: number;
  /** Always `refusal`. */
  type: MessageDeltaContentRefusalObjectType;
  refusal?: string;
}

/**
 * Always `file_citation`.
 */
export type MessageDeltaContentTextAnnotationsFileCitationObjectType = typeof MessageDeltaContentTextAnnotationsFileCitationObjectType[keyof typeof MessageDeltaContentTextAnnotationsFileCitationObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentTextAnnotationsFileCitationObjectType = {
  file_citation: 'file_citation',
} as const;

export type MessageDeltaContentTextAnnotationsFileCitationObjectFileCitation = {
  /** The ID of the specific File the citation is from. */
  file_id?: string;
  /** The specific quote in the file. */
  quote?: string;
};

/**
 * A citation within the message that points to a specific quote from a specific File associated with the assistant or the message. Generated when the assistant uses the "file_search" tool to search files.
 */
export interface MessageDeltaContentTextAnnotationsFileCitationObject {
  /** The index of the annotation in the text content part. */
  index: number;
  /** Always `file_citation`. */
  type: MessageDeltaContentTextAnnotationsFileCitationObjectType;
  /** The text in the message content that needs to be replaced. */
  text?: string;
  file_citation?: MessageDeltaContentTextAnnotationsFileCitationObjectFileCitation;
  /** @minimum 0 */
  start_index?: number;
  /** @minimum 0 */
  end_index?: number;
}

/**
 * Always `file_path`.
 */
export type MessageDeltaContentTextAnnotationsFilePathObjectType = typeof MessageDeltaContentTextAnnotationsFilePathObjectType[keyof typeof MessageDeltaContentTextAnnotationsFilePathObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentTextAnnotationsFilePathObjectType = {
  file_path: 'file_path',
} as const;

export type MessageDeltaContentTextAnnotationsFilePathObjectFilePath = {
  /** The ID of the file that was generated. */
  file_id?: string;
};

/**
 * A URL for the file that's generated when the assistant used the `code_interpreter` tool to generate a file.
 */
export interface MessageDeltaContentTextAnnotationsFilePathObject {
  /** The index of the annotation in the text content part. */
  index: number;
  /** Always `file_path`. */
  type: MessageDeltaContentTextAnnotationsFilePathObjectType;
  /** The text in the message content that needs to be replaced. */
  text?: string;
  file_path?: MessageDeltaContentTextAnnotationsFilePathObjectFilePath;
  /** @minimum 0 */
  start_index?: number;
  /** @minimum 0 */
  end_index?: number;
}

/**
 * Always `text`.
 */
export type MessageDeltaContentTextObjectType = typeof MessageDeltaContentTextObjectType[keyof typeof MessageDeltaContentTextObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaContentTextObjectType = {
  text: 'text',
} as const;

export type MessageDeltaContentTextObjectText = {
  /** The data that makes up the text. */
  value?: string;
  annotations?: TextAnnotationDelta[];
};

/**
 * The text content that is part of a message.
 */
export interface MessageDeltaContentTextObject {
  /** The index of the content part in the message. */
  index: number;
  /** Always `text`. */
  type: MessageDeltaContentTextObjectType;
  text?: MessageDeltaContentTextObjectText;
}

/**
 * The object type, which is always `thread.message.delta`.
 */
export type MessageDeltaObjectObject = typeof MessageDeltaObjectObject[keyof typeof MessageDeltaObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaObjectObject = {
  threadmessagedelta: 'thread.message.delta',
} as const;

/**
 * The entity that produced the message. One of `user` or `assistant`.
 */
export type MessageDeltaObjectDeltaRole = typeof MessageDeltaObjectDeltaRole[keyof typeof MessageDeltaObjectDeltaRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageDeltaObjectDeltaRole = {
  user: 'user',
  assistant: 'assistant',
} as const;

/**
 * The delta containing the fields that have changed on the Message.
 */
export type MessageDeltaObjectDelta = {
  /** The entity that produced the message. One of `user` or `assistant`. */
  role?: MessageDeltaObjectDeltaRole;
  /** The content of the message in array of text and/or images. */
  content?: MessageContentDelta[];
};

/**
 * Represents a message delta i.e. any changed fields on a message during streaming.

 */
export interface MessageDeltaObject {
  /** The identifier of the message, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.message.delta`. */
  object: MessageDeltaObjectObject;
  /** The delta containing the fields that have changed on the Message. */
  delta: MessageDeltaObjectDelta;
}

/**
 * The object type, which is always `thread.message`.
 */
export type MessageObjectObject = typeof MessageObjectObject[keyof typeof MessageObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageObjectObject = {
  threadmessage: 'thread.message',
} as const;

/**
 * The status of the message, which can be either `in_progress`, `incomplete`, or `completed`.
 */
export type MessageObjectStatus = typeof MessageObjectStatus[keyof typeof MessageObjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageObjectStatus = {
  in_progress: 'in_progress',
  incomplete: 'incomplete',
  completed: 'completed',
} as const;

/**
 * The reason the message is incomplete.
 */
export type MessageObjectIncompleteDetailsAnyOfReason = typeof MessageObjectIncompleteDetailsAnyOfReason[keyof typeof MessageObjectIncompleteDetailsAnyOfReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageObjectIncompleteDetailsAnyOfReason = {
  content_filter: 'content_filter',
  max_tokens: 'max_tokens',
  run_cancelled: 'run_cancelled',
  run_expired: 'run_expired',
  run_failed: 'run_failed',
} as const;

/**
 * On an incomplete message, details about why the message is incomplete.
 */
export type MessageObjectIncompleteDetailsAnyOf = {
  /** The reason the message is incomplete. */
  reason: MessageObjectIncompleteDetailsAnyOfReason;
};

export type MessageObjectIncompleteDetails = MessageObjectIncompleteDetailsAnyOf | null;

export type MessageObjectCompletedAt = number | null;

export type MessageObjectIncompleteAt = number | null;

/**
 * The entity that produced the message. One of `user` or `assistant`.
 */
export type MessageObjectRole = typeof MessageObjectRole[keyof typeof MessageObjectRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageObjectRole = {
  user: 'user',
  assistant: 'assistant',
} as const;

export type MessageObjectAssistantId = string | null;

export type MessageObjectRunId = string | null;

export type MessageObjectAttachmentsAnyOfItemToolsItem = AssistantToolsCode | AssistantToolsFileSearchTypeOnly;

export type MessageObjectAttachmentsAnyOfItem = {
  /** The ID of the file to attach to the message. */
  file_id?: string;
  /** The tools to add this file to. */
  tools?: MessageObjectAttachmentsAnyOfItemToolsItem[];
};

export type MessageObjectAttachments = MessageObjectAttachmentsAnyOfItem[] | null;

/**
 * Represents a message within a [thread](https://platform.openai.com/docs/api-reference/threads).
 */
export interface MessageObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.message`. */
  object: MessageObjectObject;
  /** The Unix timestamp (in seconds) for when the message was created. */
  created_at: number;
  /** The [thread](https://platform.openai.com/docs/api-reference/threads) ID that this message belongs to. */
  thread_id: string;
  /** The status of the message, which can be either `in_progress`, `incomplete`, or `completed`. */
  status: MessageObjectStatus;
  incomplete_details: MessageObjectIncompleteDetails;
  completed_at: MessageObjectCompletedAt;
  incomplete_at: MessageObjectIncompleteAt;
  /** The entity that produced the message. One of `user` or `assistant`. */
  role: MessageObjectRole;
  /** The content of the message in array of text and/or images. */
  content: MessageContent[];
  assistant_id: MessageObjectAssistantId;
  run_id: MessageObjectRunId;
  attachments: MessageObjectAttachments;
  metadata: Metadata;
}

/**
 * Always `text`.
 */
export type MessageRequestContentTextObjectType = typeof MessageRequestContentTextObjectType[keyof typeof MessageRequestContentTextObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageRequestContentTextObjectType = {
  text: 'text',
} as const;

/**
 * The text content that is part of a message.
 */
export interface MessageRequestContentTextObject {
  /** Always `text`. */
  type: MessageRequestContentTextObjectType;
  /** Text content to be sent to the model */
  text: string;
}

export type MessageStreamEventAnyOfEvent = typeof MessageStreamEventAnyOfEvent[keyof typeof MessageStreamEventAnyOfEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStreamEventAnyOfEvent = {
  threadmessagecreated: 'thread.message.created',
} as const;

/**
 * Occurs when a [message](https://platform.openai.com/docs/api-reference/messages/object) is created.
 */
export type MessageStreamEventAnyOf = {
  event: MessageStreamEventAnyOfEvent;
  data: MessageObject;
};

export type MessageStreamEventAnyOfThreeEvent = typeof MessageStreamEventAnyOfThreeEvent[keyof typeof MessageStreamEventAnyOfThreeEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStreamEventAnyOfThreeEvent = {
  threadmessagein_progress: 'thread.message.in_progress',
} as const;

/**
 * Occurs when a [message](https://platform.openai.com/docs/api-reference/messages/object) moves to an `in_progress` state.
 */
export type MessageStreamEventAnyOfThree = {
  event: MessageStreamEventAnyOfThreeEvent;
  data: MessageObject;
};

export type MessageStreamEventAnyOfFiveEvent = typeof MessageStreamEventAnyOfFiveEvent[keyof typeof MessageStreamEventAnyOfFiveEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStreamEventAnyOfFiveEvent = {
  threadmessagedelta: 'thread.message.delta',
} as const;

/**
 * Occurs when parts of a [Message](https://platform.openai.com/docs/api-reference/messages/object) are being streamed.
 */
export type MessageStreamEventAnyOfFive = {
  event: MessageStreamEventAnyOfFiveEvent;
  data: MessageDeltaObject;
};

export type MessageStreamEventAnyOfSevenEvent = typeof MessageStreamEventAnyOfSevenEvent[keyof typeof MessageStreamEventAnyOfSevenEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStreamEventAnyOfSevenEvent = {
  threadmessagecompleted: 'thread.message.completed',
} as const;

/**
 * Occurs when a [message](https://platform.openai.com/docs/api-reference/messages/object) is completed.
 */
export type MessageStreamEventAnyOfSeven = {
  event: MessageStreamEventAnyOfSevenEvent;
  data: MessageObject;
};

export type MessageStreamEventAnyOfNineEvent = typeof MessageStreamEventAnyOfNineEvent[keyof typeof MessageStreamEventAnyOfNineEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStreamEventAnyOfNineEvent = {
  threadmessageincomplete: 'thread.message.incomplete',
} as const;

/**
 * Occurs when a [message](https://platform.openai.com/docs/api-reference/messages/object) ends before it is completed.
 */
export type MessageStreamEventAnyOfNine = {
  event: MessageStreamEventAnyOfNineEvent;
  data: MessageObject;
};

export type MessageStreamEvent = MessageStreamEventAnyOf | MessageStreamEventAnyOfThree | MessageStreamEventAnyOfFive | MessageStreamEventAnyOfSeven | MessageStreamEventAnyOfNine;

/**
 * Set of 16 key-value pairs that can be attached to an object. This can be
useful for storing additional information about the object in a structured
format, and querying for objects via API or the dashboard.

Keys are strings with a maximum length of 64 characters. Values are strings
with a maximum length of 512 characters.

 */
export type MetadataAnyOf = {[key: string]: string};

export type Metadata = MetadataAnyOf | null;

/**
 * The object type, which is always "model".
 */
export type ModelObject = typeof ModelObject[keyof typeof ModelObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ModelObject = {
  model: 'model',
} as const;

/**
 * Describes an OpenAI model offering that can be used with the API.
 */
export interface Model {
  /** The model identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) when the model was created. */
  created: number;
  /** The object type, which is always "model". */
  object: ModelObject;
  /** The organization that owns the model. */
  owned_by: string;
}

export type ModelIds = ModelIdsShared | ModelIdsResponses;

export type ModelIdsResponses = ModelIdsShared | 'o1-pro' | 'o1-pro-2025-03-19' | 'o3-pro' | 'o3-pro-2025-06-10' | 'o3-deep-research' | 'o3-deep-research-2025-06-26' | 'o4-mini-deep-research' | 'o4-mini-deep-research-2025-06-26' | 'computer-use-preview' | 'computer-use-preview-2025-03-11' | 'gpt-5-codex' | 'gpt-5-pro' | 'gpt-5-pro-2025-10-06';

export type ModelIdsShared = string | ChatModel;

export type ModelResponsePropertiesTopLogprobs = number | null;

export type ModelResponsePropertiesTemperature = number | null;

export type ModelResponsePropertiesTopP = number | null;

export interface ModelResponseProperties {
  metadata?: Metadata;
  top_logprobs?: ModelResponsePropertiesTopLogprobs;
  temperature?: ModelResponsePropertiesTemperature;
  top_p?: ModelResponsePropertiesTopP;
  /**
   * This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
A stable identifier for your end-users.
Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#safety-identifiers).

   * @deprecated
   */
  user?: string;
  /** A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
The IDs should be a string that uniquely identifies each user. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#safety-identifiers).
 */
  safety_identifier?: string;
  /** Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](https://platform.openai.com/docs/guides/prompt-caching).
 */
  prompt_cache_key?: string;
  service_tier?: ServiceTier;
}

/**
 * ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.

 */
export type ModifyAssistantRequestModel = string | AssistantSupportedModels;

export type ModifyAssistantRequestName = string | null;

export type ModifyAssistantRequestDescription = string | null;

export type ModifyAssistantRequestInstructions = string | null;

export type ModifyAssistantRequestToolResourcesAnyOfCodeInterpreter = {
  /**
   * Overrides the list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type ModifyAssistantRequestToolResourcesAnyOfFileSearch = {
  /**
   * Overrides the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type ModifyAssistantRequestToolResourcesAnyOf = {
  code_interpreter?: ModifyAssistantRequestToolResourcesAnyOfCodeInterpreter;
  file_search?: ModifyAssistantRequestToolResourcesAnyOfFileSearch;
};

export type ModifyAssistantRequestToolResources = ModifyAssistantRequestToolResourcesAnyOf | null;

export type ModifyAssistantRequestTemperature = number | null;

export type ModifyAssistantRequestTopP = number | null;

export type ModifyAssistantRequestResponseFormat = AssistantsApiResponseFormatOption | null;

export interface ModifyAssistantRequest {
  /** ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.
 */
  model?: ModifyAssistantRequestModel;
  reasoning_effort?: ReasoningEffort;
  name?: ModifyAssistantRequestName;
  description?: ModifyAssistantRequestDescription;
  instructions?: ModifyAssistantRequestInstructions;
  /**
   * A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`.

   * @maxItems 128
   */
  tools?: AssistantTool[];
  tool_resources?: ModifyAssistantRequestToolResources;
  metadata?: Metadata;
  temperature?: ModifyAssistantRequestTemperature;
  top_p?: ModifyAssistantRequestTopP;
  response_format?: ModifyAssistantRequestResponseFormat;
}

export interface ModifyCertificateRequest {
  /** The updated name for the certificate */
  name: string;
}

export interface ModifyMessageRequest {
  metadata?: Metadata;
}

export interface ModifyRunRequest {
  metadata?: Metadata;
}

export type ModifyThreadRequestToolResourcesAnyOfCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type ModifyThreadRequestToolResourcesAnyOfFileSearch = {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type ModifyThreadRequestToolResourcesAnyOf = {
  code_interpreter?: ModifyThreadRequestToolResourcesAnyOfCodeInterpreter;
  file_search?: ModifyThreadRequestToolResourcesAnyOfFileSearch;
};

export type ModifyThreadRequestToolResources = ModifyThreadRequestToolResourcesAnyOf | null;

export interface ModifyThreadRequest {
  tool_resources?: ModifyThreadRequestToolResources;
  metadata?: Metadata;
}

/**
 * Specifies the event type. For a move action, this property is
always set to `move`.

 */
export type MoveType = typeof MoveType[keyof typeof MoveType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MoveType = {
  move: 'move',
} as const;

/**
 * A mouse move action.

 */
export interface Move {
  /** Specifies the event type. For a move action, this property is
always set to `move`.
 */
  type: MoveType;
  /** The x-coordinate to move to.
 */
  x: number;
  /** The y-coordinate to move to.
 */
  y: number;
}

/**
 * Type of noise reduction. `near_field` is for close-talking microphones such as headphones, `far_field` is for far-field microphones such as laptop or conference room microphones.

 */
export type NoiseReductionType = typeof NoiseReductionType[keyof typeof NoiseReductionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const NoiseReductionType = {
  near_field: 'near_field',
  far_field: 'far_field',
} as const;

/**
 * The object type, which is always `file`.
 */
export type OpenAIFileObject = typeof OpenAIFileObject[keyof typeof OpenAIFileObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenAIFileObject = {
  file: 'file',
} as const;

/**
 * The intended purpose of the file. Supported values are `assistants`, `assistants_output`, `batch`, `batch_output`, `fine-tune`, `fine-tune-results`, `vision`, and `user_data`.
 */
export type OpenAIFilePurpose = typeof OpenAIFilePurpose[keyof typeof OpenAIFilePurpose];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenAIFilePurpose = {
  assistants: 'assistants',
  assistants_output: 'assistants_output',
  batch: 'batch',
  batch_output: 'batch_output',
  'fine-tune': 'fine-tune',
  'fine-tune-results': 'fine-tune-results',
  vision: 'vision',
  user_data: 'user_data',
} as const;

/**
 * Deprecated. The current status of the file, which can be either `uploaded`, `processed`, or `error`.
 * @deprecated
 */
export type OpenAIFileStatus = typeof OpenAIFileStatus[keyof typeof OpenAIFileStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenAIFileStatus = {
  uploaded: 'uploaded',
  processed: 'processed',
  error: 'error',
} as const;

/**
 * The `File` object represents a document that has been uploaded to OpenAI.
 */
export interface OpenAIFile {
  /** The file identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The size of the file, in bytes. */
  bytes: number;
  /** The Unix timestamp (in seconds) for when the file was created. */
  created_at: number;
  /** The Unix timestamp (in seconds) for when the file will expire. */
  expires_at?: number;
  /** The name of the file. */
  filename: string;
  /** The object type, which is always `file`. */
  object: OpenAIFileObject;
  /** The intended purpose of the file. Supported values are `assistants`, `assistants_output`, `batch`, `batch_output`, `fine-tune`, `fine-tune-results`, `vision`, and `user_data`. */
  purpose: OpenAIFilePurpose;
  /**
   * Deprecated. The current status of the file, which can be either `uploaded`, `processed`, or `error`.
   * @deprecated
   */
  status: OpenAIFileStatus;
  /**
   * Deprecated. For details on why a fine-tuning training file failed validation, see the `error` field on `fine_tuning.job`.
   * @deprecated
   */
  status_details?: string;
}

/**
 * Always `other`.
 */
export type OtherChunkingStrategyResponseParamType = typeof OtherChunkingStrategyResponseParamType[keyof typeof OtherChunkingStrategyResponseParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OtherChunkingStrategyResponseParamType = {
  other: 'other',
} as const;

/**
 * This is returned when the chunking strategy is unknown. Typically, this is because the file was indexed before the `chunking_strategy` concept was introduced in the API.
 */
export interface OtherChunkingStrategyResponseParam {
  /** Always `other`. */
  type: OtherChunkingStrategyResponseParamType;
}

/**
 * The type of the output audio. Always `output_audio`.

 */
export type OutputAudioType = typeof OutputAudioType[keyof typeof OutputAudioType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputAudioType = {
  output_audio: 'output_audio',
} as const;

/**
 * An audio output from the model.

 */
export interface OutputAudio {
  /** The type of the output audio. Always `output_audio`.
 */
  type: OutputAudioType;
  /** Base64-encoded audio data from the model.
 */
  data: string;
  /** The transcript of the audio data from the model.
 */
  transcript: string;
}

export type OutputContent = OutputTextContent | RefusalContent | ReasoningTextContent;

export type OutputItem = OutputMessage | FileSearchToolCall | FunctionToolCall | WebSearchToolCall | ComputerToolCall | ReasoningItem | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | MCPToolCall | MCPListTools | MCPApprovalRequest | CustomToolCall;

/**
 * The type of the output message. Always `message`.

 */
export type OutputMessageType = typeof OutputMessageType[keyof typeof OutputMessageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputMessageType = {
  message: 'message',
} as const;

/**
 * The role of the output message. Always `assistant`.

 */
export type OutputMessageRole = typeof OutputMessageRole[keyof typeof OutputMessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputMessageRole = {
  assistant: 'assistant',
} as const;

/**
 * The status of the message input. One of `in_progress`, `completed`, or
`incomplete`. Populated when input items are returned via API.

 */
export type OutputMessageStatus = typeof OutputMessageStatus[keyof typeof OutputMessageStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputMessageStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * An output message from the model.

 */
export interface OutputMessage {
  /** The unique ID of the output message.
 */
  id: string;
  /** The type of the output message. Always `message`.
 */
  type: OutputMessageType;
  /** The role of the output message. Always `assistant`.
 */
  role: OutputMessageRole;
  /** The content of the output message.
 */
  content: OutputMessageContent[];
  /** The status of the message input. One of `in_progress`, `completed`, or
`incomplete`. Populated when input items are returned via API.
 */
  status: OutputMessageStatus;
}

export type OutputMessageContent = OutputTextContent | RefusalContent;

/**
 * Whether to enable [parallel function calling](https://platform.openai.com/docs/guides/function-calling#configuring-parallel-function-calling) during tool use.
 */
export type ParallelToolCalls = boolean;

export type PartialImages = number | null;

/**
 * The type of the predicted content you want to provide. This type is
currently always `content`.

 */
export type PredictionContentType = typeof PredictionContentType[keyof typeof PredictionContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PredictionContentType = {
  content: 'content',
} as const;

/**
 * The content that should be matched when generating a model response.
If generated tokens would match this content, the entire model response
can be returned much more quickly.

 */
export type PredictionContentContent = string | ChatCompletionRequestMessageContentPartText[];

/**
 * Static predicted output content, such as the content of a text file that is
being regenerated.

 */
export interface PredictionContent {
  /** The type of the predicted content you want to provide. This type is
currently always `content`.
 */
  type: PredictionContentType;
  /** The content that should be matched when generating a model response.
If generated tokens would match this content, the entire model response
can be returned much more quickly.
 */
  content: PredictionContentContent;
}

/**
 * The object type, which is always `organization.project`
 */
export type ProjectObject = typeof ProjectObject[keyof typeof ProjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectObject = {
  organizationproject: 'organization.project',
} as const;

export type ProjectArchivedAt = number | null;

/**
 * `active` or `archived`
 */
export type ProjectStatus = typeof ProjectStatus[keyof typeof ProjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectStatus = {
  active: 'active',
  archived: 'archived',
} as const;

/**
 * Represents an individual project.
 */
export interface Project {
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The object type, which is always `organization.project` */
  object: ProjectObject;
  /** The name of the project. This appears in reporting. */
  name: string;
  /** The Unix timestamp (in seconds) of when the project was created. */
  created_at: number;
  archived_at?: ProjectArchivedAt;
  /** `active` or `archived` */
  status: ProjectStatus;
}

/**
 * The object type, which is always `organization.project.api_key`
 */
export type ProjectApiKeyObject = typeof ProjectApiKeyObject[keyof typeof ProjectApiKeyObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectApiKeyObject = {
  organizationprojectapi_key: 'organization.project.api_key',
} as const;

/**
 * `user` or `service_account`
 */
export type ProjectApiKeyOwnerType = typeof ProjectApiKeyOwnerType[keyof typeof ProjectApiKeyOwnerType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectApiKeyOwnerType = {
  user: 'user',
  service_account: 'service_account',
} as const;

export type ProjectApiKeyOwner = {
  /** `user` or `service_account` */
  type?: ProjectApiKeyOwnerType;
  user?: ProjectUser;
  service_account?: ProjectServiceAccount;
};

/**
 * Represents an individual API key in a project.
 */
export interface ProjectApiKey {
  /** The object type, which is always `organization.project.api_key` */
  object: ProjectApiKeyObject;
  /** The redacted value of the API key */
  redacted_value: string;
  /** The name of the API key */
  name: string;
  /** The Unix timestamp (in seconds) of when the API key was created */
  created_at: number;
  /** The Unix timestamp (in seconds) of when the API key was last used. */
  last_used_at: number;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  owner: ProjectApiKeyOwner;
}

export type ProjectApiKeyDeleteResponseObject = typeof ProjectApiKeyDeleteResponseObject[keyof typeof ProjectApiKeyDeleteResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectApiKeyDeleteResponseObject = {
  organizationprojectapi_keydeleted: 'organization.project.api_key.deleted',
} as const;

export interface ProjectApiKeyDeleteResponse {
  object: ProjectApiKeyDeleteResponseObject;
  id: string;
  deleted: boolean;
}

export type ProjectApiKeyListResponseObject = typeof ProjectApiKeyListResponseObject[keyof typeof ProjectApiKeyListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectApiKeyListResponseObject = {
  list: 'list',
} as const;

export interface ProjectApiKeyListResponse {
  object: ProjectApiKeyListResponseObject;
  data: ProjectApiKey[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/**
 * Create the project with the specified data residency region. Your organization must have access to Data residency functionality in order to use. See [data residency controls](https://platform.openai.com/docs/guides/your-data#data-residency-controls) to review the functionality and limitations of setting this field.
 */
export type ProjectCreateRequestGeography = typeof ProjectCreateRequestGeography[keyof typeof ProjectCreateRequestGeography];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectCreateRequestGeography = {
  US: 'US',
  EU: 'EU',
  JP: 'JP',
  IN: 'IN',
  KR: 'KR',
  CA: 'CA',
  AU: 'AU',
  SG: 'SG',
} as const;

export interface ProjectCreateRequest {
  /** The friendly name of the project, this name appears in reports. */
  name: string;
  /** Create the project with the specified data residency region. Your organization must have access to Data residency functionality in order to use. See [data residency controls](https://platform.openai.com/docs/guides/your-data#data-residency-controls) to review the functionality and limitations of setting this field. */
  geography?: ProjectCreateRequestGeography;
}

export type ProjectListResponseObject = typeof ProjectListResponseObject[keyof typeof ProjectListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectListResponseObject = {
  list: 'list',
} as const;

export interface ProjectListResponse {
  object: ProjectListResponseObject;
  data: Project[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/**
 * The object type, which is always `project.rate_limit`
 */
export type ProjectRateLimitObject = typeof ProjectRateLimitObject[keyof typeof ProjectRateLimitObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectRateLimitObject = {
  projectrate_limit: 'project.rate_limit',
} as const;

/**
 * Represents a project rate limit config.
 */
export interface ProjectRateLimit {
  /** The object type, which is always `project.rate_limit` */
  object: ProjectRateLimitObject;
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The model this rate limit applies to. */
  model: string;
  /** The maximum requests per minute. */
  max_requests_per_1_minute: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute: number;
  /** The maximum images per minute. Only present for relevant models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only present for relevant models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only present for relevant models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only present for relevant models. */
  batch_1_day_max_input_tokens?: number;
}

export type ProjectRateLimitListResponseObject = typeof ProjectRateLimitListResponseObject[keyof typeof ProjectRateLimitListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectRateLimitListResponseObject = {
  list: 'list',
} as const;

export interface ProjectRateLimitListResponse {
  object: ProjectRateLimitListResponseObject;
  data: ProjectRateLimit[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface ProjectRateLimitUpdateRequest {
  /** The maximum requests per minute. */
  max_requests_per_1_minute?: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute?: number;
  /** The maximum images per minute. Only relevant for certain models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only relevant for certain models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only relevant for certain models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only relevant for certain models. */
  batch_1_day_max_input_tokens?: number;
}

/**
 * The object type, which is always `organization.project.service_account`
 */
export type ProjectServiceAccountObject = typeof ProjectServiceAccountObject[keyof typeof ProjectServiceAccountObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountObject = {
  organizationprojectservice_account: 'organization.project.service_account',
} as const;

/**
 * `owner` or `member`
 */
export type ProjectServiceAccountRole = typeof ProjectServiceAccountRole[keyof typeof ProjectServiceAccountRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountRole = {
  owner: 'owner',
  member: 'member',
} as const;

/**
 * Represents an individual service account in a project.
 */
export interface ProjectServiceAccount {
  /** The object type, which is always `organization.project.service_account` */
  object: ProjectServiceAccountObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the service account */
  name: string;
  /** `owner` or `member` */
  role: ProjectServiceAccountRole;
  /** The Unix timestamp (in seconds) of when the service account was created */
  created_at: number;
}

/**
 * The object type, which is always `organization.project.service_account.api_key`
 */
export type ProjectServiceAccountApiKeyObject = typeof ProjectServiceAccountApiKeyObject[keyof typeof ProjectServiceAccountApiKeyObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountApiKeyObject = {
  organizationprojectservice_accountapi_key: 'organization.project.service_account.api_key',
} as const;

export interface ProjectServiceAccountApiKey {
  /** The object type, which is always `organization.project.service_account.api_key` */
  object: ProjectServiceAccountApiKeyObject;
  value: string;
  name: string;
  created_at: number;
  id: string;
}

export interface ProjectServiceAccountCreateRequest {
  /** The name of the service account being created. */
  name: string;
}

export type ProjectServiceAccountCreateResponseObject = typeof ProjectServiceAccountCreateResponseObject[keyof typeof ProjectServiceAccountCreateResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountCreateResponseObject = {
  organizationprojectservice_account: 'organization.project.service_account',
} as const;

/**
 * Service accounts can only have one role of type `member`
 */
export type ProjectServiceAccountCreateResponseRole = typeof ProjectServiceAccountCreateResponseRole[keyof typeof ProjectServiceAccountCreateResponseRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountCreateResponseRole = {
  member: 'member',
} as const;

export interface ProjectServiceAccountCreateResponse {
  object: ProjectServiceAccountCreateResponseObject;
  id: string;
  name: string;
  /** Service accounts can only have one role of type `member` */
  role: ProjectServiceAccountCreateResponseRole;
  created_at: number;
  api_key: ProjectServiceAccountApiKey;
}

export type ProjectServiceAccountDeleteResponseObject = typeof ProjectServiceAccountDeleteResponseObject[keyof typeof ProjectServiceAccountDeleteResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountDeleteResponseObject = {
  organizationprojectservice_accountdeleted: 'organization.project.service_account.deleted',
} as const;

export interface ProjectServiceAccountDeleteResponse {
  object: ProjectServiceAccountDeleteResponseObject;
  id: string;
  deleted: boolean;
}

export type ProjectServiceAccountListResponseObject = typeof ProjectServiceAccountListResponseObject[keyof typeof ProjectServiceAccountListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectServiceAccountListResponseObject = {
  list: 'list',
} as const;

export interface ProjectServiceAccountListResponse {
  object: ProjectServiceAccountListResponseObject;
  data: ProjectServiceAccount[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface ProjectUpdateRequest {
  /** The updated name of the project, this name appears in reports. */
  name: string;
}

/**
 * The object type, which is always `organization.project.user`
 */
export type ProjectUserObject = typeof ProjectUserObject[keyof typeof ProjectUserObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectUserObject = {
  organizationprojectuser: 'organization.project.user',
} as const;

/**
 * `owner` or `member`
 */
export type ProjectUserRole = typeof ProjectUserRole[keyof typeof ProjectUserRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectUserRole = {
  owner: 'owner',
  member: 'member',
} as const;

/**
 * Represents an individual user in a project.
 */
export interface ProjectUser {
  /** The object type, which is always `organization.project.user` */
  object: ProjectUserObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the user */
  name: string;
  /** The email address of the user */
  email: string;
  /** `owner` or `member` */
  role: ProjectUserRole;
  /** The Unix timestamp (in seconds) of when the project was added. */
  added_at: number;
}

/**
 * `owner` or `member`
 */
export type ProjectUserCreateRequestRole = typeof ProjectUserCreateRequestRole[keyof typeof ProjectUserCreateRequestRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectUserCreateRequestRole = {
  owner: 'owner',
  member: 'member',
} as const;

export interface ProjectUserCreateRequest {
  /** The ID of the user. */
  user_id: string;
  /** `owner` or `member` */
  role: ProjectUserCreateRequestRole;
}

export type ProjectUserDeleteResponseObject = typeof ProjectUserDeleteResponseObject[keyof typeof ProjectUserDeleteResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectUserDeleteResponseObject = {
  organizationprojectuserdeleted: 'organization.project.user.deleted',
} as const;

export interface ProjectUserDeleteResponse {
  object: ProjectUserDeleteResponseObject;
  id: string;
  deleted: boolean;
}

export interface ProjectUserListResponse {
  object: string;
  data: ProjectUser[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/**
 * `owner` or `member`
 */
export type ProjectUserUpdateRequestRole = typeof ProjectUserUpdateRequestRole[keyof typeof ProjectUserUpdateRequestRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ProjectUserUpdateRequestRole = {
  owner: 'owner',
  member: 'member',
} as const;

export interface ProjectUserUpdateRequest {
  /** `owner` or `member` */
  role: ProjectUserUpdateRequestRole;
}

export type PromptAnyOfVersion = string | null;

/**
 * Reference to a prompt template and its variables.
[Learn more](https://platform.openai.com/docs/guides/text?api-mode=responses#reusable-prompts).

 */
export type PromptAnyOf = {
  /** The unique identifier of the prompt template to use. */
  id: string;
  version?: PromptAnyOfVersion;
  variables?: ResponsePromptVariables;
};

export type Prompt = PromptAnyOf | null;

/**
 * The audio format. Always `audio/pcm`.
 */
export type RealtimeAudioFormatsAnyOfType = typeof RealtimeAudioFormatsAnyOfType[keyof typeof RealtimeAudioFormatsAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeAudioFormatsAnyOfType = {
  'audio/pcm': 'audio/pcm',
} as const;

/**
 * The sample rate of the audio. Always `24000`.
 */
export type RealtimeAudioFormatsAnyOfRate = typeof RealtimeAudioFormatsAnyOfRate[keyof typeof RealtimeAudioFormatsAnyOfRate];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeAudioFormatsAnyOfRate = {
  NUMBER_24000: 24000,
} as const;

/**
 * The PCM audio format. Only a 24kHz sample rate is supported.
 */
export type RealtimeAudioFormatsAnyOf = {
  /** The audio format. Always `audio/pcm`. */
  type?: RealtimeAudioFormatsAnyOfType;
  /** The sample rate of the audio. Always `24000`. */
  rate?: RealtimeAudioFormatsAnyOfRate;
};

/**
 * The audio format. Always `audio/pcmu`.
 */
export type RealtimeAudioFormatsAnyOfFourType = typeof RealtimeAudioFormatsAnyOfFourType[keyof typeof RealtimeAudioFormatsAnyOfFourType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeAudioFormatsAnyOfFourType = {
  'audio/pcmu': 'audio/pcmu',
} as const;

/**
 * The G.711 μ-law format.
 */
export type RealtimeAudioFormatsAnyOfFour = {
  /** The audio format. Always `audio/pcmu`. */
  type?: RealtimeAudioFormatsAnyOfFourType;
};

/**
 * The audio format. Always `audio/pcma`.
 */
export type RealtimeAudioFormatsAnyOfSixType = typeof RealtimeAudioFormatsAnyOfSixType[keyof typeof RealtimeAudioFormatsAnyOfSixType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeAudioFormatsAnyOfSixType = {
  'audio/pcma': 'audio/pcma',
} as const;

/**
 * The G.711 A-law format.
 */
export type RealtimeAudioFormatsAnyOfSix = {
  /** The audio format. Always `audio/pcma`. */
  type?: RealtimeAudioFormatsAnyOfSixType;
};

export type RealtimeAudioFormats = RealtimeAudioFormatsAnyOf | RealtimeAudioFormatsAnyOfFour | RealtimeAudioFormatsAnyOfSix;

/**
 * Add a new Item to the Conversation's context, including messages, function
calls, and function call responses. This event can be used both to populate a
"history" of the conversation and to add new items mid-stream, but has the
current limitation that it cannot populate assistant audio messages.

If successful, the server will respond with a `conversation.item.created`
event, otherwise an `error` event will be sent.

 */
export interface RealtimeBetaClientEventConversationItemCreate {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `conversation.item.create`. */
  type: 'conversation.item.create';
  /** The ID of the preceding item after which the new item will be inserted.
If not set, the new item will be appended to the end of the conversation.
If set to `root`, the new item will be added to the beginning of the conversation.
If set to an existing ID, it allows an item to be inserted mid-conversation. If the
ID cannot be found, an error will be returned and the item will not be added.
 */
  previous_item_id?: string;
  item: RealtimeConversationItem;
}

/**
 * Send this event when you want to remove any item from the conversation
history. The server will respond with a `conversation.item.deleted` event,
unless the item does not exist in the conversation history, in which case the
server will respond with an error.

 */
export interface RealtimeBetaClientEventConversationItemDelete {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `conversation.item.delete`. */
  type: 'conversation.item.delete';
  /** The ID of the item to delete. */
  item_id: string;
}

/**
 * Send this event when you want to retrieve the server's representation of a specific item in the conversation history. This is useful, for example, to inspect user audio after noise cancellation and VAD.
The server will respond with a `conversation.item.retrieved` event,
unless the item does not exist in the conversation history, in which case the
server will respond with an error.

 */
export interface RealtimeBetaClientEventConversationItemRetrieve {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `conversation.item.retrieve`. */
  type: 'conversation.item.retrieve';
  /** The ID of the item to retrieve. */
  item_id: string;
}

/**
 * Send this event to truncate a previous assistant message’s audio. The server
will produce audio faster than realtime, so this event is useful when the user
interrupts to truncate audio that has already been sent to the client but not
yet played. This will synchronize the server's understanding of the audio with
the client's playback.

Truncating audio will delete the server-side text transcript to ensure there
is not text in the context that hasn't been heard by the user.

If successful, the server will respond with a `conversation.item.truncated`
event.

 */
export interface RealtimeBetaClientEventConversationItemTruncate {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `conversation.item.truncate`. */
  type: 'conversation.item.truncate';
  /** The ID of the assistant message item to truncate. Only assistant message
items can be truncated.
 */
  item_id: string;
  /** The index of the content part to truncate. Set this to 0. */
  content_index: number;
  /** Inclusive duration up to which audio is truncated, in milliseconds. If
the audio_end_ms is greater than the actual audio duration, the server
will respond with an error.
 */
  audio_end_ms: number;
}

/**
 * Send this event to append audio bytes to the input audio buffer. The audio
buffer is temporary storage you can write to and later commit. In Server VAD
mode, the audio buffer is used to detect speech and the server will decide
when to commit. When Server VAD is disabled, you must commit the audio buffer
manually.

The client may choose how much audio to place in each event up to a maximum
of 15 MiB, for example streaming smaller chunks from the client may allow the
VAD to be more responsive. Unlike made other client events, the server will
not send a confirmation response to this event.

 */
export interface RealtimeBetaClientEventInputAudioBufferAppend {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.append`. */
  type: 'input_audio_buffer.append';
  /** Base64-encoded audio bytes. This must be in the format specified by the
`input_audio_format` field in the session configuration.
 */
  audio: string;
}

/**
 * Send this event to clear the audio bytes in the buffer. The server will
respond with an `input_audio_buffer.cleared` event.

 */
export interface RealtimeBetaClientEventInputAudioBufferClear {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.clear`. */
  type: 'input_audio_buffer.clear';
}

/**
 * Send this event to commit the user input audio buffer, which will create a
new user message item in the conversation. This event will produce an error
if the input audio buffer is empty. When in Server VAD mode, the client does
not need to send this event, the server will commit the audio buffer
automatically.

Committing the input audio buffer will trigger input audio transcription
(if enabled in session configuration), but it will not create a response
from the model. The server will respond with an `input_audio_buffer.committed`
event.

 */
export interface RealtimeBetaClientEventInputAudioBufferCommit {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.commit`. */
  type: 'input_audio_buffer.commit';
}

/**
 * **WebRTC Only:** Emit to cut off the current audio response. This will trigger the server to
stop generating audio and emit a `output_audio_buffer.cleared` event. This
event should be preceded by a `response.cancel` client event to stop the
generation of the current response.
[Learn more](https://platform.openai.com/docs/guides/realtime-conversations#client-and-server-events-for-audio-in-webrtc).

 */
export interface RealtimeBetaClientEventOutputAudioBufferClear {
  /** The unique ID of the client event used for error handling. */
  event_id?: string;
  /** The event type, must be `output_audio_buffer.clear`. */
  type: 'output_audio_buffer.clear';
}

/**
 * Send this event to cancel an in-progress response. The server will respond
with a `response.done` event with a status of `response.status=cancelled`. If
there is no response to cancel, the server will respond with an error.

 */
export interface RealtimeBetaClientEventResponseCancel {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `response.cancel`. */
  type: 'response.cancel';
  /** A specific response ID to cancel - if not provided, will cancel an
in-progress response in the default conversation.
 */
  response_id?: string;
}

/**
 * This event instructs the server to create a Response, which means triggering
model inference. When in Server VAD mode, the server will create Responses
automatically.

A Response will include at least one Item, and may have two, in which case
the second will be a function call. These Items will be appended to the
conversation history.

The server will respond with a `response.created` event, events for Items
and content created, and finally a `response.done` event to indicate the
Response is complete.

The `response.create` event can optionally include inference configuration like
`instructions`, and `temperature`. These fields will override the Session's
configuration for this Response only.

Responses can be created out-of-band of the default Conversation, meaning that they can
have arbitrary input, and it's possible to disable writing the output to the Conversation.
Only one Response can write to the default Conversation at a time, but otherwise multiple
Responses can be created in parallel.

Clients can set `conversation` to `none` to create a Response that does not write to the default
Conversation. Arbitrary input can be provided with the `input` field, which is an array accepting
raw Items and references to existing Items.

 */
export interface RealtimeBetaClientEventResponseCreate {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `response.create`. */
  type: 'response.create';
  response?: RealtimeBetaResponseCreateParams;
}

/**
 * Send this event to update the session’s default configuration.
The client may send this event at any time to update any field,
except for `voice`. However, note that once a session has been
initialized with a particular `model`, it can’t be changed to
another model using `session.update`.

When the server receives a `session.update`, it will respond
with a `session.updated` event showing the full, effective configuration.
Only the fields that are present are updated. To clear a field like
`instructions`, pass an empty string.

 */
export interface RealtimeBetaClientEventSessionUpdate {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `session.update`. */
  type: 'session.update';
  session: RealtimeSessionCreateRequest;
}

/**
 * Send this event to update a transcription session.

 */
export interface RealtimeBetaClientEventTranscriptionSessionUpdate {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `transcription_session.update`. */
  type: 'transcription_session.update';
  session: RealtimeTranscriptionSessionCreateRequest;
}

/**
 * The final status of the response (`completed`, `cancelled`, `failed`, or
`incomplete`, `in_progress`).

 */
export type RealtimeBetaResponseStatus = typeof RealtimeBetaResponseStatus[keyof typeof RealtimeBetaResponseStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseStatus = {
  completed: 'completed',
  cancelled: 'cancelled',
  failed: 'failed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The type of error that caused the response to fail, corresponding
with the `status` field (`completed`, `cancelled`, `incomplete`,
`failed`).

 */
export type RealtimeBetaResponseStatusDetailsType = typeof RealtimeBetaResponseStatusDetailsType[keyof typeof RealtimeBetaResponseStatusDetailsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseStatusDetailsType = {
  completed: 'completed',
  cancelled: 'cancelled',
  incomplete: 'incomplete',
  failed: 'failed',
} as const;

/**
 * The reason the Response did not complete. For a `cancelled` Response,
one of `turn_detected` (the server VAD detected a new start of speech)
or `client_cancelled` (the client sent a cancel event). For an
`incomplete` Response, one of `max_output_tokens` or `content_filter`
(the server-side safety filter activated and cut off the response).

 */
export type RealtimeBetaResponseStatusDetailsReason = typeof RealtimeBetaResponseStatusDetailsReason[keyof typeof RealtimeBetaResponseStatusDetailsReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseStatusDetailsReason = {
  turn_detected: 'turn_detected',
  client_cancelled: 'client_cancelled',
  max_output_tokens: 'max_output_tokens',
  content_filter: 'content_filter',
} as const;

/**
 * A description of the error that caused the response to fail,
populated when the `status` is `failed`.

 */
export type RealtimeBetaResponseStatusDetailsError = {
  /** The type of error. */
  type?: string;
  /** Error code, if any. */
  code?: string;
};

/**
 * Additional details about the status.
 */
export type RealtimeBetaResponseStatusDetails = {
  /** The type of error that caused the response to fail, corresponding
with the `status` field (`completed`, `cancelled`, `incomplete`,
`failed`).
 */
  type?: RealtimeBetaResponseStatusDetailsType;
  /** The reason the Response did not complete. For a `cancelled` Response,
one of `turn_detected` (the server VAD detected a new start of speech)
or `client_cancelled` (the client sent a cancel event). For an
`incomplete` Response, one of `max_output_tokens` or `content_filter`
(the server-side safety filter activated and cut off the response).
 */
  reason?: RealtimeBetaResponseStatusDetailsReason;
  /** A description of the error that caused the response to fail,
populated when the `status` is `failed`.
 */
  error?: RealtimeBetaResponseStatusDetailsError;
};

/**
 * Details about the cached tokens used as input for the Response.
 */
export type RealtimeBetaResponseUsageInputTokenDetailsCachedTokensDetails = {
  /** The number of cached text tokens used as input for the Response. */
  text_tokens?: number;
  /** The number of cached image tokens used as input for the Response. */
  image_tokens?: number;
  /** The number of cached audio tokens used as input for the Response. */
  audio_tokens?: number;
};

/**
 * Details about the input tokens used in the Response.
 */
export type RealtimeBetaResponseUsageInputTokenDetails = {
  /** The number of cached tokens used as input for the Response. */
  cached_tokens?: number;
  /** The number of text tokens used as input for the Response. */
  text_tokens?: number;
  /** The number of image tokens used as input for the Response. */
  image_tokens?: number;
  /** The number of audio tokens used as input for the Response. */
  audio_tokens?: number;
  /** Details about the cached tokens used as input for the Response. */
  cached_tokens_details?: RealtimeBetaResponseUsageInputTokenDetailsCachedTokensDetails;
};

/**
 * Details about the output tokens used in the Response.
 */
export type RealtimeBetaResponseUsageOutputTokenDetails = {
  /** The number of text tokens used in the Response. */
  text_tokens?: number;
  /** The number of audio tokens used in the Response. */
  audio_tokens?: number;
};

/**
 * Usage statistics for the Response, this will correspond to billing. A
Realtime API session will maintain a conversation context and append new
Items to the Conversation, thus output from previous turns (text and
audio tokens) will become the input for later turns.

 */
export type RealtimeBetaResponseUsage = {
  /** The total number of tokens in the Response including input and output
text and audio tokens.
 */
  total_tokens?: number;
  /** The number of input tokens used in the Response, including text and
audio tokens.
 */
  input_tokens?: number;
  /** The number of output tokens sent in the Response, including text and
audio tokens.
 */
  output_tokens?: number;
  /** Details about the input tokens used in the Response. */
  input_token_details?: RealtimeBetaResponseUsageInputTokenDetails;
  /** Details about the output tokens used in the Response. */
  output_token_details?: RealtimeBetaResponseUsageOutputTokenDetails;
};

export type RealtimeBetaResponseModalitiesItem = typeof RealtimeBetaResponseModalitiesItem[keyof typeof RealtimeBetaResponseModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.

 */
export type RealtimeBetaResponseOutputAudioFormat = typeof RealtimeBetaResponseOutputAudioFormat[keyof typeof RealtimeBetaResponseOutputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseOutputAudioFormat = {
  pcm16: 'pcm16',
  g711_ulaw: 'g711_ulaw',
  g711_alaw: 'g711_alaw',
} as const;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls, that was used in this response.

 */
export type RealtimeBetaResponseMaxOutputTokens = number | 'inf';

/**
 * The response resource.
 */
export interface RealtimeBetaResponse {
  /** The unique ID of the response. */
  id?: string;
  /** The object type, must be `realtime.response`. */
  object?: 'realtime.response';
  /** The final status of the response (`completed`, `cancelled`, `failed`, or
`incomplete`, `in_progress`).
 */
  status?: RealtimeBetaResponseStatus;
  /** Additional details about the status. */
  status_details?: RealtimeBetaResponseStatusDetails;
  /** The list of output items generated by the response. */
  output?: RealtimeConversationItem[];
  metadata?: Metadata;
  /** Usage statistics for the Response, this will correspond to billing. A
Realtime API session will maintain a conversation context and append new
Items to the Conversation, thus output from previous turns (text and
audio tokens) will become the input for later turns.
 */
  usage?: RealtimeBetaResponseUsage;
  /** Which conversation the response is added to, determined by the `conversation`
field in the `response.create` event. If `auto`, the response will be added to
the default conversation and the value of `conversation_id` will be an id like
`conv_1234`. If `none`, the response will not be added to any conversation and
the value of `conversation_id` will be `null`. If responses are being triggered
by server VAD, the response will be added to the default conversation, thus
the `conversation_id` will be an id like `conv_1234`.
 */
  conversation_id?: string;
  /** The voice the model used to respond.
Current voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, and `verse`.
 */
  voice?: VoiceIdsShared;
  /** The set of modalities the model used to respond. If there are multiple modalities,
the model will pick one, for example if `modalities` is `["text", "audio"]`, the model
could be responding in either text or audio.
 */
  modalities?: RealtimeBetaResponseModalitiesItem[];
  /** The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 */
  output_audio_format?: RealtimeBetaResponseOutputAudioFormat;
  /** Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.
 */
  temperature?: number;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls, that was used in this response.
 */
  max_output_tokens?: RealtimeBetaResponseMaxOutputTokens;
}

export type RealtimeBetaResponseCreateParamsModalitiesItem = typeof RealtimeBetaResponseCreateParamsModalitiesItem[keyof typeof RealtimeBetaResponseCreateParamsModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseCreateParamsModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.

 */
export type RealtimeBetaResponseCreateParamsOutputAudioFormat = typeof RealtimeBetaResponseCreateParamsOutputAudioFormat[keyof typeof RealtimeBetaResponseCreateParamsOutputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseCreateParamsOutputAudioFormat = {
  pcm16: 'pcm16',
  g711_ulaw: 'g711_ulaw',
  g711_alaw: 'g711_alaw',
} as const;

/**
 * The type of the tool, i.e. `function`.
 */
export type RealtimeBetaResponseCreateParamsToolsItemType = typeof RealtimeBetaResponseCreateParamsToolsItemType[keyof typeof RealtimeBetaResponseCreateParamsToolsItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaResponseCreateParamsToolsItemType = {
  function: 'function',
} as const;

/**
 * Parameters of the function in JSON Schema.
 */
export type RealtimeBetaResponseCreateParamsToolsItemParameters = { [key: string]: unknown };

export type RealtimeBetaResponseCreateParamsToolsItem = {
  /** The type of the tool, i.e. `function`. */
  type?: RealtimeBetaResponseCreateParamsToolsItemType;
  /** The name of the function. */
  name?: string;
  /** The description of the function, including guidance on when and how
to call it, and guidance about what to tell the user when calling
(if anything).
 */
  description?: string;
  /** Parameters of the function in JSON Schema. */
  parameters?: RealtimeBetaResponseCreateParamsToolsItemParameters;
};

/**
 * How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.

 */
export type RealtimeBetaResponseCreateParamsToolChoice = ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMCP;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeBetaResponseCreateParamsMaxOutputTokens = number | 'inf';

/**
 * Controls which conversation the response is added to. Currently supports
`auto` and `none`, with `auto` as the default value. The `auto` value
means that the contents of the response will be added to the default
conversation. Set this to `none` to create an out-of-band response which
will not add items to default conversation.

 */
export type RealtimeBetaResponseCreateParamsConversation = string | 'auto' | 'none';

/**
 * Create a new Realtime response with these parameters
 */
export interface RealtimeBetaResponseCreateParams {
  /** The set of modalities the model can respond with. To disable audio,
set this to ["text"].
 */
  modalities?: RealtimeBetaResponseCreateParamsModalitiesItem[];
  /** The default system instructions (i.e. system message) prepended to model
calls. This field allows the client to guide the model on desired
responses. The model can be instructed on response content and format,
(e.g. "be extremely succinct", "act friendly", "here are examples of good
responses") and on audio behavior (e.g. "talk quickly", "inject emotion
into your voice", "laugh frequently"). The instructions are not guaranteed
to be followed by the model, but they provide guidance to the model on the
desired behavior.

Note that the server sets default instructions which will be used if this
field is not set and are visible in the `session.created` event at the
start of the session.
 */
  instructions?: string;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, and `verse`.
 */
  voice?: VoiceIdsShared;
  /** The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 */
  output_audio_format?: RealtimeBetaResponseCreateParamsOutputAudioFormat;
  /** Tools (functions) available to the model. */
  tools?: RealtimeBetaResponseCreateParamsToolsItem[];
  /** How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.
 */
  tool_choice?: RealtimeBetaResponseCreateParamsToolChoice;
  /** Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.
 */
  temperature?: number;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_output_tokens?: RealtimeBetaResponseCreateParamsMaxOutputTokens;
  /** Controls which conversation the response is added to. Currently supports
`auto` and `none`, with `auto` as the default value. The `auto` value
means that the contents of the response will be added to the default
conversation. Set this to `none` to create an out-of-band response which
will not add items to default conversation.
 */
  conversation?: RealtimeBetaResponseCreateParamsConversation;
  metadata?: Metadata;
  prompt?: Prompt;
  /** Input items to include in the prompt for the model. Using this field
creates a new context for this Response instead of using the default
conversation. An empty array `[]` will clear the context for this Response.
Note that this can include references to items from the default conversation.
 */
  input?: RealtimeConversationItem[];
}

export type RealtimeBetaServerEventConversationItemCreatedPreviousItemId = string | null;

/**
 * Returned when a conversation item is created. There are several scenarios that produce this event:
  - The server is generating a Response, which if successful will produce
    either one or two Items, which will be of type `message`
    (role `assistant`) or type `function_call`.
  - The input audio buffer has been committed, either by the client or the
    server (in `server_vad` mode). The server will take the content of the
    input audio buffer and add it to a new user message Item.
  - The client has sent a `conversation.item.create` event to add a new Item
    to the Conversation.

 */
export interface RealtimeBetaServerEventConversationItemCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.created`. */
  type: 'conversation.item.created';
  previous_item_id?: RealtimeBetaServerEventConversationItemCreatedPreviousItemId;
  item: RealtimeConversationItem;
}

/**
 * Returned when an item in the conversation is deleted by the client with a
`conversation.item.delete` event. This event is used to synchronize the
server's understanding of the conversation history with the client's view.

 */
export interface RealtimeBetaServerEventConversationItemDeleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.deleted`. */
  type: 'conversation.item.deleted';
  /** The ID of the item that was deleted. */
  item_id: string;
}

/**
 * The event type, must be
`conversation.item.input_audio_transcription.completed`.

 */
export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedType = typeof RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedType[keyof typeof RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedType = {
  conversationiteminput_audio_transcriptioncompleted: 'conversation.item.input_audio_transcription.completed',
} as const;

export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedLogprobs = LogProbProperties[] | null;

/**
 * Usage statistics for the transcription.
 */
export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/**
 * This event is the output of audio transcription for user audio written to the
user audio buffer. Transcription begins when the input audio buffer is
committed by the client or server (in `server_vad` mode). Transcription runs
asynchronously with Response creation, so this event may come before or after
the Response events.

Realtime API models accept audio natively, and thus input transcription is a
separate process run on a separate ASR (Automatic Speech Recognition) model.
The transcript may diverge somewhat from the model's interpretation, and
should be treated as a rough guide.

 */
export interface RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be
`conversation.item.input_audio_transcription.completed`.
 */
  type: RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedType;
  /** The ID of the user message item containing the audio. */
  item_id: string;
  /** The index of the content part containing the audio. */
  content_index: number;
  /** The transcribed text. */
  transcript: string;
  logprobs?: RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedLogprobs;
  /** Usage statistics for the transcription. */
  usage: RealtimeBetaServerEventConversationItemInputAudioTranscriptionCompletedUsage;
}

export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionDeltaLogprobs = LogProbProperties[] | null;

/**
 * Returned when the text value of an input audio transcription content part is updated.

 */
export interface RealtimeBetaServerEventConversationItemInputAudioTranscriptionDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.input_audio_transcription.delta`. */
  type: 'conversation.item.input_audio_transcription.delta';
  /** The ID of the item. */
  item_id: string;
  /** The index of the content part in the item's content array. */
  content_index?: number;
  /** The text delta. */
  delta?: string;
  logprobs?: RealtimeBetaServerEventConversationItemInputAudioTranscriptionDeltaLogprobs;
}

/**
 * The event type, must be
`conversation.item.input_audio_transcription.failed`.

 */
export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedType = typeof RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedType[keyof typeof RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedType = {
  conversationiteminput_audio_transcriptionfailed: 'conversation.item.input_audio_transcription.failed',
} as const;

/**
 * Details of the transcription error.
 */
export type RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedError = {
  /** The type of error. */
  type?: string;
  /** Error code, if any. */
  code?: string;
  /** A human-readable error message. */
  message?: string;
  /** Parameter related to the error, if any. */
  param?: string;
};

/**
 * Returned when input audio transcription is configured, and a transcription
request for a user message failed. These events are separate from other
`error` events so that the client can identify the related Item.

 */
export interface RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be
`conversation.item.input_audio_transcription.failed`.
 */
  type: RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedType;
  /** The ID of the user message item. */
  item_id: string;
  /** The index of the content part containing the audio. */
  content_index: number;
  /** Details of the transcription error. */
  error: RealtimeBetaServerEventConversationItemInputAudioTranscriptionFailedError;
}

/**
 * Returned when an input audio transcription segment is identified for an item.
 */
export interface RealtimeBetaServerEventConversationItemInputAudioTranscriptionSegment {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.input_audio_transcription.segment`. */
  type: 'conversation.item.input_audio_transcription.segment';
  /** The ID of the item containing the input audio content. */
  item_id: string;
  /** The index of the input audio content part within the item. */
  content_index: number;
  /** The text for this segment. */
  text: string;
  /** The segment identifier. */
  id: string;
  /** The detected speaker label for this segment. */
  speaker: string;
  /** Start time of the segment in seconds. */
  start: number;
  /** End time of the segment in seconds. */
  end: number;
}

/**
 * Returned when a conversation item is retrieved with `conversation.item.retrieve`.

 */
export interface RealtimeBetaServerEventConversationItemRetrieved {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.retrieved`. */
  type: 'conversation.item.retrieved';
  item: RealtimeConversationItem;
}

/**
 * Returned when an earlier assistant audio message item is truncated by the
client with a `conversation.item.truncate` event. This event is used to
synchronize the server's understanding of the audio with the client's playback.

This action will truncate the audio and remove the server-side text transcript
to ensure there is no text in the context that hasn't been heard by the user.

 */
export interface RealtimeBetaServerEventConversationItemTruncated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.truncated`. */
  type: 'conversation.item.truncated';
  /** The ID of the assistant message item that was truncated. */
  item_id: string;
  /** The index of the content part that was truncated. */
  content_index: number;
  /** The duration up to which the audio was truncated, in milliseconds.
 */
  audio_end_ms: number;
}

export type RealtimeBetaServerEventErrorErrorCode = string | null;

export type RealtimeBetaServerEventErrorErrorParam = string | null;

export type RealtimeBetaServerEventErrorErrorEventId = string | null;

/**
 * Details of the error.
 */
export type RealtimeBetaServerEventErrorError = {
  /** The type of error (e.g., "invalid_request_error", "server_error").
 */
  type: string;
  code?: RealtimeBetaServerEventErrorErrorCode;
  /** A human-readable error message. */
  message: string;
  param?: RealtimeBetaServerEventErrorErrorParam;
  event_id?: RealtimeBetaServerEventErrorErrorEventId;
};

/**
 * Returned when an error occurs, which could be a client problem or a server
problem. Most errors are recoverable and the session will stay open, we
recommend to implementors to monitor and log error messages by default.

 */
export interface RealtimeBetaServerEventError {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `error`. */
  type: 'error';
  /** Details of the error. */
  error: RealtimeBetaServerEventErrorError;
}

/**
 * Returned when the input audio buffer is cleared by the client with a
`input_audio_buffer.clear` event.

 */
export interface RealtimeBetaServerEventInputAudioBufferCleared {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.cleared`. */
  type: 'input_audio_buffer.cleared';
}

export type RealtimeBetaServerEventInputAudioBufferCommittedPreviousItemId = string | null;

/**
 * Returned when an input audio buffer is committed, either by the client or
automatically in server VAD mode. The `item_id` property is the ID of the user
message item that will be created, thus a `conversation.item.created` event
will also be sent to the client.

 */
export interface RealtimeBetaServerEventInputAudioBufferCommitted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.committed`. */
  type: 'input_audio_buffer.committed';
  previous_item_id?: RealtimeBetaServerEventInputAudioBufferCommittedPreviousItemId;
  /** The ID of the user message item that will be created. */
  item_id: string;
}

/**
 * Sent by the server when in `server_vad` mode to indicate that speech has been
detected in the audio buffer. This can happen any time audio is added to the
buffer (unless speech is already detected). The client may want to use this
event to interrupt audio playback or provide visual feedback to the user.

The client should expect to receive a `input_audio_buffer.speech_stopped` event
when speech stops. The `item_id` property is the ID of the user message item
that will be created when speech stops and will also be included in the
`input_audio_buffer.speech_stopped` event (unless the client manually commits
the audio buffer during VAD activation).

 */
export interface RealtimeBetaServerEventInputAudioBufferSpeechStarted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.speech_started`. */
  type: 'input_audio_buffer.speech_started';
  /** Milliseconds from the start of all audio written to the buffer during the
session when speech was first detected. This will correspond to the
beginning of audio sent to the model, and thus includes the
`prefix_padding_ms` configured in the Session.
 */
  audio_start_ms: number;
  /** The ID of the user message item that will be created when speech stops.
 */
  item_id: string;
}

/**
 * Returned in `server_vad` mode when the server detects the end of speech in
the audio buffer. The server will also send an `conversation.item.created`
event with the user message item that is created from the audio buffer.

 */
export interface RealtimeBetaServerEventInputAudioBufferSpeechStopped {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.speech_stopped`. */
  type: 'input_audio_buffer.speech_stopped';
  /** Milliseconds since the session started when speech stopped. This will
correspond to the end of audio sent to the model, and thus includes the
`min_silence_duration_ms` configured in the Session.
 */
  audio_end_ms: number;
  /** The ID of the user message item that will be created. */
  item_id: string;
}

/**
 * Returned when listing MCP tools has completed for an item.
 */
export interface RealtimeBetaServerEventMCPListToolsCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.completed`. */
  type: 'mcp_list_tools.completed';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * Returned when listing MCP tools has failed for an item.
 */
export interface RealtimeBetaServerEventMCPListToolsFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.failed`. */
  type: 'mcp_list_tools.failed';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * Returned when listing MCP tools is in progress for an item.
 */
export interface RealtimeBetaServerEventMCPListToolsInProgress {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.in_progress`. */
  type: 'mcp_list_tools.in_progress';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * The name of the rate limit (`requests`, `tokens`).

 */
export type RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItemName = typeof RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItemName[keyof typeof RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItemName];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItemName = {
  requests: 'requests',
  tokens: 'tokens',
} as const;

export type RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItem = {
  /** The name of the rate limit (`requests`, `tokens`).
 */
  name?: RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItemName;
  /** The maximum allowed value for the rate limit. */
  limit?: number;
  /** The remaining value before the limit is reached. */
  remaining?: number;
  /** Seconds until the rate limit resets. */
  reset_seconds?: number;
};

/**
 * Emitted at the beginning of a Response to indicate the updated rate limits.
When a Response is created some tokens will be "reserved" for the output
tokens, the rate limits shown here reflect that reservation, which is then
adjusted accordingly once the Response is completed.

 */
export interface RealtimeBetaServerEventRateLimitsUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `rate_limits.updated`. */
  type: 'rate_limits.updated';
  /** List of rate limit information. */
  rate_limits: RealtimeBetaServerEventRateLimitsUpdatedRateLimitsItem[];
}

/**
 * Returned when the model-generated audio is updated.
 */
export interface RealtimeBetaServerEventResponseAudioDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio.delta`. */
  type: 'response.output_audio.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** Base64-encoded audio data delta. */
  delta: string;
}

/**
 * Returned when the model-generated audio is done. Also emitted when a Response
is interrupted, incomplete, or cancelled.

 */
export interface RealtimeBetaServerEventResponseAudioDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio.done`. */
  type: 'response.output_audio.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
}

/**
 * Returned when the model-generated transcription of audio output is updated.

 */
export interface RealtimeBetaServerEventResponseAudioTranscriptDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio_transcript.delta`. */
  type: 'response.output_audio_transcript.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The transcript delta. */
  delta: string;
}

/**
 * Returned when the model-generated transcription of audio output is done
streaming. Also emitted when a Response is interrupted, incomplete, or
cancelled.

 */
export interface RealtimeBetaServerEventResponseAudioTranscriptDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio_transcript.done`. */
  type: 'response.output_audio_transcript.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The final transcript of the audio. */
  transcript: string;
}

/**
 * The content type ("text", "audio").
 */
export type RealtimeBetaServerEventResponseContentPartAddedPartType = typeof RealtimeBetaServerEventResponseContentPartAddedPartType[keyof typeof RealtimeBetaServerEventResponseContentPartAddedPartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaServerEventResponseContentPartAddedPartType = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The content part that was added.
 */
export type RealtimeBetaServerEventResponseContentPartAddedPart = {
  /** The content type ("text", "audio"). */
  type?: RealtimeBetaServerEventResponseContentPartAddedPartType;
  /** The text content (if type is "text"). */
  text?: string;
  /** Base64-encoded audio data (if type is "audio"). */
  audio?: string;
  /** The transcript of the audio (if type is "audio"). */
  transcript?: string;
};

/**
 * Returned when a new content part is added to an assistant message item during
response generation.

 */
export interface RealtimeBetaServerEventResponseContentPartAdded {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.content_part.added`. */
  type: 'response.content_part.added';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item to which the content part was added. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The content part that was added. */
  part: RealtimeBetaServerEventResponseContentPartAddedPart;
}

/**
 * The content type ("text", "audio").
 */
export type RealtimeBetaServerEventResponseContentPartDonePartType = typeof RealtimeBetaServerEventResponseContentPartDonePartType[keyof typeof RealtimeBetaServerEventResponseContentPartDonePartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeBetaServerEventResponseContentPartDonePartType = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The content part that is done.
 */
export type RealtimeBetaServerEventResponseContentPartDonePart = {
  /** The content type ("text", "audio"). */
  type?: RealtimeBetaServerEventResponseContentPartDonePartType;
  /** The text content (if type is "text"). */
  text?: string;
  /** Base64-encoded audio data (if type is "audio"). */
  audio?: string;
  /** The transcript of the audio (if type is "audio"). */
  transcript?: string;
};

/**
 * Returned when a content part is done streaming in an assistant message item.
Also emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeBetaServerEventResponseContentPartDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.content_part.done`. */
  type: 'response.content_part.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The content part that is done. */
  part: RealtimeBetaServerEventResponseContentPartDonePart;
}

/**
 * Returned when a new Response is created. The first event of response creation,
where the response is in an initial state of `in_progress`.

 */
export interface RealtimeBetaServerEventResponseCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.created`. */
  type: 'response.created';
  response: RealtimeBetaResponse;
}

/**
 * Returned when a Response is done streaming. Always emitted, no matter the
final state. The Response object included in the `response.done` event will
include all output Items in the Response but will omit the raw audio data.

 */
export interface RealtimeBetaServerEventResponseDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.done`. */
  type: 'response.done';
  response: RealtimeBetaResponse;
}

/**
 * Returned when the model-generated function call arguments are updated.

 */
export interface RealtimeBetaServerEventResponseFunctionCallArgumentsDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.function_call_arguments.delta`.
 */
  type: 'response.function_call_arguments.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the function call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the function call. */
  call_id: string;
  /** The arguments delta as a JSON string. */
  delta: string;
}

/**
 * Returned when the model-generated function call arguments are done streaming.
Also emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeBetaServerEventResponseFunctionCallArgumentsDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.function_call_arguments.done`.
 */
  type: 'response.function_call_arguments.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the function call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the function call. */
  call_id: string;
  /** The final arguments as a JSON string. */
  arguments: string;
}

export type RealtimeBetaServerEventResponseMCPCallArgumentsDeltaObfuscation = string | null;

/**
 * Returned when MCP tool call arguments are updated during response generation.
 */
export interface RealtimeBetaServerEventResponseMCPCallArgumentsDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call_arguments.delta`. */
  type: 'response.mcp_call_arguments.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the MCP tool call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The JSON-encoded arguments delta. */
  delta: string;
  obfuscation?: RealtimeBetaServerEventResponseMCPCallArgumentsDeltaObfuscation;
}

/**
 * Returned when MCP tool call arguments are finalized during response generation.
 */
export interface RealtimeBetaServerEventResponseMCPCallArgumentsDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call_arguments.done`. */
  type: 'response.mcp_call_arguments.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the MCP tool call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The final JSON-encoded arguments string. */
  arguments: string;
}

/**
 * Returned when an MCP tool call has completed successfully.
 */
export interface RealtimeBetaServerEventResponseMCPCallCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.completed`. */
  type: 'response.mcp_call.completed';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when an MCP tool call has failed.
 */
export interface RealtimeBetaServerEventResponseMCPCallFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.failed`. */
  type: 'response.mcp_call.failed';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when an MCP tool call has started and is in progress.
 */
export interface RealtimeBetaServerEventResponseMCPCallInProgress {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.in_progress`. */
  type: 'response.mcp_call.in_progress';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when a new Item is created during Response generation.
 */
export interface RealtimeBetaServerEventResponseOutputItemAdded {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_item.added`. */
  type: 'response.output_item.added';
  /** The ID of the Response to which the item belongs. */
  response_id: string;
  /** The index of the output item in the Response. */
  output_index: number;
  item: RealtimeConversationItem;
}

/**
 * Returned when an Item is done streaming. Also emitted when a Response is
interrupted, incomplete, or cancelled.

 */
export interface RealtimeBetaServerEventResponseOutputItemDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_item.done`. */
  type: 'response.output_item.done';
  /** The ID of the Response to which the item belongs. */
  response_id: string;
  /** The index of the output item in the Response. */
  output_index: number;
  item: RealtimeConversationItem;
}

/**
 * Returned when the text value of an "output_text" content part is updated.
 */
export interface RealtimeBetaServerEventResponseTextDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_text.delta`. */
  type: 'response.output_text.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The text delta. */
  delta: string;
}

/**
 * Returned when the text value of an "output_text" content part is done streaming. Also
emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeBetaServerEventResponseTextDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_text.done`. */
  type: 'response.output_text.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The final text content. */
  text: string;
}

/**
 * Returned when a Session is created. Emitted automatically when a new
connection is established as the first server event. This event will contain
the default Session configuration.

 */
export interface RealtimeBetaServerEventSessionCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `session.created`. */
  type: 'session.created';
  session: RealtimeSession;
}

/**
 * Returned when a session is updated with a `session.update` event, unless
there is an error.

 */
export interface RealtimeBetaServerEventSessionUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `session.updated`. */
  type: 'session.updated';
  session: RealtimeSession;
}

/**
 * Returned when a transcription session is created.

 */
export interface RealtimeBetaServerEventTranscriptionSessionCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `transcription_session.created`. */
  type: 'transcription_session.created';
  session: RealtimeTranscriptionSessionCreateResponse;
}

/**
 * Returned when a transcription session is updated with a `transcription_session.update` event, unless
there is an error.

 */
export interface RealtimeBetaServerEventTranscriptionSessionUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `transcription_session.updated`. */
  type: 'transcription_session.updated';
  session: RealtimeTranscriptionSessionCreateResponse;
}

/**
 * Parameters required to initiate a realtime call and receive the SDP answer
needed to complete a WebRTC peer connection. Provide an SDP offer generated
by your client and optionally configure the session that will answer the call.
 */
export interface RealtimeCallCreateRequest {
  /** WebRTC Session Description Protocol (SDP) offer generated by the caller. */
  sdp: string;
  /** Optional session configuration to apply before the realtime session is
created. Use the same parameters you would send in a [`create client secret`](https://platform.openai.com/docs/api-reference/realtime-sessions/create-realtime-client-secret)
request. */
  session?: RealtimeSessionCreateRequestGA;
}

/**
 * Parameters required to transfer a SIP call to a new destination using the
Realtime API.
 */
export interface RealtimeCallReferRequest {
  /** URI that should appear in the SIP Refer-To header. Supports values like
`tel:+14155550123` or `sip:agent@example.com`. */
  target_uri: string;
}

/**
 * Parameters used to decline an incoming SIP call handled by the Realtime API.
 */
export interface RealtimeCallRejectRequest {
  /** SIP response code to send back to the caller. Defaults to `603` (Decline)
when omitted. */
  status_code?: number;
}

/**
 * A realtime client event.

 */
export type RealtimeClientEvent = RealtimeClientEventConversationItemCreate | RealtimeClientEventConversationItemDelete | RealtimeClientEventConversationItemRetrieve | RealtimeClientEventConversationItemTruncate | RealtimeClientEventInputAudioBufferAppend | RealtimeClientEventInputAudioBufferClear | RealtimeClientEventOutputAudioBufferClear | RealtimeClientEventInputAudioBufferCommit | RealtimeClientEventResponseCancel | RealtimeClientEventResponseCreate | RealtimeClientEventSessionUpdate;

/**
 * Add a new Item to the Conversation's context, including messages, function
calls, and function call responses. This event can be used both to populate a
"history" of the conversation and to add new items mid-stream, but has the
current limitation that it cannot populate assistant audio messages.

If successful, the server will respond with a `conversation.item.created`
event, otherwise an `error` event will be sent.

 */
export interface RealtimeClientEventConversationItemCreate {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `conversation.item.create`. */
  type: 'conversation.item.create';
  /** The ID of the preceding item after which the new item will be inserted.
If not set, the new item will be appended to the end of the conversation.
If set to `root`, the new item will be added to the beginning of the conversation.
If set to an existing ID, it allows an item to be inserted mid-conversation. If the
ID cannot be found, an error will be returned and the item will not be added.
 */
  previous_item_id?: string;
  item: RealtimeConversationItem;
}

/**
 * Send this event when you want to remove any item from the conversation
history. The server will respond with a `conversation.item.deleted` event,
unless the item does not exist in the conversation history, in which case the
server will respond with an error.

 */
export interface RealtimeClientEventConversationItemDelete {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `conversation.item.delete`. */
  type: 'conversation.item.delete';
  /** The ID of the item to delete. */
  item_id: string;
}

/**
 * Send this event when you want to retrieve the server's representation of a specific item in the conversation history. This is useful, for example, to inspect user audio after noise cancellation and VAD.
The server will respond with a `conversation.item.retrieved` event,
unless the item does not exist in the conversation history, in which case the
server will respond with an error.

 */
export interface RealtimeClientEventConversationItemRetrieve {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `conversation.item.retrieve`. */
  type: 'conversation.item.retrieve';
  /** The ID of the item to retrieve. */
  item_id: string;
}

/**
 * Send this event to truncate a previous assistant message’s audio. The server
will produce audio faster than realtime, so this event is useful when the user
interrupts to truncate audio that has already been sent to the client but not
yet played. This will synchronize the server's understanding of the audio with
the client's playback.

Truncating audio will delete the server-side text transcript to ensure there
is not text in the context that hasn't been heard by the user.

If successful, the server will respond with a `conversation.item.truncated`
event.

 */
export interface RealtimeClientEventConversationItemTruncate {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `conversation.item.truncate`. */
  type: 'conversation.item.truncate';
  /** The ID of the assistant message item to truncate. Only assistant message
items can be truncated.
 */
  item_id: string;
  /** The index of the content part to truncate. Set this to `0`. */
  content_index: number;
  /** Inclusive duration up to which audio is truncated, in milliseconds. If
the audio_end_ms is greater than the actual audio duration, the server
will respond with an error.
 */
  audio_end_ms: number;
}

/**
 * Send this event to append audio bytes to the input audio buffer. The audio
buffer is temporary storage you can write to and later commit. A "commit" will create a new
user message item in the conversation history from the buffer content and clear the buffer.
Input audio transcription (if enabled) will be generated when the buffer is committed.

If VAD is enabled the audio buffer is used to detect speech and the server will decide
when to commit. When Server VAD is disabled, you must commit the audio buffer
manually. Input audio noise reduction operates on writes to the audio buffer.

The client may choose how much audio to place in each event up to a maximum
of 15 MiB, for example streaming smaller chunks from the client may allow the
VAD to be more responsive. Unlike most other client events, the server will
not send a confirmation response to this event.

 */
export interface RealtimeClientEventInputAudioBufferAppend {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.append`. */
  type: 'input_audio_buffer.append';
  /** Base64-encoded audio bytes. This must be in the format specified by the
`input_audio_format` field in the session configuration.
 */
  audio: string;
}

/**
 * Send this event to clear the audio bytes in the buffer. The server will
respond with an `input_audio_buffer.cleared` event.

 */
export interface RealtimeClientEventInputAudioBufferClear {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.clear`. */
  type: 'input_audio_buffer.clear';
}

/**
 * Send this event to commit the user input audio buffer, which will create a  new user message item in the conversation. This event will produce an error  if the input audio buffer is empty. When in Server VAD mode, the client does  not need to send this event, the server will commit the audio buffer automatically.

Committing the input audio buffer will trigger input audio transcription  (if enabled in session configuration), but it will not create a response  from the model. The server will respond with an `input_audio_buffer.committed` event.

 */
export interface RealtimeClientEventInputAudioBufferCommit {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `input_audio_buffer.commit`. */
  type: 'input_audio_buffer.commit';
}

/**
 * **WebRTC Only:** Emit to cut off the current audio response. This will trigger the server to
stop generating audio and emit a `output_audio_buffer.cleared` event. This
event should be preceded by a `response.cancel` client event to stop the
generation of the current response.
[Learn more](https://platform.openai.com/docs/guides/realtime-conversations#client-and-server-events-for-audio-in-webrtc).

 */
export interface RealtimeClientEventOutputAudioBufferClear {
  /** The unique ID of the client event used for error handling. */
  event_id?: string;
  /** The event type, must be `output_audio_buffer.clear`. */
  type: 'output_audio_buffer.clear';
}

/**
 * Send this event to cancel an in-progress response. The server will respond
with a `response.done` event with a status of `response.status=cancelled`. If
there is no response to cancel, the server will respond with an error. It's safe
to call `response.cancel` even if no response is in progress, an error will be
returned the session will remain unaffected.

 */
export interface RealtimeClientEventResponseCancel {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `response.cancel`. */
  type: 'response.cancel';
  /** A specific response ID to cancel - if not provided, will cancel an
in-progress response in the default conversation.
 */
  response_id?: string;
}

/**
 * This event instructs the server to create a Response, which means triggering
model inference. When in Server VAD mode, the server will create Responses
automatically.

A Response will include at least one Item, and may have two, in which case
the second will be a function call. These Items will be appended to the
conversation history by default.

The server will respond with a `response.created` event, events for Items
and content created, and finally a `response.done` event to indicate the
Response is complete.

The `response.create` event includes inference configuration like
`instructions` and `tools`. If these are set, they will override the Session's
configuration for this Response only.

Responses can be created out-of-band of the default Conversation, meaning that they can
have arbitrary input, and it's possible to disable writing the output to the Conversation.
Only one Response can write to the default Conversation at a time, but otherwise multiple
Responses can be created in parallel. The `metadata` field is a good way to disambiguate
multiple simultaneous Responses.

Clients can set `conversation` to `none` to create a Response that does not write to the default
Conversation. Arbitrary input can be provided with the `input` field, which is an array accepting
raw Items and references to existing Items.

 */
export interface RealtimeClientEventResponseCreate {
  /**
   * Optional client-generated ID used to identify this event.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `response.create`. */
  type: 'response.create';
  response?: RealtimeResponseCreateParams;
}

/**
 * Update the Realtime session. Choose either a realtime
session or a transcription session.

 */
export type RealtimeClientEventSessionUpdateSession = RealtimeSessionCreateRequestGA | RealtimeTranscriptionSessionCreateRequestGA;

/**
 * Send this event to update the session’s configuration.
The client may send this event at any time to update any field
except for `voice` and `model`. `voice` can be updated only if there have been no other audio outputs yet.

When the server receives a `session.update`, it will respond
with a `session.updated` event showing the full, effective configuration.
Only the fields that are present in the `session.update` are updated. To clear a field like
`instructions`, pass an empty string. To clear a field like `tools`, pass an empty array.
To clear a field like `turn_detection`, pass `null`.

 */
export interface RealtimeClientEventSessionUpdate {
  /**
   * Optional client-generated ID used to identify this event. This is an arbitrary string that a client may assign. It will be passed back if there is an error with the event, but the corresponding `session.updated` event will not include it.
   * @maxLength 512
   */
  event_id?: string;
  /** The event type, must be `session.update`. */
  type: 'session.update';
  /** Update the Realtime session. Choose either a realtime
session or a transcription session.
 */
  session: RealtimeClientEventSessionUpdateSession;
}

/**
 * Send this event to update a transcription session.

 */
export interface RealtimeClientEventTranscriptionSessionUpdate {
  /** Optional client-generated ID used to identify this event. */
  event_id?: string;
  /** The event type, must be `transcription_session.update`. */
  type: 'transcription_session.update';
  session: RealtimeTranscriptionSessionCreateRequest;
}

/**
 * A single item within a Realtime conversation.
 */
export type RealtimeConversationItem = RealtimeConversationItemMessageSystem | RealtimeConversationItemMessageUser | RealtimeConversationItemMessageAssistant | RealtimeConversationItemFunctionCall | RealtimeConversationItemFunctionCallOutput | RealtimeMCPApprovalResponse | RealtimeMCPListTools | RealtimeMCPToolCall | RealtimeMCPApprovalRequest;

/**
 * Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item.
 */
export type RealtimeConversationItemFunctionCallObject = typeof RealtimeConversationItemFunctionCallObject[keyof typeof RealtimeConversationItemFunctionCallObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The type of the item. Always `function_call`.
 */
export type RealtimeConversationItemFunctionCallType = typeof RealtimeConversationItemFunctionCallType[keyof typeof RealtimeConversationItemFunctionCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallType = {
  function_call: 'function_call',
} as const;

/**
 * The status of the item. Has no effect on the conversation.
 */
export type RealtimeConversationItemFunctionCallStatus = typeof RealtimeConversationItemFunctionCallStatus[keyof typeof RealtimeConversationItemFunctionCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * A function call item in a Realtime conversation.
 */
export interface RealtimeConversationItemFunctionCall {
  /** The unique ID of the item. This may be provided by the client or generated by the server. */
  id?: string;
  /** Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item. */
  object?: RealtimeConversationItemFunctionCallObject;
  /** The type of the item. Always `function_call`. */
  type: RealtimeConversationItemFunctionCallType;
  /** The status of the item. Has no effect on the conversation. */
  status?: RealtimeConversationItemFunctionCallStatus;
  /** The ID of the function call. */
  call_id?: string;
  /** The name of the function being called. */
  name: string;
  /** The arguments of the function call. This is a JSON-encoded string representing the arguments passed to the function, for example `{"arg1": "value1", "arg2": 42}`. */
  arguments: string;
}

/**
 * Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item.
 */
export type RealtimeConversationItemFunctionCallOutputObject = typeof RealtimeConversationItemFunctionCallOutputObject[keyof typeof RealtimeConversationItemFunctionCallOutputObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallOutputObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The type of the item. Always `function_call_output`.
 */
export type RealtimeConversationItemFunctionCallOutputType = typeof RealtimeConversationItemFunctionCallOutputType[keyof typeof RealtimeConversationItemFunctionCallOutputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallOutputType = {
  function_call_output: 'function_call_output',
} as const;

/**
 * The status of the item. Has no effect on the conversation.
 */
export type RealtimeConversationItemFunctionCallOutputStatus = typeof RealtimeConversationItemFunctionCallOutputStatus[keyof typeof RealtimeConversationItemFunctionCallOutputStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemFunctionCallOutputStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * A function call output item in a Realtime conversation.
 */
export interface RealtimeConversationItemFunctionCallOutput {
  /** The unique ID of the item. This may be provided by the client or generated by the server. */
  id?: string;
  /** Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item. */
  object?: RealtimeConversationItemFunctionCallOutputObject;
  /** The type of the item. Always `function_call_output`. */
  type: RealtimeConversationItemFunctionCallOutputType;
  /** The status of the item. Has no effect on the conversation. */
  status?: RealtimeConversationItemFunctionCallOutputStatus;
  /** The ID of the function call this output is for. */
  call_id: string;
  /** The output of the function call, this is free text and can contain any information or simply be empty. */
  output: string;
}

/**
 * Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item.
 */
export type RealtimeConversationItemMessageAssistantObject = typeof RealtimeConversationItemMessageAssistantObject[keyof typeof RealtimeConversationItemMessageAssistantObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageAssistantObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The type of the item. Always `message`.
 */
export type RealtimeConversationItemMessageAssistantType = typeof RealtimeConversationItemMessageAssistantType[keyof typeof RealtimeConversationItemMessageAssistantType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageAssistantType = {
  message: 'message',
} as const;

/**
 * The status of the item. Has no effect on the conversation.
 */
export type RealtimeConversationItemMessageAssistantStatus = typeof RealtimeConversationItemMessageAssistantStatus[keyof typeof RealtimeConversationItemMessageAssistantStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageAssistantStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The role of the message sender. Always `assistant`.
 */
export type RealtimeConversationItemMessageAssistantRole = typeof RealtimeConversationItemMessageAssistantRole[keyof typeof RealtimeConversationItemMessageAssistantRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageAssistantRole = {
  assistant: 'assistant',
} as const;

/**
 * The content type, `output_text` or `output_audio` depending on the session `output_modalities` configuration.
 */
export type RealtimeConversationItemMessageAssistantContentItemType = typeof RealtimeConversationItemMessageAssistantContentItemType[keyof typeof RealtimeConversationItemMessageAssistantContentItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageAssistantContentItemType = {
  output_text: 'output_text',
  output_audio: 'output_audio',
} as const;

export type RealtimeConversationItemMessageAssistantContentItem = {
  /** The content type, `output_text` or `output_audio` depending on the session `output_modalities` configuration. */
  type?: RealtimeConversationItemMessageAssistantContentItemType;
  /** The text content. */
  text?: string;
  /** Base64-encoded audio bytes, these will be parsed as the format specified in the session output audio type configuration. This defaults to PCM 16-bit 24kHz mono if not specified. */
  audio?: string;
  /** The transcript of the audio content, this will always be present if the output type is `audio`. */
  transcript?: string;
};

/**
 * An assistant message item in a Realtime conversation.
 */
export interface RealtimeConversationItemMessageAssistant {
  /** The unique ID of the item. This may be provided by the client or generated by the server. */
  id?: string;
  /** Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item. */
  object?: RealtimeConversationItemMessageAssistantObject;
  /** The type of the item. Always `message`. */
  type: RealtimeConversationItemMessageAssistantType;
  /** The status of the item. Has no effect on the conversation. */
  status?: RealtimeConversationItemMessageAssistantStatus;
  /** The role of the message sender. Always `assistant`. */
  role: RealtimeConversationItemMessageAssistantRole;
  /** The content of the message. */
  content: RealtimeConversationItemMessageAssistantContentItem[];
}

/**
 * Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item.
 */
export type RealtimeConversationItemMessageSystemObject = typeof RealtimeConversationItemMessageSystemObject[keyof typeof RealtimeConversationItemMessageSystemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageSystemObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The type of the item. Always `message`.
 */
export type RealtimeConversationItemMessageSystemType = typeof RealtimeConversationItemMessageSystemType[keyof typeof RealtimeConversationItemMessageSystemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageSystemType = {
  message: 'message',
} as const;

/**
 * The status of the item. Has no effect on the conversation.
 */
export type RealtimeConversationItemMessageSystemStatus = typeof RealtimeConversationItemMessageSystemStatus[keyof typeof RealtimeConversationItemMessageSystemStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageSystemStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The role of the message sender. Always `system`.
 */
export type RealtimeConversationItemMessageSystemRole = typeof RealtimeConversationItemMessageSystemRole[keyof typeof RealtimeConversationItemMessageSystemRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageSystemRole = {
  system: 'system',
} as const;

/**
 * The content type. Always `input_text` for system messages.
 */
export type RealtimeConversationItemMessageSystemContentItemType = typeof RealtimeConversationItemMessageSystemContentItemType[keyof typeof RealtimeConversationItemMessageSystemContentItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageSystemContentItemType = {
  input_text: 'input_text',
} as const;

export type RealtimeConversationItemMessageSystemContentItem = {
  /** The content type. Always `input_text` for system messages. */
  type?: RealtimeConversationItemMessageSystemContentItemType;
  /** The text content. */
  text?: string;
};

/**
 * A system message in a Realtime conversation can be used to provide additional context or instructions to the model. This is similar but distinct from the instruction prompt provided at the start of a conversation, as system messages can be added at any point in the conversation. For major changes to the conversation's behavior, use instructions, but for smaller updates (e.g. "the user is now asking about a different topic"), use system messages.
 */
export interface RealtimeConversationItemMessageSystem {
  /** The unique ID of the item. This may be provided by the client or generated by the server. */
  id?: string;
  /** Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item. */
  object?: RealtimeConversationItemMessageSystemObject;
  /** The type of the item. Always `message`. */
  type: RealtimeConversationItemMessageSystemType;
  /** The status of the item. Has no effect on the conversation. */
  status?: RealtimeConversationItemMessageSystemStatus;
  /** The role of the message sender. Always `system`. */
  role: RealtimeConversationItemMessageSystemRole;
  /** The content of the message. */
  content: RealtimeConversationItemMessageSystemContentItem[];
}

/**
 * Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item.
 */
export type RealtimeConversationItemMessageUserObject = typeof RealtimeConversationItemMessageUserObject[keyof typeof RealtimeConversationItemMessageUserObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The type of the item. Always `message`.
 */
export type RealtimeConversationItemMessageUserType = typeof RealtimeConversationItemMessageUserType[keyof typeof RealtimeConversationItemMessageUserType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserType = {
  message: 'message',
} as const;

/**
 * The status of the item. Has no effect on the conversation.
 */
export type RealtimeConversationItemMessageUserStatus = typeof RealtimeConversationItemMessageUserStatus[keyof typeof RealtimeConversationItemMessageUserStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The role of the message sender. Always `user`.
 */
export type RealtimeConversationItemMessageUserRole = typeof RealtimeConversationItemMessageUserRole[keyof typeof RealtimeConversationItemMessageUserRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserRole = {
  user: 'user',
} as const;

/**
 * The content type (`input_text`, `input_audio`, or `input_image`).
 */
export type RealtimeConversationItemMessageUserContentItemType = typeof RealtimeConversationItemMessageUserContentItemType[keyof typeof RealtimeConversationItemMessageUserContentItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserContentItemType = {
  input_text: 'input_text',
  input_audio: 'input_audio',
  input_image: 'input_image',
} as const;

/**
 * The detail level of the image (for `input_image`). `auto` will default to `high`.
 */
export type RealtimeConversationItemMessageUserContentItemDetail = typeof RealtimeConversationItemMessageUserContentItemDetail[keyof typeof RealtimeConversationItemMessageUserContentItemDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemMessageUserContentItemDetail = {
  auto: 'auto',
  low: 'low',
  high: 'high',
} as const;

export type RealtimeConversationItemMessageUserContentItem = {
  /** The content type (`input_text`, `input_audio`, or `input_image`). */
  type?: RealtimeConversationItemMessageUserContentItemType;
  /** The text content (for `input_text`). */
  text?: string;
  /** Base64-encoded audio bytes (for `input_audio`), these will be parsed as the format specified in the session input audio type configuration. This defaults to PCM 16-bit 24kHz mono if not specified. */
  audio?: string;
  /** Base64-encoded image bytes (for `input_image`) as a data URI. For example `data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...`. Supported formats are PNG and JPEG. */
  image_url?: string;
  /** The detail level of the image (for `input_image`). `auto` will default to `high`. */
  detail?: RealtimeConversationItemMessageUserContentItemDetail;
  /** Transcript of the audio (for `input_audio`). This is not sent to the model, but will be attached to the message item for reference. */
  transcript?: string;
};

/**
 * A user message item in a Realtime conversation.
 */
export interface RealtimeConversationItemMessageUser {
  /** The unique ID of the item. This may be provided by the client or generated by the server. */
  id?: string;
  /** Identifier for the API object being returned - always `realtime.item`. Optional when creating a new item. */
  object?: RealtimeConversationItemMessageUserObject;
  /** The type of the item. Always `message`. */
  type: RealtimeConversationItemMessageUserType;
  /** The status of the item. Has no effect on the conversation. */
  status?: RealtimeConversationItemMessageUserStatus;
  /** The role of the message sender. Always `user`. */
  role: RealtimeConversationItemMessageUserRole;
  /** The content of the message. */
  content: RealtimeConversationItemMessageUserContentItem[];
}

/**
 * The type of the item (`message`, `function_call`, `function_call_output`, `item_reference`).

 */
export type RealtimeConversationItemWithReferenceType = typeof RealtimeConversationItemWithReferenceType[keyof typeof RealtimeConversationItemWithReferenceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemWithReferenceType = {
  message: 'message',
  function_call: 'function_call',
  function_call_output: 'function_call_output',
  item_reference: 'item_reference',
} as const;

/**
 * Identifier for the API object being returned - always `realtime.item`.

 */
export type RealtimeConversationItemWithReferenceObject = typeof RealtimeConversationItemWithReferenceObject[keyof typeof RealtimeConversationItemWithReferenceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemWithReferenceObject = {
  realtimeitem: 'realtime.item',
} as const;

/**
 * The status of the item (`completed`, `incomplete`, `in_progress`). These have no effect
on the conversation, but are accepted for consistency with the
`conversation.item.created` event.

 */
export type RealtimeConversationItemWithReferenceStatus = typeof RealtimeConversationItemWithReferenceStatus[keyof typeof RealtimeConversationItemWithReferenceStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemWithReferenceStatus = {
  completed: 'completed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The role of the message sender (`user`, `assistant`, `system`), only
applicable for `message` items.

 */
export type RealtimeConversationItemWithReferenceRole = typeof RealtimeConversationItemWithReferenceRole[keyof typeof RealtimeConversationItemWithReferenceRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemWithReferenceRole = {
  user: 'user',
  assistant: 'assistant',
  system: 'system',
} as const;

/**
 * The content type (`input_text`, `input_audio`, `item_reference`, `text`).

 */
export type RealtimeConversationItemWithReferenceContentItemType = typeof RealtimeConversationItemWithReferenceContentItemType[keyof typeof RealtimeConversationItemWithReferenceContentItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeConversationItemWithReferenceContentItemType = {
  input_text: 'input_text',
  input_audio: 'input_audio',
  item_reference: 'item_reference',
  text: 'text',
} as const;

export type RealtimeConversationItemWithReferenceContentItem = {
  /** The content type (`input_text`, `input_audio`, `item_reference`, `text`).
 */
  type?: RealtimeConversationItemWithReferenceContentItemType;
  /** The text content, used for `input_text` and `text` content types.
 */
  text?: string;
  /** ID of a previous conversation item to reference (for `item_reference`
content types in `response.create` events). These can reference both
client and server created items.
 */
  id?: string;
  /** Base64-encoded audio bytes, used for `input_audio` content type.
 */
  audio?: string;
  /** The transcript of the audio, used for `input_audio` content type.
 */
  transcript?: string;
};

/**
 * The item to add to the conversation.
 */
export interface RealtimeConversationItemWithReference {
  /** For an item of type (`message` | `function_call` | `function_call_output`)
this field allows the client to assign the unique ID of the item. It is
not required because the server will generate one if not provided.

For an item of type `item_reference`, this field is required and is a
reference to any item that has previously existed in the conversation.
 */
  id?: string;
  /** The type of the item (`message`, `function_call`, `function_call_output`, `item_reference`).
 */
  type?: RealtimeConversationItemWithReferenceType;
  /** Identifier for the API object being returned - always `realtime.item`.
 */
  object?: RealtimeConversationItemWithReferenceObject;
  /** The status of the item (`completed`, `incomplete`, `in_progress`). These have no effect
on the conversation, but are accepted for consistency with the
`conversation.item.created` event.
 */
  status?: RealtimeConversationItemWithReferenceStatus;
  /** The role of the message sender (`user`, `assistant`, `system`), only
applicable for `message` items.
 */
  role?: RealtimeConversationItemWithReferenceRole;
  /** The content of the message, applicable for `message` items.
- Message items of role `system` support only `input_text` content
- Message items of role `user` support `input_text` and `input_audio`
  content
- Message items of role `assistant` support `text` content.
 */
  content?: RealtimeConversationItemWithReferenceContentItem[];
  /** The ID of the function call (for `function_call` and
`function_call_output` items). If passed on a `function_call_output`
item, the server will check that a `function_call` item with the same
ID exists in the conversation history.
 */
  call_id?: string;
  /** The name of the function being called (for `function_call` items).
 */
  name?: string;
  /** The arguments of the function call (for `function_call` items).
 */
  arguments?: string;
  /** The output of the function call (for `function_call_output` items).
 */
  output?: string;
}

/**
 * The anchor point for the client secret expiration, meaning that `seconds` will be added to the `created_at` time of the client secret to produce an expiration timestamp. Only `created_at` is currently supported.

 */
export type RealtimeCreateClientSecretRequestExpiresAfterAnchor = typeof RealtimeCreateClientSecretRequestExpiresAfterAnchor[keyof typeof RealtimeCreateClientSecretRequestExpiresAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeCreateClientSecretRequestExpiresAfterAnchor = {
  created_at: 'created_at',
} as const;

/**
 * Configuration for the client secret expiration. Expiration refers to the time after which
a client secret will no longer be valid for creating sessions. The session itself may
continue after that time once started. A secret can be used to create multiple sessions
until it expires.

 */
export type RealtimeCreateClientSecretRequestExpiresAfter = {
  /** The anchor point for the client secret expiration, meaning that `seconds` will be added to the `created_at` time of the client secret to produce an expiration timestamp. Only `created_at` is currently supported.
 */
  anchor?: RealtimeCreateClientSecretRequestExpiresAfterAnchor;
  /**
   * The number of seconds from the anchor point to the expiration. Select a value between `10` and `7200` (2 hours). This default to 600 seconds (10 minutes) if not specified.

   * @minimum 10
   * @maximum 7200
   */
  seconds?: number;
};

/**
 * Session configuration to use for the client secret. Choose either a realtime
session or a transcription session.

 */
export type RealtimeCreateClientSecretRequestSession = RealtimeSessionCreateRequestGA | RealtimeTranscriptionSessionCreateRequestGA;

/**
 * Create a session and client secret for the Realtime API. The request can specify
either a realtime or a transcription session configuration.
[Learn more about the Realtime API](https://platform.openai.com/docs/guides/realtime).

 */
export interface RealtimeCreateClientSecretRequest {
  /** Configuration for the client secret expiration. Expiration refers to the time after which
a client secret will no longer be valid for creating sessions. The session itself may
continue after that time once started. A secret can be used to create multiple sessions
until it expires.
 */
  expires_after?: RealtimeCreateClientSecretRequestExpiresAfter;
  /** Session configuration to use for the client secret. Choose either a realtime
session or a transcription session.
 */
  session?: RealtimeCreateClientSecretRequestSession;
}

/**
 * The session configuration for either a realtime or transcription session.

 */
export type RealtimeCreateClientSecretResponseSession = RealtimeSessionCreateResponseGA | RealtimeTranscriptionSessionCreateResponseGA;

/**
 * Response from creating a session and client secret for the Realtime API.

 */
export interface RealtimeCreateClientSecretResponse {
  /** The generated client secret value. */
  value: string;
  /** Expiration timestamp for the client secret, in seconds since epoch. */
  expires_at: number;
  /** The session configuration for either a realtime or transcription session.
 */
  session: RealtimeCreateClientSecretResponseSession;
}

/**
 * The type of the tool, i.e. `function`.
 */
export type RealtimeFunctionToolType = typeof RealtimeFunctionToolType[keyof typeof RealtimeFunctionToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeFunctionToolType = {
  function: 'function',
} as const;

/**
 * Parameters of the function in JSON Schema.
 */
export type RealtimeFunctionToolParameters = { [key: string]: unknown };

export interface RealtimeFunctionTool {
  /** The type of the tool, i.e. `function`. */
  type?: RealtimeFunctionToolType;
  /** The name of the function. */
  name?: string;
  /** The description of the function, including guidance on when and how
to call it, and guidance about what to tell the user when calling
(if anything).
 */
  description?: string;
  /** Parameters of the function in JSON Schema. */
  parameters?: RealtimeFunctionToolParameters;
}

/**
 * The type of the item. Always `mcp_approval_request`.
 */
export type RealtimeMCPApprovalRequestType = typeof RealtimeMCPApprovalRequestType[keyof typeof RealtimeMCPApprovalRequestType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPApprovalRequestType = {
  mcp_approval_request: 'mcp_approval_request',
} as const;

/**
 * A Realtime item requesting human approval of a tool invocation.

 */
export interface RealtimeMCPApprovalRequest {
  /** The type of the item. Always `mcp_approval_request`. */
  type: RealtimeMCPApprovalRequestType;
  /** The unique ID of the approval request. */
  id: string;
  /** The label of the MCP server making the request. */
  server_label: string;
  /** The name of the tool to run. */
  name: string;
  /** A JSON string of arguments for the tool. */
  arguments: string;
}

/**
 * The type of the item. Always `mcp_approval_response`.
 */
export type RealtimeMCPApprovalResponseType = typeof RealtimeMCPApprovalResponseType[keyof typeof RealtimeMCPApprovalResponseType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPApprovalResponseType = {
  mcp_approval_response: 'mcp_approval_response',
} as const;

export type RealtimeMCPApprovalResponseReason = string | null;

/**
 * A Realtime item responding to an MCP approval request.

 */
export interface RealtimeMCPApprovalResponse {
  /** The type of the item. Always `mcp_approval_response`. */
  type: RealtimeMCPApprovalResponseType;
  /** The unique ID of the approval response. */
  id: string;
  /** The ID of the approval request being answered. */
  approval_request_id: string;
  /** Whether the request was approved. */
  approve: boolean;
  reason?: RealtimeMCPApprovalResponseReason;
}

export type RealtimeMCPHTTPErrorType = typeof RealtimeMCPHTTPErrorType[keyof typeof RealtimeMCPHTTPErrorType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPHTTPErrorType = {
  http_error: 'http_error',
} as const;

export interface RealtimeMCPHTTPError {
  type: RealtimeMCPHTTPErrorType;
  code: number;
  message: string;
}

/**
 * The type of the item. Always `mcp_list_tools`.
 */
export type RealtimeMCPListToolsType = typeof RealtimeMCPListToolsType[keyof typeof RealtimeMCPListToolsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPListToolsType = {
  mcp_list_tools: 'mcp_list_tools',
} as const;

/**
 * A Realtime item listing tools available on an MCP server.

 */
export interface RealtimeMCPListTools {
  /** The type of the item. Always `mcp_list_tools`. */
  type: RealtimeMCPListToolsType;
  /** The unique ID of the list. */
  id?: string;
  /** The label of the MCP server. */
  server_label: string;
  /** The tools available on the server. */
  tools: MCPListToolsTool[];
}

export type RealtimeMCPProtocolErrorType = typeof RealtimeMCPProtocolErrorType[keyof typeof RealtimeMCPProtocolErrorType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPProtocolErrorType = {
  protocol_error: 'protocol_error',
} as const;

export interface RealtimeMCPProtocolError {
  type: RealtimeMCPProtocolErrorType;
  code: number;
  message: string;
}

/**
 * The type of the item. Always `mcp_call`.
 */
export type RealtimeMCPToolCallType = typeof RealtimeMCPToolCallType[keyof typeof RealtimeMCPToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPToolCallType = {
  mcp_call: 'mcp_call',
} as const;

export type RealtimeMCPToolCallApprovalRequestId = string | null;

export type RealtimeMCPToolCallOutput = string | null;

/**
 * The error from the tool call, if any.
 */
export type RealtimeMCPToolCallErrorAnyOf = RealtimeMCPProtocolError | RealtimeMCPToolExecutionError | RealtimeMCPHTTPError;

export type RealtimeMCPToolCallError = RealtimeMCPToolCallErrorAnyOf | null;

/**
 * A Realtime item representing an invocation of a tool on an MCP server.

 */
export interface RealtimeMCPToolCall {
  /** The type of the item. Always `mcp_call`. */
  type: RealtimeMCPToolCallType;
  /** The unique ID of the tool call. */
  id: string;
  /** The label of the MCP server running the tool. */
  server_label: string;
  /** The name of the tool that was run. */
  name: string;
  /** A JSON string of the arguments passed to the tool. */
  arguments: string;
  approval_request_id?: RealtimeMCPToolCallApprovalRequestId;
  output?: RealtimeMCPToolCallOutput;
  error?: RealtimeMCPToolCallError;
}

export type RealtimeMCPToolExecutionErrorType = typeof RealtimeMCPToolExecutionErrorType[keyof typeof RealtimeMCPToolExecutionErrorType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeMCPToolExecutionErrorType = {
  tool_execution_error: 'tool_execution_error',
} as const;

export interface RealtimeMCPToolExecutionError {
  type: RealtimeMCPToolExecutionErrorType;
  message: string;
}

/**
 * The final status of the response (`completed`, `cancelled`, `failed`, or
`incomplete`, `in_progress`).

 */
export type RealtimeResponseStatus = typeof RealtimeResponseStatus[keyof typeof RealtimeResponseStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeResponseStatus = {
  completed: 'completed',
  cancelled: 'cancelled',
  failed: 'failed',
  incomplete: 'incomplete',
  in_progress: 'in_progress',
} as const;

/**
 * The type of error that caused the response to fail, corresponding
with the `status` field (`completed`, `cancelled`, `incomplete`,
`failed`).

 */
export type RealtimeResponseStatusDetailsType = typeof RealtimeResponseStatusDetailsType[keyof typeof RealtimeResponseStatusDetailsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeResponseStatusDetailsType = {
  completed: 'completed',
  cancelled: 'cancelled',
  incomplete: 'incomplete',
  failed: 'failed',
} as const;

/**
 * The reason the Response did not complete. For a `cancelled` Response,  one of `turn_detected` (the server VAD detected a new start of speech)  or `client_cancelled` (the client sent a cancel event). For an  `incomplete` Response, one of `max_output_tokens` or `content_filter` (the server-side safety filter activated and cut off the response).

 */
export type RealtimeResponseStatusDetailsReason = typeof RealtimeResponseStatusDetailsReason[keyof typeof RealtimeResponseStatusDetailsReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeResponseStatusDetailsReason = {
  turn_detected: 'turn_detected',
  client_cancelled: 'client_cancelled',
  max_output_tokens: 'max_output_tokens',
  content_filter: 'content_filter',
} as const;

/**
 * A description of the error that caused the response to fail,
populated when the `status` is `failed`.

 */
export type RealtimeResponseStatusDetailsError = {
  /** The type of error. */
  type?: string;
  /** Error code, if any. */
  code?: string;
};

/**
 * Additional details about the status.
 */
export type RealtimeResponseStatusDetails = {
  /** The type of error that caused the response to fail, corresponding
with the `status` field (`completed`, `cancelled`, `incomplete`,
`failed`).
 */
  type?: RealtimeResponseStatusDetailsType;
  /** The reason the Response did not complete. For a `cancelled` Response,  one of `turn_detected` (the server VAD detected a new start of speech)  or `client_cancelled` (the client sent a cancel event). For an  `incomplete` Response, one of `max_output_tokens` or `content_filter` (the server-side safety filter activated and cut off the response).
 */
  reason?: RealtimeResponseStatusDetailsReason;
  /** A description of the error that caused the response to fail,
populated when the `status` is `failed`.
 */
  error?: RealtimeResponseStatusDetailsError;
};

export type RealtimeResponseAudioOutput = {
  /** The format of the output audio. */
  format?: RealtimeAudioFormats;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, `verse`, `marin`, and `cedar`. We recommend `marin` and `cedar` for
best quality.
 */
  voice?: VoiceIdsShared;
};

/**
 * Configuration for audio output.
 */
export type RealtimeResponseAudio = {
  output?: RealtimeResponseAudioOutput;
};

/**
 * Details about the cached tokens used as input for the Response.
 */
export type RealtimeResponseUsageInputTokenDetailsCachedTokensDetails = {
  /** The number of cached text tokens used as input for the Response. */
  text_tokens?: number;
  /** The number of cached image tokens used as input for the Response. */
  image_tokens?: number;
  /** The number of cached audio tokens used as input for the Response. */
  audio_tokens?: number;
};

/**
 * Details about the input tokens used in the Response. Cached tokens are tokens from previous turns in the conversation that are included as context for the current response. Cached tokens here are counted as a subset of input tokens, meaning input tokens will include cached and uncached tokens.
 */
export type RealtimeResponseUsageInputTokenDetails = {
  /** The number of cached tokens used as input for the Response. */
  cached_tokens?: number;
  /** The number of text tokens used as input for the Response. */
  text_tokens?: number;
  /** The number of image tokens used as input for the Response. */
  image_tokens?: number;
  /** The number of audio tokens used as input for the Response. */
  audio_tokens?: number;
  /** Details about the cached tokens used as input for the Response. */
  cached_tokens_details?: RealtimeResponseUsageInputTokenDetailsCachedTokensDetails;
};

/**
 * Details about the output tokens used in the Response.
 */
export type RealtimeResponseUsageOutputTokenDetails = {
  /** The number of text tokens used in the Response. */
  text_tokens?: number;
  /** The number of audio tokens used in the Response. */
  audio_tokens?: number;
};

/**
 * Usage statistics for the Response, this will correspond to billing. A
Realtime API session will maintain a conversation context and append new
Items to the Conversation, thus output from previous turns (text and
audio tokens) will become the input for later turns.

 */
export type RealtimeResponseUsage = {
  /** The total number of tokens in the Response including input and output
text and audio tokens.
 */
  total_tokens?: number;
  /** The number of input tokens used in the Response, including text and
audio tokens.
 */
  input_tokens?: number;
  /** The number of output tokens sent in the Response, including text and
audio tokens.
 */
  output_tokens?: number;
  /** Details about the input tokens used in the Response. Cached tokens are tokens from previous turns in the conversation that are included as context for the current response. Cached tokens here are counted as a subset of input tokens, meaning input tokens will include cached and uncached tokens. */
  input_token_details?: RealtimeResponseUsageInputTokenDetails;
  /** Details about the output tokens used in the Response. */
  output_token_details?: RealtimeResponseUsageOutputTokenDetails;
};

export type RealtimeResponseOutputModalitiesItem = typeof RealtimeResponseOutputModalitiesItem[keyof typeof RealtimeResponseOutputModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeResponseOutputModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls, that was used in this response.

 */
export type RealtimeResponseMaxOutputTokens = number | 'inf';

/**
 * The response resource.
 */
export interface RealtimeResponse {
  /** The unique ID of the response, will look like `resp_1234`. */
  id?: string;
  /** The object type, must be `realtime.response`. */
  object?: 'realtime.response';
  /** The final status of the response (`completed`, `cancelled`, `failed`, or
`incomplete`, `in_progress`).
 */
  status?: RealtimeResponseStatus;
  /** Additional details about the status. */
  status_details?: RealtimeResponseStatusDetails;
  /** The list of output items generated by the response. */
  output?: RealtimeConversationItem[];
  metadata?: Metadata;
  /** Configuration for audio output. */
  audio?: RealtimeResponseAudio;
  /** Usage statistics for the Response, this will correspond to billing. A
Realtime API session will maintain a conversation context and append new
Items to the Conversation, thus output from previous turns (text and
audio tokens) will become the input for later turns.
 */
  usage?: RealtimeResponseUsage;
  /** Which conversation the response is added to, determined by the `conversation`
field in the `response.create` event. If `auto`, the response will be added to
the default conversation and the value of `conversation_id` will be an id like
`conv_1234`. If `none`, the response will not be added to any conversation and
the value of `conversation_id` will be `null`. If responses are being triggered
automatically by VAD the response will be added to the default conversation
 */
  conversation_id?: string;
  /** The set of modalities the model used to respond, currently the only possible values are
`[\"audio\"]`, `[\"text\"]`. Audio output always include a text transcript. Setting the
output to mode `text` will disable audio output from the model.
 */
  output_modalities?: RealtimeResponseOutputModalitiesItem[];
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls, that was used in this response.
 */
  max_output_tokens?: RealtimeResponseMaxOutputTokens;
}

export type RealtimeResponseCreateParamsOutputModalitiesItem = typeof RealtimeResponseCreateParamsOutputModalitiesItem[keyof typeof RealtimeResponseCreateParamsOutputModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeResponseCreateParamsOutputModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

export type RealtimeResponseCreateParamsAudioOutput = {
  /** The format of the output audio. */
  format?: RealtimeAudioFormats;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, `verse`, `marin`, and `cedar`. We recommend `marin` and `cedar` for
best quality.
 */
  voice?: VoiceIdsShared;
};

/**
 * Configuration for audio input and output.
 */
export type RealtimeResponseCreateParamsAudio = {
  output?: RealtimeResponseCreateParamsAudioOutput;
};

export type RealtimeResponseCreateParamsToolsItem = RealtimeFunctionTool | MCPTool;

/**
 * How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.

 */
export type RealtimeResponseCreateParamsToolChoice = ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMCP;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeResponseCreateParamsMaxOutputTokens = number | 'inf';

/**
 * Controls which conversation the response is added to. Currently supports
`auto` and `none`, with `auto` as the default value. The `auto` value
means that the contents of the response will be added to the default
conversation. Set this to `none` to create an out-of-band response which
will not add items to default conversation.

 */
export type RealtimeResponseCreateParamsConversation = string | 'auto' | 'none';

/**
 * Create a new Realtime response with these parameters
 */
export interface RealtimeResponseCreateParams {
  /** The set of modalities the model used to respond, currently the only possible values are
`[\"audio\"]`, `[\"text\"]`. Audio output always include a text transcript. Setting the
output to mode `text` will disable audio output from the model.
 */
  output_modalities?: RealtimeResponseCreateParamsOutputModalitiesItem[];
  /** The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
 */
  instructions?: string;
  /** Configuration for audio input and output. */
  audio?: RealtimeResponseCreateParamsAudio;
  /** Tools available to the model. */
  tools?: RealtimeResponseCreateParamsToolsItem[];
  /** How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.
 */
  tool_choice?: RealtimeResponseCreateParamsToolChoice;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_output_tokens?: RealtimeResponseCreateParamsMaxOutputTokens;
  /** Controls which conversation the response is added to. Currently supports
`auto` and `none`, with `auto` as the default value. The `auto` value
means that the contents of the response will be added to the default
conversation. Set this to `none` to create an out-of-band response which
will not add items to default conversation.
 */
  conversation?: RealtimeResponseCreateParamsConversation;
  metadata?: Metadata;
  prompt?: Prompt;
  /** Input items to include in the prompt for the model. Using this field
creates a new context for this Response instead of using the default
conversation. An empty array `[]` will clear the context for this Response.
Note that this can include references to items that previously appeared in the session
using their id.
 */
  input?: RealtimeConversationItem[];
}

/**
 * A realtime server event.

 */
export type RealtimeServerEvent = RealtimeServerEventConversationCreated | RealtimeServerEventConversationItemCreated | RealtimeServerEventConversationItemDeleted | RealtimeServerEventConversationItemInputAudioTranscriptionCompleted | RealtimeServerEventConversationItemInputAudioTranscriptionDelta | RealtimeServerEventConversationItemInputAudioTranscriptionFailed | RealtimeServerEventConversationItemRetrieved | RealtimeServerEventConversationItemTruncated | RealtimeServerEventError | RealtimeServerEventInputAudioBufferCleared | RealtimeServerEventInputAudioBufferCommitted | RealtimeServerEventInputAudioBufferSpeechStarted | RealtimeServerEventInputAudioBufferSpeechStopped | RealtimeServerEventRateLimitsUpdated | RealtimeServerEventResponseAudioDelta | RealtimeServerEventResponseAudioDone | RealtimeServerEventResponseAudioTranscriptDelta | RealtimeServerEventResponseAudioTranscriptDone | RealtimeServerEventResponseContentPartAdded | RealtimeServerEventResponseContentPartDone | RealtimeServerEventResponseCreated | RealtimeServerEventResponseDone | RealtimeServerEventResponseFunctionCallArgumentsDelta | RealtimeServerEventResponseFunctionCallArgumentsDone | RealtimeServerEventResponseOutputItemAdded | RealtimeServerEventResponseOutputItemDone | RealtimeServerEventResponseTextDelta | RealtimeServerEventResponseTextDone | RealtimeServerEventSessionCreated | RealtimeServerEventSessionUpdated | RealtimeServerEventOutputAudioBufferStarted | RealtimeServerEventOutputAudioBufferStopped | RealtimeServerEventOutputAudioBufferCleared | RealtimeServerEventConversationItemAdded | RealtimeServerEventConversationItemDone | RealtimeServerEventInputAudioBufferTimeoutTriggered | RealtimeServerEventConversationItemInputAudioTranscriptionSegment | RealtimeServerEventMCPListToolsInProgress | RealtimeServerEventMCPListToolsCompleted | RealtimeServerEventMCPListToolsFailed | RealtimeServerEventResponseMCPCallArgumentsDelta | RealtimeServerEventResponseMCPCallArgumentsDone | RealtimeServerEventResponseMCPCallInProgress | RealtimeServerEventResponseMCPCallCompleted | RealtimeServerEventResponseMCPCallFailed;

/**
 * The conversation resource.
 */
export type RealtimeServerEventConversationCreatedConversation = {
  /** The unique ID of the conversation. */
  id?: string;
  /** The object type, must be `realtime.conversation`. */
  object?: 'realtime.conversation';
};

/**
 * Returned when a conversation is created. Emitted right after session creation.

 */
export interface RealtimeServerEventConversationCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.created`. */
  type: 'conversation.created';
  /** The conversation resource. */
  conversation: RealtimeServerEventConversationCreatedConversation;
}

export type RealtimeServerEventConversationItemAddedPreviousItemId = string | null;

/**
 * Sent by the server when an Item is added to the default Conversation. This can happen in several cases:
- When the client sends a `conversation.item.create` event.
- When the input audio buffer is committed. In this case the item will be a user message containing the audio from the buffer.
- When the model is generating a Response. In this case the `conversation.item.added` event will be sent when the model starts generating a specific Item, and thus it will not yet have any content (and `status` will be `in_progress`).

The event will include the full content of the Item (except when model is generating a Response) except for audio data, which can be retrieved separately with a `conversation.item.retrieve` event if necessary.

 */
export interface RealtimeServerEventConversationItemAdded {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.added`. */
  type: 'conversation.item.added';
  previous_item_id?: RealtimeServerEventConversationItemAddedPreviousItemId;
  item: RealtimeConversationItem;
}

export type RealtimeServerEventConversationItemCreatedPreviousItemId = string | null;

/**
 * Returned when a conversation item is created. There are several scenarios that produce this event:
  - The server is generating a Response, which if successful will produce
    either one or two Items, which will be of type `message`
    (role `assistant`) or type `function_call`.
  - The input audio buffer has been committed, either by the client or the
    server (in `server_vad` mode). The server will take the content of the
    input audio buffer and add it to a new user message Item.
  - The client has sent a `conversation.item.create` event to add a new Item
    to the Conversation.

 */
export interface RealtimeServerEventConversationItemCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.created`. */
  type: 'conversation.item.created';
  previous_item_id?: RealtimeServerEventConversationItemCreatedPreviousItemId;
  item: RealtimeConversationItem;
}

/**
 * Returned when an item in the conversation is deleted by the client with a
`conversation.item.delete` event. This event is used to synchronize the
server's understanding of the conversation history with the client's view.

 */
export interface RealtimeServerEventConversationItemDeleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.deleted`. */
  type: 'conversation.item.deleted';
  /** The ID of the item that was deleted. */
  item_id: string;
}

export type RealtimeServerEventConversationItemDonePreviousItemId = string | null;

/**
 * Returned when a conversation item is finalized.

The event will include the full content of the Item except for audio data, which can be retrieved separately with a `conversation.item.retrieve` event if needed.

 */
export interface RealtimeServerEventConversationItemDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.done`. */
  type: 'conversation.item.done';
  previous_item_id?: RealtimeServerEventConversationItemDonePreviousItemId;
  item: RealtimeConversationItem;
}

/**
 * The event type, must be
`conversation.item.input_audio_transcription.completed`.

 */
export type RealtimeServerEventConversationItemInputAudioTranscriptionCompletedType = typeof RealtimeServerEventConversationItemInputAudioTranscriptionCompletedType[keyof typeof RealtimeServerEventConversationItemInputAudioTranscriptionCompletedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeServerEventConversationItemInputAudioTranscriptionCompletedType = {
  conversationiteminput_audio_transcriptioncompleted: 'conversation.item.input_audio_transcription.completed',
} as const;

export type RealtimeServerEventConversationItemInputAudioTranscriptionCompletedLogprobs = LogProbProperties[] | null;

/**
 * Usage statistics for the transcription, this is billed according to the ASR model's pricing rather than the realtime model's pricing.
 */
export type RealtimeServerEventConversationItemInputAudioTranscriptionCompletedUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/**
 * This event is the output of audio transcription for user audio written to the
user audio buffer. Transcription begins when the input audio buffer is
committed by the client or server (when VAD is enabled). Transcription runs
asynchronously with Response creation, so this event may come before or after
the Response events.

Realtime API models accept audio natively, and thus input transcription is a
separate process run on a separate ASR (Automatic Speech Recognition) model.
The transcript may diverge somewhat from the model's interpretation, and
should be treated as a rough guide.

 */
export interface RealtimeServerEventConversationItemInputAudioTranscriptionCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be
`conversation.item.input_audio_transcription.completed`.
 */
  type: RealtimeServerEventConversationItemInputAudioTranscriptionCompletedType;
  /** The ID of the item containing the audio that is being transcribed. */
  item_id: string;
  /** The index of the content part containing the audio. */
  content_index: number;
  /** The transcribed text. */
  transcript: string;
  logprobs?: RealtimeServerEventConversationItemInputAudioTranscriptionCompletedLogprobs;
  /** Usage statistics for the transcription, this is billed according to the ASR model's pricing rather than the realtime model's pricing. */
  usage: RealtimeServerEventConversationItemInputAudioTranscriptionCompletedUsage;
}

export type RealtimeServerEventConversationItemInputAudioTranscriptionDeltaLogprobs = LogProbProperties[] | null;

/**
 * Returned when the text value of an input audio transcription content part is updated with incremental transcription results.

 */
export interface RealtimeServerEventConversationItemInputAudioTranscriptionDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.input_audio_transcription.delta`. */
  type: 'conversation.item.input_audio_transcription.delta';
  /** The ID of the item containing the audio that is being transcribed. */
  item_id: string;
  /** The index of the content part in the item's content array. */
  content_index?: number;
  /** The text delta. */
  delta?: string;
  logprobs?: RealtimeServerEventConversationItemInputAudioTranscriptionDeltaLogprobs;
}

/**
 * The event type, must be
`conversation.item.input_audio_transcription.failed`.

 */
export type RealtimeServerEventConversationItemInputAudioTranscriptionFailedType = typeof RealtimeServerEventConversationItemInputAudioTranscriptionFailedType[keyof typeof RealtimeServerEventConversationItemInputAudioTranscriptionFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeServerEventConversationItemInputAudioTranscriptionFailedType = {
  conversationiteminput_audio_transcriptionfailed: 'conversation.item.input_audio_transcription.failed',
} as const;

/**
 * Details of the transcription error.
 */
export type RealtimeServerEventConversationItemInputAudioTranscriptionFailedError = {
  /** The type of error. */
  type?: string;
  /** Error code, if any. */
  code?: string;
  /** A human-readable error message. */
  message?: string;
  /** Parameter related to the error, if any. */
  param?: string;
};

/**
 * Returned when input audio transcription is configured, and a transcription
request for a user message failed. These events are separate from other
`error` events so that the client can identify the related Item.

 */
export interface RealtimeServerEventConversationItemInputAudioTranscriptionFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be
`conversation.item.input_audio_transcription.failed`.
 */
  type: RealtimeServerEventConversationItemInputAudioTranscriptionFailedType;
  /** The ID of the user message item. */
  item_id: string;
  /** The index of the content part containing the audio. */
  content_index: number;
  /** Details of the transcription error. */
  error: RealtimeServerEventConversationItemInputAudioTranscriptionFailedError;
}

/**
 * Returned when an input audio transcription segment is identified for an item.
 */
export interface RealtimeServerEventConversationItemInputAudioTranscriptionSegment {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.input_audio_transcription.segment`. */
  type: 'conversation.item.input_audio_transcription.segment';
  /** The ID of the item containing the input audio content. */
  item_id: string;
  /** The index of the input audio content part within the item. */
  content_index: number;
  /** The text for this segment. */
  text: string;
  /** The segment identifier. */
  id: string;
  /** The detected speaker label for this segment. */
  speaker: string;
  /** Start time of the segment in seconds. */
  start: number;
  /** End time of the segment in seconds. */
  end: number;
}

/**
 * Returned when a conversation item is retrieved with `conversation.item.retrieve`. This is provided as a way to fetch the server's representation of an item, for example to get access to the post-processed audio data after noise cancellation and VAD. It includes the full content of the Item, including audio data.

 */
export interface RealtimeServerEventConversationItemRetrieved {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.retrieved`. */
  type: 'conversation.item.retrieved';
  item: RealtimeConversationItem;
}

/**
 * Returned when an earlier assistant audio message item is truncated by the
client with a `conversation.item.truncate` event. This event is used to
synchronize the server's understanding of the audio with the client's playback.

This action will truncate the audio and remove the server-side text transcript
to ensure there is no text in the context that hasn't been heard by the user.

 */
export interface RealtimeServerEventConversationItemTruncated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `conversation.item.truncated`. */
  type: 'conversation.item.truncated';
  /** The ID of the assistant message item that was truncated. */
  item_id: string;
  /** The index of the content part that was truncated. */
  content_index: number;
  /** The duration up to which the audio was truncated, in milliseconds.
 */
  audio_end_ms: number;
}

export type RealtimeServerEventErrorErrorCode = string | null;

export type RealtimeServerEventErrorErrorParam = string | null;

export type RealtimeServerEventErrorErrorEventId = string | null;

/**
 * Details of the error.
 */
export type RealtimeServerEventErrorError = {
  /** The type of error (e.g., "invalid_request_error", "server_error").
 */
  type: string;
  code?: RealtimeServerEventErrorErrorCode;
  /** A human-readable error message. */
  message: string;
  param?: RealtimeServerEventErrorErrorParam;
  event_id?: RealtimeServerEventErrorErrorEventId;
};

/**
 * Returned when an error occurs, which could be a client problem or a server
problem. Most errors are recoverable and the session will stay open, we
recommend to implementors to monitor and log error messages by default.

 */
export interface RealtimeServerEventError {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `error`. */
  type: 'error';
  /** Details of the error. */
  error: RealtimeServerEventErrorError;
}

/**
 * Returned when the input audio buffer is cleared by the client with a
`input_audio_buffer.clear` event.

 */
export interface RealtimeServerEventInputAudioBufferCleared {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.cleared`. */
  type: 'input_audio_buffer.cleared';
}

export type RealtimeServerEventInputAudioBufferCommittedPreviousItemId = string | null;

/**
 * Returned when an input audio buffer is committed, either by the client or
automatically in server VAD mode. The `item_id` property is the ID of the user
message item that will be created, thus a `conversation.item.created` event
will also be sent to the client.

 */
export interface RealtimeServerEventInputAudioBufferCommitted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.committed`. */
  type: 'input_audio_buffer.committed';
  previous_item_id?: RealtimeServerEventInputAudioBufferCommittedPreviousItemId;
  /** The ID of the user message item that will be created. */
  item_id: string;
}

/**
 * Sent by the server when in `server_vad` mode to indicate that speech has been
detected in the audio buffer. This can happen any time audio is added to the
buffer (unless speech is already detected). The client may want to use this
event to interrupt audio playback or provide visual feedback to the user.

The client should expect to receive a `input_audio_buffer.speech_stopped` event
when speech stops. The `item_id` property is the ID of the user message item
that will be created when speech stops and will also be included in the
`input_audio_buffer.speech_stopped` event (unless the client manually commits
the audio buffer during VAD activation).

 */
export interface RealtimeServerEventInputAudioBufferSpeechStarted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.speech_started`. */
  type: 'input_audio_buffer.speech_started';
  /** Milliseconds from the start of all audio written to the buffer during the
session when speech was first detected. This will correspond to the
beginning of audio sent to the model, and thus includes the
`prefix_padding_ms` configured in the Session.
 */
  audio_start_ms: number;
  /** The ID of the user message item that will be created when speech stops.
 */
  item_id: string;
}

/**
 * Returned in `server_vad` mode when the server detects the end of speech in
the audio buffer. The server will also send an `conversation.item.created`
event with the user message item that is created from the audio buffer.

 */
export interface RealtimeServerEventInputAudioBufferSpeechStopped {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.speech_stopped`. */
  type: 'input_audio_buffer.speech_stopped';
  /** Milliseconds since the session started when speech stopped. This will
correspond to the end of audio sent to the model, and thus includes the
`min_silence_duration_ms` configured in the Session.
 */
  audio_end_ms: number;
  /** The ID of the user message item that will be created. */
  item_id: string;
}

/**
 * Returned when the Server VAD timeout is triggered for the input audio buffer. This is configured
with `idle_timeout_ms` in the `turn_detection` settings of the session, and it indicates that
there hasn't been any speech detected for the configured duration.

The `audio_start_ms` and `audio_end_ms` fields indicate the segment of audio after the last
model response up to the triggering time, as an offset from the beginning of audio written
to the input audio buffer. This means it demarcates the segment of audio that was silent and
the difference between the start and end values will roughly match the configured timeout.

The empty audio will be committed to the conversation as an `input_audio` item (there will be a
`input_audio_buffer.committed` event) and a model response will be generated. There may be speech
that didn't trigger VAD but is still detected by the model, so the model may respond with
something relevant to the conversation or a prompt to continue speaking.

 */
export interface RealtimeServerEventInputAudioBufferTimeoutTriggered {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `input_audio_buffer.timeout_triggered`. */
  type: 'input_audio_buffer.timeout_triggered';
  /** Millisecond offset of audio written to the input audio buffer that was after the playback time of the last model response. */
  audio_start_ms: number;
  /** Millisecond offset of audio written to the input audio buffer at the time the timeout was triggered. */
  audio_end_ms: number;
  /** The ID of the item associated with this segment. */
  item_id: string;
}

/**
 * Returned when listing MCP tools has completed for an item.
 */
export interface RealtimeServerEventMCPListToolsCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.completed`. */
  type: 'mcp_list_tools.completed';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * Returned when listing MCP tools has failed for an item.
 */
export interface RealtimeServerEventMCPListToolsFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.failed`. */
  type: 'mcp_list_tools.failed';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * Returned when listing MCP tools is in progress for an item.
 */
export interface RealtimeServerEventMCPListToolsInProgress {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `mcp_list_tools.in_progress`. */
  type: 'mcp_list_tools.in_progress';
  /** The ID of the MCP list tools item. */
  item_id: string;
}

/**
 * **WebRTC Only:** Emitted when the output audio buffer is cleared. This happens either in VAD
mode when the user has interrupted (`input_audio_buffer.speech_started`),
or when the client has emitted the `output_audio_buffer.clear` event to manually
cut off the current audio response.
[Learn more](https://platform.openai.com/docs/guides/realtime-conversations#client-and-server-events-for-audio-in-webrtc).

 */
export interface RealtimeServerEventOutputAudioBufferCleared {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `output_audio_buffer.cleared`. */
  type: 'output_audio_buffer.cleared';
  /** The unique ID of the response that produced the audio. */
  response_id: string;
}

/**
 * **WebRTC Only:** Emitted when the server begins streaming audio to the client. This event is
emitted after an audio content part has been added (`response.content_part.added`)
to the response.
[Learn more](https://platform.openai.com/docs/guides/realtime-conversations#client-and-server-events-for-audio-in-webrtc).

 */
export interface RealtimeServerEventOutputAudioBufferStarted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `output_audio_buffer.started`. */
  type: 'output_audio_buffer.started';
  /** The unique ID of the response that produced the audio. */
  response_id: string;
}

/**
 * **WebRTC Only:** Emitted when the output audio buffer has been completely drained on the server,
and no more audio is forthcoming. This event is emitted after the full response
data has been sent to the client (`response.done`).
[Learn more](https://platform.openai.com/docs/guides/realtime-conversations#client-and-server-events-for-audio-in-webrtc).

 */
export interface RealtimeServerEventOutputAudioBufferStopped {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `output_audio_buffer.stopped`. */
  type: 'output_audio_buffer.stopped';
  /** The unique ID of the response that produced the audio. */
  response_id: string;
}

/**
 * The name of the rate limit (`requests`, `tokens`).

 */
export type RealtimeServerEventRateLimitsUpdatedRateLimitsItemName = typeof RealtimeServerEventRateLimitsUpdatedRateLimitsItemName[keyof typeof RealtimeServerEventRateLimitsUpdatedRateLimitsItemName];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeServerEventRateLimitsUpdatedRateLimitsItemName = {
  requests: 'requests',
  tokens: 'tokens',
} as const;

export type RealtimeServerEventRateLimitsUpdatedRateLimitsItem = {
  /** The name of the rate limit (`requests`, `tokens`).
 */
  name?: RealtimeServerEventRateLimitsUpdatedRateLimitsItemName;
  /** The maximum allowed value for the rate limit. */
  limit?: number;
  /** The remaining value before the limit is reached. */
  remaining?: number;
  /** Seconds until the rate limit resets. */
  reset_seconds?: number;
};

/**
 * Emitted at the beginning of a Response to indicate the updated rate limits.
When a Response is created some tokens will be "reserved" for the output
tokens, the rate limits shown here reflect that reservation, which is then
adjusted accordingly once the Response is completed.

 */
export interface RealtimeServerEventRateLimitsUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `rate_limits.updated`. */
  type: 'rate_limits.updated';
  /** List of rate limit information. */
  rate_limits: RealtimeServerEventRateLimitsUpdatedRateLimitsItem[];
}

/**
 * Returned when the model-generated audio is updated.
 */
export interface RealtimeServerEventResponseAudioDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio.delta`. */
  type: 'response.output_audio.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** Base64-encoded audio data delta. */
  delta: string;
}

/**
 * Returned when the model-generated audio is done. Also emitted when a Response
is interrupted, incomplete, or cancelled.

 */
export interface RealtimeServerEventResponseAudioDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio.done`. */
  type: 'response.output_audio.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
}

/**
 * Returned when the model-generated transcription of audio output is updated.

 */
export interface RealtimeServerEventResponseAudioTranscriptDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio_transcript.delta`. */
  type: 'response.output_audio_transcript.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The transcript delta. */
  delta: string;
}

/**
 * Returned when the model-generated transcription of audio output is done
streaming. Also emitted when a Response is interrupted, incomplete, or
cancelled.

 */
export interface RealtimeServerEventResponseAudioTranscriptDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_audio_transcript.done`. */
  type: 'response.output_audio_transcript.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The final transcript of the audio. */
  transcript: string;
}

/**
 * The content type ("text", "audio").
 */
export type RealtimeServerEventResponseContentPartAddedPartType = typeof RealtimeServerEventResponseContentPartAddedPartType[keyof typeof RealtimeServerEventResponseContentPartAddedPartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeServerEventResponseContentPartAddedPartType = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The content part that was added.
 */
export type RealtimeServerEventResponseContentPartAddedPart = {
  /** The content type ("text", "audio"). */
  type?: RealtimeServerEventResponseContentPartAddedPartType;
  /** The text content (if type is "text"). */
  text?: string;
  /** Base64-encoded audio data (if type is "audio"). */
  audio?: string;
  /** The transcript of the audio (if type is "audio"). */
  transcript?: string;
};

/**
 * Returned when a new content part is added to an assistant message item during
response generation.

 */
export interface RealtimeServerEventResponseContentPartAdded {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.content_part.added`. */
  type: 'response.content_part.added';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item to which the content part was added. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The content part that was added. */
  part: RealtimeServerEventResponseContentPartAddedPart;
}

/**
 * The content type ("text", "audio").
 */
export type RealtimeServerEventResponseContentPartDonePartType = typeof RealtimeServerEventResponseContentPartDonePartType[keyof typeof RealtimeServerEventResponseContentPartDonePartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeServerEventResponseContentPartDonePartType = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The content part that is done.
 */
export type RealtimeServerEventResponseContentPartDonePart = {
  /** The content type ("text", "audio"). */
  type?: RealtimeServerEventResponseContentPartDonePartType;
  /** The text content (if type is "text"). */
  text?: string;
  /** Base64-encoded audio data (if type is "audio"). */
  audio?: string;
  /** The transcript of the audio (if type is "audio"). */
  transcript?: string;
};

/**
 * Returned when a content part is done streaming in an assistant message item.
Also emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeServerEventResponseContentPartDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.content_part.done`. */
  type: 'response.content_part.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The content part that is done. */
  part: RealtimeServerEventResponseContentPartDonePart;
}

/**
 * Returned when a new Response is created. The first event of response creation,
where the response is in an initial state of `in_progress`.

 */
export interface RealtimeServerEventResponseCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.created`. */
  type: 'response.created';
  response: RealtimeResponse;
}

/**
 * Returned when a Response is done streaming. Always emitted, no matter the
final state. The Response object included in the `response.done` event will
include all output Items in the Response but will omit the raw audio data.

Clients should check the `status` field of the Response to determine if it was successful
(`completed`) or if there was another outcome: `cancelled`, `failed`, or `incomplete`.

A response will contain all output items that were generated during the response, excluding
any audio content.

 */
export interface RealtimeServerEventResponseDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.done`. */
  type: 'response.done';
  response: RealtimeResponse;
}

/**
 * Returned when the model-generated function call arguments are updated.

 */
export interface RealtimeServerEventResponseFunctionCallArgumentsDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.function_call_arguments.delta`.
 */
  type: 'response.function_call_arguments.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the function call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the function call. */
  call_id: string;
  /** The arguments delta as a JSON string. */
  delta: string;
}

/**
 * Returned when the model-generated function call arguments are done streaming.
Also emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeServerEventResponseFunctionCallArgumentsDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.function_call_arguments.done`.
 */
  type: 'response.function_call_arguments.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the function call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the function call. */
  call_id: string;
  /** The final arguments as a JSON string. */
  arguments: string;
}

export type RealtimeServerEventResponseMCPCallArgumentsDeltaObfuscation = string | null;

/**
 * Returned when MCP tool call arguments are updated during response generation.
 */
export interface RealtimeServerEventResponseMCPCallArgumentsDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call_arguments.delta`. */
  type: 'response.mcp_call_arguments.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the MCP tool call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The JSON-encoded arguments delta. */
  delta: string;
  obfuscation?: RealtimeServerEventResponseMCPCallArgumentsDeltaObfuscation;
}

/**
 * Returned when MCP tool call arguments are finalized during response generation.
 */
export interface RealtimeServerEventResponseMCPCallArgumentsDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call_arguments.done`. */
  type: 'response.mcp_call_arguments.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the MCP tool call item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The final JSON-encoded arguments string. */
  arguments: string;
}

/**
 * Returned when an MCP tool call has completed successfully.
 */
export interface RealtimeServerEventResponseMCPCallCompleted {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.completed`. */
  type: 'response.mcp_call.completed';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when an MCP tool call has failed.
 */
export interface RealtimeServerEventResponseMCPCallFailed {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.failed`. */
  type: 'response.mcp_call.failed';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when an MCP tool call has started and is in progress.
 */
export interface RealtimeServerEventResponseMCPCallInProgress {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.mcp_call.in_progress`. */
  type: 'response.mcp_call.in_progress';
  /** The index of the output item in the response. */
  output_index: number;
  /** The ID of the MCP tool call item. */
  item_id: string;
}

/**
 * Returned when a new Item is created during Response generation.
 */
export interface RealtimeServerEventResponseOutputItemAdded {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_item.added`. */
  type: 'response.output_item.added';
  /** The ID of the Response to which the item belongs. */
  response_id: string;
  /** The index of the output item in the Response. */
  output_index: number;
  item: RealtimeConversationItem;
}

/**
 * Returned when an Item is done streaming. Also emitted when a Response is
interrupted, incomplete, or cancelled.

 */
export interface RealtimeServerEventResponseOutputItemDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_item.done`. */
  type: 'response.output_item.done';
  /** The ID of the Response to which the item belongs. */
  response_id: string;
  /** The index of the output item in the Response. */
  output_index: number;
  item: RealtimeConversationItem;
}

/**
 * Returned when the text value of an "output_text" content part is updated.
 */
export interface RealtimeServerEventResponseTextDelta {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_text.delta`. */
  type: 'response.output_text.delta';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The text delta. */
  delta: string;
}

/**
 * Returned when the text value of an "output_text" content part is done streaming. Also
emitted when a Response is interrupted, incomplete, or cancelled.

 */
export interface RealtimeServerEventResponseTextDone {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `response.output_text.done`. */
  type: 'response.output_text.done';
  /** The ID of the response. */
  response_id: string;
  /** The ID of the item. */
  item_id: string;
  /** The index of the output item in the response. */
  output_index: number;
  /** The index of the content part in the item's content array. */
  content_index: number;
  /** The final text content. */
  text: string;
}

/**
 * The session configuration.
 */
export type RealtimeServerEventSessionCreatedSession = RealtimeSessionCreateRequestGA | RealtimeTranscriptionSessionCreateRequestGA;

/**
 * Returned when a Session is created. Emitted automatically when a new
connection is established as the first server event. This event will contain
the default Session configuration.

 */
export interface RealtimeServerEventSessionCreated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `session.created`. */
  type: 'session.created';
  /** The session configuration. */
  session: RealtimeServerEventSessionCreatedSession;
}

/**
 * The session configuration.
 */
export type RealtimeServerEventSessionUpdatedSession = RealtimeSessionCreateRequestGA | RealtimeTranscriptionSessionCreateRequestGA;

/**
 * Returned when a session is updated with a `session.update` event, unless
there is an error.

 */
export interface RealtimeServerEventSessionUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `session.updated`. */
  type: 'session.updated';
  /** The session configuration. */
  session: RealtimeServerEventSessionUpdatedSession;
}

/**
 * Returned when a transcription session is updated with a `transcription_session.update` event, unless
there is an error.

 */
export interface RealtimeServerEventTranscriptionSessionUpdated {
  /** The unique ID of the server event. */
  event_id: string;
  /** The event type, must be `transcription_session.updated`. */
  type: 'transcription_session.updated';
  session: RealtimeTranscriptionSessionCreateResponse;
}

/**
 * The object type. Always `realtime.session`.
 */
export type RealtimeSessionObject = typeof RealtimeSessionObject[keyof typeof RealtimeSessionObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionObject = {
  realtimesession: 'realtime.session',
} as const;

export type RealtimeSessionModalitiesItem = typeof RealtimeSessionModalitiesItem[keyof typeof RealtimeSessionModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The Realtime model used for this session.

 */
export type RealtimeSessionModel = typeof RealtimeSessionModel[keyof typeof RealtimeSessionModel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionModel = {
  'gpt-realtime': 'gpt-realtime',
  'gpt-realtime-2025-08-28': 'gpt-realtime-2025-08-28',
  'gpt-4o-realtime-preview': 'gpt-4o-realtime-preview',
  'gpt-4o-realtime-preview-2024-10-01': 'gpt-4o-realtime-preview-2024-10-01',
  'gpt-4o-realtime-preview-2024-12-17': 'gpt-4o-realtime-preview-2024-12-17',
  'gpt-4o-realtime-preview-2025-06-03': 'gpt-4o-realtime-preview-2025-06-03',
  'gpt-4o-mini-realtime-preview': 'gpt-4o-mini-realtime-preview',
  'gpt-4o-mini-realtime-preview-2024-12-17': 'gpt-4o-mini-realtime-preview-2024-12-17',
  'gpt-realtime-mini': 'gpt-realtime-mini',
  'gpt-realtime-mini-2025-10-06': 'gpt-realtime-mini-2025-10-06',
  'gpt-audio-mini': 'gpt-audio-mini',
  'gpt-audio-mini-2025-10-06': 'gpt-audio-mini-2025-10-06',
} as const;

/**
 * The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
single channel (mono), and little-endian byte order.

 */
export type RealtimeSessionInputAudioFormat = typeof RealtimeSessionInputAudioFormat[keyof typeof RealtimeSessionInputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionInputAudioFormat = {
  pcm16: 'pcm16',
  g711_ulaw: 'g711_ulaw',
  g711_alaw: 'g711_alaw',
} as const;

/**
 * The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, output audio is sampled at a rate of 24kHz.

 */
export type RealtimeSessionOutputAudioFormat = typeof RealtimeSessionOutputAudioFormat[keyof typeof RealtimeSessionOutputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionOutputAudioFormat = {
  pcm16: 'pcm16',
  g711_ulaw: 'g711_ulaw',
  g711_alaw: 'g711_alaw',
} as const;

export type RealtimeSessionInputAudioTranscription = AudioTranscription | null;

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

 */
export type RealtimeSessionInputAudioNoiseReduction = {
  type?: NoiseReductionType;
};

/**
 * The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.

 */
export type RealtimeSessionTracingAnyOfAnyOfMetadata = { [key: string]: unknown };

/**
 * Granular configuration for tracing.

 */
export type RealtimeSessionTracingAnyOfAnyOf = {
  /** The name of the workflow to attach to this trace. This is used to
name the trace in the traces dashboard.
 */
  workflow_name?: string;
  /** The group id to attach to this trace to enable filtering and
grouping in the traces dashboard.
 */
  group_id?: string;
  /** The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.
 */
  metadata?: RealtimeSessionTracingAnyOfAnyOfMetadata;
};

/**
 * Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

 */
export type RealtimeSessionTracingAnyOf = 'auto' | RealtimeSessionTracingAnyOfAnyOf;

export type RealtimeSessionTracing = RealtimeSessionTracingAnyOf | null;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeSessionMaxResponseOutputTokens = number | 'inf';

export type RealtimeSessionPrompt = Prompt | null;

export type RealtimeSessionIncludeAnyOfItem = typeof RealtimeSessionIncludeAnyOfItem[keyof typeof RealtimeSessionIncludeAnyOfItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionIncludeAnyOfItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

export type RealtimeSessionInclude = RealtimeSessionIncludeAnyOfItem[] | null;

/**
 * Realtime session object for the beta interface.
 */
export interface RealtimeSession {
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`.
 */
  id?: string;
  /** The object type. Always `realtime.session`. */
  object?: RealtimeSessionObject;
  /** The set of modalities the model can respond with. To disable audio,
set this to ["text"].
 */
  modalities?: RealtimeSessionModalitiesItem[];
  /** The Realtime model used for this session.
 */
  model?: RealtimeSessionModel;
  /** The default system instructions (i.e. system message) prepended to model
calls. This field allows the client to guide the model on desired
responses. The model can be instructed on response content and format,
(e.g. "be extremely succinct", "act friendly", "here are examples of good
responses") and on audio behavior (e.g. "talk quickly", "inject emotion
into your voice", "laugh frequently"). The instructions are not
guaranteed to be followed by the model, but they provide guidance to the
model on the desired behavior.


Note that the server sets default instructions which will be used if this
field is not set and are visible in the `session.created` event at the
start of the session.
 */
  instructions?: string;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, and `verse`.
 */
  voice?: VoiceIdsShared;
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
single channel (mono), and little-endian byte order.
 */
  input_audio_format?: RealtimeSessionInputAudioFormat;
  /** The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, output audio is sampled at a rate of 24kHz.
 */
  output_audio_format?: RealtimeSessionOutputAudioFormat;
  input_audio_transcription?: RealtimeSessionInputAudioTranscription;
  turn_detection?: RealtimeTurnDetection;
  /** Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
  input_audio_noise_reduction?: RealtimeSessionInputAudioNoiseReduction;
  /**
   * The speed of the model's spoken response. 1.0 is the default speed. 0.25 is
the minimum speed. 1.5 is the maximum speed. This value can only be changed
in between model turns, not while a response is in progress.

   * @minimum 0.25
   * @maximum 1.5
   */
  speed?: number;
  tracing?: RealtimeSessionTracing;
  /** Tools (functions) available to the model. */
  tools?: RealtimeFunctionTool[];
  /** How the model chooses tools. Options are `auto`, `none`, `required`, or
specify a function.
 */
  tool_choice?: string;
  /** Sampling temperature for the model, limited to [0.6, 1.2]. For audio models a temperature of 0.8 is highly recommended for best performance.
 */
  temperature?: number;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_response_output_tokens?: RealtimeSessionMaxResponseOutputTokens;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  prompt?: RealtimeSessionPrompt;
  include?: RealtimeSessionInclude;
}

/**
 * Ephemeral key returned by the API.
 */
export type RealtimeSessionCreateRequestClientSecret = {
  /** Ephemeral key usable in client environments to authenticate connections
to the Realtime API. Use this in client-side environments rather than
a standard API token, which should only be used server-side.
 */
  value: string;
  /** Timestamp for when the token expires. Currently, all tokens expire
after one minute.
 */
  expires_at: number;
};

export type RealtimeSessionCreateRequestModalitiesItem = typeof RealtimeSessionCreateRequestModalitiesItem[keyof typeof RealtimeSessionCreateRequestModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateRequestModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * Configuration for input audio transcription, defaults to off and can be
set to `null` to turn off once on. Input audio transcription is not native
to the model, since the model consumes audio directly. Transcription runs
asynchronously and should be treated as rough guidance
rather than the representation understood by the model.

 */
export type RealtimeSessionCreateRequestInputAudioTranscription = {
  /** The model to use for transcription.
 */
  model?: string;
};

/**
 * The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.

 */
export type RealtimeSessionCreateRequestTracingAnyOfMetadata = { [key: string]: unknown };

/**
 * Granular configuration for tracing.

 */
export type RealtimeSessionCreateRequestTracingAnyOf = {
  /** The name of the workflow to attach to this trace. This is used to
name the trace in the traces dashboard.
 */
  workflow_name?: string;
  /** The group id to attach to this trace to enable filtering and
grouping in the traces dashboard.
 */
  group_id?: string;
  /** The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.
 */
  metadata?: RealtimeSessionCreateRequestTracingAnyOfMetadata;
};

/**
 * Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

 */
export type RealtimeSessionCreateRequestTracing = 'auto' | RealtimeSessionCreateRequestTracingAnyOf;

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

 */
export type RealtimeSessionCreateRequestTurnDetection = {
  /** Type of turn detection, only `server_vad` is currently supported.
 */
  type?: string;
  /** Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
};

/**
 * The type of the tool, i.e. `function`.
 */
export type RealtimeSessionCreateRequestToolsItemType = typeof RealtimeSessionCreateRequestToolsItemType[keyof typeof RealtimeSessionCreateRequestToolsItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateRequestToolsItemType = {
  function: 'function',
} as const;

/**
 * Parameters of the function in JSON Schema.
 */
export type RealtimeSessionCreateRequestToolsItemParameters = { [key: string]: unknown };

export type RealtimeSessionCreateRequestToolsItem = {
  /** The type of the tool, i.e. `function`. */
  type?: RealtimeSessionCreateRequestToolsItemType;
  /** The name of the function. */
  name?: string;
  /** The description of the function, including guidance on when and how
to call it, and guidance about what to tell the user when calling
(if anything).
 */
  description?: string;
  /** Parameters of the function in JSON Schema. */
  parameters?: RealtimeSessionCreateRequestToolsItemParameters;
};

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeSessionCreateRequestMaxResponseOutputTokens = number | 'inf';

/**
 * A new Realtime session configuration, with an ephemeral key. Default TTL
for keys is one minute.

 */
export interface RealtimeSessionCreateRequest {
  /** Ephemeral key returned by the API. */
  client_secret: RealtimeSessionCreateRequestClientSecret;
  /** The set of modalities the model can respond with. To disable audio,
set this to ["text"].
 */
  modalities?: RealtimeSessionCreateRequestModalitiesItem[];
  /** The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
 */
  instructions?: string;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, and `verse`.
 */
  voice?: VoiceIdsShared;
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 */
  input_audio_format?: string;
  /** The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 */
  output_audio_format?: string;
  /** Configuration for input audio transcription, defaults to off and can be
set to `null` to turn off once on. Input audio transcription is not native
to the model, since the model consumes audio directly. Transcription runs
asynchronously and should be treated as rough guidance
rather than the representation understood by the model.
 */
  input_audio_transcription?: RealtimeSessionCreateRequestInputAudioTranscription;
  /**
   * The speed of the model's spoken response. 1.0 is the default speed. 0.25 is
the minimum speed. 1.5 is the maximum speed. This value can only be changed
in between model turns, not while a response is in progress.

   * @minimum 0.25
   * @maximum 1.5
   */
  speed?: number;
  /** Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.
 */
  tracing?: RealtimeSessionCreateRequestTracing;
  /** Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.
 */
  turn_detection?: RealtimeSessionCreateRequestTurnDetection;
  /** Tools (functions) available to the model. */
  tools?: RealtimeSessionCreateRequestToolsItem[];
  /** How the model chooses tools. Options are `auto`, `none`, `required`, or
specify a function.
 */
  tool_choice?: string;
  /** Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.
 */
  temperature?: number;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_response_output_tokens?: RealtimeSessionCreateRequestMaxResponseOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

/**
 * The type of session to create. Always `realtime` for the Realtime API.

 */
export type RealtimeSessionCreateRequestGAType = typeof RealtimeSessionCreateRequestGAType[keyof typeof RealtimeSessionCreateRequestGAType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateRequestGAType = {
  realtime: 'realtime',
} as const;

export type RealtimeSessionCreateRequestGAOutputModalitiesItem = typeof RealtimeSessionCreateRequestGAOutputModalitiesItem[keyof typeof RealtimeSessionCreateRequestGAOutputModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateRequestGAOutputModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The Realtime model used for this session.

 */
export type RealtimeSessionCreateRequestGAModel = string | 'gpt-realtime' | 'gpt-realtime-2025-08-28' | 'gpt-4o-realtime-preview' | 'gpt-4o-realtime-preview-2024-10-01' | 'gpt-4o-realtime-preview-2024-12-17' | 'gpt-4o-realtime-preview-2025-06-03' | 'gpt-4o-mini-realtime-preview' | 'gpt-4o-mini-realtime-preview-2024-12-17' | 'gpt-realtime-mini' | 'gpt-realtime-mini-2025-10-06' | 'gpt-audio-mini' | 'gpt-audio-mini-2025-10-06';

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

 */
export type RealtimeSessionCreateRequestGAAudioInputNoiseReduction = {
  type?: NoiseReductionType;
};

export type RealtimeSessionCreateRequestGAAudioInput = {
  /** The format of the input audio. */
  format?: RealtimeAudioFormats;
  /** Configuration for input audio transcription, defaults to off and can be set to `null` to turn off once on. Input audio transcription is not native to the model, since the model consumes audio directly. Transcription runs asynchronously through [the /audio/transcriptions endpoint](https://platform.openai.com/docs/api-reference/audio/createTranscription) and should be treated as guidance of input audio content rather than precisely what the model heard. The client can optionally set the language and prompt for transcription, these offer additional guidance to the transcription service.
 */
  transcription?: AudioTranscription;
  /** Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
  noise_reduction?: RealtimeSessionCreateRequestGAAudioInputNoiseReduction;
  turn_detection?: RealtimeTurnDetection;
};

export type RealtimeSessionCreateRequestGAAudioOutput = {
  /** The format of the output audio. */
  format?: RealtimeAudioFormats;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, `verse`, `marin`, and `cedar`. We recommend `marin` and `cedar` for
best quality.
 */
  voice?: VoiceIdsShared;
  /**
   * The speed of the model's spoken response as a multiple of the original speed.
1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum speed. This value can only be changed in between model turns, not while a response is in progress.

This parameter is a post-processing adjustment to the audio after it is generated, it's
also possible to prompt the model to speak faster or slower.

   * @minimum 0.25
   * @maximum 1.5
   */
  speed?: number;
};

/**
 * Configuration for input and output audio.

 */
export type RealtimeSessionCreateRequestGAAudio = {
  input?: RealtimeSessionCreateRequestGAAudioInput;
  output?: RealtimeSessionCreateRequestGAAudioOutput;
};

export type RealtimeSessionCreateRequestGAIncludeItem = typeof RealtimeSessionCreateRequestGAIncludeItem[keyof typeof RealtimeSessionCreateRequestGAIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateRequestGAIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

/**
 * The arbitrary metadata to attach to this trace to enable
filtering in the Traces Dashboard.

 */
export type RealtimeSessionCreateRequestGATracingAnyOfMetadata = { [key: string]: unknown };

/**
 * Granular configuration for tracing.

 */
export type RealtimeSessionCreateRequestGATracingAnyOf = {
  /** The name of the workflow to attach to this trace. This is used to
name the trace in the Traces Dashboard.
 */
  workflow_name?: string;
  /** The group id to attach to this trace to enable filtering and
grouping in the Traces Dashboard.
 */
  group_id?: string;
  /** The arbitrary metadata to attach to this trace to enable
filtering in the Traces Dashboard.
 */
  metadata?: RealtimeSessionCreateRequestGATracingAnyOfMetadata;
};

/**
 * Realtime API can write session traces to the [Traces Dashboard](/logs?api=traces). Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

 * @nullable
 */
export type RealtimeSessionCreateRequestGATracing = 'auto' | RealtimeSessionCreateRequestGATracingAnyOf | null;

export type RealtimeSessionCreateRequestGAToolsItem = RealtimeFunctionTool | MCPTool;

/**
 * How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.

 */
export type RealtimeSessionCreateRequestGAToolChoice = ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMCP;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeSessionCreateRequestGAMaxOutputTokens = number | 'inf';

/**
 * Realtime session object configuration.
 */
export interface RealtimeSessionCreateRequestGA {
  /** The type of session to create. Always `realtime` for the Realtime API.
 */
  type: RealtimeSessionCreateRequestGAType;
  /** The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
that the model will respond with audio plus a transcript. `["text"]` can be used to make
the model respond with text only. It is not possible to request both `text` and `audio` at the same time.
 */
  output_modalities?: RealtimeSessionCreateRequestGAOutputModalitiesItem[];
  /** The Realtime model used for this session.
 */
  model?: RealtimeSessionCreateRequestGAModel;
  /** The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.

Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
 */
  instructions?: string;
  /** Configuration for input and output audio.
 */
  audio?: RealtimeSessionCreateRequestGAAudio;
  /** Additional fields to include in server outputs.

`item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
 */
  include?: RealtimeSessionCreateRequestGAIncludeItem[];
  /**
   * Realtime API can write session traces to the [Traces Dashboard](/logs?api=traces). Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

   * @nullable
   */
  tracing?: RealtimeSessionCreateRequestGATracing;
  /** Tools available to the model. */
  tools?: RealtimeSessionCreateRequestGAToolsItem[];
  /** How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.
 */
  tool_choice?: RealtimeSessionCreateRequestGAToolChoice;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_output_tokens?: RealtimeSessionCreateRequestGAMaxOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

export type RealtimeSessionCreateResponseIncludeItem = typeof RealtimeSessionCreateResponseIncludeItem[keyof typeof RealtimeSessionCreateResponseIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateResponseIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

export type RealtimeSessionCreateResponseOutputModalitiesItem = typeof RealtimeSessionCreateResponseOutputModalitiesItem[keyof typeof RealtimeSessionCreateResponseOutputModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateResponseOutputModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * Configuration for input audio noise reduction.

 */
export type RealtimeSessionCreateResponseAudioInputNoiseReduction = {
  type?: NoiseReductionType;
};

/**
 * Configuration for turn detection.

 */
export type RealtimeSessionCreateResponseAudioInputTurnDetection = {
  /** Type of turn detection, only `server_vad` is currently supported.
 */
  type?: string;
  threshold?: number;
  prefix_padding_ms?: number;
  silence_duration_ms?: number;
};

export type RealtimeSessionCreateResponseAudioInput = {
  format?: RealtimeAudioFormats;
  /** Configuration for input audio transcription.
 */
  transcription?: AudioTranscription;
  /** Configuration for input audio noise reduction.
 */
  noise_reduction?: RealtimeSessionCreateResponseAudioInputNoiseReduction;
  /** Configuration for turn detection.
 */
  turn_detection?: RealtimeSessionCreateResponseAudioInputTurnDetection;
};

export type RealtimeSessionCreateResponseAudioOutput = {
  format?: RealtimeAudioFormats;
  voice?: VoiceIdsShared;
  speed?: number;
};

/**
 * Configuration for input and output audio for the session.

 */
export type RealtimeSessionCreateResponseAudio = {
  input?: RealtimeSessionCreateResponseAudioInput;
  output?: RealtimeSessionCreateResponseAudioOutput;
};

/**
 * The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.

 */
export type RealtimeSessionCreateResponseTracingAnyOfMetadata = { [key: string]: unknown };

/**
 * Granular configuration for tracing.

 */
export type RealtimeSessionCreateResponseTracingAnyOf = {
  /** The name of the workflow to attach to this trace. This is used to
name the trace in the traces dashboard.
 */
  workflow_name?: string;
  /** The group id to attach to this trace to enable filtering and
grouping in the traces dashboard.
 */
  group_id?: string;
  /** The arbitrary metadata to attach to this trace to enable
filtering in the traces dashboard.
 */
  metadata?: RealtimeSessionCreateResponseTracingAnyOfMetadata;
};

/**
 * Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

 */
export type RealtimeSessionCreateResponseTracing = 'auto' | RealtimeSessionCreateResponseTracingAnyOf;

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

 */
export type RealtimeSessionCreateResponseTurnDetection = {
  /** Type of turn detection, only `server_vad` is currently supported.
 */
  type?: string;
  /** Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
};

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeSessionCreateResponseMaxOutputTokens = number | 'inf';

/**
 * A Realtime session configuration object.

 */
export interface RealtimeSessionCreateResponse {
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`.
 */
  id?: string;
  /** The object type. Always `realtime.session`. */
  object?: string;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /** Additional fields to include in server outputs.
- `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
 */
  include?: RealtimeSessionCreateResponseIncludeItem[];
  /** The Realtime model used for this session. */
  model?: string;
  /** The set of modalities the model can respond with. To disable audio,
set this to ["text"].
 */
  output_modalities?: RealtimeSessionCreateResponseOutputModalitiesItem[];
  /** The default system instructions (i.e. system message) prepended to model
calls. This field allows the client to guide the model on desired
responses. The model can be instructed on response content and format,
(e.g. "be extremely succinct", "act friendly", "here are examples of good
responses") and on audio behavior (e.g. "talk quickly", "inject emotion
into your voice", "laugh frequently"). The instructions are not guaranteed
to be followed by the model, but they provide guidance to the model on the
desired behavior.

Note that the server sets default instructions which will be used if this
field is not set and are visible in the `session.created` event at the
start of the session.
 */
  instructions?: string;
  /** Configuration for input and output audio for the session.
 */
  audio?: RealtimeSessionCreateResponseAudio;
  /** Configuration options for tracing. Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.
 */
  tracing?: RealtimeSessionCreateResponseTracing;
  /** Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.
 */
  turn_detection?: RealtimeSessionCreateResponseTurnDetection;
  /** Tools (functions) available to the model. */
  tools?: RealtimeFunctionTool[];
  /** How the model chooses tools. Options are `auto`, `none`, `required`, or
specify a function.
 */
  tool_choice?: string;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_output_tokens?: RealtimeSessionCreateResponseMaxOutputTokens;
}

/**
 * Ephemeral key returned by the API.
 */
export type RealtimeSessionCreateResponseGAClientSecret = {
  /** Ephemeral key usable in client environments to authenticate connections to the Realtime API. Use this in client-side environments rather than a standard API token, which should only be used server-side.
 */
  value: string;
  /** Timestamp for when the token expires. Currently, all tokens expire
after one minute.
 */
  expires_at: number;
};

/**
 * The type of session to create. Always `realtime` for the Realtime API.

 */
export type RealtimeSessionCreateResponseGAType = typeof RealtimeSessionCreateResponseGAType[keyof typeof RealtimeSessionCreateResponseGAType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateResponseGAType = {
  realtime: 'realtime',
} as const;

export type RealtimeSessionCreateResponseGAOutputModalitiesItem = typeof RealtimeSessionCreateResponseGAOutputModalitiesItem[keyof typeof RealtimeSessionCreateResponseGAOutputModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateResponseGAOutputModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * The Realtime model used for this session.

 */
export type RealtimeSessionCreateResponseGAModel = string | 'gpt-realtime' | 'gpt-realtime-2025-08-28' | 'gpt-4o-realtime-preview' | 'gpt-4o-realtime-preview-2024-10-01' | 'gpt-4o-realtime-preview-2024-12-17' | 'gpt-4o-realtime-preview-2025-06-03' | 'gpt-4o-mini-realtime-preview' | 'gpt-4o-mini-realtime-preview-2024-12-17' | 'gpt-realtime-mini' | 'gpt-realtime-mini-2025-10-06' | 'gpt-audio-mini' | 'gpt-audio-mini-2025-10-06';

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

 */
export type RealtimeSessionCreateResponseGAAudioInputNoiseReduction = {
  type?: NoiseReductionType;
};

export type RealtimeSessionCreateResponseGAAudioInput = {
  /** The format of the input audio. */
  format?: RealtimeAudioFormats;
  /** Configuration for input audio transcription, defaults to off and can be set to `null` to turn off once on. Input audio transcription is not native to the model, since the model consumes audio directly. Transcription runs asynchronously through [the /audio/transcriptions endpoint](https://platform.openai.com/docs/api-reference/audio/createTranscription) and should be treated as guidance of input audio content rather than precisely what the model heard. The client can optionally set the language and prompt for transcription, these offer additional guidance to the transcription service.
 */
  transcription?: AudioTranscription;
  /** Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
  noise_reduction?: RealtimeSessionCreateResponseGAAudioInputNoiseReduction;
  turn_detection?: RealtimeTurnDetection;
};

export type RealtimeSessionCreateResponseGAAudioOutput = {
  /** The format of the output audio. */
  format?: RealtimeAudioFormats;
  /** The voice the model uses to respond. Voice cannot be changed during the
session once the model has responded with audio at least once. Current
voice options are `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`,
`shimmer`, `verse`, `marin`, and `cedar`. We recommend `marin` and `cedar` for
best quality.
 */
  voice?: VoiceIdsShared;
  /**
   * The speed of the model's spoken response as a multiple of the original speed.
1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum speed. This value can only be changed in between model turns, not while a response is in progress.

This parameter is a post-processing adjustment to the audio after it is generated, it's
also possible to prompt the model to speak faster or slower.

   * @minimum 0.25
   * @maximum 1.5
   */
  speed?: number;
};

/**
 * Configuration for input and output audio.

 */
export type RealtimeSessionCreateResponseGAAudio = {
  input?: RealtimeSessionCreateResponseGAAudioInput;
  output?: RealtimeSessionCreateResponseGAAudioOutput;
};

export type RealtimeSessionCreateResponseGAIncludeItem = typeof RealtimeSessionCreateResponseGAIncludeItem[keyof typeof RealtimeSessionCreateResponseGAIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeSessionCreateResponseGAIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

/**
 * The arbitrary metadata to attach to this trace to enable
filtering in the Traces Dashboard.

 */
export type RealtimeSessionCreateResponseGATracingAnyOfAnyOfMetadata = { [key: string]: unknown };

/**
 * Granular configuration for tracing.

 */
export type RealtimeSessionCreateResponseGATracingAnyOfAnyOf = {
  /** The name of the workflow to attach to this trace. This is used to
name the trace in the Traces Dashboard.
 */
  workflow_name?: string;
  /** The group id to attach to this trace to enable filtering and
grouping in the Traces Dashboard.
 */
  group_id?: string;
  /** The arbitrary metadata to attach to this trace to enable
filtering in the Traces Dashboard.
 */
  metadata?: RealtimeSessionCreateResponseGATracingAnyOfAnyOfMetadata;
};

/**
 * Realtime API can write session traces to the [Traces Dashboard](/logs?api=traces). Set to null to disable tracing. Once
tracing is enabled for a session, the configuration cannot be modified.

`auto` will create a trace for the session with default values for the
workflow name, group id, and metadata.

 */
export type RealtimeSessionCreateResponseGATracingAnyOf = 'auto' | RealtimeSessionCreateResponseGATracingAnyOfAnyOf;

export type RealtimeSessionCreateResponseGATracing = RealtimeSessionCreateResponseGATracingAnyOf | null;

export type RealtimeSessionCreateResponseGAToolsItem = RealtimeFunctionTool | MCPTool;

/**
 * How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.

 */
export type RealtimeSessionCreateResponseGAToolChoice = ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMCP;

/**
 * Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.

 */
export type RealtimeSessionCreateResponseGAMaxOutputTokens = number | 'inf';

/**
 * A new Realtime session configuration, with an ephemeral key. Default TTL
for keys is one minute.

 */
export interface RealtimeSessionCreateResponseGA {
  /** Ephemeral key returned by the API. */
  client_secret: RealtimeSessionCreateResponseGAClientSecret;
  /** The type of session to create. Always `realtime` for the Realtime API.
 */
  type: RealtimeSessionCreateResponseGAType;
  /** The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
that the model will respond with audio plus a transcript. `["text"]` can be used to make
the model respond with text only. It is not possible to request both `text` and `audio` at the same time.
 */
  output_modalities?: RealtimeSessionCreateResponseGAOutputModalitiesItem[];
  /** The Realtime model used for this session.
 */
  model?: RealtimeSessionCreateResponseGAModel;
  /** The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.

Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
 */
  instructions?: string;
  /** Configuration for input and output audio.
 */
  audio?: RealtimeSessionCreateResponseGAAudio;
  /** Additional fields to include in server outputs.

`item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
 */
  include?: RealtimeSessionCreateResponseGAIncludeItem[];
  tracing?: RealtimeSessionCreateResponseGATracing;
  /** Tools available to the model. */
  tools?: RealtimeSessionCreateResponseGAToolsItem[];
  /** How the model chooses tools. Provide one of the string modes or force a specific
function/MCP tool.
 */
  tool_choice?: RealtimeSessionCreateResponseGAToolChoice;
  /** Maximum number of output tokens for a single assistant response,
inclusive of tool calls. Provide an integer between 1 and 4096 to
limit output tokens, or `inf` for the maximum available tokens for a
given model. Defaults to `inf`.
 */
  max_output_tokens?: RealtimeSessionCreateResponseGAMaxOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

/**
 * Type of turn detection. Only `server_vad` is currently supported for transcription sessions.

 */
export type RealtimeTranscriptionSessionCreateRequestTurnDetectionType = typeof RealtimeTranscriptionSessionCreateRequestTurnDetectionType[keyof typeof RealtimeTranscriptionSessionCreateRequestTurnDetectionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateRequestTurnDetectionType = {
  server_vad: 'server_vad',
} as const;

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.

 */
export type RealtimeTranscriptionSessionCreateRequestTurnDetection = {
  /** Type of turn detection. Only `server_vad` is currently supported for transcription sessions.
 */
  type?: RealtimeTranscriptionSessionCreateRequestTurnDetectionType;
  /** Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
};

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

 */
export type RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction = {
  type?: NoiseReductionType;
};

/**
 * The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
single channel (mono), and little-endian byte order.

 */
export type RealtimeTranscriptionSessionCreateRequestInputAudioFormat = typeof RealtimeTranscriptionSessionCreateRequestInputAudioFormat[keyof typeof RealtimeTranscriptionSessionCreateRequestInputAudioFormat];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateRequestInputAudioFormat = {
  pcm16: 'pcm16',
  g711_ulaw: 'g711_ulaw',
  g711_alaw: 'g711_alaw',
} as const;

export type RealtimeTranscriptionSessionCreateRequestIncludeItem = typeof RealtimeTranscriptionSessionCreateRequestIncludeItem[keyof typeof RealtimeTranscriptionSessionCreateRequestIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateRequestIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

/**
 * Realtime transcription session object configuration.
 */
export interface RealtimeTranscriptionSessionCreateRequest {
  /** Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.
 */
  turn_detection?: RealtimeTranscriptionSessionCreateRequestTurnDetection;
  /** Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
  input_audio_noise_reduction?: RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction;
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
single channel (mono), and little-endian byte order.
 */
  input_audio_format?: RealtimeTranscriptionSessionCreateRequestInputAudioFormat;
  /** Configuration for input audio transcription. The client can optionally set the language and prompt for transcription, these offer additional guidance to the transcription service.
 */
  input_audio_transcription?: AudioTranscription;
  /** The set of items to include in the transcription. Current available items are:
`item.input_audio_transcription.logprobs`
 */
  include?: RealtimeTranscriptionSessionCreateRequestIncludeItem[];
}

/**
 * The type of session to create. Always `transcription` for transcription sessions.

 */
export type RealtimeTranscriptionSessionCreateRequestGAType = typeof RealtimeTranscriptionSessionCreateRequestGAType[keyof typeof RealtimeTranscriptionSessionCreateRequestGAType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateRequestGAType = {
  transcription: 'transcription',
} as const;

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.

 */
export type RealtimeTranscriptionSessionCreateRequestGAAudioInputNoiseReduction = {
  type?: NoiseReductionType;
};

export type RealtimeTranscriptionSessionCreateRequestGAAudioInput = {
  format?: RealtimeAudioFormats;
  /** Configuration for input audio transcription, defaults to off and can be set to `null` to turn off once on. Input audio transcription is not native to the model, since the model consumes audio directly. Transcription runs asynchronously through [the /audio/transcriptions endpoint](https://platform.openai.com/docs/api-reference/audio/createTranscription) and should be treated as guidance of input audio content rather than precisely what the model heard. The client can optionally set the language and prompt for transcription, these offer additional guidance to the transcription service.
 */
  transcription?: AudioTranscription;
  /** Configuration for input audio noise reduction. This can be set to `null` to turn off.
Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
  noise_reduction?: RealtimeTranscriptionSessionCreateRequestGAAudioInputNoiseReduction;
  turn_detection?: RealtimeTurnDetection;
};

/**
 * Configuration for input and output audio.

 */
export type RealtimeTranscriptionSessionCreateRequestGAAudio = {
  input?: RealtimeTranscriptionSessionCreateRequestGAAudioInput;
};

export type RealtimeTranscriptionSessionCreateRequestGAIncludeItem = typeof RealtimeTranscriptionSessionCreateRequestGAIncludeItem[keyof typeof RealtimeTranscriptionSessionCreateRequestGAIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateRequestGAIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

/**
 * Realtime transcription session object configuration.
 */
export interface RealtimeTranscriptionSessionCreateRequestGA {
  /** The type of session to create. Always `transcription` for transcription sessions.
 */
  type: RealtimeTranscriptionSessionCreateRequestGAType;
  /** Configuration for input and output audio.
 */
  audio?: RealtimeTranscriptionSessionCreateRequestGAAudio;
  /** Additional fields to include in server outputs.

`item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
 */
  include?: RealtimeTranscriptionSessionCreateRequestGAIncludeItem[];
}

/**
 * Ephemeral key returned by the API. Only present when the session is
created on the server via REST API.

 */
export type RealtimeTranscriptionSessionCreateResponseClientSecret = {
  /** Ephemeral key usable in client environments to authenticate connections
to the Realtime API. Use this in client-side environments rather than
a standard API token, which should only be used server-side.
 */
  value: string;
  /** Timestamp for when the token expires. Currently, all tokens expire
after one minute.
 */
  expires_at: number;
};

export type RealtimeTranscriptionSessionCreateResponseModalitiesItem = typeof RealtimeTranscriptionSessionCreateResponseModalitiesItem[keyof typeof RealtimeTranscriptionSessionCreateResponseModalitiesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateResponseModalitiesItem = {
  text: 'text',
  audio: 'audio',
} as const;

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

 */
export type RealtimeTranscriptionSessionCreateResponseTurnDetection = {
  /** Type of turn detection, only `server_vad` is currently supported.
 */
  type?: string;
  /** Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
};

/**
 * A new Realtime transcription session configuration.

When a session is created on the server via REST API, the session object
also contains an ephemeral key. Default TTL for keys is 10 minutes. This
property is not present when a session is updated via the WebSocket API.

 */
export interface RealtimeTranscriptionSessionCreateResponse {
  /** Ephemeral key returned by the API. Only present when the session is
created on the server via REST API.
 */
  client_secret: RealtimeTranscriptionSessionCreateResponseClientSecret;
  /** The set of modalities the model can respond with. To disable audio,
set this to ["text"].
 */
  modalities?: RealtimeTranscriptionSessionCreateResponseModalitiesItem[];
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 */
  input_audio_format?: string;
  /** Configuration of the transcription model.
 */
  input_audio_transcription?: AudioTranscription;
  /** Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.
 */
  turn_detection?: RealtimeTranscriptionSessionCreateResponseTurnDetection;
}

/**
 * The type of session. Always `transcription` for transcription sessions.

 */
export type RealtimeTranscriptionSessionCreateResponseGAType = typeof RealtimeTranscriptionSessionCreateResponseGAType[keyof typeof RealtimeTranscriptionSessionCreateResponseGAType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateResponseGAType = {
  transcription: 'transcription',
} as const;

export type RealtimeTranscriptionSessionCreateResponseGAIncludeItem = typeof RealtimeTranscriptionSessionCreateResponseGAIncludeItem[keyof typeof RealtimeTranscriptionSessionCreateResponseGAIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTranscriptionSessionCreateResponseGAIncludeItem = {
  iteminput_audio_transcriptionlogprobs: 'item.input_audio_transcription.logprobs',
} as const;

/**
 * Configuration for input audio noise reduction.

 */
export type RealtimeTranscriptionSessionCreateResponseGAAudioInputNoiseReduction = {
  type?: NoiseReductionType;
};

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.

 */
export type RealtimeTranscriptionSessionCreateResponseGAAudioInputTurnDetection = {
  /** Type of turn detection, only `server_vad` is currently supported.
 */
  type?: string;
  /** Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
};

export type RealtimeTranscriptionSessionCreateResponseGAAudioInput = {
  format?: RealtimeAudioFormats;
  /** Configuration of the transcription model.
 */
  transcription?: AudioTranscription;
  /** Configuration for input audio noise reduction.
 */
  noise_reduction?: RealtimeTranscriptionSessionCreateResponseGAAudioInputNoiseReduction;
  /** Configuration for turn detection. Can be set to `null` to turn off. Server
VAD means that the model will detect the start and end of speech based on
audio volume and respond at the end of user speech.
 */
  turn_detection?: RealtimeTranscriptionSessionCreateResponseGAAudioInputTurnDetection;
};

/**
 * Configuration for input audio for the session.

 */
export type RealtimeTranscriptionSessionCreateResponseGAAudio = {
  input?: RealtimeTranscriptionSessionCreateResponseGAAudioInput;
};

/**
 * A Realtime transcription session configuration object.

 */
export interface RealtimeTranscriptionSessionCreateResponseGA {
  /** The type of session. Always `transcription` for transcription sessions.
 */
  type: RealtimeTranscriptionSessionCreateResponseGAType;
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`.
 */
  id: string;
  /** The object type. Always `realtime.transcription_session`. */
  object: string;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /** Additional fields to include in server outputs.
- `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
 */
  include?: RealtimeTranscriptionSessionCreateResponseGAIncludeItem[];
  /** Configuration for input audio for the session.
 */
  audio?: RealtimeTranscriptionSessionCreateResponseGAAudio;
}

/**
 * Use retention ratio truncation.
 */
export type RealtimeTruncationAnyOfType = typeof RealtimeTruncationAnyOfType[keyof typeof RealtimeTruncationAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTruncationAnyOfType = {
  retention_ratio: 'retention_ratio',
} as const;

/**
 * Retain a fraction of the conversation tokens when the conversation exceeds the input token limit. This allows you to amortize truncations across multiple turns, which can help improve cached token usage.
 */
export type RealtimeTruncationAnyOf = {
  /** Use retention ratio truncation. */
  type: RealtimeTruncationAnyOfType;
  /**
   * Fraction of post-instruction conversation tokens to retain (0.0 - 1.0) when the conversation exceeds the input token limit.

   * @minimum 0
   * @maximum 1
   */
  retention_ratio: number;
};

/**
 * Controls how the realtime conversation is truncated prior to model inference.
The default is `auto`.

 */
export type RealtimeTruncation = 'auto' | 'disabled' | RealtimeTruncationAnyOf;

export type RealtimeTurnDetectionAnyOfAnyOfIdleTimeoutMs = number | null;

/**
 * Server-side voice activity detection (VAD) which flips on when user speech is detected and off after a period of silence.
 */
export type RealtimeTurnDetectionAnyOfAnyOf = {
  /** Type of turn detection, `server_vad` to turn on simple Server VAD.
 */
  type: 'server_vad';
  /** Used only for `server_vad` mode. Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
  /** Used only for `server_vad` mode. Amount of audio to include before the VAD detected speech (in
milliseconds). Defaults to 300ms.
 */
  prefix_padding_ms?: number;
  /** Used only for `server_vad` mode. Duration of silence to detect speech stop (in milliseconds). Defaults
to 500ms. With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
  /** Whether or not to automatically generate a response when a VAD stop event occurs.
 */
  create_response?: boolean;
  /** Whether or not to automatically interrupt any ongoing response with output to the default
conversation (i.e. `conversation` of `auto`) when a VAD start event occurs.
 */
  interrupt_response?: boolean;
  idle_timeout_ms?: RealtimeTurnDetectionAnyOfAnyOfIdleTimeoutMs;
};

/**
 * Used only for `semantic_vad` mode. The eagerness of the model to respond. `low` will wait longer for the user to continue speaking, `high` will respond more quickly. `auto` is the default and is equivalent to `medium`. `low`, `medium`, and `high` have max timeouts of 8s, 4s, and 2s respectively.

 */
export type RealtimeTurnDetectionAnyOfAnyOfThreeEagerness = typeof RealtimeTurnDetectionAnyOfAnyOfThreeEagerness[keyof typeof RealtimeTurnDetectionAnyOfAnyOfThreeEagerness];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RealtimeTurnDetectionAnyOfAnyOfThreeEagerness = {
  low: 'low',
  medium: 'medium',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * Server-side semantic turn detection which uses a model to determine when the user has finished speaking.
 */
export type RealtimeTurnDetectionAnyOfAnyOfThree = {
  /** Type of turn detection, `semantic_vad` to turn on Semantic VAD.
 */
  type: 'semantic_vad';
  /** Used only for `semantic_vad` mode. The eagerness of the model to respond. `low` will wait longer for the user to continue speaking, `high` will respond more quickly. `auto` is the default and is equivalent to `medium`. `low`, `medium`, and `high` have max timeouts of 8s, 4s, and 2s respectively.
 */
  eagerness?: RealtimeTurnDetectionAnyOfAnyOfThreeEagerness;
  /** Whether or not to automatically generate a response when a VAD stop event occurs.
 */
  create_response?: boolean;
  /** Whether or not to automatically interrupt any ongoing response with output to the default
conversation (i.e. `conversation` of `auto`) when a VAD start event occurs.
 */
  interrupt_response?: boolean;
};

/**
 * Configuration for turn detection, ether Server VAD or Semantic VAD. This can be set to `null` to turn off, in which case the client must manually trigger model response.

Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.

Semantic VAD is more advanced and uses a turn detection model (in conjunction with VAD) to semantically estimate whether the user has finished speaking, then dynamically sets a timeout based on this probability. For example, if user audio trails off with "uhhm", the model will score a low probability of turn end and wait longer for the user to continue speaking. This can be useful for more natural conversations, but may have a higher latency.

 */
export type RealtimeTurnDetectionAnyOf = RealtimeTurnDetectionAnyOfAnyOf | RealtimeTurnDetectionAnyOfAnyOfThree;

export type RealtimeTurnDetection = RealtimeTurnDetectionAnyOf | null;

export type ReasoningSummary = 'auto' | 'concise' | 'detailed' | null;

export type ReasoningGenerateSummary = 'auto' | 'concise' | 'detailed' | null;

/**
 * **gpt-5 and o-series models only**

Configuration options for
[reasoning models](https://platform.openai.com/docs/guides/reasoning).

 */
export interface Reasoning {
  effort?: ReasoningEffort;
  summary?: ReasoningSummary;
  generate_summary?: ReasoningGenerateSummary;
}

export type ReasoningEffort = 'minimal' | 'low' | 'medium' | 'high' | null;

/**
 * The type of the object. Always `reasoning`.

 */
export type ReasoningItemType = typeof ReasoningItemType[keyof typeof ReasoningItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReasoningItemType = {
  reasoning: 'reasoning',
} as const;

export type ReasoningItemEncryptedContent = string | null;

/**
 * The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.

 */
export type ReasoningItemStatus = typeof ReasoningItemStatus[keyof typeof ReasoningItemStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReasoningItemStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

/**
 * A description of the chain of thought used by a reasoning model while generating
a response. Be sure to include these items in your `input` to the Responses API
for subsequent turns of a conversation if you are manually
[managing context](https://platform.openai.com/docs/guides/conversation-state).

 */
export interface ReasoningItem {
  /** The type of the object. Always `reasoning`.
 */
  type: ReasoningItemType;
  /** The unique identifier of the reasoning content.
 */
  id: string;
  encrypted_content?: ReasoningItemEncryptedContent;
  /** Reasoning summary content.
 */
  summary: Summary[];
  /** Reasoning text content.
 */
  content?: ReasoningTextContent[];
  /** The status of the item. One of `in_progress`, `completed`, or
`incomplete`. Populated when items are returned via API.
 */
  status?: ReasoningItemStatus;
}

/**
 * The object type of this resource - always set to `response`.

 */
export type ResponseAllOfObject = typeof ResponseAllOfObject[keyof typeof ResponseAllOfObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAllOfObject = {
  response: 'response',
} as const;

/**
 * The status of the response generation. One of `completed`, `failed`,
`in_progress`, `cancelled`, `queued`, or `incomplete`.

 */
export type ResponseAllOfStatus = typeof ResponseAllOfStatus[keyof typeof ResponseAllOfStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAllOfStatus = {
  completed: 'completed',
  failed: 'failed',
  in_progress: 'in_progress',
  cancelled: 'cancelled',
  queued: 'queued',
  incomplete: 'incomplete',
} as const;

/**
 * The reason why the response is incomplete.
 */
export type ResponseAllOfIncompleteDetailsAnyOfReason = typeof ResponseAllOfIncompleteDetailsAnyOfReason[keyof typeof ResponseAllOfIncompleteDetailsAnyOfReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAllOfIncompleteDetailsAnyOfReason = {
  max_output_tokens: 'max_output_tokens',
  content_filter: 'content_filter',
} as const;

/**
 * Details about why the response is incomplete.

 */
export type ResponseAllOfIncompleteDetailsAnyOf = {
  /** The reason why the response is incomplete. */
  reason?: ResponseAllOfIncompleteDetailsAnyOfReason;
};

export type ResponseAllOfIncompleteDetails = ResponseAllOfIncompleteDetailsAnyOf | null;

/**
 * A system (or developer) message inserted into the model's context.

When using along with `previous_response_id`, the instructions from a previous
response will not be carried over to the next response. This makes it simple
to swap out system (or developer) messages in new responses.

 */
export type ResponseAllOfInstructionsAnyOf = string | InputItem[];

export type ResponseAllOfInstructions = ResponseAllOfInstructionsAnyOf | null;

export type ResponseAllOfOutputText = string | null;

export type ResponseAllOfConversation = Conversation2 | null;

export type ResponseAllOf = {
  /** Unique identifier for this Response.
 */
  id: string;
  /** The object type of this resource - always set to `response`.
 */
  object: ResponseAllOfObject;
  /** The status of the response generation. One of `completed`, `failed`,
`in_progress`, `cancelled`, `queued`, or `incomplete`.
 */
  status?: ResponseAllOfStatus;
  /** Unix timestamp (in seconds) of when this Response was created.
 */
  created_at: number;
  error: ResponseError;
  incomplete_details: ResponseAllOfIncompleteDetails;
  /** An array of content items generated by the model.

- The length and order of items in the `output` array is dependent
  on the model's response.
- Rather than accessing the first item in the `output` array and
  assuming it's an `assistant` message with the content generated by
  the model, you might consider using the `output_text` property where
  supported in SDKs.
 */
  output: OutputItem[];
  instructions: ResponseAllOfInstructions;
  output_text?: ResponseAllOfOutputText;
  usage?: ResponseUsage;
  /** Whether to allow the model to run tool calls in parallel.
 */
  parallel_tool_calls: boolean;
  conversation?: ResponseAllOfConversation;
};

export type Response = ModelResponseProperties & ResponseProperties & ResponseAllOf & Required<Pick<ModelResponseProperties & ResponseProperties & ResponseAllOf, 'model' | 'tools' | 'metadata' | 'tool_choice' | 'temperature' | 'top_p'>>;

/**
 * The type of the event. Always `response.audio.delta`.

 */
export type ResponseAudioDeltaEventType = typeof ResponseAudioDeltaEventType[keyof typeof ResponseAudioDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAudioDeltaEventType = {
  responseaudiodelta: 'response.audio.delta',
} as const;

/**
 * Emitted when there is a partial audio response.
 */
export interface ResponseAudioDeltaEvent {
  /** The type of the event. Always `response.audio.delta`.
 */
  type: ResponseAudioDeltaEventType;
  /** A sequence number for this chunk of the stream response.
 */
  sequence_number: number;
  /** A chunk of Base64 encoded response audio bytes.
 */
  delta: string;
}

/**
 * The type of the event. Always `response.audio.done`.

 */
export type ResponseAudioDoneEventType = typeof ResponseAudioDoneEventType[keyof typeof ResponseAudioDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAudioDoneEventType = {
  responseaudiodone: 'response.audio.done',
} as const;

/**
 * Emitted when the audio response is complete.
 */
export interface ResponseAudioDoneEvent {
  /** The type of the event. Always `response.audio.done`.
 */
  type: ResponseAudioDoneEventType;
  /** The sequence number of the delta.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.audio.transcript.delta`.

 */
export type ResponseAudioTranscriptDeltaEventType = typeof ResponseAudioTranscriptDeltaEventType[keyof typeof ResponseAudioTranscriptDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAudioTranscriptDeltaEventType = {
  responseaudiotranscriptdelta: 'response.audio.transcript.delta',
} as const;

/**
 * Emitted when there is a partial transcript of audio.
 */
export interface ResponseAudioTranscriptDeltaEvent {
  /** The type of the event. Always `response.audio.transcript.delta`.
 */
  type: ResponseAudioTranscriptDeltaEventType;
  /** The partial transcript of the audio response.
 */
  delta: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.audio.transcript.done`.

 */
export type ResponseAudioTranscriptDoneEventType = typeof ResponseAudioTranscriptDoneEventType[keyof typeof ResponseAudioTranscriptDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseAudioTranscriptDoneEventType = {
  responseaudiotranscriptdone: 'response.audio.transcript.done',
} as const;

/**
 * Emitted when the full audio transcript is completed.
 */
export interface ResponseAudioTranscriptDoneEvent {
  /** The type of the event. Always `response.audio.transcript.done`.
 */
  type: ResponseAudioTranscriptDoneEventType;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.code_interpreter_call_code.delta`.
 */
export type ResponseCodeInterpreterCallCodeDeltaEventType = typeof ResponseCodeInterpreterCallCodeDeltaEventType[keyof typeof ResponseCodeInterpreterCallCodeDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCodeInterpreterCallCodeDeltaEventType = {
  responsecode_interpreter_call_codedelta: 'response.code_interpreter_call_code.delta',
} as const;

/**
 * Emitted when a partial code snippet is streamed by the code interpreter.
 */
export interface ResponseCodeInterpreterCallCodeDeltaEvent {
  /** The type of the event. Always `response.code_interpreter_call_code.delta`. */
  type: ResponseCodeInterpreterCallCodeDeltaEventType;
  /** The index of the output item in the response for which the code is being streamed. */
  output_index: number;
  /** The unique identifier of the code interpreter tool call item. */
  item_id: string;
  /** The partial code snippet being streamed by the code interpreter. */
  delta: string;
  /** The sequence number of this event, used to order streaming events. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.code_interpreter_call_code.done`.
 */
export type ResponseCodeInterpreterCallCodeDoneEventType = typeof ResponseCodeInterpreterCallCodeDoneEventType[keyof typeof ResponseCodeInterpreterCallCodeDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCodeInterpreterCallCodeDoneEventType = {
  responsecode_interpreter_call_codedone: 'response.code_interpreter_call_code.done',
} as const;

/**
 * Emitted when the code snippet is finalized by the code interpreter.
 */
export interface ResponseCodeInterpreterCallCodeDoneEvent {
  /** The type of the event. Always `response.code_interpreter_call_code.done`. */
  type: ResponseCodeInterpreterCallCodeDoneEventType;
  /** The index of the output item in the response for which the code is finalized. */
  output_index: number;
  /** The unique identifier of the code interpreter tool call item. */
  item_id: string;
  /** The final code snippet output by the code interpreter. */
  code: string;
  /** The sequence number of this event, used to order streaming events. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.code_interpreter_call.completed`.
 */
export type ResponseCodeInterpreterCallCompletedEventType = typeof ResponseCodeInterpreterCallCompletedEventType[keyof typeof ResponseCodeInterpreterCallCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCodeInterpreterCallCompletedEventType = {
  responsecode_interpreter_callcompleted: 'response.code_interpreter_call.completed',
} as const;

/**
 * Emitted when the code interpreter call is completed.
 */
export interface ResponseCodeInterpreterCallCompletedEvent {
  /** The type of the event. Always `response.code_interpreter_call.completed`. */
  type: ResponseCodeInterpreterCallCompletedEventType;
  /** The index of the output item in the response for which the code interpreter call is completed. */
  output_index: number;
  /** The unique identifier of the code interpreter tool call item. */
  item_id: string;
  /** The sequence number of this event, used to order streaming events. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.code_interpreter_call.in_progress`.
 */
export type ResponseCodeInterpreterCallInProgressEventType = typeof ResponseCodeInterpreterCallInProgressEventType[keyof typeof ResponseCodeInterpreterCallInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCodeInterpreterCallInProgressEventType = {
  responsecode_interpreter_callin_progress: 'response.code_interpreter_call.in_progress',
} as const;

/**
 * Emitted when a code interpreter call is in progress.
 */
export interface ResponseCodeInterpreterCallInProgressEvent {
  /** The type of the event. Always `response.code_interpreter_call.in_progress`. */
  type: ResponseCodeInterpreterCallInProgressEventType;
  /** The index of the output item in the response for which the code interpreter call is in progress. */
  output_index: number;
  /** The unique identifier of the code interpreter tool call item. */
  item_id: string;
  /** The sequence number of this event, used to order streaming events. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.code_interpreter_call.interpreting`.
 */
export type ResponseCodeInterpreterCallInterpretingEventType = typeof ResponseCodeInterpreterCallInterpretingEventType[keyof typeof ResponseCodeInterpreterCallInterpretingEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCodeInterpreterCallInterpretingEventType = {
  responsecode_interpreter_callinterpreting: 'response.code_interpreter_call.interpreting',
} as const;

/**
 * Emitted when the code interpreter is actively interpreting the code snippet.
 */
export interface ResponseCodeInterpreterCallInterpretingEvent {
  /** The type of the event. Always `response.code_interpreter_call.interpreting`. */
  type: ResponseCodeInterpreterCallInterpretingEventType;
  /** The index of the output item in the response for which the code interpreter is interpreting code. */
  output_index: number;
  /** The unique identifier of the code interpreter tool call item. */
  item_id: string;
  /** The sequence number of this event, used to order streaming events. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.completed`.

 */
export type ResponseCompletedEventType = typeof ResponseCompletedEventType[keyof typeof ResponseCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCompletedEventType = {
  responsecompleted: 'response.completed',
} as const;

/**
 * Emitted when the model response is complete.
 */
export interface ResponseCompletedEvent {
  /** The type of the event. Always `response.completed`.
 */
  type: ResponseCompletedEventType;
  /** Properties of the completed response.
 */
  response: Response;
  /** The sequence number for this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.content_part.added`.

 */
export type ResponseContentPartAddedEventType = typeof ResponseContentPartAddedEventType[keyof typeof ResponseContentPartAddedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseContentPartAddedEventType = {
  responsecontent_partadded: 'response.content_part.added',
} as const;

/**
 * Emitted when a new content part is added.
 */
export interface ResponseContentPartAddedEvent {
  /** The type of the event. Always `response.content_part.added`.
 */
  type: ResponseContentPartAddedEventType;
  /** The ID of the output item that the content part was added to.
 */
  item_id: string;
  /** The index of the output item that the content part was added to.
 */
  output_index: number;
  /** The index of the content part that was added.
 */
  content_index: number;
  /** The content part that was added.
 */
  part: OutputContent;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.content_part.done`.

 */
export type ResponseContentPartDoneEventType = typeof ResponseContentPartDoneEventType[keyof typeof ResponseContentPartDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseContentPartDoneEventType = {
  responsecontent_partdone: 'response.content_part.done',
} as const;

/**
 * Emitted when a content part is done.
 */
export interface ResponseContentPartDoneEvent {
  /** The type of the event. Always `response.content_part.done`.
 */
  type: ResponseContentPartDoneEventType;
  /** The ID of the output item that the content part was added to.
 */
  item_id: string;
  /** The index of the output item that the content part was added to.
 */
  output_index: number;
  /** The index of the content part that is done.
 */
  content_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The content part that is done.
 */
  part: OutputContent;
}

/**
 * The type of the event. Always `response.created`.

 */
export type ResponseCreatedEventType = typeof ResponseCreatedEventType[keyof typeof ResponseCreatedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCreatedEventType = {
  responsecreated: 'response.created',
} as const;

/**
 * An event that is emitted when a response is created.

 */
export interface ResponseCreatedEvent {
  /** The type of the event. Always `response.created`.
 */
  type: ResponseCreatedEventType;
  /** The response that was created.
 */
  response: Response;
  /** The sequence number for this event. */
  sequence_number: number;
}

/**
 * The event type identifier.
 */
export type ResponseCustomToolCallInputDeltaEventType = typeof ResponseCustomToolCallInputDeltaEventType[keyof typeof ResponseCustomToolCallInputDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCustomToolCallInputDeltaEventType = {
  responsecustom_tool_call_inputdelta: 'response.custom_tool_call_input.delta',
} as const;

/**
 * Event representing a delta (partial update) to the input of a custom tool call.

 */
export interface ResponseCustomToolCallInputDeltaEvent {
  /** The event type identifier. */
  type: ResponseCustomToolCallInputDeltaEventType;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The index of the output this delta applies to. */
  output_index: number;
  /** Unique identifier for the API item associated with this event. */
  item_id: string;
  /** The incremental input data (delta) for the custom tool call. */
  delta: string;
}

/**
 * The event type identifier.
 */
export type ResponseCustomToolCallInputDoneEventType = typeof ResponseCustomToolCallInputDoneEventType[keyof typeof ResponseCustomToolCallInputDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseCustomToolCallInputDoneEventType = {
  responsecustom_tool_call_inputdone: 'response.custom_tool_call_input.done',
} as const;

/**
 * Event indicating that input for a custom tool call is complete.

 */
export interface ResponseCustomToolCallInputDoneEvent {
  /** The event type identifier. */
  type: ResponseCustomToolCallInputDoneEventType;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The index of the output this event applies to. */
  output_index: number;
  /** Unique identifier for the API item associated with this event. */
  item_id: string;
  /** The complete input data for the custom tool call. */
  input: string;
}

/**
 * An error object returned when the model fails to generate a Response.

 */
export type ResponseErrorAnyOf = {
  code: ResponseErrorCode;
  /** A human-readable description of the error.
 */
  message: string;
};

export type ResponseError = ResponseErrorAnyOf | null;

/**
 * The error code for the response.

 */
export type ResponseErrorCode = typeof ResponseErrorCode[keyof typeof ResponseErrorCode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseErrorCode = {
  server_error: 'server_error',
  rate_limit_exceeded: 'rate_limit_exceeded',
  invalid_prompt: 'invalid_prompt',
  vector_store_timeout: 'vector_store_timeout',
  invalid_image: 'invalid_image',
  invalid_image_format: 'invalid_image_format',
  invalid_base64_image: 'invalid_base64_image',
  invalid_image_url: 'invalid_image_url',
  image_too_large: 'image_too_large',
  image_too_small: 'image_too_small',
  image_parse_error: 'image_parse_error',
  image_content_policy_violation: 'image_content_policy_violation',
  invalid_image_mode: 'invalid_image_mode',
  image_file_too_large: 'image_file_too_large',
  unsupported_image_media_type: 'unsupported_image_media_type',
  empty_image_file: 'empty_image_file',
  failed_to_download_image: 'failed_to_download_image',
  image_file_not_found: 'image_file_not_found',
} as const;

/**
 * The type of the event. Always `error`.

 */
export type ResponseErrorEventType = typeof ResponseErrorEventType[keyof typeof ResponseErrorEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseErrorEventType = {
  error: 'error',
} as const;

export type ResponseErrorEventCode = string | null;

export type ResponseErrorEventParam = string | null;

/**
 * Emitted when an error occurs.
 */
export interface ResponseErrorEvent {
  /** The type of the event. Always `error`.
 */
  type: ResponseErrorEventType;
  code: ResponseErrorEventCode;
  /** The error message.
 */
  message: string;
  param: ResponseErrorEventParam;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.failed`.

 */
export type ResponseFailedEventType = typeof ResponseFailedEventType[keyof typeof ResponseFailedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFailedEventType = {
  responsefailed: 'response.failed',
} as const;

/**
 * An event that is emitted when a response fails.

 */
export interface ResponseFailedEvent {
  /** The type of the event. Always `response.failed`.
 */
  type: ResponseFailedEventType;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The response that failed.
 */
  response: Response;
}

/**
 * The type of the event. Always `response.file_search_call.completed`.

 */
export type ResponseFileSearchCallCompletedEventType = typeof ResponseFileSearchCallCompletedEventType[keyof typeof ResponseFileSearchCallCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFileSearchCallCompletedEventType = {
  responsefile_search_callcompleted: 'response.file_search_call.completed',
} as const;

/**
 * Emitted when a file search call is completed (results found).
 */
export interface ResponseFileSearchCallCompletedEvent {
  /** The type of the event. Always `response.file_search_call.completed`.
 */
  type: ResponseFileSearchCallCompletedEventType;
  /** The index of the output item that the file search call is initiated.
 */
  output_index: number;
  /** The ID of the output item that the file search call is initiated.
 */
  item_id: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.file_search_call.in_progress`.

 */
export type ResponseFileSearchCallInProgressEventType = typeof ResponseFileSearchCallInProgressEventType[keyof typeof ResponseFileSearchCallInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFileSearchCallInProgressEventType = {
  responsefile_search_callin_progress: 'response.file_search_call.in_progress',
} as const;

/**
 * Emitted when a file search call is initiated.
 */
export interface ResponseFileSearchCallInProgressEvent {
  /** The type of the event. Always `response.file_search_call.in_progress`.
 */
  type: ResponseFileSearchCallInProgressEventType;
  /** The index of the output item that the file search call is initiated.
 */
  output_index: number;
  /** The ID of the output item that the file search call is initiated.
 */
  item_id: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.file_search_call.searching`.

 */
export type ResponseFileSearchCallSearchingEventType = typeof ResponseFileSearchCallSearchingEventType[keyof typeof ResponseFileSearchCallSearchingEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFileSearchCallSearchingEventType = {
  responsefile_search_callsearching: 'response.file_search_call.searching',
} as const;

/**
 * Emitted when a file search is currently searching.
 */
export interface ResponseFileSearchCallSearchingEvent {
  /** The type of the event. Always `response.file_search_call.searching`.
 */
  type: ResponseFileSearchCallSearchingEventType;
  /** The index of the output item that the file search call is searching.
 */
  output_index: number;
  /** The ID of the output item that the file search call is initiated.
 */
  item_id: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of response format being defined. Always `json_object`.
 */
export type ResponseFormatJsonObjectType = typeof ResponseFormatJsonObjectType[keyof typeof ResponseFormatJsonObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFormatJsonObjectType = {
  json_object: 'json_object',
} as const;

/**
 * JSON object response format. An older method of generating JSON responses.
Using `json_schema` is recommended for models that support it. Note that the
model will not generate JSON without a system or user message instructing it
to do so.

 */
export interface ResponseFormatJsonObject {
  /** The type of response format being defined. Always `json_object`. */
  type: ResponseFormatJsonObjectType;
}

/**
 * The type of response format being defined. Always `json_schema`.
 */
export type ResponseFormatJsonSchemaType = typeof ResponseFormatJsonSchemaType[keyof typeof ResponseFormatJsonSchemaType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFormatJsonSchemaType = {
  json_schema: 'json_schema',
} as const;

export type ResponseFormatJsonSchemaJsonSchemaStrict = boolean | null;

/**
 * Structured Outputs configuration options, including a JSON Schema.

 */
export type ResponseFormatJsonSchemaJsonSchema = {
  /** A description of what the response format is for, used by the model to
determine how to respond in the format.
 */
  description?: string;
  /** The name of the response format. Must be a-z, A-Z, 0-9, or contain
underscores and dashes, with a maximum length of 64.
 */
  name: string;
  schema?: ResponseFormatJsonSchemaSchema;
  strict?: ResponseFormatJsonSchemaJsonSchemaStrict;
};

/**
 * JSON Schema response format. Used to generate structured JSON responses.
Learn more about [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs).

 */
export interface ResponseFormatJsonSchema {
  /** The type of response format being defined. Always `json_schema`. */
  type: ResponseFormatJsonSchemaType;
  /** Structured Outputs configuration options, including a JSON Schema.
 */
  json_schema: ResponseFormatJsonSchemaJsonSchema;
}

/**
 * The schema for the response format, described as a JSON Schema object.
Learn how to build JSON schemas [here](https://json-schema.org/).

 */
export interface ResponseFormatJsonSchemaSchema { [key: string]: unknown }

/**
 * The type of response format being defined. Always `text`.
 */
export type ResponseFormatTextType = typeof ResponseFormatTextType[keyof typeof ResponseFormatTextType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFormatTextType = {
  text: 'text',
} as const;

/**
 * Default response format. Used to generate text responses.

 */
export interface ResponseFormatText {
  /** The type of response format being defined. Always `text`. */
  type: ResponseFormatTextType;
}

/**
 * The type of response format being defined. Always `grammar`.
 */
export type ResponseFormatTextGrammarType = typeof ResponseFormatTextGrammarType[keyof typeof ResponseFormatTextGrammarType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFormatTextGrammarType = {
  grammar: 'grammar',
} as const;

/**
 * A custom grammar for the model to follow when generating text.
Learn more in the [custom grammars guide](https://platform.openai.com/docs/guides/custom-grammars).

 */
export interface ResponseFormatTextGrammar {
  /** The type of response format being defined. Always `grammar`. */
  type: ResponseFormatTextGrammarType;
  /** The custom grammar for the model to follow. */
  grammar: string;
}

/**
 * The type of response format being defined. Always `python`.
 */
export type ResponseFormatTextPythonType = typeof ResponseFormatTextPythonType[keyof typeof ResponseFormatTextPythonType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFormatTextPythonType = {
  python: 'python',
} as const;

/**
 * Configure the model to generate valid Python code. See the
[custom grammars guide](https://platform.openai.com/docs/guides/custom-grammars) for more details.

 */
export interface ResponseFormatTextPython {
  /** The type of response format being defined. Always `python`. */
  type: ResponseFormatTextPythonType;
}

/**
 * The type of the event. Always `response.function_call_arguments.delta`.

 */
export type ResponseFunctionCallArgumentsDeltaEventType = typeof ResponseFunctionCallArgumentsDeltaEventType[keyof typeof ResponseFunctionCallArgumentsDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFunctionCallArgumentsDeltaEventType = {
  responsefunction_call_argumentsdelta: 'response.function_call_arguments.delta',
} as const;

/**
 * Emitted when there is a partial function-call arguments delta.
 */
export interface ResponseFunctionCallArgumentsDeltaEvent {
  /** The type of the event. Always `response.function_call_arguments.delta`.
 */
  type: ResponseFunctionCallArgumentsDeltaEventType;
  /** The ID of the output item that the function-call arguments delta is added to.
 */
  item_id: string;
  /** The index of the output item that the function-call arguments delta is added to.
 */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The function-call arguments delta that is added.
 */
  delta: string;
}

export type ResponseFunctionCallArgumentsDoneEventType = typeof ResponseFunctionCallArgumentsDoneEventType[keyof typeof ResponseFunctionCallArgumentsDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseFunctionCallArgumentsDoneEventType = {
  responsefunction_call_argumentsdone: 'response.function_call_arguments.done',
} as const;

/**
 * Emitted when function-call arguments are finalized.
 */
export interface ResponseFunctionCallArgumentsDoneEvent {
  type: ResponseFunctionCallArgumentsDoneEventType;
  /** The ID of the item. */
  item_id: string;
  /** The name of the function that was called. */
  name: string;
  /** The index of the output item. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The function-call arguments. */
  arguments: string;
}

/**
 * The type of the event. Always 'response.image_generation_call.completed'.
 */
export type ResponseImageGenCallCompletedEventType = typeof ResponseImageGenCallCompletedEventType[keyof typeof ResponseImageGenCallCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseImageGenCallCompletedEventType = {
  responseimage_generation_callcompleted: 'response.image_generation_call.completed',
} as const;

/**
 * Emitted when an image generation tool call has completed and the final image is available.

 */
export interface ResponseImageGenCallCompletedEvent {
  /** The type of the event. Always 'response.image_generation_call.completed'. */
  type: ResponseImageGenCallCompletedEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The unique identifier of the image generation item being processed. */
  item_id: string;
}

/**
 * The type of the event. Always 'response.image_generation_call.generating'.
 */
export type ResponseImageGenCallGeneratingEventType = typeof ResponseImageGenCallGeneratingEventType[keyof typeof ResponseImageGenCallGeneratingEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseImageGenCallGeneratingEventType = {
  responseimage_generation_callgenerating: 'response.image_generation_call.generating',
} as const;

/**
 * Emitted when an image generation tool call is actively generating an image (intermediate state).

 */
export interface ResponseImageGenCallGeneratingEvent {
  /** The type of the event. Always 'response.image_generation_call.generating'. */
  type: ResponseImageGenCallGeneratingEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the image generation item being processed. */
  item_id: string;
  /** The sequence number of the image generation item being processed. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.image_generation_call.in_progress'.
 */
export type ResponseImageGenCallInProgressEventType = typeof ResponseImageGenCallInProgressEventType[keyof typeof ResponseImageGenCallInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseImageGenCallInProgressEventType = {
  responseimage_generation_callin_progress: 'response.image_generation_call.in_progress',
} as const;

/**
 * Emitted when an image generation tool call is in progress.

 */
export interface ResponseImageGenCallInProgressEvent {
  /** The type of the event. Always 'response.image_generation_call.in_progress'. */
  type: ResponseImageGenCallInProgressEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the image generation item being processed. */
  item_id: string;
  /** The sequence number of the image generation item being processed. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.image_generation_call.partial_image'.
 */
export type ResponseImageGenCallPartialImageEventType = typeof ResponseImageGenCallPartialImageEventType[keyof typeof ResponseImageGenCallPartialImageEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseImageGenCallPartialImageEventType = {
  responseimage_generation_callpartial_image: 'response.image_generation_call.partial_image',
} as const;

/**
 * Emitted when a partial image is available during image generation streaming.

 */
export interface ResponseImageGenCallPartialImageEvent {
  /** The type of the event. Always 'response.image_generation_call.partial_image'. */
  type: ResponseImageGenCallPartialImageEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the image generation item being processed. */
  item_id: string;
  /** The sequence number of the image generation item being processed. */
  sequence_number: number;
  /** 0-based index for the partial image (backend is 1-based, but this is 0-based for the user). */
  partial_image_index: number;
  /** Base64-encoded partial image data, suitable for rendering as an image. */
  partial_image_b64: string;
}

/**
 * The type of the event. Always `response.in_progress`.

 */
export type ResponseInProgressEventType = typeof ResponseInProgressEventType[keyof typeof ResponseInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseInProgressEventType = {
  responsein_progress: 'response.in_progress',
} as const;

/**
 * Emitted when the response is in progress.
 */
export interface ResponseInProgressEvent {
  /** The type of the event. Always `response.in_progress`.
 */
  type: ResponseInProgressEventType;
  /** The response that is in progress.
 */
  response: Response;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.incomplete`.

 */
export type ResponseIncompleteEventType = typeof ResponseIncompleteEventType[keyof typeof ResponseIncompleteEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseIncompleteEventType = {
  responseincomplete: 'response.incomplete',
} as const;

/**
 * An event that is emitted when a response finishes as incomplete.

 */
export interface ResponseIncompleteEvent {
  /** The type of the event. Always `response.incomplete`.
 */
  type: ResponseIncompleteEventType;
  /** The response that was incomplete.
 */
  response: Response;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * A list of Response items.
 */
export interface ResponseItemList {
  /** The type of object returned, must be `list`. */
  object: 'list';
  /** A list of items used to generate this response. */
  data: ItemResource[];
  /** Whether there are more items available. */
  has_more: boolean;
  /** The ID of the first item in the list. */
  first_id: string;
  /** The ID of the last item in the list. */
  last_id: string;
}

export type ResponseLogProbTopLogprobsItem = {
  /** A possible text token. */
  token?: string;
  /** The log probability of this token. */
  logprob?: number;
};

/**
 * A logprob is the logarithmic probability that the model assigns to producing
a particular token at a given position in the sequence. Less-negative (higher)
logprob values indicate greater model confidence in that token choice.

 */
export interface ResponseLogProb {
  /** A possible text token. */
  token: string;
  /** The log probability of this token.
 */
  logprob: number;
  /** The log probability of the top 20 most likely tokens.
 */
  top_logprobs?: ResponseLogProbTopLogprobsItem[];
}

/**
 * The type of the event. Always 'response.mcp_call_arguments.delta'.
 */
export type ResponseMCPCallArgumentsDeltaEventType = typeof ResponseMCPCallArgumentsDeltaEventType[keyof typeof ResponseMCPCallArgumentsDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPCallArgumentsDeltaEventType = {
  responsemcp_call_argumentsdelta: 'response.mcp_call_arguments.delta',
} as const;

/**
 * Emitted when there is a delta (partial update) to the arguments of an MCP tool call.

 */
export interface ResponseMCPCallArgumentsDeltaEvent {
  /** The type of the event. Always 'response.mcp_call_arguments.delta'. */
  type: ResponseMCPCallArgumentsDeltaEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the MCP tool call item being processed. */
  item_id: string;
  /** A JSON string containing the partial update to the arguments for the MCP tool call.
 */
  delta: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_call_arguments.done'.
 */
export type ResponseMCPCallArgumentsDoneEventType = typeof ResponseMCPCallArgumentsDoneEventType[keyof typeof ResponseMCPCallArgumentsDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPCallArgumentsDoneEventType = {
  responsemcp_call_argumentsdone: 'response.mcp_call_arguments.done',
} as const;

/**
 * Emitted when the arguments for an MCP tool call are finalized.

 */
export interface ResponseMCPCallArgumentsDoneEvent {
  /** The type of the event. Always 'response.mcp_call_arguments.done'. */
  type: ResponseMCPCallArgumentsDoneEventType;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the MCP tool call item being processed. */
  item_id: string;
  /** A JSON string containing the finalized arguments for the MCP tool call.
 */
  arguments: string;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_call.completed'.
 */
export type ResponseMCPCallCompletedEventType = typeof ResponseMCPCallCompletedEventType[keyof typeof ResponseMCPCallCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPCallCompletedEventType = {
  responsemcp_callcompleted: 'response.mcp_call.completed',
} as const;

/**
 * Emitted when an MCP  tool call has completed successfully.

 */
export interface ResponseMCPCallCompletedEvent {
  /** The type of the event. Always 'response.mcp_call.completed'. */
  type: ResponseMCPCallCompletedEventType;
  /** The ID of the MCP tool call item that completed. */
  item_id: string;
  /** The index of the output item that completed. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_call.failed'.
 */
export type ResponseMCPCallFailedEventType = typeof ResponseMCPCallFailedEventType[keyof typeof ResponseMCPCallFailedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPCallFailedEventType = {
  responsemcp_callfailed: 'response.mcp_call.failed',
} as const;

/**
 * Emitted when an MCP  tool call has failed.

 */
export interface ResponseMCPCallFailedEvent {
  /** The type of the event. Always 'response.mcp_call.failed'. */
  type: ResponseMCPCallFailedEventType;
  /** The ID of the MCP tool call item that failed. */
  item_id: string;
  /** The index of the output item that failed. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_call.in_progress'.
 */
export type ResponseMCPCallInProgressEventType = typeof ResponseMCPCallInProgressEventType[keyof typeof ResponseMCPCallInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPCallInProgressEventType = {
  responsemcp_callin_progress: 'response.mcp_call.in_progress',
} as const;

/**
 * Emitted when an MCP  tool call is in progress.

 */
export interface ResponseMCPCallInProgressEvent {
  /** The type of the event. Always 'response.mcp_call.in_progress'. */
  type: ResponseMCPCallInProgressEventType;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The unique identifier of the MCP tool call item being processed. */
  item_id: string;
}

/**
 * The type of the event. Always 'response.mcp_list_tools.completed'.
 */
export type ResponseMCPListToolsCompletedEventType = typeof ResponseMCPListToolsCompletedEventType[keyof typeof ResponseMCPListToolsCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPListToolsCompletedEventType = {
  responsemcp_list_toolscompleted: 'response.mcp_list_tools.completed',
} as const;

/**
 * Emitted when the list of available MCP tools has been successfully retrieved.

 */
export interface ResponseMCPListToolsCompletedEvent {
  /** The type of the event. Always 'response.mcp_list_tools.completed'. */
  type: ResponseMCPListToolsCompletedEventType;
  /** The ID of the MCP tool call item that produced this output. */
  item_id: string;
  /** The index of the output item that was processed. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_list_tools.failed'.
 */
export type ResponseMCPListToolsFailedEventType = typeof ResponseMCPListToolsFailedEventType[keyof typeof ResponseMCPListToolsFailedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPListToolsFailedEventType = {
  responsemcp_list_toolsfailed: 'response.mcp_list_tools.failed',
} as const;

/**
 * Emitted when the attempt to list available MCP tools has failed.

 */
export interface ResponseMCPListToolsFailedEvent {
  /** The type of the event. Always 'response.mcp_list_tools.failed'. */
  type: ResponseMCPListToolsFailedEventType;
  /** The ID of the MCP tool call item that failed. */
  item_id: string;
  /** The index of the output item that failed. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always 'response.mcp_list_tools.in_progress'.
 */
export type ResponseMCPListToolsInProgressEventType = typeof ResponseMCPListToolsInProgressEventType[keyof typeof ResponseMCPListToolsInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseMCPListToolsInProgressEventType = {
  responsemcp_list_toolsin_progress: 'response.mcp_list_tools.in_progress',
} as const;

/**
 * Emitted when the system is in the process of retrieving the list of available MCP tools.

 */
export interface ResponseMCPListToolsInProgressEvent {
  /** The type of the event. Always 'response.mcp_list_tools.in_progress'. */
  type: ResponseMCPListToolsInProgressEventType;
  /** The ID of the MCP tool call item that is being processed. */
  item_id: string;
  /** The index of the output item that is being processed. */
  output_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
}

export type ResponseModalitiesAnyOfItem = typeof ResponseModalitiesAnyOfItem[keyof typeof ResponseModalitiesAnyOfItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseModalitiesAnyOfItem = {
  text: 'text',
  audio: 'audio',
} as const;

export type ResponseModalities = ResponseModalitiesAnyOfItem[] | null;

/**
 * The type of the event. Always `response.output_item.added`.

 */
export type ResponseOutputItemAddedEventType = typeof ResponseOutputItemAddedEventType[keyof typeof ResponseOutputItemAddedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseOutputItemAddedEventType = {
  responseoutput_itemadded: 'response.output_item.added',
} as const;

/**
 * Emitted when a new output item is added.
 */
export interface ResponseOutputItemAddedEvent {
  /** The type of the event. Always `response.output_item.added`.
 */
  type: ResponseOutputItemAddedEventType;
  /** The index of the output item that was added.
 */
  output_index: number;
  /** The sequence number of this event.
 */
  sequence_number: number;
  /** The output item that was added.
 */
  item: OutputItem;
}

/**
 * The type of the event. Always `response.output_item.done`.

 */
export type ResponseOutputItemDoneEventType = typeof ResponseOutputItemDoneEventType[keyof typeof ResponseOutputItemDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseOutputItemDoneEventType = {
  responseoutput_itemdone: 'response.output_item.done',
} as const;

/**
 * Emitted when an output item is marked done.
 */
export interface ResponseOutputItemDoneEvent {
  /** The type of the event. Always `response.output_item.done`.
 */
  type: ResponseOutputItemDoneEventType;
  /** The index of the output item that was marked done.
 */
  output_index: number;
  /** The sequence number of this event.
 */
  sequence_number: number;
  /** The output item that was marked done.
 */
  item: OutputItem;
}

/**
 * The type of the event. Always 'response.output_text.annotation.added'.
 */
export type ResponseOutputTextAnnotationAddedEventType = typeof ResponseOutputTextAnnotationAddedEventType[keyof typeof ResponseOutputTextAnnotationAddedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseOutputTextAnnotationAddedEventType = {
  responseoutput_textannotationadded: 'response.output_text.annotation.added',
} as const;

/**
 * The annotation object being added. (See annotation schema for details.)
 */
export type ResponseOutputTextAnnotationAddedEventAnnotation = { [key: string]: unknown };

/**
 * Emitted when an annotation is added to output text content.

 */
export interface ResponseOutputTextAnnotationAddedEvent {
  /** The type of the event. Always 'response.output_text.annotation.added'. */
  type: ResponseOutputTextAnnotationAddedEventType;
  /** The unique identifier of the item to which the annotation is being added. */
  item_id: string;
  /** The index of the output item in the response's output array. */
  output_index: number;
  /** The index of the content part within the output item. */
  content_index: number;
  /** The index of the annotation within the content part. */
  annotation_index: number;
  /** The sequence number of this event. */
  sequence_number: number;
  /** The annotation object being added. (See annotation schema for details.) */
  annotation: ResponseOutputTextAnnotationAddedEventAnnotation;
}

/**
 * Optional map of values to substitute in for variables in your
prompt. The substitution values can either be strings, or other
Response input types like images or files.

 */
export type ResponsePromptVariablesAnyOf = {[key: string]: string | InputTextContent | InputImageContent | InputFileContent};

export type ResponsePromptVariables = ResponsePromptVariablesAnyOf | null;

export type ResponsePropertiesPreviousResponseId = string | null;

export type ResponsePropertiesReasoning = Reasoning | null;

export type ResponsePropertiesBackground = boolean | null;

export type ResponsePropertiesMaxOutputTokens = number | null;

export type ResponsePropertiesMaxToolCalls = number | null;

export type ResponsePropertiesTruncation = 'auto' | 'disabled' | null;

export interface ResponseProperties {
  previous_response_id?: ResponsePropertiesPreviousResponseId;
  /** Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI
offers a wide range of models with different capabilities, performance
characteristics, and price points. Refer to the [model guide](https://platform.openai.com/docs/models)
to browse and compare available models.
 */
  model?: ModelIdsResponses;
  reasoning?: ResponsePropertiesReasoning;
  background?: ResponsePropertiesBackground;
  max_output_tokens?: ResponsePropertiesMaxOutputTokens;
  max_tool_calls?: ResponsePropertiesMaxToolCalls;
  text?: ResponseTextParam;
  tools?: ToolsArray;
  tool_choice?: ToolChoiceParam;
  prompt?: Prompt;
  truncation?: ResponsePropertiesTruncation;
}

/**
 * The type of the event. Always 'response.queued'.
 */
export type ResponseQueuedEventType = typeof ResponseQueuedEventType[keyof typeof ResponseQueuedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseQueuedEventType = {
  responsequeued: 'response.queued',
} as const;

/**
 * Emitted when a response is queued and waiting to be processed.

 */
export interface ResponseQueuedEvent {
  /** The type of the event. Always 'response.queued'. */
  type: ResponseQueuedEventType;
  /** The full response object that is queued. */
  response: Response;
  /** The sequence number for this event. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.reasoning_summary_part.added`.

 */
export type ResponseReasoningSummaryPartAddedEventType = typeof ResponseReasoningSummaryPartAddedEventType[keyof typeof ResponseReasoningSummaryPartAddedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryPartAddedEventType = {
  responsereasoning_summary_partadded: 'response.reasoning_summary_part.added',
} as const;

/**
 * The type of the summary part. Always `summary_text`.
 */
export type ResponseReasoningSummaryPartAddedEventPartType = typeof ResponseReasoningSummaryPartAddedEventPartType[keyof typeof ResponseReasoningSummaryPartAddedEventPartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryPartAddedEventPartType = {
  summary_text: 'summary_text',
} as const;

/**
 * The summary part that was added.

 */
export type ResponseReasoningSummaryPartAddedEventPart = {
  /** The type of the summary part. Always `summary_text`. */
  type: ResponseReasoningSummaryPartAddedEventPartType;
  /** The text of the summary part. */
  text: string;
};

/**
 * Emitted when a new reasoning summary part is added.
 */
export interface ResponseReasoningSummaryPartAddedEvent {
  /** The type of the event. Always `response.reasoning_summary_part.added`.
 */
  type: ResponseReasoningSummaryPartAddedEventType;
  /** The ID of the item this summary part is associated with.
 */
  item_id: string;
  /** The index of the output item this summary part is associated with.
 */
  output_index: number;
  /** The index of the summary part within the reasoning summary.
 */
  summary_index: number;
  /** The sequence number of this event.
 */
  sequence_number: number;
  /** The summary part that was added.
 */
  part: ResponseReasoningSummaryPartAddedEventPart;
}

/**
 * The type of the event. Always `response.reasoning_summary_part.done`.

 */
export type ResponseReasoningSummaryPartDoneEventType = typeof ResponseReasoningSummaryPartDoneEventType[keyof typeof ResponseReasoningSummaryPartDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryPartDoneEventType = {
  responsereasoning_summary_partdone: 'response.reasoning_summary_part.done',
} as const;

/**
 * The type of the summary part. Always `summary_text`.
 */
export type ResponseReasoningSummaryPartDoneEventPartType = typeof ResponseReasoningSummaryPartDoneEventPartType[keyof typeof ResponseReasoningSummaryPartDoneEventPartType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryPartDoneEventPartType = {
  summary_text: 'summary_text',
} as const;

/**
 * The completed summary part.

 */
export type ResponseReasoningSummaryPartDoneEventPart = {
  /** The type of the summary part. Always `summary_text`. */
  type: ResponseReasoningSummaryPartDoneEventPartType;
  /** The text of the summary part. */
  text: string;
};

/**
 * Emitted when a reasoning summary part is completed.
 */
export interface ResponseReasoningSummaryPartDoneEvent {
  /** The type of the event. Always `response.reasoning_summary_part.done`.
 */
  type: ResponseReasoningSummaryPartDoneEventType;
  /** The ID of the item this summary part is associated with.
 */
  item_id: string;
  /** The index of the output item this summary part is associated with.
 */
  output_index: number;
  /** The index of the summary part within the reasoning summary.
 */
  summary_index: number;
  /** The sequence number of this event.
 */
  sequence_number: number;
  /** The completed summary part.
 */
  part: ResponseReasoningSummaryPartDoneEventPart;
}

/**
 * The type of the event. Always `response.reasoning_summary_text.delta`.

 */
export type ResponseReasoningSummaryTextDeltaEventType = typeof ResponseReasoningSummaryTextDeltaEventType[keyof typeof ResponseReasoningSummaryTextDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryTextDeltaEventType = {
  responsereasoning_summary_textdelta: 'response.reasoning_summary_text.delta',
} as const;

/**
 * Emitted when a delta is added to a reasoning summary text.
 */
export interface ResponseReasoningSummaryTextDeltaEvent {
  /** The type of the event. Always `response.reasoning_summary_text.delta`.
 */
  type: ResponseReasoningSummaryTextDeltaEventType;
  /** The ID of the item this summary text delta is associated with.
 */
  item_id: string;
  /** The index of the output item this summary text delta is associated with.
 */
  output_index: number;
  /** The index of the summary part within the reasoning summary.
 */
  summary_index: number;
  /** The text delta that was added to the summary.
 */
  delta: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.reasoning_summary_text.done`.

 */
export type ResponseReasoningSummaryTextDoneEventType = typeof ResponseReasoningSummaryTextDoneEventType[keyof typeof ResponseReasoningSummaryTextDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningSummaryTextDoneEventType = {
  responsereasoning_summary_textdone: 'response.reasoning_summary_text.done',
} as const;

/**
 * Emitted when a reasoning summary text is completed.
 */
export interface ResponseReasoningSummaryTextDoneEvent {
  /** The type of the event. Always `response.reasoning_summary_text.done`.
 */
  type: ResponseReasoningSummaryTextDoneEventType;
  /** The ID of the item this summary text is associated with.
 */
  item_id: string;
  /** The index of the output item this summary text is associated with.
 */
  output_index: number;
  /** The index of the summary part within the reasoning summary.
 */
  summary_index: number;
  /** The full text of the completed reasoning summary.
 */
  text: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.reasoning_text.delta`.

 */
export type ResponseReasoningTextDeltaEventType = typeof ResponseReasoningTextDeltaEventType[keyof typeof ResponseReasoningTextDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningTextDeltaEventType = {
  responsereasoning_textdelta: 'response.reasoning_text.delta',
} as const;

/**
 * Emitted when a delta is added to a reasoning text.
 */
export interface ResponseReasoningTextDeltaEvent {
  /** The type of the event. Always `response.reasoning_text.delta`.
 */
  type: ResponseReasoningTextDeltaEventType;
  /** The ID of the item this reasoning text delta is associated with.
 */
  item_id: string;
  /** The index of the output item this reasoning text delta is associated with.
 */
  output_index: number;
  /** The index of the reasoning content part this delta is associated with.
 */
  content_index: number;
  /** The text delta that was added to the reasoning content.
 */
  delta: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.reasoning_text.done`.

 */
export type ResponseReasoningTextDoneEventType = typeof ResponseReasoningTextDoneEventType[keyof typeof ResponseReasoningTextDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseReasoningTextDoneEventType = {
  responsereasoning_textdone: 'response.reasoning_text.done',
} as const;

/**
 * Emitted when a reasoning text is completed.
 */
export interface ResponseReasoningTextDoneEvent {
  /** The type of the event. Always `response.reasoning_text.done`.
 */
  type: ResponseReasoningTextDoneEventType;
  /** The ID of the item this reasoning text is associated with.
 */
  item_id: string;
  /** The index of the output item this reasoning text is associated with.
 */
  output_index: number;
  /** The index of the reasoning content part.
 */
  content_index: number;
  /** The full text of the completed reasoning content.
 */
  text: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.refusal.delta`.

 */
export type ResponseRefusalDeltaEventType = typeof ResponseRefusalDeltaEventType[keyof typeof ResponseRefusalDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseRefusalDeltaEventType = {
  responserefusaldelta: 'response.refusal.delta',
} as const;

/**
 * Emitted when there is a partial refusal text.
 */
export interface ResponseRefusalDeltaEvent {
  /** The type of the event. Always `response.refusal.delta`.
 */
  type: ResponseRefusalDeltaEventType;
  /** The ID of the output item that the refusal text is added to.
 */
  item_id: string;
  /** The index of the output item that the refusal text is added to.
 */
  output_index: number;
  /** The index of the content part that the refusal text is added to.
 */
  content_index: number;
  /** The refusal text that is added.
 */
  delta: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.refusal.done`.

 */
export type ResponseRefusalDoneEventType = typeof ResponseRefusalDoneEventType[keyof typeof ResponseRefusalDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseRefusalDoneEventType = {
  responserefusaldone: 'response.refusal.done',
} as const;

/**
 * Emitted when refusal text is finalized.
 */
export interface ResponseRefusalDoneEvent {
  /** The type of the event. Always `response.refusal.done`.
 */
  type: ResponseRefusalDoneEventType;
  /** The ID of the output item that the refusal text is finalized.
 */
  item_id: string;
  /** The index of the output item that the refusal text is finalized.
 */
  output_index: number;
  /** The index of the content part that the refusal text is finalized.
 */
  content_index: number;
  /** The refusal text that is finalized.
 */
  refusal: string;
  /** The sequence number of this event.
 */
  sequence_number: number;
}

export type ResponseStreamEvent = ResponseAudioDeltaEvent | ResponseAudioDoneEvent | ResponseAudioTranscriptDeltaEvent | ResponseAudioTranscriptDoneEvent | ResponseCodeInterpreterCallCodeDeltaEvent | ResponseCodeInterpreterCallCodeDoneEvent | ResponseCodeInterpreterCallCompletedEvent | ResponseCodeInterpreterCallInProgressEvent | ResponseCodeInterpreterCallInterpretingEvent | ResponseCompletedEvent | ResponseContentPartAddedEvent | ResponseContentPartDoneEvent | ResponseCreatedEvent | ResponseErrorEvent | ResponseFileSearchCallCompletedEvent | ResponseFileSearchCallInProgressEvent | ResponseFileSearchCallSearchingEvent | ResponseFunctionCallArgumentsDeltaEvent | ResponseFunctionCallArgumentsDoneEvent | ResponseInProgressEvent | ResponseFailedEvent | ResponseIncompleteEvent | ResponseOutputItemAddedEvent | ResponseOutputItemDoneEvent | ResponseReasoningSummaryPartAddedEvent | ResponseReasoningSummaryPartDoneEvent | ResponseReasoningSummaryTextDeltaEvent | ResponseReasoningSummaryTextDoneEvent | ResponseReasoningTextDeltaEvent | ResponseReasoningTextDoneEvent | ResponseRefusalDeltaEvent | ResponseRefusalDoneEvent | ResponseTextDeltaEvent | ResponseTextDoneEvent | ResponseWebSearchCallCompletedEvent | ResponseWebSearchCallInProgressEvent | ResponseWebSearchCallSearchingEvent | ResponseImageGenCallCompletedEvent | ResponseImageGenCallGeneratingEvent | ResponseImageGenCallInProgressEvent | ResponseImageGenCallPartialImageEvent | ResponseMCPCallArgumentsDeltaEvent | ResponseMCPCallArgumentsDoneEvent | ResponseMCPCallCompletedEvent | ResponseMCPCallFailedEvent | ResponseMCPCallInProgressEvent | ResponseMCPListToolsCompletedEvent | ResponseMCPListToolsFailedEvent | ResponseMCPListToolsInProgressEvent | ResponseOutputTextAnnotationAddedEvent | ResponseQueuedEvent | ResponseCustomToolCallInputDeltaEvent | ResponseCustomToolCallInputDoneEvent;

/**
 * Options for streaming responses. Only set this when you set `stream: true`.

 */
export type ResponseStreamOptionsAnyOf = {
  /** When true, stream obfuscation will be enabled. Stream obfuscation adds
random characters to an `obfuscation` field on streaming delta events to
normalize payload sizes as a mitigation to certain side-channel attacks.
These obfuscation fields are included by default, but add a small amount
of overhead to the data stream. You can set `include_obfuscation` to
false to optimize for bandwidth if you trust the network links between
your application and the OpenAI API.
 */
  include_obfuscation?: boolean;
};

export type ResponseStreamOptions = ResponseStreamOptionsAnyOf | null;

/**
 * The type of the event. Always `response.output_text.delta`.

 */
export type ResponseTextDeltaEventType = typeof ResponseTextDeltaEventType[keyof typeof ResponseTextDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseTextDeltaEventType = {
  responseoutput_textdelta: 'response.output_text.delta',
} as const;

/**
 * Emitted when there is an additional text delta.
 */
export interface ResponseTextDeltaEvent {
  /** The type of the event. Always `response.output_text.delta`.
 */
  type: ResponseTextDeltaEventType;
  /** The ID of the output item that the text delta was added to.
 */
  item_id: string;
  /** The index of the output item that the text delta was added to.
 */
  output_index: number;
  /** The index of the content part that the text delta was added to.
 */
  content_index: number;
  /** The text delta that was added.
 */
  delta: string;
  /** The sequence number for this event. */
  sequence_number: number;
  /** The log probabilities of the tokens in the delta.
 */
  logprobs: ResponseLogProb[];
}

/**
 * The type of the event. Always `response.output_text.done`.

 */
export type ResponseTextDoneEventType = typeof ResponseTextDoneEventType[keyof typeof ResponseTextDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseTextDoneEventType = {
  responseoutput_textdone: 'response.output_text.done',
} as const;

/**
 * Emitted when text content is finalized.
 */
export interface ResponseTextDoneEvent {
  /** The type of the event. Always `response.output_text.done`.
 */
  type: ResponseTextDoneEventType;
  /** The ID of the output item that the text content is finalized.
 */
  item_id: string;
  /** The index of the output item that the text content is finalized.
 */
  output_index: number;
  /** The index of the content part that the text content is finalized.
 */
  content_index: number;
  /** The text content that is finalized.
 */
  text: string;
  /** The sequence number for this event. */
  sequence_number: number;
  /** The log probabilities of the tokens in the delta.
 */
  logprobs: ResponseLogProb[];
}

/**
 * Configuration options for a text response from the model. Can be plain
text or structured JSON data. Learn more:
- [Text inputs and outputs](https://platform.openai.com/docs/guides/text)
- [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs)

 */
export interface ResponseTextParam {
  format?: TextResponseFormatConfiguration;
  verbosity?: Verbosity;
}

/**
 * A detailed breakdown of the input tokens.
 */
export type ResponseUsageInputTokensDetails = {
  /** The number of tokens that were retrieved from the cache.
[More on prompt caching](https://platform.openai.com/docs/guides/prompt-caching).
 */
  cached_tokens: number;
};

/**
 * A detailed breakdown of the output tokens.
 */
export type ResponseUsageOutputTokensDetails = {
  /** The number of reasoning tokens. */
  reasoning_tokens: number;
};

/**
 * Represents token usage details including input tokens, output tokens,
a breakdown of output tokens, and the total tokens used.

 */
export interface ResponseUsage {
  /** The number of input tokens. */
  input_tokens: number;
  /** A detailed breakdown of the input tokens. */
  input_tokens_details: ResponseUsageInputTokensDetails;
  /** The number of output tokens. */
  output_tokens: number;
  /** A detailed breakdown of the output tokens. */
  output_tokens_details: ResponseUsageOutputTokensDetails;
  /** The total number of tokens used. */
  total_tokens: number;
}

/**
 * The type of the event. Always `response.web_search_call.completed`.

 */
export type ResponseWebSearchCallCompletedEventType = typeof ResponseWebSearchCallCompletedEventType[keyof typeof ResponseWebSearchCallCompletedEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseWebSearchCallCompletedEventType = {
  responseweb_search_callcompleted: 'response.web_search_call.completed',
} as const;

/**
 * Emitted when a web search call is completed.
 */
export interface ResponseWebSearchCallCompletedEvent {
  /** The type of the event. Always `response.web_search_call.completed`.
 */
  type: ResponseWebSearchCallCompletedEventType;
  /** The index of the output item that the web search call is associated with.
 */
  output_index: number;
  /** Unique ID for the output item associated with the web search call.
 */
  item_id: string;
  /** The sequence number of the web search call being processed. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.web_search_call.in_progress`.

 */
export type ResponseWebSearchCallInProgressEventType = typeof ResponseWebSearchCallInProgressEventType[keyof typeof ResponseWebSearchCallInProgressEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseWebSearchCallInProgressEventType = {
  responseweb_search_callin_progress: 'response.web_search_call.in_progress',
} as const;

/**
 * Emitted when a web search call is initiated.
 */
export interface ResponseWebSearchCallInProgressEvent {
  /** The type of the event. Always `response.web_search_call.in_progress`.
 */
  type: ResponseWebSearchCallInProgressEventType;
  /** The index of the output item that the web search call is associated with.
 */
  output_index: number;
  /** Unique ID for the output item associated with the web search call.
 */
  item_id: string;
  /** The sequence number of the web search call being processed. */
  sequence_number: number;
}

/**
 * The type of the event. Always `response.web_search_call.searching`.

 */
export type ResponseWebSearchCallSearchingEventType = typeof ResponseWebSearchCallSearchingEventType[keyof typeof ResponseWebSearchCallSearchingEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseWebSearchCallSearchingEventType = {
  responseweb_search_callsearching: 'response.web_search_call.searching',
} as const;

/**
 * Emitted when a web search call is executing.
 */
export interface ResponseWebSearchCallSearchingEvent {
  /** The type of the event. Always `response.web_search_call.searching`.
 */
  type: ResponseWebSearchCallSearchingEventType;
  /** The index of the output item that the web search call is associated with.
 */
  output_index: number;
  /** Unique ID for the output item associated with the web search call.
 */
  item_id: string;
  /** The sequence number of the web search call being processed. */
  sequence_number: number;
}

/**
 * Usage statistics related to the run. This value will be `null` if the run is not in a terminal state (i.e. `in_progress`, `queued`, etc.).
 */
export type RunCompletionUsageAnyOf = {
  /** Number of completion tokens used over the course of the run. */
  completion_tokens: number;
  /** Number of prompt tokens used over the course of the run. */
  prompt_tokens: number;
  /** Total number of tokens used (prompt + completion). */
  total_tokens: number;
};

export type RunCompletionUsage = RunCompletionUsageAnyOf | null;

/**
 * The grader used for the fine-tuning job.
 */
export type RunGraderRequestGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

/**
 * The dataset item provided to the grader. This will be used to populate
the `item` namespace. See [the guide](https://platform.openai.com/docs/guides/graders) for more details.

 */
export type RunGraderRequestItem = { [key: string]: unknown };

export interface RunGraderRequest {
  /** The grader used for the fine-tuning job. */
  grader: RunGraderRequestGrader;
  /** The dataset item provided to the grader. This will be used to populate
the `item` namespace. See [the guide](https://platform.openai.com/docs/guides/graders) for more details.
 */
  item?: RunGraderRequestItem;
  /** The model sample to be evaluated. This value will be used to populate
the `sample` namespace. See [the guide](https://platform.openai.com/docs/guides/graders) for more details.
The `output_json` variable will be populated if the model sample is a
valid JSON string.
 */
  model_sample: string;
}

export type RunGraderResponseMetadataErrorsPythonGraderServerErrorType = string | null;

export type RunGraderResponseMetadataErrorsPythonGraderRuntimeErrorDetails = string | null;

export type RunGraderResponseMetadataErrorsModelGraderServerErrorDetails = string | null;

export type RunGraderResponseMetadataErrors = {
  formula_parse_error: boolean;
  sample_parse_error: boolean;
  truncated_observation_error: boolean;
  unresponsive_reward_error: boolean;
  invalid_variable_error: boolean;
  other_error: boolean;
  python_grader_server_error: boolean;
  python_grader_server_error_type: RunGraderResponseMetadataErrorsPythonGraderServerErrorType;
  python_grader_runtime_error: boolean;
  python_grader_runtime_error_details: RunGraderResponseMetadataErrorsPythonGraderRuntimeErrorDetails;
  model_grader_server_error: boolean;
  model_grader_refusal_error: boolean;
  model_grader_parse_error: boolean;
  model_grader_server_error_details: RunGraderResponseMetadataErrorsModelGraderServerErrorDetails;
};

export type RunGraderResponseMetadataScores = {[key: string]: unknown};

export type RunGraderResponseMetadataTokenUsage = number | null;

export type RunGraderResponseMetadataSampledModelName = string | null;

export type RunGraderResponseMetadata = {
  name: string;
  type: string;
  errors: RunGraderResponseMetadataErrors;
  execution_time: number;
  scores: RunGraderResponseMetadataScores;
  token_usage: RunGraderResponseMetadataTokenUsage;
  sampled_model_name: RunGraderResponseMetadataSampledModelName;
};

export type RunGraderResponseSubRewards = {[key: string]: unknown};

export type RunGraderResponseModelGraderTokenUsagePerModel = {[key: string]: unknown};

export interface RunGraderResponse {
  reward: number;
  metadata: RunGraderResponseMetadata;
  sub_rewards: RunGraderResponseSubRewards;
  model_grader_token_usage_per_model: RunGraderResponseModelGraderTokenUsagePerModel;
}

/**
 * The object type, which is always `thread.run`.
 */
export type RunObjectObject = typeof RunObjectObject[keyof typeof RunObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunObjectObject = {
  threadrun: 'thread.run',
} as const;

/**
 * For now, this is always `submit_tool_outputs`.
 */
export type RunObjectRequiredActionType = typeof RunObjectRequiredActionType[keyof typeof RunObjectRequiredActionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunObjectRequiredActionType = {
  submit_tool_outputs: 'submit_tool_outputs',
} as const;

/**
 * Details on the tool outputs needed for this run to continue.
 */
export type RunObjectRequiredActionSubmitToolOutputs = {
  /** A list of the relevant tool calls. */
  tool_calls: RunToolCallObject[];
};

/**
 * Details on the action required to continue the run. Will be `null` if no action is required.
 * @nullable
 */
export type RunObjectRequiredAction = {
  /** For now, this is always `submit_tool_outputs`. */
  type: RunObjectRequiredActionType;
  /** Details on the tool outputs needed for this run to continue. */
  submit_tool_outputs: RunObjectRequiredActionSubmitToolOutputs;
} | null;

/**
 * One of `server_error`, `rate_limit_exceeded`, or `invalid_prompt`.
 */
export type RunObjectLastErrorCode = typeof RunObjectLastErrorCode[keyof typeof RunObjectLastErrorCode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunObjectLastErrorCode = {
  server_error: 'server_error',
  rate_limit_exceeded: 'rate_limit_exceeded',
  invalid_prompt: 'invalid_prompt',
} as const;

/**
 * The last error associated with this run. Will be `null` if there are no errors.
 * @nullable
 */
export type RunObjectLastError = {
  /** One of `server_error`, `rate_limit_exceeded`, or `invalid_prompt`. */
  code: RunObjectLastErrorCode;
  /** A human-readable description of the error. */
  message: string;
} | null;

/**
 * The reason why the run is incomplete. This will point to which specific token limit was reached over the course of the run.
 */
export type RunObjectIncompleteDetailsReason = typeof RunObjectIncompleteDetailsReason[keyof typeof RunObjectIncompleteDetailsReason];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunObjectIncompleteDetailsReason = {
  max_completion_tokens: 'max_completion_tokens',
  max_prompt_tokens: 'max_prompt_tokens',
} as const;

/**
 * Details on why the run is incomplete. Will be `null` if the run is not incomplete.
 * @nullable
 */
export type RunObjectIncompleteDetails = {
  /** The reason why the run is incomplete. This will point to which specific token limit was reached over the course of the run. */
  reason?: RunObjectIncompleteDetailsReason;
} | null;

/**
 * @nullable
 */
export type RunObjectTruncationStrategyAllOf = unknown | null;

export type RunObjectTruncationStrategy = TruncationObject & RunObjectTruncationStrategyAllOf;

/**
 * @nullable
 */
export type RunObjectToolChoiceAllOf = unknown | null;

export type RunObjectToolChoice = AssistantsApiToolChoiceOption & RunObjectToolChoiceAllOf;

/**
 * Represents an execution run on a [thread](https://platform.openai.com/docs/api-reference/threads).
 */
export interface RunObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.run`. */
  object: RunObjectObject;
  /** The Unix timestamp (in seconds) for when the run was created. */
  created_at: number;
  /** The ID of the [thread](https://platform.openai.com/docs/api-reference/threads) that was executed on as a part of this run. */
  thread_id: string;
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) used for execution of this run. */
  assistant_id: string;
  status: RunStatus;
  /**
   * Details on the action required to continue the run. Will be `null` if no action is required.
   * @nullable
   */
  required_action: RunObjectRequiredAction;
  /**
   * The last error associated with this run. Will be `null` if there are no errors.
   * @nullable
   */
  last_error: RunObjectLastError;
  /**
   * The Unix timestamp (in seconds) for when the run will expire.
   * @nullable
   */
  expires_at: number | null;
  /**
   * The Unix timestamp (in seconds) for when the run was started.
   * @nullable
   */
  started_at: number | null;
  /**
   * The Unix timestamp (in seconds) for when the run was cancelled.
   * @nullable
   */
  cancelled_at: number | null;
  /**
   * The Unix timestamp (in seconds) for when the run failed.
   * @nullable
   */
  failed_at: number | null;
  /**
   * The Unix timestamp (in seconds) for when the run was completed.
   * @nullable
   */
  completed_at: number | null;
  /**
   * Details on why the run is incomplete. Will be `null` if the run is not incomplete.
   * @nullable
   */
  incomplete_details: RunObjectIncompleteDetails;
  /** The model that the [assistant](https://platform.openai.com/docs/api-reference/assistants) used for this run. */
  model: string;
  /** The instructions that the [assistant](https://platform.openai.com/docs/api-reference/assistants) used for this run. */
  instructions: string;
  /**
   * The list of tools that the [assistant](https://platform.openai.com/docs/api-reference/assistants) used for this run.
   * @maxItems 20
   */
  tools: AssistantTool[];
  metadata: Metadata;
  usage: RunCompletionUsage;
  /**
   * The sampling temperature used for this run. If not set, defaults to 1.
   * @nullable
   */
  temperature?: number | null;
  /**
   * The nucleus sampling value used for this run. If not set, defaults to 1.
   * @nullable
   */
  top_p?: number | null;
  /**
   * The maximum number of prompt tokens specified to have been used over the course of the run.

   * @minimum 256
   * @nullable
   */
  max_prompt_tokens: number | null;
  /**
   * The maximum number of completion tokens specified to have been used over the course of the run.

   * @minimum 256
   * @nullable
   */
  max_completion_tokens: number | null;
  truncation_strategy: RunObjectTruncationStrategy;
  tool_choice: RunObjectToolChoice;
  parallel_tool_calls: ParallelToolCalls;
  /** @nullable */
  response_format: AssistantsApiResponseFormatOption;
}

/**
 * Usage statistics related to the run step. This value will be `null` while the run step's status is `in_progress`.
 */
export type RunStepCompletionUsageAnyOf = {
  /** Number of completion tokens used over the course of the run step. */
  completion_tokens: number;
  /** Number of prompt tokens used over the course of the run step. */
  prompt_tokens: number;
  /** Total number of tokens used (prompt + completion). */
  total_tokens: number;
};

export type RunStepCompletionUsage = RunStepCompletionUsageAnyOf | null;

/**
 * The object type, which is always `thread.run.step.delta`.
 */
export type RunStepDeltaObjectObject = typeof RunStepDeltaObjectObject[keyof typeof RunStepDeltaObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaObjectObject = {
  threadrunstepdelta: 'thread.run.step.delta',
} as const;

/**
 * Represents a run step delta i.e. any changed fields on a run step during streaming.

 */
export interface RunStepDeltaObject {
  /** The identifier of the run step, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.run.step.delta`. */
  object: RunStepDeltaObjectObject;
  delta: RunStepDeltaObjectDelta;
}

/**
 * Always `message_creation`.
 */
export type RunStepDeltaStepDetailsMessageCreationObjectType = typeof RunStepDeltaStepDetailsMessageCreationObjectType[keyof typeof RunStepDeltaStepDetailsMessageCreationObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsMessageCreationObjectType = {
  message_creation: 'message_creation',
} as const;

export type RunStepDeltaStepDetailsMessageCreationObjectMessageCreation = {
  /** The ID of the message that was created by this run step. */
  message_id?: string;
};

/**
 * Details of the message creation by the run step.
 */
export interface RunStepDeltaStepDetailsMessageCreationObject {
  /** Always `message_creation`. */
  type: RunStepDeltaStepDetailsMessageCreationObjectType;
  message_creation?: RunStepDeltaStepDetailsMessageCreationObjectMessageCreation;
}

/**
 * The type of tool call. This is always going to be `code_interpreter` for this type of tool call.
 */
export type RunStepDeltaStepDetailsToolCallsCodeObjectType = typeof RunStepDeltaStepDetailsToolCallsCodeObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsCodeObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsCodeObjectType = {
  code_interpreter: 'code_interpreter',
} as const;

export type RunStepDeltaStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem = RunStepDeltaStepDetailsToolCallsCodeOutputLogsObject | RunStepDeltaStepDetailsToolCallsCodeOutputImageObject;

/**
 * The Code Interpreter tool call definition.
 */
export type RunStepDeltaStepDetailsToolCallsCodeObjectCodeInterpreter = {
  /** The input to the Code Interpreter tool call. */
  input?: string;
  /** The outputs from the Code Interpreter tool call. Code Interpreter can output one or more items, including text (`logs`) or images (`image`). Each of these are represented by a different object type. */
  outputs?: RunStepDeltaStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem[];
};

/**
 * Details of the Code Interpreter tool call the run step was involved in.
 */
export interface RunStepDeltaStepDetailsToolCallsCodeObject {
  /** The index of the tool call in the tool calls array. */
  index: number;
  /** The ID of the tool call. */
  id?: string;
  /** The type of tool call. This is always going to be `code_interpreter` for this type of tool call. */
  type: RunStepDeltaStepDetailsToolCallsCodeObjectType;
  /** The Code Interpreter tool call definition. */
  code_interpreter?: RunStepDeltaStepDetailsToolCallsCodeObjectCodeInterpreter;
}

/**
 * Always `image`.
 */
export type RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectType = typeof RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectType = {
  image: 'image',
} as const;

export type RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectImage = {
  /** The [file](https://platform.openai.com/docs/api-reference/files) ID of the image. */
  file_id?: string;
};

export interface RunStepDeltaStepDetailsToolCallsCodeOutputImageObject {
  /** The index of the output in the outputs array. */
  index: number;
  /** Always `image`. */
  type: RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectType;
  image?: RunStepDeltaStepDetailsToolCallsCodeOutputImageObjectImage;
}

/**
 * Always `logs`.
 */
export type RunStepDeltaStepDetailsToolCallsCodeOutputLogsObjectType = typeof RunStepDeltaStepDetailsToolCallsCodeOutputLogsObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsCodeOutputLogsObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsCodeOutputLogsObjectType = {
  logs: 'logs',
} as const;

/**
 * Text output from the Code Interpreter tool call as part of a run step.
 */
export interface RunStepDeltaStepDetailsToolCallsCodeOutputLogsObject {
  /** The index of the output in the outputs array. */
  index: number;
  /** Always `logs`. */
  type: RunStepDeltaStepDetailsToolCallsCodeOutputLogsObjectType;
  /** The text output from the Code Interpreter tool call. */
  logs?: string;
}

/**
 * The type of tool call. This is always going to be `file_search` for this type of tool call.
 */
export type RunStepDeltaStepDetailsToolCallsFileSearchObjectType = typeof RunStepDeltaStepDetailsToolCallsFileSearchObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsFileSearchObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsFileSearchObjectType = {
  file_search: 'file_search',
} as const;

/**
 * For now, this is always going to be an empty object.
 */
export type RunStepDeltaStepDetailsToolCallsFileSearchObjectFileSearch = { [key: string]: unknown };

export interface RunStepDeltaStepDetailsToolCallsFileSearchObject {
  /** The index of the tool call in the tool calls array. */
  index: number;
  /** The ID of the tool call object. */
  id?: string;
  /** The type of tool call. This is always going to be `file_search` for this type of tool call. */
  type: RunStepDeltaStepDetailsToolCallsFileSearchObjectType;
  /** For now, this is always going to be an empty object. */
  file_search: RunStepDeltaStepDetailsToolCallsFileSearchObjectFileSearch;
}

/**
 * The type of tool call. This is always going to be `function` for this type of tool call.
 */
export type RunStepDeltaStepDetailsToolCallsFunctionObjectType = typeof RunStepDeltaStepDetailsToolCallsFunctionObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsFunctionObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsFunctionObjectType = {
  function: 'function',
} as const;

export type RunStepDeltaStepDetailsToolCallsFunctionObjectFunctionOutput = string | null;

/**
 * The definition of the function that was called.
 */
export type RunStepDeltaStepDetailsToolCallsFunctionObjectFunction = {
  /** The name of the function. */
  name?: string;
  /** The arguments passed to the function. */
  arguments?: string;
  output?: RunStepDeltaStepDetailsToolCallsFunctionObjectFunctionOutput;
};

export interface RunStepDeltaStepDetailsToolCallsFunctionObject {
  /** The index of the tool call in the tool calls array. */
  index: number;
  /** The ID of the tool call object. */
  id?: string;
  /** The type of tool call. This is always going to be `function` for this type of tool call. */
  type: RunStepDeltaStepDetailsToolCallsFunctionObjectType;
  /** The definition of the function that was called. */
  function?: RunStepDeltaStepDetailsToolCallsFunctionObjectFunction;
}

/**
 * Always `tool_calls`.
 */
export type RunStepDeltaStepDetailsToolCallsObjectType = typeof RunStepDeltaStepDetailsToolCallsObjectType[keyof typeof RunStepDeltaStepDetailsToolCallsObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDeltaStepDetailsToolCallsObjectType = {
  tool_calls: 'tool_calls',
} as const;

/**
 * Details of the tool call.
 */
export interface RunStepDeltaStepDetailsToolCallsObject {
  /** Always `tool_calls`. */
  type: RunStepDeltaStepDetailsToolCallsObjectType;
  /** An array of tool calls the run step was involved in. These can be associated with one of three types of tools: `code_interpreter`, `file_search`, or `function`.
 */
  tool_calls?: RunStepDeltaStepDetailsToolCall[];
}

/**
 * Always `message_creation`.
 */
export type RunStepDetailsMessageCreationObjectType = typeof RunStepDetailsMessageCreationObjectType[keyof typeof RunStepDetailsMessageCreationObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsMessageCreationObjectType = {
  message_creation: 'message_creation',
} as const;

export type RunStepDetailsMessageCreationObjectMessageCreation = {
  /** The ID of the message that was created by this run step. */
  message_id: string;
};

/**
 * Details of the message creation by the run step.
 */
export interface RunStepDetailsMessageCreationObject {
  /** Always `message_creation`. */
  type: RunStepDetailsMessageCreationObjectType;
  message_creation: RunStepDetailsMessageCreationObjectMessageCreation;
}

/**
 * The type of tool call. This is always going to be `code_interpreter` for this type of tool call.
 */
export type RunStepDetailsToolCallsCodeObjectType = typeof RunStepDetailsToolCallsCodeObjectType[keyof typeof RunStepDetailsToolCallsCodeObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsCodeObjectType = {
  code_interpreter: 'code_interpreter',
} as const;

export type RunStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem = RunStepDetailsToolCallsCodeOutputLogsObject | RunStepDetailsToolCallsCodeOutputImageObject;

/**
 * The Code Interpreter tool call definition.
 */
export type RunStepDetailsToolCallsCodeObjectCodeInterpreter = {
  /** The input to the Code Interpreter tool call. */
  input: string;
  /** The outputs from the Code Interpreter tool call. Code Interpreter can output one or more items, including text (`logs`) or images (`image`). Each of these are represented by a different object type. */
  outputs: RunStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem[];
};

/**
 * Details of the Code Interpreter tool call the run step was involved in.
 */
export interface RunStepDetailsToolCallsCodeObject {
  /** The ID of the tool call. */
  id: string;
  /** The type of tool call. This is always going to be `code_interpreter` for this type of tool call. */
  type: RunStepDetailsToolCallsCodeObjectType;
  /** The Code Interpreter tool call definition. */
  code_interpreter: RunStepDetailsToolCallsCodeObjectCodeInterpreter;
}

/**
 * Always `image`.
 */
export type RunStepDetailsToolCallsCodeOutputImageObjectType = typeof RunStepDetailsToolCallsCodeOutputImageObjectType[keyof typeof RunStepDetailsToolCallsCodeOutputImageObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsCodeOutputImageObjectType = {
  image: 'image',
} as const;

export type RunStepDetailsToolCallsCodeOutputImageObjectImage = {
  /** The [file](https://platform.openai.com/docs/api-reference/files) ID of the image. */
  file_id: string;
};

export interface RunStepDetailsToolCallsCodeOutputImageObject {
  /** Always `image`. */
  type: RunStepDetailsToolCallsCodeOutputImageObjectType;
  image: RunStepDetailsToolCallsCodeOutputImageObjectImage;
}

/**
 * Always `logs`.
 */
export type RunStepDetailsToolCallsCodeOutputLogsObjectType = typeof RunStepDetailsToolCallsCodeOutputLogsObjectType[keyof typeof RunStepDetailsToolCallsCodeOutputLogsObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsCodeOutputLogsObjectType = {
  logs: 'logs',
} as const;

/**
 * Text output from the Code Interpreter tool call as part of a run step.
 */
export interface RunStepDetailsToolCallsCodeOutputLogsObject {
  /** Always `logs`. */
  type: RunStepDetailsToolCallsCodeOutputLogsObjectType;
  /** The text output from the Code Interpreter tool call. */
  logs: string;
}

/**
 * The type of tool call. This is always going to be `file_search` for this type of tool call.
 */
export type RunStepDetailsToolCallsFileSearchObjectType = typeof RunStepDetailsToolCallsFileSearchObjectType[keyof typeof RunStepDetailsToolCallsFileSearchObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsFileSearchObjectType = {
  file_search: 'file_search',
} as const;

/**
 * For now, this is always going to be an empty object.
 */
export type RunStepDetailsToolCallsFileSearchObjectFileSearch = {
  ranking_options?: RunStepDetailsToolCallsFileSearchRankingOptionsObject;
  /** The results of the file search. */
  results?: RunStepDetailsToolCallsFileSearchResultObject[];
};

export interface RunStepDetailsToolCallsFileSearchObject {
  /** The ID of the tool call object. */
  id: string;
  /** The type of tool call. This is always going to be `file_search` for this type of tool call. */
  type: RunStepDetailsToolCallsFileSearchObjectType;
  /** For now, this is always going to be an empty object. */
  file_search: RunStepDetailsToolCallsFileSearchObjectFileSearch;
}

/**
 * The ranking options for the file search.
 */
export interface RunStepDetailsToolCallsFileSearchRankingOptionsObject {
  ranker: FileSearchRanker;
  /**
   * The score threshold for the file search. All values must be a floating point number between 0 and 1.
   * @minimum 0
   * @maximum 1
   */
  score_threshold: number;
}

/**
 * The type of the content.
 */
export type RunStepDetailsToolCallsFileSearchResultObjectContentItemType = typeof RunStepDetailsToolCallsFileSearchResultObjectContentItemType[keyof typeof RunStepDetailsToolCallsFileSearchResultObjectContentItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsFileSearchResultObjectContentItemType = {
  text: 'text',
} as const;

export type RunStepDetailsToolCallsFileSearchResultObjectContentItem = {
  /** The type of the content. */
  type?: RunStepDetailsToolCallsFileSearchResultObjectContentItemType;
  /** The text content of the file. */
  text?: string;
};

/**
 * A result instance of the file search.
 */
export interface RunStepDetailsToolCallsFileSearchResultObject {
  /** The ID of the file that result was found in. */
  file_id: string;
  /** The name of the file that result was found in. */
  file_name: string;
  /**
   * The score of the result. All values must be a floating point number between 0 and 1.
   * @minimum 0
   * @maximum 1
   */
  score: number;
  /** The content of the result that was found. The content is only included if requested via the include query parameter. */
  content?: RunStepDetailsToolCallsFileSearchResultObjectContentItem[];
}

/**
 * The type of tool call. This is always going to be `function` for this type of tool call.
 */
export type RunStepDetailsToolCallsFunctionObjectType = typeof RunStepDetailsToolCallsFunctionObjectType[keyof typeof RunStepDetailsToolCallsFunctionObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsFunctionObjectType = {
  function: 'function',
} as const;

export type RunStepDetailsToolCallsFunctionObjectFunctionOutput = string | null;

/**
 * The definition of the function that was called.
 */
export type RunStepDetailsToolCallsFunctionObjectFunction = {
  /** The name of the function. */
  name: string;
  /** The arguments passed to the function. */
  arguments: string;
  output: RunStepDetailsToolCallsFunctionObjectFunctionOutput;
};

export interface RunStepDetailsToolCallsFunctionObject {
  /** The ID of the tool call object. */
  id: string;
  /** The type of tool call. This is always going to be `function` for this type of tool call. */
  type: RunStepDetailsToolCallsFunctionObjectType;
  /** The definition of the function that was called. */
  function: RunStepDetailsToolCallsFunctionObjectFunction;
}

/**
 * Always `tool_calls`.
 */
export type RunStepDetailsToolCallsObjectType = typeof RunStepDetailsToolCallsObjectType[keyof typeof RunStepDetailsToolCallsObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepDetailsToolCallsObjectType = {
  tool_calls: 'tool_calls',
} as const;

/**
 * Details of the tool call.
 */
export interface RunStepDetailsToolCallsObject {
  /** Always `tool_calls`. */
  type: RunStepDetailsToolCallsObjectType;
  /** An array of tool calls the run step was involved in. These can be associated with one of three types of tools: `code_interpreter`, `file_search`, or `function`.
 */
  tool_calls: RunStepDetailsToolCall[];
}

/**
 * The object type, which is always `thread.run.step`.
 */
export type RunStepObjectObject = typeof RunStepObjectObject[keyof typeof RunStepObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepObjectObject = {
  threadrunstep: 'thread.run.step',
} as const;

/**
 * The type of run step, which can be either `message_creation` or `tool_calls`.
 */
export type RunStepObjectType = typeof RunStepObjectType[keyof typeof RunStepObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepObjectType = {
  message_creation: 'message_creation',
  tool_calls: 'tool_calls',
} as const;

/**
 * The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`.
 */
export type RunStepObjectStatus = typeof RunStepObjectStatus[keyof typeof RunStepObjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepObjectStatus = {
  in_progress: 'in_progress',
  cancelled: 'cancelled',
  failed: 'failed',
  completed: 'completed',
  expired: 'expired',
} as const;

/**
 * The details of the run step.
 */
export type RunStepObjectStepDetails = RunStepDetailsMessageCreationObject | RunStepDetailsToolCallsObject;

/**
 * One of `server_error` or `rate_limit_exceeded`.
 */
export type RunStepObjectLastErrorAnyOfCode = typeof RunStepObjectLastErrorAnyOfCode[keyof typeof RunStepObjectLastErrorAnyOfCode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepObjectLastErrorAnyOfCode = {
  server_error: 'server_error',
  rate_limit_exceeded: 'rate_limit_exceeded',
} as const;

/**
 * The last error associated with this run step. Will be `null` if there are no errors.
 */
export type RunStepObjectLastErrorAnyOf = {
  /** One of `server_error` or `rate_limit_exceeded`. */
  code: RunStepObjectLastErrorAnyOfCode;
  /** A human-readable description of the error. */
  message: string;
};

export type RunStepObjectLastError = RunStepObjectLastErrorAnyOf | null;

export type RunStepObjectExpiredAt = number | null;

export type RunStepObjectCancelledAt = number | null;

export type RunStepObjectFailedAt = number | null;

export type RunStepObjectCompletedAt = number | null;

/**
 * Represents a step in execution of a run.

 */
export interface RunStepObject {
  /** The identifier of the run step, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.run.step`. */
  object: RunStepObjectObject;
  /** The Unix timestamp (in seconds) for when the run step was created. */
  created_at: number;
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) associated with the run step. */
  assistant_id: string;
  /** The ID of the [thread](https://platform.openai.com/docs/api-reference/threads) that was run. */
  thread_id: string;
  /** The ID of the [run](https://platform.openai.com/docs/api-reference/runs) that this run step is a part of. */
  run_id: string;
  /** The type of run step, which can be either `message_creation` or `tool_calls`. */
  type: RunStepObjectType;
  /** The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`. */
  status: RunStepObjectStatus;
  /** The details of the run step. */
  step_details: RunStepObjectStepDetails;
  last_error: RunStepObjectLastError;
  expired_at: RunStepObjectExpiredAt;
  cancelled_at: RunStepObjectCancelledAt;
  failed_at: RunStepObjectFailedAt;
  completed_at: RunStepObjectCompletedAt;
  metadata: Metadata;
  usage: RunStepCompletionUsage;
}

export type RunStepStreamEventAnyOfEvent = typeof RunStepStreamEventAnyOfEvent[keyof typeof RunStepStreamEventAnyOfEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfEvent = {
  threadrunstepcreated: 'thread.run.step.created',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) is created.
 */
export type RunStepStreamEventAnyOf = {
  event: RunStepStreamEventAnyOfEvent;
  data: RunStepObject;
};

export type RunStepStreamEventAnyOfThreeEvent = typeof RunStepStreamEventAnyOfThreeEvent[keyof typeof RunStepStreamEventAnyOfThreeEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfThreeEvent = {
  threadrunstepin_progress: 'thread.run.step.in_progress',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) moves to an `in_progress` state.
 */
export type RunStepStreamEventAnyOfThree = {
  event: RunStepStreamEventAnyOfThreeEvent;
  data: RunStepObject;
};

export type RunStepStreamEventAnyOfFiveEvent = typeof RunStepStreamEventAnyOfFiveEvent[keyof typeof RunStepStreamEventAnyOfFiveEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfFiveEvent = {
  threadrunstepdelta: 'thread.run.step.delta',
} as const;

/**
 * Occurs when parts of a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) are being streamed.
 */
export type RunStepStreamEventAnyOfFive = {
  event: RunStepStreamEventAnyOfFiveEvent;
  data: RunStepDeltaObject;
};

export type RunStepStreamEventAnyOfSevenEvent = typeof RunStepStreamEventAnyOfSevenEvent[keyof typeof RunStepStreamEventAnyOfSevenEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfSevenEvent = {
  threadrunstepcompleted: 'thread.run.step.completed',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) is completed.
 */
export type RunStepStreamEventAnyOfSeven = {
  event: RunStepStreamEventAnyOfSevenEvent;
  data: RunStepObject;
};

export type RunStepStreamEventAnyOfNineEvent = typeof RunStepStreamEventAnyOfNineEvent[keyof typeof RunStepStreamEventAnyOfNineEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfNineEvent = {
  threadrunstepfailed: 'thread.run.step.failed',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) fails.
 */
export type RunStepStreamEventAnyOfNine = {
  event: RunStepStreamEventAnyOfNineEvent;
  data: RunStepObject;
};

export type RunStepStreamEventAnyOfOneoneEvent = typeof RunStepStreamEventAnyOfOneoneEvent[keyof typeof RunStepStreamEventAnyOfOneoneEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfOneoneEvent = {
  threadrunstepcancelled: 'thread.run.step.cancelled',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) is cancelled.
 */
export type RunStepStreamEventAnyOfOneone = {
  event: RunStepStreamEventAnyOfOneoneEvent;
  data: RunStepObject;
};

export type RunStepStreamEventAnyOfOnethreeEvent = typeof RunStepStreamEventAnyOfOnethreeEvent[keyof typeof RunStepStreamEventAnyOfOnethreeEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStepStreamEventAnyOfOnethreeEvent = {
  threadrunstepexpired: 'thread.run.step.expired',
} as const;

/**
 * Occurs when a [run step](https://platform.openai.com/docs/api-reference/run-steps/step-object) expires.
 */
export type RunStepStreamEventAnyOfOnethree = {
  event: RunStepStreamEventAnyOfOnethreeEvent;
  data: RunStepObject;
};

export type RunStepStreamEvent = RunStepStreamEventAnyOf | RunStepStreamEventAnyOfThree | RunStepStreamEventAnyOfFive | RunStepStreamEventAnyOfSeven | RunStepStreamEventAnyOfNine | RunStepStreamEventAnyOfOneone | RunStepStreamEventAnyOfOnethree;

export type RunStreamEventAnyOfEvent = typeof RunStreamEventAnyOfEvent[keyof typeof RunStreamEventAnyOfEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfEvent = {
  threadruncreated: 'thread.run.created',
} as const;

/**
 * Occurs when a new [run](https://platform.openai.com/docs/api-reference/runs/object) is created.
 */
export type RunStreamEventAnyOf = {
  event: RunStreamEventAnyOfEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfThreeEvent = typeof RunStreamEventAnyOfThreeEvent[keyof typeof RunStreamEventAnyOfThreeEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfThreeEvent = {
  threadrunqueued: 'thread.run.queued',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) moves to a `queued` status.
 */
export type RunStreamEventAnyOfThree = {
  event: RunStreamEventAnyOfThreeEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfFiveEvent = typeof RunStreamEventAnyOfFiveEvent[keyof typeof RunStreamEventAnyOfFiveEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfFiveEvent = {
  threadrunin_progress: 'thread.run.in_progress',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) moves to an `in_progress` status.
 */
export type RunStreamEventAnyOfFive = {
  event: RunStreamEventAnyOfFiveEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfSevenEvent = typeof RunStreamEventAnyOfSevenEvent[keyof typeof RunStreamEventAnyOfSevenEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfSevenEvent = {
  threadrunrequires_action: 'thread.run.requires_action',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) moves to a `requires_action` status.
 */
export type RunStreamEventAnyOfSeven = {
  event: RunStreamEventAnyOfSevenEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfNineEvent = typeof RunStreamEventAnyOfNineEvent[keyof typeof RunStreamEventAnyOfNineEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfNineEvent = {
  threadruncompleted: 'thread.run.completed',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) is completed.
 */
export type RunStreamEventAnyOfNine = {
  event: RunStreamEventAnyOfNineEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfOneoneEvent = typeof RunStreamEventAnyOfOneoneEvent[keyof typeof RunStreamEventAnyOfOneoneEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfOneoneEvent = {
  threadrunincomplete: 'thread.run.incomplete',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) ends with status `incomplete`.
 */
export type RunStreamEventAnyOfOneone = {
  event: RunStreamEventAnyOfOneoneEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfOnethreeEvent = typeof RunStreamEventAnyOfOnethreeEvent[keyof typeof RunStreamEventAnyOfOnethreeEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfOnethreeEvent = {
  threadrunfailed: 'thread.run.failed',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) fails.
 */
export type RunStreamEventAnyOfOnethree = {
  event: RunStreamEventAnyOfOnethreeEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfOnefiveEvent = typeof RunStreamEventAnyOfOnefiveEvent[keyof typeof RunStreamEventAnyOfOnefiveEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfOnefiveEvent = {
  threadruncancelling: 'thread.run.cancelling',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) moves to a `cancelling` status.
 */
export type RunStreamEventAnyOfOnefive = {
  event: RunStreamEventAnyOfOnefiveEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfOnesevenEvent = typeof RunStreamEventAnyOfOnesevenEvent[keyof typeof RunStreamEventAnyOfOnesevenEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfOnesevenEvent = {
  threadruncancelled: 'thread.run.cancelled',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) is cancelled.
 */
export type RunStreamEventAnyOfOneseven = {
  event: RunStreamEventAnyOfOnesevenEvent;
  data: RunObject;
};

export type RunStreamEventAnyOfOnenineEvent = typeof RunStreamEventAnyOfOnenineEvent[keyof typeof RunStreamEventAnyOfOnenineEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStreamEventAnyOfOnenineEvent = {
  threadrunexpired: 'thread.run.expired',
} as const;

/**
 * Occurs when a [run](https://platform.openai.com/docs/api-reference/runs/object) expires.
 */
export type RunStreamEventAnyOfOnenine = {
  event: RunStreamEventAnyOfOnenineEvent;
  data: RunObject;
};

export type RunStreamEvent = RunStreamEventAnyOf | RunStreamEventAnyOfThree | RunStreamEventAnyOfFive | RunStreamEventAnyOfSeven | RunStreamEventAnyOfNine | RunStreamEventAnyOfOneone | RunStreamEventAnyOfOnethree | RunStreamEventAnyOfOnefive | RunStreamEventAnyOfOneseven | RunStreamEventAnyOfOnenine;

/**
 * The type of tool call the output is required for. For now, this is always `function`.
 */
export type RunToolCallObjectType = typeof RunToolCallObjectType[keyof typeof RunToolCallObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunToolCallObjectType = {
  function: 'function',
} as const;

/**
 * The function definition.
 */
export type RunToolCallObjectFunction = {
  /** The name of the function. */
  name: string;
  /** The arguments that the model expects you to pass to the function. */
  arguments: string;
};

/**
 * Tool call objects
 */
export interface RunToolCallObject {
  /** The ID of the tool call. This ID must be referenced when you submit the tool outputs in using the [Submit tool outputs to run](https://platform.openai.com/docs/api-reference/runs/submitToolOutputs) endpoint. */
  id: string;
  /** The type of tool call the output is required for. For now, this is always `function`. */
  type: RunToolCallObjectType;
  /** The function definition. */
  function: RunToolCallObjectFunction;
}

/**
 * Specifies the event type. For a screenshot action, this property is
always set to `screenshot`.

 */
export type ScreenshotType = typeof ScreenshotType[keyof typeof ScreenshotType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ScreenshotType = {
  screenshot: 'screenshot',
} as const;

/**
 * A screenshot action.

 */
export interface Screenshot {
  /** Specifies the event type. For a screenshot action, this property is
always set to `screenshot`.
 */
  type: ScreenshotType;
}

/**
 * Specifies the event type. For a scroll action, this property is
always set to `scroll`.

 */
export type ScrollType = typeof ScrollType[keyof typeof ScrollType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ScrollType = {
  scroll: 'scroll',
} as const;

/**
 * A scroll action.

 */
export interface Scroll {
  /** Specifies the event type. For a scroll action, this property is
always set to `scroll`.
 */
  type: ScrollType;
  /** The x-coordinate where the scroll occurred.
 */
  x: number;
  /** The y-coordinate where the scroll occurred.
 */
  y: number;
  /** The horizontal scroll distance.
 */
  scroll_x: number;
  /** The vertical scroll distance.
 */
  scroll_y: number;
}

export type ServiceTier = 'auto' | 'default' | 'flex' | 'scale' | 'priority' | null;

/**
 * The type of the event. Always `speech.audio.delta`.

 */
export type SpeechAudioDeltaEventType = typeof SpeechAudioDeltaEventType[keyof typeof SpeechAudioDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpeechAudioDeltaEventType = {
  speechaudiodelta: 'speech.audio.delta',
} as const;

/**
 * Emitted for each chunk of audio data generated during speech synthesis.
 */
export interface SpeechAudioDeltaEvent {
  /** The type of the event. Always `speech.audio.delta`.
 */
  type: SpeechAudioDeltaEventType;
  /** A chunk of Base64-encoded audio data.
 */
  audio: string;
}

/**
 * The type of the event. Always `speech.audio.done`.

 */
export type SpeechAudioDoneEventType = typeof SpeechAudioDoneEventType[keyof typeof SpeechAudioDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpeechAudioDoneEventType = {
  speechaudiodone: 'speech.audio.done',
} as const;

/**
 * Token usage statistics for the request.

 */
export type SpeechAudioDoneEventUsage = {
  /** Number of input tokens in the prompt. */
  input_tokens: number;
  /** Number of output tokens generated. */
  output_tokens: number;
  /** Total number of tokens used (input + output). */
  total_tokens: number;
};

/**
 * Emitted when the speech synthesis is complete and all audio has been streamed.
 */
export interface SpeechAudioDoneEvent {
  /** The type of the event. Always `speech.audio.done`.
 */
  type: SpeechAudioDoneEventType;
  /** Token usage statistics for the request.
 */
  usage: SpeechAudioDoneEventUsage;
}

export interface StaticChunkingStrategy {
  /**
   * The maximum number of tokens in each chunk. The default value is `800`. The minimum value is `100` and the maximum value is `4096`.
   * @minimum 100
   * @maximum 4096
   */
  max_chunk_size_tokens: number;
  /** The number of tokens that overlap between chunks. The default value is `400`.

Note that the overlap must not exceed half of `max_chunk_size_tokens`.
 */
  chunk_overlap_tokens: number;
}

/**
 * Always `static`.
 */
export type StaticChunkingStrategyRequestParamType = typeof StaticChunkingStrategyRequestParamType[keyof typeof StaticChunkingStrategyRequestParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StaticChunkingStrategyRequestParamType = {
  static: 'static',
} as const;

/**
 * Customize your own chunking strategy by setting chunk size and chunk overlap.
 */
export interface StaticChunkingStrategyRequestParam {
  /** Always `static`. */
  type: StaticChunkingStrategyRequestParamType;
  static: StaticChunkingStrategy;
}

/**
 * Always `static`.
 */
export type StaticChunkingStrategyResponseParamType = typeof StaticChunkingStrategyResponseParamType[keyof typeof StaticChunkingStrategyResponseParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StaticChunkingStrategyResponseParamType = {
  static: 'static',
} as const;

export interface StaticChunkingStrategyResponseParam {
  /** Always `static`. */
  type: StaticChunkingStrategyResponseParamType;
  static: StaticChunkingStrategy;
}

/**
 * Not supported with latest reasoning models `o3` and `o4-mini`.

Up to 4 sequences where the API will stop generating further tokens. The
returned text will not contain the stop sequence.

 * @nullable
 */
export type StopConfiguration = string | null | string[] | null;

export type SubmitToolOutputsRunRequestToolOutputsItem = {
  /** The ID of the tool call in the `required_action` object within the run object the output is being submitted for. */
  tool_call_id?: string;
  /** The output of the tool call to be submitted to continue the run. */
  output?: string;
};

export type SubmitToolOutputsRunRequestStream = boolean | null;

export interface SubmitToolOutputsRunRequest {
  /** A list of tools for which the outputs are being submitted. */
  tool_outputs: SubmitToolOutputsRunRequestToolOutputsItem[];
  stream?: SubmitToolOutputsRunRequestStream;
}

/**
 * An object specifying the format that the model must output.

Configuring `{ "type": "json_schema" }` enables Structured Outputs,
which ensures the model will match your supplied JSON schema. Learn more in the
[Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).

The default format is `{ "type": "text" }` with no additional options.

**Not recommended for gpt-4o and newer models:**

Setting to `{ "type": "json_object" }` enables the older JSON mode, which
ensures the message the model generates is valid JSON. Using `json_schema`
is preferred for models that support it.

 */
export type TextResponseFormatConfiguration = ResponseFormatText | TextResponseFormatJsonSchema | ResponseFormatJsonObject;

/**
 * The type of response format being defined. Always `json_schema`.
 */
export type TextResponseFormatJsonSchemaType = typeof TextResponseFormatJsonSchemaType[keyof typeof TextResponseFormatJsonSchemaType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TextResponseFormatJsonSchemaType = {
  json_schema: 'json_schema',
} as const;

export type TextResponseFormatJsonSchemaStrict = boolean | null;

/**
 * JSON Schema response format. Used to generate structured JSON responses.
Learn more about [Structured Outputs](https://platform.openai.com/docs/guides/structured-outputs).

 */
export interface TextResponseFormatJsonSchema {
  /** The type of response format being defined. Always `json_schema`. */
  type: TextResponseFormatJsonSchemaType;
  /** A description of what the response format is for, used by the model to
determine how to respond in the format.
 */
  description?: string;
  /** The name of the response format. Must be a-z, A-Z, 0-9, or contain
underscores and dashes, with a maximum length of 64.
 */
  name: string;
  schema: ResponseFormatJsonSchemaSchema;
  strict?: TextResponseFormatJsonSchemaStrict;
}

/**
 * The object type, which is always `thread`.
 */
export type ThreadObjectObject = typeof ThreadObjectObject[keyof typeof ThreadObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ThreadObjectObject = {
  thread: 'thread',
} as const;

export type ThreadObjectToolResourcesAnyOfCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type ThreadObjectToolResourcesAnyOfFileSearch = {
  /**
   * The [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 */
export type ThreadObjectToolResourcesAnyOf = {
  code_interpreter?: ThreadObjectToolResourcesAnyOfCodeInterpreter;
  file_search?: ThreadObjectToolResourcesAnyOfFileSearch;
};

export type ThreadObjectToolResources = ThreadObjectToolResourcesAnyOf | null;

/**
 * Represents a thread that contains [messages](https://platform.openai.com/docs/api-reference/messages).
 */
export interface ThreadObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread`. */
  object: ThreadObjectObject;
  /** The Unix timestamp (in seconds) for when the thread was created. */
  created_at: number;
  tool_resources: ThreadObjectToolResources;
  metadata: Metadata;
}

export type ThreadStreamEventAnyOfEvent = typeof ThreadStreamEventAnyOfEvent[keyof typeof ThreadStreamEventAnyOfEvent];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ThreadStreamEventAnyOfEvent = {
  threadcreated: 'thread.created',
} as const;

/**
 * Occurs when a new [thread](https://platform.openai.com/docs/api-reference/threads/object) is created.
 */
export type ThreadStreamEventAnyOf = {
  /** Whether to enable input audio transcription. */
  enabled?: boolean;
  event: ThreadStreamEventAnyOfEvent;
  data: ThreadObject;
};

export type ThreadStreamEvent = ThreadStreamEventAnyOf;

export interface ToggleCertificatesRequest {
  /**
   * @minItems 1
   * @maxItems 10
   */
  certificate_ids: string[];
}

/**
 * A tool that can be used to generate a response.

 */
export type Tool = FunctionTool | FileSearchTool | ComputerUsePreviewTool | WebSearchTool | MCPTool | CodeInterpreterTool | ImageGenTool | LocalShellToolParam | CustomToolParam | WebSearchPreviewTool;

/**
 * Allowed tool configuration type. Always `allowed_tools`.
 */
export type ToolChoiceAllowedType = typeof ToolChoiceAllowedType[keyof typeof ToolChoiceAllowedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceAllowedType = {
  allowed_tools: 'allowed_tools',
} as const;

/**
 * Constrains the tools available to the model to a pre-defined set.

`auto` allows the model to pick from among the allowed tools and generate a
message.

`required` requires the model to call one or more of the allowed tools.

 */
export type ToolChoiceAllowedMode = typeof ToolChoiceAllowedMode[keyof typeof ToolChoiceAllowedMode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceAllowedMode = {
  auto: 'auto',
  required: 'required',
} as const;

/**
 * A tool definition that the model should be allowed to call.

 */
export type ToolChoiceAllowedToolsItem = { [key: string]: unknown };

/**
 * Constrains the tools available to the model to a pre-defined set.

 */
export interface ToolChoiceAllowed {
  /** Allowed tool configuration type. Always `allowed_tools`. */
  type: ToolChoiceAllowedType;
  /** Constrains the tools available to the model to a pre-defined set.

`auto` allows the model to pick from among the allowed tools and generate a
message.

`required` requires the model to call one or more of the allowed tools.
 */
  mode: ToolChoiceAllowedMode;
  /** A list of tool definitions that the model should be allowed to call.

For the Responses API, the list of tool definitions might look like:
```json
[
  { "type": "function", "name": "get_weather" },
  { "type": "mcp", "server_label": "deepwiki" },
  { "type": "image_generation" }
]
```
 */
  tools: ToolChoiceAllowedToolsItem[];
}

/**
 * For custom tool calling, the type is always `custom`.
 */
export type ToolChoiceCustomType = typeof ToolChoiceCustomType[keyof typeof ToolChoiceCustomType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceCustomType = {
  custom: 'custom',
} as const;

/**
 * Use this option to force the model to call a specific custom tool.

 */
export interface ToolChoiceCustom {
  /** For custom tool calling, the type is always `custom`. */
  type: ToolChoiceCustomType;
  /** The name of the custom tool to call. */
  name: string;
}

/**
 * For function calling, the type is always `function`.
 */
export type ToolChoiceFunctionType = typeof ToolChoiceFunctionType[keyof typeof ToolChoiceFunctionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceFunctionType = {
  function: 'function',
} as const;

/**
 * Use this option to force the model to call a specific function.

 */
export interface ToolChoiceFunction {
  /** For function calling, the type is always `function`. */
  type: ToolChoiceFunctionType;
  /** The name of the function to call. */
  name: string;
}

/**
 * For MCP tools, the type is always `mcp`.
 */
export type ToolChoiceMCPType = typeof ToolChoiceMCPType[keyof typeof ToolChoiceMCPType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceMCPType = {
  mcp: 'mcp',
} as const;

export type ToolChoiceMCPName = string | null;

/**
 * Use this option to force the model to call a specific tool on a remote MCP server.

 */
export interface ToolChoiceMCP {
  /** For MCP tools, the type is always `mcp`. */
  type: ToolChoiceMCPType;
  /** The label of the MCP server to use.
 */
  server_label: string;
  name?: ToolChoiceMCPName;
}

/**
 * Controls which (if any) tool is called by the model.

`none` means the model will not call any tool and instead generates a message.

`auto` means the model can pick between generating a message or calling one or
more tools.

`required` means the model must call one or more tools.

 */
export type ToolChoiceOptions = typeof ToolChoiceOptions[keyof typeof ToolChoiceOptions];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceOptions = {
  none: 'none',
  auto: 'auto',
  required: 'required',
} as const;

/**
 * How the model should select which tool (or tools) to use when generating
a response. See the `tools` parameter to see how to specify which tools
the model can call.

 */
export type ToolChoiceParam = ToolChoiceOptions | ToolChoiceAllowed | ToolChoiceTypes | ToolChoiceFunction | ToolChoiceMCP | ToolChoiceCustom;

/**
 * The type of hosted tool the model should to use. Learn more about
[built-in tools](https://platform.openai.com/docs/guides/tools).

Allowed values are:
- `file_search`
- `web_search_preview`
- `computer_use_preview`
- `code_interpreter`
- `image_generation`

 */
export type ToolChoiceTypesType = typeof ToolChoiceTypesType[keyof typeof ToolChoiceTypesType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ToolChoiceTypesType = {
  file_search: 'file_search',
  web_search_preview: 'web_search_preview',
  computer_use_preview: 'computer_use_preview',
  web_search_preview_2025_03_11: 'web_search_preview_2025_03_11',
  image_generation: 'image_generation',
  code_interpreter: 'code_interpreter',
} as const;

/**
 * Indicates that the model should use a built-in tool to generate a response.
[Learn more about built-in tools](https://platform.openai.com/docs/guides/tools).

 */
export interface ToolChoiceTypes {
  /** The type of hosted tool the model should to use. Learn more about
[built-in tools](https://platform.openai.com/docs/guides/tools).

Allowed values are:
- `file_search`
- `web_search_preview`
- `computer_use_preview`
- `code_interpreter`
- `image_generation`
 */
  type: ToolChoiceTypesType;
}

/**
 * An array of tools the model may call while generating a response. You
can specify which tool to use by setting the `tool_choice` parameter.

We support the following categories of tools:
- **Built-in tools**: Tools that are provided by OpenAI that extend the
  model's capabilities, like [web search](https://platform.openai.com/docs/guides/tools-web-search)
  or [file search](https://platform.openai.com/docs/guides/tools-file-search). Learn more about
  [built-in tools](https://platform.openai.com/docs/guides/tools).
- **MCP Tools**: Integrations with third-party systems via custom MCP servers
  or predefined connectors such as Google Drive and SharePoint. Learn more about
  [MCP Tools](https://platform.openai.com/docs/guides/tools-connectors-mcp).
- **Function calls (custom tools)**: Functions that are defined by you,
  enabling the model to call your own code with strongly typed arguments
  and outputs. Learn more about
  [function calling](https://platform.openai.com/docs/guides/function-calling). You can also use
  custom tools to call your own code.

 */
export type ToolsArray = Tool[];

/**
 * The type of the event. Always `transcript.text.delta`.

 */
export type TranscriptTextDeltaEventType = typeof TranscriptTextDeltaEventType[keyof typeof TranscriptTextDeltaEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptTextDeltaEventType = {
  transcripttextdelta: 'transcript.text.delta',
} as const;

export type TranscriptTextDeltaEventLogprobsItem = {
  /** The token that was used to generate the log probability.
 */
  token?: string;
  /** The log probability of the token.
 */
  logprob?: number;
  /** The bytes that were used to generate the log probability.
 */
  bytes?: number[];
};

/**
 * Emitted when there is an additional text delta. This is also the first event emitted when the transcription starts. Only emitted when you [create a transcription](https://platform.openai.com/docs/api-reference/audio/create-transcription) with the `Stream` parameter set to `true`.
 */
export interface TranscriptTextDeltaEvent {
  /** The type of the event. Always `transcript.text.delta`.
 */
  type: TranscriptTextDeltaEventType;
  /** The text delta that was additionally transcribed.
 */
  delta: string;
  /** The log probabilities of the delta. Only included if you [create a transcription](https://platform.openai.com/docs/api-reference/audio/create-transcription) with the `include[]` parameter set to `logprobs`.
 */
  logprobs?: TranscriptTextDeltaEventLogprobsItem[];
  /** Identifier of the diarized segment that this delta belongs to. Only present when using `gpt-4o-transcribe-diarize`.
 */
  segment_id?: string;
}

/**
 * The type of the event. Always `transcript.text.done`.

 */
export type TranscriptTextDoneEventType = typeof TranscriptTextDoneEventType[keyof typeof TranscriptTextDoneEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptTextDoneEventType = {
  transcripttextdone: 'transcript.text.done',
} as const;

export type TranscriptTextDoneEventLogprobsItem = {
  /** The token that was used to generate the log probability.
 */
  token?: string;
  /** The log probability of the token.
 */
  logprob?: number;
  /** The bytes that were used to generate the log probability.
 */
  bytes?: number[];
};

/**
 * Emitted when the transcription is complete. Contains the complete transcription text. Only emitted when you [create a transcription](https://platform.openai.com/docs/api-reference/audio/create-transcription) with the `Stream` parameter set to `true`.
 */
export interface TranscriptTextDoneEvent {
  /** The type of the event. Always `transcript.text.done`.
 */
  type: TranscriptTextDoneEventType;
  /** The text that was transcribed.
 */
  text: string;
  /** The log probabilities of the individual tokens in the transcription. Only included if you [create a transcription](https://platform.openai.com/docs/api-reference/audio/create-transcription) with the `include[]` parameter set to `logprobs`.
 */
  logprobs?: TranscriptTextDoneEventLogprobsItem[];
  usage?: TranscriptTextUsageTokens;
}

/**
 * The type of the event. Always `transcript.text.segment`.
 */
export type TranscriptTextSegmentEventType = typeof TranscriptTextSegmentEventType[keyof typeof TranscriptTextSegmentEventType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptTextSegmentEventType = {
  transcripttextsegment: 'transcript.text.segment',
} as const;

/**
 * Emitted when a diarized transcription returns a completed segment with speaker information. Only emitted when you [create a transcription](https://platform.openai.com/docs/api-reference/audio/create-transcription) with `stream` set to `true` and `response_format` set to `diarized_json`.

 */
export interface TranscriptTextSegmentEvent {
  /** The type of the event. Always `transcript.text.segment`. */
  type: TranscriptTextSegmentEventType;
  /** Unique identifier for the segment. */
  id: string;
  /** Start timestamp of the segment in seconds. */
  start: number;
  /** End timestamp of the segment in seconds. */
  end: number;
  /** Transcript text for this segment. */
  text: string;
  /** Speaker label for this segment. */
  speaker: string;
}

/**
 * The type of the usage object. Always `duration` for this variant.
 */
export type TranscriptTextUsageDurationType = typeof TranscriptTextUsageDurationType[keyof typeof TranscriptTextUsageDurationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptTextUsageDurationType = {
  duration: 'duration',
} as const;

/**
 * Usage statistics for models billed by audio input duration.
 */
export interface TranscriptTextUsageDuration {
  /** The type of the usage object. Always `duration` for this variant. */
  type: TranscriptTextUsageDurationType;
  /** Duration of the input audio in seconds. */
  seconds: number;
}

/**
 * The type of the usage object. Always `tokens` for this variant.
 */
export type TranscriptTextUsageTokensType = typeof TranscriptTextUsageTokensType[keyof typeof TranscriptTextUsageTokensType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptTextUsageTokensType = {
  tokens: 'tokens',
} as const;

/**
 * Details about the input tokens billed for this request.
 */
export type TranscriptTextUsageTokensInputTokenDetails = {
  /** Number of text tokens billed for this request. */
  text_tokens?: number;
  /** Number of audio tokens billed for this request. */
  audio_tokens?: number;
};

/**
 * Usage statistics for models billed by token usage.
 */
export interface TranscriptTextUsageTokens {
  /** The type of the usage object. Always `tokens` for this variant. */
  type: TranscriptTextUsageTokensType;
  /** Number of input tokens billed for this request. */
  input_tokens: number;
  /** Details about the input tokens billed for this request. */
  input_token_details?: TranscriptTextUsageTokensInputTokenDetails;
  /** Number of output tokens generated. */
  output_tokens: number;
  /** Total number of tokens used (input + output). */
  total_tokens: number;
}

/**
 * Controls how the audio is cut into chunks. When set to `"auto"`, the server first normalizes loudness and then uses voice activity detection (VAD) to choose boundaries. `server_vad` object can be provided to tweak VAD detection parameters manually. If unset, the audio is transcribed as a single block. Required when using `gpt-4o-transcribe-diarize` for inputs longer than 30 seconds.
 */
export type TranscriptionChunkingStrategyAnyOf = 'auto' | VadConfig;

export type TranscriptionChunkingStrategy = TranscriptionChunkingStrategyAnyOf | null;

/**
 * The type of the segment. Always `transcript.text.segment`.

 */
export type TranscriptionDiarizedSegmentType = typeof TranscriptionDiarizedSegmentType[keyof typeof TranscriptionDiarizedSegmentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptionDiarizedSegmentType = {
  transcripttextsegment: 'transcript.text.segment',
} as const;

/**
 * A segment of diarized transcript text with speaker metadata.
 */
export interface TranscriptionDiarizedSegment {
  /** The type of the segment. Always `transcript.text.segment`.
 */
  type: TranscriptionDiarizedSegmentType;
  /** Unique identifier for the segment. */
  id: string;
  /** Start timestamp of the segment in seconds. */
  start: number;
  /** End timestamp of the segment in seconds. */
  end: number;
  /** Transcript text for this segment. */
  text: string;
  /** Speaker label for this segment. When known speakers are provided, the label matches `known_speaker_names[]`. Otherwise speakers are labeled sequentially using capital letters (`A`, `B`, ...).
 */
  speaker: string;
}

export type TranscriptionInclude = typeof TranscriptionInclude[keyof typeof TranscriptionInclude];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TranscriptionInclude = {
  logprobs: 'logprobs',
} as const;

export interface TranscriptionSegment {
  /** Unique identifier of the segment. */
  id: number;
  /** Seek offset of the segment. */
  seek: number;
  /** Start time of the segment in seconds. */
  start: number;
  /** End time of the segment in seconds. */
  end: number;
  /** Text content of the segment. */
  text: string;
  /** Array of token IDs for the text content. */
  tokens: number[];
  /** Temperature parameter used for generating the segment. */
  temperature: number;
  /** Average logprob of the segment. If the value is lower than -1, consider the logprobs failed. */
  avg_logprob: number;
  /** Compression ratio of the segment. If the value is greater than 2.4, consider the compression failed. */
  compression_ratio: number;
  /** Probability of no speech in the segment. If the value is higher than 1.0 and the `avg_logprob` is below -1, consider this segment silent. */
  no_speech_prob: number;
}

export interface TranscriptionWord {
  /** The text content of the word. */
  word: string;
  /** Start time of the word in seconds. */
  start: number;
  /** End time of the word in seconds. */
  end: number;
}

/**
 * The truncation strategy to use for the thread. The default is `auto`. If set to `last_messages`, the thread will be truncated to the n most recent messages in the thread. When set to `auto`, messages in the middle of the thread will be dropped to fit the context length of the model, `max_prompt_tokens`.
 */
export type TruncationObjectType = typeof TruncationObjectType[keyof typeof TruncationObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TruncationObjectType = {
  auto: 'auto',
  last_messages: 'last_messages',
} as const;

export type TruncationObjectLastMessages = number | null;

/**
 * Controls for how a thread will be truncated prior to the run. Use this to control the initial context window of the run.
 */
export interface TruncationObject {
  /** The truncation strategy to use for the thread. The default is `auto`. If set to `last_messages`, the thread will be truncated to the n most recent messages in the thread. When set to `auto`, messages in the middle of the thread will be dropped to fit the context length of the model, `max_prompt_tokens`. */
  type: TruncationObjectType;
  last_messages?: TruncationObjectLastMessages;
}

/**
 * Specifies the event type. For a type action, this property is
always set to `type`.

 */
export type TypeType = typeof TypeType[keyof typeof TypeType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TypeType = {
  type: 'type',
} as const;

/**
 * An action to type in text.

 */
export interface Type {
  /** Specifies the event type. For a type action, this property is
always set to `type`.
 */
  type: TypeType;
  /** The text to type.
 */
  text: string;
}

export interface UpdateVectorStoreFileAttributesRequest {
  attributes: VectorStoreFileAttributes;
}

/**
 * @nullable
 */
export type UpdateVectorStoreRequestExpiresAfterAllOf = unknown | null;

export type UpdateVectorStoreRequestExpiresAfter = VectorStoreExpirationAfter & UpdateVectorStoreRequestExpiresAfterAllOf;

export interface UpdateVectorStoreRequest {
  /**
   * The name of the vector store.
   * @nullable
   */
  name?: string | null;
  expires_after?: UpdateVectorStoreRequestExpiresAfter;
  metadata?: Metadata;
}

/**
 * The status of the Upload.
 */
export type UploadStatus = typeof UploadStatus[keyof typeof UploadStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadStatus = {
  pending: 'pending',
  completed: 'completed',
  cancelled: 'cancelled',
  expired: 'expired',
} as const;

/**
 * The object type, which is always "upload".
 */
export type UploadObject = typeof UploadObject[keyof typeof UploadObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadObject = {
  upload: 'upload',
} as const;

/**
 * The ready File object after the Upload is completed.
 * @nullable
 */
export type UploadFileAllOf = unknown | null;

export type UploadFile = OpenAIFile & UploadFileAllOf;

/**
 * The Upload object can accept byte chunks in the form of Parts.

 */
export interface Upload {
  /** The Upload unique identifier, which can be referenced in API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the Upload was created. */
  created_at: number;
  /** The name of the file to be uploaded. */
  filename: string;
  /** The intended number of bytes to be uploaded. */
  bytes: number;
  /** The intended purpose of the file. [Please refer here](https://platform.openai.com/docs/api-reference/files/object#files/object-purpose) for acceptable values. */
  purpose: string;
  /** The status of the Upload. */
  status: UploadStatus;
  /** The Unix timestamp (in seconds) for when the Upload will expire. */
  expires_at: number;
  /** The object type, which is always "upload". */
  object: UploadObject;
  file?: UploadFile;
}

export interface UploadCertificateRequest {
  /** An optional name for the certificate */
  name?: string;
  /** The certificate content in PEM format */
  content: string;
}

/**
 * The object type, which is always `upload.part`.
 */
export type UploadPartObject = typeof UploadPartObject[keyof typeof UploadPartObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadPartObject = {
  uploadpart: 'upload.part',
} as const;

/**
 * The upload Part represents a chunk of bytes we can add to an Upload object.

 */
export interface UploadPart {
  /** The upload Part unique identifier, which can be referenced in API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the Part was created. */
  created_at: number;
  /** The ID of the Upload object that this Part was added to. */
  upload_id: string;
  /** The object type, which is always `upload.part`. */
  object: UploadPartObject;
}

export type UsageAudioSpeechesResultObject = typeof UsageAudioSpeechesResultObject[keyof typeof UsageAudioSpeechesResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioSpeechesResultObject = {
  organizationusageaudio_speechesresult: 'organization.usage.audio_speeches.result',
} as const;

export type UsageAudioSpeechesResultProjectId = string | null;

export type UsageAudioSpeechesResultUserId = string | null;

export type UsageAudioSpeechesResultApiKeyId = string | null;

export type UsageAudioSpeechesResultModel = string | null;

/**
 * The aggregated audio speeches usage details of the specific time bucket.
 */
export interface UsageAudioSpeechesResult {
  object: UsageAudioSpeechesResultObject;
  /** The number of characters processed. */
  characters: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: UsageAudioSpeechesResultProjectId;
  user_id?: UsageAudioSpeechesResultUserId;
  api_key_id?: UsageAudioSpeechesResultApiKeyId;
  model?: UsageAudioSpeechesResultModel;
}

export type UsageAudioTranscriptionsResultObject = typeof UsageAudioTranscriptionsResultObject[keyof typeof UsageAudioTranscriptionsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioTranscriptionsResultObject = {
  organizationusageaudio_transcriptionsresult: 'organization.usage.audio_transcriptions.result',
} as const;

export type UsageAudioTranscriptionsResultProjectId = string | null;

export type UsageAudioTranscriptionsResultUserId = string | null;

export type UsageAudioTranscriptionsResultApiKeyId = string | null;

export type UsageAudioTranscriptionsResultModel = string | null;

/**
 * The aggregated audio transcriptions usage details of the specific time bucket.
 */
export interface UsageAudioTranscriptionsResult {
  object: UsageAudioTranscriptionsResultObject;
  /** The number of seconds processed. */
  seconds: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: UsageAudioTranscriptionsResultProjectId;
  user_id?: UsageAudioTranscriptionsResultUserId;
  api_key_id?: UsageAudioTranscriptionsResultApiKeyId;
  model?: UsageAudioTranscriptionsResultModel;
}

export type UsageCodeInterpreterSessionsResultObject = typeof UsageCodeInterpreterSessionsResultObject[keyof typeof UsageCodeInterpreterSessionsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCodeInterpreterSessionsResultObject = {
  organizationusagecode_interpreter_sessionsresult: 'organization.usage.code_interpreter_sessions.result',
} as const;

export type UsageCodeInterpreterSessionsResultProjectId = string | null;

/**
 * The aggregated code interpreter sessions usage details of the specific time bucket.
 */
export interface UsageCodeInterpreterSessionsResult {
  object: UsageCodeInterpreterSessionsResultObject;
  /** The number of code interpreter sessions. */
  num_sessions?: number;
  project_id?: UsageCodeInterpreterSessionsResultProjectId;
}

export type UsageCompletionsResultObject = typeof UsageCompletionsResultObject[keyof typeof UsageCompletionsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCompletionsResultObject = {
  organizationusagecompletionsresult: 'organization.usage.completions.result',
} as const;

export type UsageCompletionsResultProjectId = string | null;

export type UsageCompletionsResultUserId = string | null;

export type UsageCompletionsResultApiKeyId = string | null;

export type UsageCompletionsResultModel = string | null;

export type UsageCompletionsResultBatch = boolean | null;

export type UsageCompletionsResultServiceTier = string | null;

/**
 * The aggregated completions usage details of the specific time bucket.
 */
export interface UsageCompletionsResult {
  object: UsageCompletionsResultObject;
  /** The aggregated number of text input tokens used, including cached tokens. For customers subscribe to scale tier, this includes scale tier tokens. */
  input_tokens: number;
  /** The aggregated number of text input tokens that has been cached from previous requests. For customers subscribe to scale tier, this includes scale tier tokens. */
  input_cached_tokens?: number;
  /** The aggregated number of text output tokens used. For customers subscribe to scale tier, this includes scale tier tokens. */
  output_tokens: number;
  /** The aggregated number of audio input tokens used, including cached tokens. */
  input_audio_tokens?: number;
  /** The aggregated number of audio output tokens used. */
  output_audio_tokens?: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: UsageCompletionsResultProjectId;
  user_id?: UsageCompletionsResultUserId;
  api_key_id?: UsageCompletionsResultApiKeyId;
  model?: UsageCompletionsResultModel;
  batch?: UsageCompletionsResultBatch;
  service_tier?: UsageCompletionsResultServiceTier;
}

export type UsageEmbeddingsResultObject = typeof UsageEmbeddingsResultObject[keyof typeof UsageEmbeddingsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageEmbeddingsResultObject = {
  organizationusageembeddingsresult: 'organization.usage.embeddings.result',
} as const;

export type UsageEmbeddingsResultProjectId = string | null;

export type UsageEmbeddingsResultUserId = string | null;

export type UsageEmbeddingsResultApiKeyId = string | null;

export type UsageEmbeddingsResultModel = string | null;

/**
 * The aggregated embeddings usage details of the specific time bucket.
 */
export interface UsageEmbeddingsResult {
  object: UsageEmbeddingsResultObject;
  /** The aggregated number of input tokens used. */
  input_tokens: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: UsageEmbeddingsResultProjectId;
  user_id?: UsageEmbeddingsResultUserId;
  api_key_id?: UsageEmbeddingsResultApiKeyId;
  model?: UsageEmbeddingsResultModel;
}

export type UsageImagesResultObject = typeof UsageImagesResultObject[keyof typeof UsageImagesResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageImagesResultObject = {
  organizationusageimagesresult: 'organization.usage.images.result',
} as const;

export type UsageImagesResultSource = string | null;

export type UsageImagesResultSize = string | null;

export type UsageImagesResultProjectId = string | null;

export type UsageImagesResultUserId = string | null;

export type UsageImagesResultApiKeyId = string | null;

export type UsageImagesResultModel = string | null;

/**
 * The aggregated images usage details of the specific time bucket.
 */
export interface UsageImagesResult {
  object: UsageImagesResultObject;
  /** The number of images processed. */
  images: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  source?: UsageImagesResultSource;
  size?: UsageImagesResultSize;
  project_id?: UsageImagesResultProjectId;
  user_id?: UsageImagesResultUserId;
  api_key_id?: UsageImagesResultApiKeyId;
  model?: UsageImagesResultModel;
}

export type UsageModerationsResultObject = typeof UsageModerationsResultObject[keyof typeof UsageModerationsResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageModerationsResultObject = {
  organizationusagemoderationsresult: 'organization.usage.moderations.result',
} as const;

export type UsageModerationsResultProjectId = string | null;

export type UsageModerationsResultUserId = string | null;

export type UsageModerationsResultApiKeyId = string | null;

export type UsageModerationsResultModel = string | null;

/**
 * The aggregated moderations usage details of the specific time bucket.
 */
export interface UsageModerationsResult {
  object: UsageModerationsResultObject;
  /** The aggregated number of input tokens used. */
  input_tokens: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: UsageModerationsResultProjectId;
  user_id?: UsageModerationsResultUserId;
  api_key_id?: UsageModerationsResultApiKeyId;
  model?: UsageModerationsResultModel;
}

export type UsageResponseObject = typeof UsageResponseObject[keyof typeof UsageResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageResponseObject = {
  page: 'page',
} as const;

export interface UsageResponse {
  object: UsageResponseObject;
  data: UsageTimeBucket[];
  has_more: boolean;
  next_page: string;
}

export type UsageTimeBucketObject = typeof UsageTimeBucketObject[keyof typeof UsageTimeBucketObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageTimeBucketObject = {
  bucket: 'bucket',
} as const;

export type UsageTimeBucketResultItem = UsageCompletionsResult | UsageEmbeddingsResult | UsageModerationsResult | UsageImagesResult | UsageAudioSpeechesResult | UsageAudioTranscriptionsResult | UsageVectorStoresResult | UsageCodeInterpreterSessionsResult | CostsResult;

export interface UsageTimeBucket {
  object: UsageTimeBucketObject;
  start_time: number;
  end_time: number;
  result: UsageTimeBucketResultItem[];
}

export type UsageVectorStoresResultObject = typeof UsageVectorStoresResultObject[keyof typeof UsageVectorStoresResultObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageVectorStoresResultObject = {
  organizationusagevector_storesresult: 'organization.usage.vector_stores.result',
} as const;

export type UsageVectorStoresResultProjectId = string | null;

/**
 * The aggregated vector stores usage details of the specific time bucket.
 */
export interface UsageVectorStoresResult {
  object: UsageVectorStoresResultObject;
  /** The vector stores usage in bytes. */
  usage_bytes: number;
  project_id?: UsageVectorStoresResultProjectId;
}

/**
 * The object type, which is always `organization.user`
 */
export type UserObject = typeof UserObject[keyof typeof UserObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserObject = {
  organizationuser: 'organization.user',
} as const;

/**
 * `owner` or `reader`
 */
export type UserRole = typeof UserRole[keyof typeof UserRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserRole = {
  owner: 'owner',
  reader: 'reader',
} as const;

/**
 * Represents an individual `user` within an organization.
 */
export interface User {
  /** The object type, which is always `organization.user` */
  object: UserObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the user */
  name: string;
  /** The email address of the user */
  email: string;
  /** `owner` or `reader` */
  role: UserRole;
  /** The Unix timestamp (in seconds) of when the user was added. */
  added_at: number;
}

export type UserDeleteResponseObject = typeof UserDeleteResponseObject[keyof typeof UserDeleteResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserDeleteResponseObject = {
  organizationuserdeleted: 'organization.user.deleted',
} as const;

export interface UserDeleteResponse {
  object: UserDeleteResponseObject;
  id: string;
  deleted: boolean;
}

export type UserListResponseObject = typeof UserListResponseObject[keyof typeof UserListResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserListResponseObject = {
  list: 'list',
} as const;

export interface UserListResponse {
  object: UserListResponseObject;
  data: User[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/**
 * `owner` or `reader`
 */
export type UserRoleUpdateRequestRole = typeof UserRoleUpdateRequestRole[keyof typeof UserRoleUpdateRequestRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserRoleUpdateRequestRole = {
  owner: 'owner',
  reader: 'reader',
} as const;

export interface UserRoleUpdateRequest {
  /** `owner` or `reader` */
  role: UserRoleUpdateRequestRole;
}

/**
 * Must be set to `server_vad` to enable manual chunking using server side VAD.
 */
export type VadConfigType = typeof VadConfigType[keyof typeof VadConfigType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VadConfigType = {
  server_vad: 'server_vad',
} as const;

export interface VadConfig {
  /** Must be set to `server_vad` to enable manual chunking using server side VAD. */
  type: VadConfigType;
  /** Amount of audio to include before the VAD detected speech (in
milliseconds).
 */
  prefix_padding_ms?: number;
  /** Duration of silence to detect speech stop (in milliseconds).
With shorter values the model will respond more quickly,
but may jump in on short pauses from the user.
 */
  silence_duration_ms?: number;
  /** Sensitivity threshold (0.0 to 1.0) for voice activity detection. A
higher threshold will require louder audio to activate the model, and
thus might perform better in noisy environments.
 */
  threshold?: number;
}

/**
 * The grader used for the fine-tuning job.
 */
export type ValidateGraderRequestGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

export interface ValidateGraderRequest {
  /** The grader used for the fine-tuning job. */
  grader: ValidateGraderRequestGrader;
}

/**
 * The grader used for the fine-tuning job.
 */
export type ValidateGraderResponseGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

export interface ValidateGraderResponse {
  /** The grader used for the fine-tuning job. */
  grader?: ValidateGraderResponseGrader;
}

/**
 * Anchor timestamp after which the expiration policy applies. Supported anchors: `last_active_at`.
 */
export type VectorStoreExpirationAfterAnchor = typeof VectorStoreExpirationAfterAnchor[keyof typeof VectorStoreExpirationAfterAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreExpirationAfterAnchor = {
  last_active_at: 'last_active_at',
} as const;

/**
 * The expiration policy for a vector store.
 */
export interface VectorStoreExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `last_active_at`. */
  anchor: VectorStoreExpirationAfterAnchor;
  /**
   * The number of days after the anchor time that the vector store will expire.
   * @minimum 1
   * @maximum 365
   */
  days: number;
}

/**
 * Set of 16 key-value pairs that can be attached to an object. This can be
useful for storing additional information about the object in a structured
format, and querying for objects via API or the dashboard. Keys are strings
with a maximum length of 64 characters. Values are strings with a maximum
length of 512 characters, booleans, or numbers.

 */
export type VectorStoreFileAttributesAnyOf = {[key: string]: string | number | boolean};

export type VectorStoreFileAttributes = VectorStoreFileAttributesAnyOf | null;

/**
 * The object type, which is always `vector_store.file_batch`.
 */
export type VectorStoreFileBatchObjectObject = typeof VectorStoreFileBatchObjectObject[keyof typeof VectorStoreFileBatchObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileBatchObjectObject = {
  vector_storefiles_batch: 'vector_store.files_batch',
} as const;

/**
 * The status of the vector store files batch, which can be either `in_progress`, `completed`, `cancelled` or `failed`.
 */
export type VectorStoreFileBatchObjectStatus = typeof VectorStoreFileBatchObjectStatus[keyof typeof VectorStoreFileBatchObjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileBatchObjectStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  cancelled: 'cancelled',
  failed: 'failed',
} as const;

export type VectorStoreFileBatchObjectFileCounts = {
  /** The number of files that are currently being processed. */
  in_progress: number;
  /** The number of files that have been processed. */
  completed: number;
  /** The number of files that have failed to process. */
  failed: number;
  /** The number of files that where cancelled. */
  cancelled: number;
  /** The total number of files. */
  total: number;
};

/**
 * A batch of files attached to a vector store.
 */
export interface VectorStoreFileBatchObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store.file_batch`. */
  object: VectorStoreFileBatchObjectObject;
  /** The Unix timestamp (in seconds) for when the vector store files batch was created. */
  created_at: number;
  /** The ID of the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) that the [File](https://platform.openai.com/docs/api-reference/files) is attached to. */
  vector_store_id: string;
  /** The status of the vector store files batch, which can be either `in_progress`, `completed`, `cancelled` or `failed`. */
  status: VectorStoreFileBatchObjectStatus;
  file_counts: VectorStoreFileBatchObjectFileCounts;
}

/**
 * The object type, which is always `vector_store.file_content.page`
 */
export type VectorStoreFileContentResponseObject = typeof VectorStoreFileContentResponseObject[keyof typeof VectorStoreFileContentResponseObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileContentResponseObject = {
  vector_storefile_contentpage: 'vector_store.file_content.page',
} as const;

export type VectorStoreFileContentResponseDataItem = {
  /** The content type (currently only `"text"`) */
  type?: string;
  /** The text content */
  text?: string;
};

export type VectorStoreFileContentResponseNextPage = string | null;

/**
 * Represents the parsed content of a vector store file.
 */
export interface VectorStoreFileContentResponse {
  /** The object type, which is always `vector_store.file_content.page` */
  object: VectorStoreFileContentResponseObject;
  /** Parsed content of the file. */
  data: VectorStoreFileContentResponseDataItem[];
  /** Indicates if there are more content pages to fetch. */
  has_more: boolean;
  next_page: VectorStoreFileContentResponseNextPage;
}

/**
 * The object type, which is always `vector_store.file`.
 */
export type VectorStoreFileObjectObject = typeof VectorStoreFileObjectObject[keyof typeof VectorStoreFileObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileObjectObject = {
  vector_storefile: 'vector_store.file',
} as const;

/**
 * The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use.
 */
export type VectorStoreFileObjectStatus = typeof VectorStoreFileObjectStatus[keyof typeof VectorStoreFileObjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileObjectStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  cancelled: 'cancelled',
  failed: 'failed',
} as const;

/**
 * One of `server_error`, `unsupported_file`, or `invalid_file`.
 */
export type VectorStoreFileObjectLastErrorAnyOfCode = typeof VectorStoreFileObjectLastErrorAnyOfCode[keyof typeof VectorStoreFileObjectLastErrorAnyOfCode];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreFileObjectLastErrorAnyOfCode = {
  server_error: 'server_error',
  unsupported_file: 'unsupported_file',
  invalid_file: 'invalid_file',
} as const;

/**
 * The last error associated with this vector store file. Will be `null` if there are no errors.
 */
export type VectorStoreFileObjectLastErrorAnyOf = {
  /** One of `server_error`, `unsupported_file`, or `invalid_file`. */
  code: VectorStoreFileObjectLastErrorAnyOfCode;
  /** A human-readable description of the error. */
  message: string;
};

export type VectorStoreFileObjectLastError = VectorStoreFileObjectLastErrorAnyOf | null;

/**
 * A list of files attached to a vector store.
 */
export interface VectorStoreFileObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store.file`. */
  object: VectorStoreFileObjectObject;
  /** The total vector store usage in bytes. Note that this may be different from the original file size. */
  usage_bytes: number;
  /** The Unix timestamp (in seconds) for when the vector store file was created. */
  created_at: number;
  /** The ID of the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) that the [File](https://platform.openai.com/docs/api-reference/files) is attached to. */
  vector_store_id: string;
  /** The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use. */
  status: VectorStoreFileObjectStatus;
  last_error: VectorStoreFileObjectLastError;
  chunking_strategy?: ChunkingStrategyResponse;
  attributes?: VectorStoreFileAttributes;
}

/**
 * The object type, which is always `vector_store`.
 */
export type VectorStoreObjectObject = typeof VectorStoreObjectObject[keyof typeof VectorStoreObjectObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreObjectObject = {
  vector_store: 'vector_store',
} as const;

export type VectorStoreObjectFileCounts = {
  /** The number of files that are currently being processed. */
  in_progress: number;
  /** The number of files that have been successfully processed. */
  completed: number;
  /** The number of files that have failed to process. */
  failed: number;
  /** The number of files that were cancelled. */
  cancelled: number;
  /** The total number of files. */
  total: number;
};

/**
 * The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use.
 */
export type VectorStoreObjectStatus = typeof VectorStoreObjectStatus[keyof typeof VectorStoreObjectStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreObjectStatus = {
  expired: 'expired',
  in_progress: 'in_progress',
  completed: 'completed',
} as const;

export type VectorStoreObjectExpiresAt = number | null;

export type VectorStoreObjectLastActiveAt = number | null;

/**
 * A vector store is a collection of processed files can be used by the `file_search` tool.
 */
export interface VectorStoreObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store`. */
  object: VectorStoreObjectObject;
  /** The Unix timestamp (in seconds) for when the vector store was created. */
  created_at: number;
  /** The name of the vector store. */
  name: string;
  /** The total number of bytes used by the files in the vector store. */
  usage_bytes: number;
  file_counts: VectorStoreObjectFileCounts;
  /** The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use. */
  status: VectorStoreObjectStatus;
  expires_after?: VectorStoreExpirationAfter;
  expires_at?: VectorStoreObjectExpiresAt;
  last_active_at: VectorStoreObjectLastActiveAt;
  metadata: Metadata;
}

/**
 * A query string for a search
 */
export type VectorStoreSearchRequestQuery = string | string[];

/**
 * A filter to apply based on file attributes.
 */
export type VectorStoreSearchRequestFilters = ComparisonFilter | CompoundFilter;

/**
 * Enable re-ranking; set to `none` to disable, which can help reduce latency.
 */
export type VectorStoreSearchRequestRankingOptionsRanker = typeof VectorStoreSearchRequestRankingOptionsRanker[keyof typeof VectorStoreSearchRequestRankingOptionsRanker];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreSearchRequestRankingOptionsRanker = {
  none: 'none',
  auto: 'auto',
  'default-2024-11-15': 'default-2024-11-15',
} as const;

/**
 * Ranking options for search.
 */
export type VectorStoreSearchRequestRankingOptions = {
  /** Enable re-ranking; set to `none` to disable, which can help reduce latency. */
  ranker?: VectorStoreSearchRequestRankingOptionsRanker;
  /**
   * @minimum 0
   * @maximum 1
   */
  score_threshold?: number;
};

export interface VectorStoreSearchRequest {
  /** A query string for a search */
  query: VectorStoreSearchRequestQuery;
  /** Whether to rewrite the natural language query for vector search. */
  rewrite_query?: boolean;
  /**
   * The maximum number of results to return. This number should be between 1 and 50 inclusive.
   * @minimum 1
   * @maximum 50
   */
  max_num_results?: number;
  /** A filter to apply based on file attributes. */
  filters?: VectorStoreSearchRequestFilters;
  /** Ranking options for search. */
  ranking_options?: VectorStoreSearchRequestRankingOptions;
}

/**
 * The type of content.
 */
export type VectorStoreSearchResultContentObjectType = typeof VectorStoreSearchResultContentObjectType[keyof typeof VectorStoreSearchResultContentObjectType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreSearchResultContentObjectType = {
  text: 'text',
} as const;

export interface VectorStoreSearchResultContentObject {
  /** The type of content. */
  type: VectorStoreSearchResultContentObjectType;
  /** The text content returned from search. */
  text: string;
}

export interface VectorStoreSearchResultItem {
  /** The ID of the vector store file. */
  file_id: string;
  /** The name of the vector store file. */
  filename: string;
  /**
   * The similarity score for the result.
   * @minimum 0
   * @maximum 1
   */
  score: number;
  attributes: VectorStoreFileAttributes;
  /** Content chunks from the file. */
  content: VectorStoreSearchResultContentObject[];
}

/**
 * The object type, which is always `vector_store.search_results.page`
 */
export type VectorStoreSearchResultsPageObject = typeof VectorStoreSearchResultsPageObject[keyof typeof VectorStoreSearchResultsPageObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VectorStoreSearchResultsPageObject = {
  vector_storesearch_resultspage: 'vector_store.search_results.page',
} as const;

export type VectorStoreSearchResultsPageNextPage = string | null;

export interface VectorStoreSearchResultsPage {
  /** The object type, which is always `vector_store.search_results.page` */
  object: VectorStoreSearchResultsPageObject;
  search_query: string[];
  /** The list of search result items. */
  data: VectorStoreSearchResultItem[];
  /** Indicates if there are more results to fetch. */
  has_more: boolean;
  next_page: VectorStoreSearchResultsPageNextPage;
}

export type Verbosity = 'low' | 'medium' | 'high' | null;

export type VoiceIdsShared = string | 'alloy' | 'ash' | 'ballad' | 'coral' | 'echo' | 'sage' | 'shimmer' | 'verse' | 'marin' | 'cedar';

/**
 * Specifies the event type. For a wait action, this property is
always set to `wait`.

 */
export type WaitType = typeof WaitType[keyof typeof WaitType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WaitType = {
  wait: 'wait',
} as const;

/**
 * A wait action.

 */
export interface Wait {
  /** Specifies the event type. For a wait action, this property is
always set to `wait`.
 */
  type: WaitType;
}

/**
 * The action type.

 */
export type WebSearchActionFindType = typeof WebSearchActionFindType[keyof typeof WebSearchActionFindType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchActionFindType = {
  find: 'find',
} as const;

/**
 * Action type "find": Searches for a pattern within a loaded page.

 */
export interface WebSearchActionFind {
  /** The action type.
 */
  type: WebSearchActionFindType;
  /** The URL of the page searched for the pattern.
 */
  url: string;
  /** The pattern or text to search for within the page.
 */
  pattern: string;
}

/**
 * The action type.

 */
export type WebSearchActionOpenPageType = typeof WebSearchActionOpenPageType[keyof typeof WebSearchActionOpenPageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchActionOpenPageType = {
  open_page: 'open_page',
} as const;

/**
 * Action type "open_page" - Opens a specific URL from search results.

 */
export interface WebSearchActionOpenPage {
  /** The action type.
 */
  type: WebSearchActionOpenPageType;
  /** The URL opened by the model.
 */
  url: string;
}

/**
 * The action type.

 */
export type WebSearchActionSearchType = typeof WebSearchActionSearchType[keyof typeof WebSearchActionSearchType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchActionSearchType = {
  search: 'search',
} as const;

/**
 * The type of source. Always `url`.

 */
export type WebSearchActionSearchSourcesItemType = typeof WebSearchActionSearchSourcesItemType[keyof typeof WebSearchActionSearchSourcesItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchActionSearchSourcesItemType = {
  url: 'url',
} as const;

/**
 * A source used in the search.

 */
export type WebSearchActionSearchSourcesItem = {
  /** The type of source. Always `url`.
 */
  type: WebSearchActionSearchSourcesItemType;
  /** The URL of the source.
 */
  url: string;
};

/**
 * Action type "search" - Performs a web search query.

 */
export interface WebSearchActionSearch {
  /** The action type.
 */
  type: WebSearchActionSearchType;
  /** The search query.
 */
  query: string;
  /** The sources used in the search.
 */
  sources?: WebSearchActionSearchSourcesItem[];
}

/**
 * The type of location approximation. Always `approximate`.
 */
export type WebSearchApproximateLocationAnyOfType = typeof WebSearchApproximateLocationAnyOfType[keyof typeof WebSearchApproximateLocationAnyOfType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchApproximateLocationAnyOfType = {
  approximate: 'approximate',
} as const;

export type WebSearchApproximateLocationAnyOfCountry = string | null;

export type WebSearchApproximateLocationAnyOfRegion = string | null;

export type WebSearchApproximateLocationAnyOfCity = string | null;

export type WebSearchApproximateLocationAnyOfTimezone = string | null;

/**
 * The approximate location of the user.

 */
export type WebSearchApproximateLocationAnyOf = {
  /** The type of location approximation. Always `approximate`. */
  type?: WebSearchApproximateLocationAnyOfType;
  country?: WebSearchApproximateLocationAnyOfCountry;
  region?: WebSearchApproximateLocationAnyOfRegion;
  city?: WebSearchApproximateLocationAnyOfCity;
  timezone?: WebSearchApproximateLocationAnyOfTimezone;
};

export type WebSearchApproximateLocation = WebSearchApproximateLocationAnyOf | null;

/**
 * High level guidance for the amount of context window space to use for the
search. One of `low`, `medium`, or `high`. `medium` is the default.

 */
export type WebSearchContextSize = typeof WebSearchContextSize[keyof typeof WebSearchContextSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchContextSize = {
  low: 'low',
  medium: 'medium',
  high: 'high',
} as const;

/**
 * Approximate location parameters for the search.
 */
export interface WebSearchLocation {
  /** The two-letter
[ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1) of the user,
e.g. `US`.
 */
  country?: string;
  /** Free text input for the region of the user, e.g. `California`.
 */
  region?: string;
  /** Free text input for the city of the user, e.g. `San Francisco`.
 */
  city?: string;
  /** The [IANA timezone](https://timeapi.io/documentation/iana-timezones)
of the user, e.g. `America/Los_Angeles`.
 */
  timezone?: string;
}

/**
 * The type of the web search tool. One of `web_search` or `web_search_2025_08_26`.
 */
export type WebSearchToolType = typeof WebSearchToolType[keyof typeof WebSearchToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchToolType = {
  web_search: 'web_search',
  web_search_2025_08_26: 'web_search_2025_08_26',
} as const;

export type WebSearchToolFiltersAnyOfAllowedDomains = string[] | null;

/**
 * Filters for the search.

 */
export type WebSearchToolFiltersAnyOf = {
  allowed_domains?: WebSearchToolFiltersAnyOfAllowedDomains;
};

export type WebSearchToolFilters = WebSearchToolFiltersAnyOf | null;

/**
 * High level guidance for the amount of context window space to use for the search. One of `low`, `medium`, or `high`. `medium` is the default.
 */
export type WebSearchToolSearchContextSize = typeof WebSearchToolSearchContextSize[keyof typeof WebSearchToolSearchContextSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchToolSearchContextSize = {
  low: 'low',
  medium: 'medium',
  high: 'high',
} as const;

/**
 * Search the Internet for sources related to the prompt. Learn more about the
[web search tool](https://platform.openai.com/docs/guides/tools-web-search).

 */
export interface WebSearchTool {
  /** The type of the web search tool. One of `web_search` or `web_search_2025_08_26`. */
  type: WebSearchToolType;
  filters?: WebSearchToolFilters;
  user_location?: WebSearchApproximateLocation;
  /** High level guidance for the amount of context window space to use for the search. One of `low`, `medium`, or `high`. `medium` is the default. */
  search_context_size?: WebSearchToolSearchContextSize;
}

/**
 * The type of the web search tool call. Always `web_search_call`.

 */
export type WebSearchToolCallType = typeof WebSearchToolCallType[keyof typeof WebSearchToolCallType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchToolCallType = {
  web_search_call: 'web_search_call',
} as const;

/**
 * The status of the web search tool call.

 */
export type WebSearchToolCallStatus = typeof WebSearchToolCallStatus[keyof typeof WebSearchToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchToolCallStatus = {
  in_progress: 'in_progress',
  searching: 'searching',
  completed: 'completed',
  failed: 'failed',
} as const;

/**
 * An object describing the specific action taken in this web search call.
Includes details on how the model used the web (search, open_page, find).

 */
export type WebSearchToolCallAction = WebSearchActionSearch | WebSearchActionOpenPage | WebSearchActionFind;

/**
 * The results of a web search tool call. See the
[web search guide](https://platform.openai.com/docs/guides/tools-web-search) for more information.

 */
export interface WebSearchToolCall {
  /** The unique ID of the web search tool call.
 */
  id: string;
  /** The type of the web search tool call. Always `web_search_call`.
 */
  type: WebSearchToolCallType;
  /** The status of the web search tool call.
 */
  status: WebSearchToolCallStatus;
  /** An object describing the specific action taken in this web search call.
Includes details on how the model used the web (search, open_page, find).
 */
  action: WebSearchToolCallAction;
}

/**
 * Event data payload.

 */
export type WebhookBatchCancelledData = {
  /** The unique ID of the batch API request.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookBatchCancelledObject = typeof WebhookBatchCancelledObject[keyof typeof WebhookBatchCancelledObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchCancelledObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `batch.cancelled`.

 */
export type WebhookBatchCancelledType = typeof WebhookBatchCancelledType[keyof typeof WebhookBatchCancelledType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchCancelledType = {
  batchcancelled: 'batch.cancelled',
} as const;

/**
 * Sent when a batch API request has been cancelled.

 */
export interface WebhookBatchCancelled {
  /** The Unix timestamp (in seconds) of when the batch API request was cancelled.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookBatchCancelledData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookBatchCancelledObject;
  /** The type of the event. Always `batch.cancelled`.
 */
  type: WebhookBatchCancelledType;
}

/**
 * Event data payload.

 */
export type WebhookBatchCompletedData = {
  /** The unique ID of the batch API request.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookBatchCompletedObject = typeof WebhookBatchCompletedObject[keyof typeof WebhookBatchCompletedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchCompletedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `batch.completed`.

 */
export type WebhookBatchCompletedType = typeof WebhookBatchCompletedType[keyof typeof WebhookBatchCompletedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchCompletedType = {
  batchcompleted: 'batch.completed',
} as const;

/**
 * Sent when a batch API request has been completed.

 */
export interface WebhookBatchCompleted {
  /** The Unix timestamp (in seconds) of when the batch API request was completed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookBatchCompletedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookBatchCompletedObject;
  /** The type of the event. Always `batch.completed`.
 */
  type: WebhookBatchCompletedType;
}

/**
 * Event data payload.

 */
export type WebhookBatchExpiredData = {
  /** The unique ID of the batch API request.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookBatchExpiredObject = typeof WebhookBatchExpiredObject[keyof typeof WebhookBatchExpiredObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchExpiredObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `batch.expired`.

 */
export type WebhookBatchExpiredType = typeof WebhookBatchExpiredType[keyof typeof WebhookBatchExpiredType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchExpiredType = {
  batchexpired: 'batch.expired',
} as const;

/**
 * Sent when a batch API request has expired.

 */
export interface WebhookBatchExpired {
  /** The Unix timestamp (in seconds) of when the batch API request expired.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookBatchExpiredData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookBatchExpiredObject;
  /** The type of the event. Always `batch.expired`.
 */
  type: WebhookBatchExpiredType;
}

/**
 * Event data payload.

 */
export type WebhookBatchFailedData = {
  /** The unique ID of the batch API request.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookBatchFailedObject = typeof WebhookBatchFailedObject[keyof typeof WebhookBatchFailedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchFailedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `batch.failed`.

 */
export type WebhookBatchFailedType = typeof WebhookBatchFailedType[keyof typeof WebhookBatchFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookBatchFailedType = {
  batchfailed: 'batch.failed',
} as const;

/**
 * Sent when a batch API request has failed.

 */
export interface WebhookBatchFailed {
  /** The Unix timestamp (in seconds) of when the batch API request failed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookBatchFailedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookBatchFailedObject;
  /** The type of the event. Always `batch.failed`.
 */
  type: WebhookBatchFailedType;
}

/**
 * Event data payload.

 */
export type WebhookEvalRunCanceledData = {
  /** The unique ID of the eval run.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookEvalRunCanceledObject = typeof WebhookEvalRunCanceledObject[keyof typeof WebhookEvalRunCanceledObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunCanceledObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `eval.run.canceled`.

 */
export type WebhookEvalRunCanceledType = typeof WebhookEvalRunCanceledType[keyof typeof WebhookEvalRunCanceledType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunCanceledType = {
  evalruncanceled: 'eval.run.canceled',
} as const;

/**
 * Sent when an eval run has been canceled.

 */
export interface WebhookEvalRunCanceled {
  /** The Unix timestamp (in seconds) of when the eval run was canceled.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookEvalRunCanceledData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookEvalRunCanceledObject;
  /** The type of the event. Always `eval.run.canceled`.
 */
  type: WebhookEvalRunCanceledType;
}

/**
 * Event data payload.

 */
export type WebhookEvalRunFailedData = {
  /** The unique ID of the eval run.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookEvalRunFailedObject = typeof WebhookEvalRunFailedObject[keyof typeof WebhookEvalRunFailedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunFailedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `eval.run.failed`.

 */
export type WebhookEvalRunFailedType = typeof WebhookEvalRunFailedType[keyof typeof WebhookEvalRunFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunFailedType = {
  evalrunfailed: 'eval.run.failed',
} as const;

/**
 * Sent when an eval run has failed.

 */
export interface WebhookEvalRunFailed {
  /** The Unix timestamp (in seconds) of when the eval run failed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookEvalRunFailedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookEvalRunFailedObject;
  /** The type of the event. Always `eval.run.failed`.
 */
  type: WebhookEvalRunFailedType;
}

/**
 * Event data payload.

 */
export type WebhookEvalRunSucceededData = {
  /** The unique ID of the eval run.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookEvalRunSucceededObject = typeof WebhookEvalRunSucceededObject[keyof typeof WebhookEvalRunSucceededObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunSucceededObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `eval.run.succeeded`.

 */
export type WebhookEvalRunSucceededType = typeof WebhookEvalRunSucceededType[keyof typeof WebhookEvalRunSucceededType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookEvalRunSucceededType = {
  evalrunsucceeded: 'eval.run.succeeded',
} as const;

/**
 * Sent when an eval run has succeeded.

 */
export interface WebhookEvalRunSucceeded {
  /** The Unix timestamp (in seconds) of when the eval run succeeded.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookEvalRunSucceededData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookEvalRunSucceededObject;
  /** The type of the event. Always `eval.run.succeeded`.
 */
  type: WebhookEvalRunSucceededType;
}

/**
 * Event data payload.

 */
export type WebhookFineTuningJobCancelledData = {
  /** The unique ID of the fine-tuning job.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookFineTuningJobCancelledObject = typeof WebhookFineTuningJobCancelledObject[keyof typeof WebhookFineTuningJobCancelledObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobCancelledObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `fine_tuning.job.cancelled`.

 */
export type WebhookFineTuningJobCancelledType = typeof WebhookFineTuningJobCancelledType[keyof typeof WebhookFineTuningJobCancelledType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobCancelledType = {
  fine_tuningjobcancelled: 'fine_tuning.job.cancelled',
} as const;

/**
 * Sent when a fine-tuning job has been cancelled.

 */
export interface WebhookFineTuningJobCancelled {
  /** The Unix timestamp (in seconds) of when the fine-tuning job was cancelled.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookFineTuningJobCancelledData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookFineTuningJobCancelledObject;
  /** The type of the event. Always `fine_tuning.job.cancelled`.
 */
  type: WebhookFineTuningJobCancelledType;
}

/**
 * Event data payload.

 */
export type WebhookFineTuningJobFailedData = {
  /** The unique ID of the fine-tuning job.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookFineTuningJobFailedObject = typeof WebhookFineTuningJobFailedObject[keyof typeof WebhookFineTuningJobFailedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobFailedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `fine_tuning.job.failed`.

 */
export type WebhookFineTuningJobFailedType = typeof WebhookFineTuningJobFailedType[keyof typeof WebhookFineTuningJobFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobFailedType = {
  fine_tuningjobfailed: 'fine_tuning.job.failed',
} as const;

/**
 * Sent when a fine-tuning job has failed.

 */
export interface WebhookFineTuningJobFailed {
  /** The Unix timestamp (in seconds) of when the fine-tuning job failed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookFineTuningJobFailedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookFineTuningJobFailedObject;
  /** The type of the event. Always `fine_tuning.job.failed`.
 */
  type: WebhookFineTuningJobFailedType;
}

/**
 * Event data payload.

 */
export type WebhookFineTuningJobSucceededData = {
  /** The unique ID of the fine-tuning job.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookFineTuningJobSucceededObject = typeof WebhookFineTuningJobSucceededObject[keyof typeof WebhookFineTuningJobSucceededObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobSucceededObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `fine_tuning.job.succeeded`.

 */
export type WebhookFineTuningJobSucceededType = typeof WebhookFineTuningJobSucceededType[keyof typeof WebhookFineTuningJobSucceededType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookFineTuningJobSucceededType = {
  fine_tuningjobsucceeded: 'fine_tuning.job.succeeded',
} as const;

/**
 * Sent when a fine-tuning job has succeeded.

 */
export interface WebhookFineTuningJobSucceeded {
  /** The Unix timestamp (in seconds) of when the fine-tuning job succeeded.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookFineTuningJobSucceededData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookFineTuningJobSucceededObject;
  /** The type of the event. Always `fine_tuning.job.succeeded`.
 */
  type: WebhookFineTuningJobSucceededType;
}

/**
 * A header from the SIP Invite.

 */
export type WebhookRealtimeCallIncomingDataSipHeadersItem = {
  /** Name of the SIP Header.
 */
  name: string;
  /** Value of the SIP Header.
 */
  value: string;
};

/**
 * Event data payload.

 */
export type WebhookRealtimeCallIncomingData = {
  /** The unique ID of this call.
 */
  call_id: string;
  /** Headers from the SIP Invite.
 */
  sip_headers: WebhookRealtimeCallIncomingDataSipHeadersItem[];
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookRealtimeCallIncomingObject = typeof WebhookRealtimeCallIncomingObject[keyof typeof WebhookRealtimeCallIncomingObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookRealtimeCallIncomingObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `realtime.call.incoming`.

 */
export type WebhookRealtimeCallIncomingType = typeof WebhookRealtimeCallIncomingType[keyof typeof WebhookRealtimeCallIncomingType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookRealtimeCallIncomingType = {
  realtimecallincoming: 'realtime.call.incoming',
} as const;

/**
 * Sent when Realtime API Receives a incoming SIP call.

 */
export interface WebhookRealtimeCallIncoming {
  /** The Unix timestamp (in seconds) of when the model response was completed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookRealtimeCallIncomingData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookRealtimeCallIncomingObject;
  /** The type of the event. Always `realtime.call.incoming`.
 */
  type: WebhookRealtimeCallIncomingType;
}

/**
 * Event data payload.

 */
export type WebhookResponseCancelledData = {
  /** The unique ID of the model response.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookResponseCancelledObject = typeof WebhookResponseCancelledObject[keyof typeof WebhookResponseCancelledObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseCancelledObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `response.cancelled`.

 */
export type WebhookResponseCancelledType = typeof WebhookResponseCancelledType[keyof typeof WebhookResponseCancelledType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseCancelledType = {
  responsecancelled: 'response.cancelled',
} as const;

/**
 * Sent when a background response has been cancelled.

 */
export interface WebhookResponseCancelled {
  /** The Unix timestamp (in seconds) of when the model response was cancelled.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookResponseCancelledData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookResponseCancelledObject;
  /** The type of the event. Always `response.cancelled`.
 */
  type: WebhookResponseCancelledType;
}

/**
 * Event data payload.

 */
export type WebhookResponseCompletedData = {
  /** The unique ID of the model response.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookResponseCompletedObject = typeof WebhookResponseCompletedObject[keyof typeof WebhookResponseCompletedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseCompletedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `response.completed`.

 */
export type WebhookResponseCompletedType = typeof WebhookResponseCompletedType[keyof typeof WebhookResponseCompletedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseCompletedType = {
  responsecompleted: 'response.completed',
} as const;

/**
 * Sent when a background response has been completed.

 */
export interface WebhookResponseCompleted {
  /** The Unix timestamp (in seconds) of when the model response was completed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookResponseCompletedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookResponseCompletedObject;
  /** The type of the event. Always `response.completed`.
 */
  type: WebhookResponseCompletedType;
}

/**
 * Event data payload.

 */
export type WebhookResponseFailedData = {
  /** The unique ID of the model response.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookResponseFailedObject = typeof WebhookResponseFailedObject[keyof typeof WebhookResponseFailedObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseFailedObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `response.failed`.

 */
export type WebhookResponseFailedType = typeof WebhookResponseFailedType[keyof typeof WebhookResponseFailedType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseFailedType = {
  responsefailed: 'response.failed',
} as const;

/**
 * Sent when a background response has failed.

 */
export interface WebhookResponseFailed {
  /** The Unix timestamp (in seconds) of when the model response failed.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookResponseFailedData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookResponseFailedObject;
  /** The type of the event. Always `response.failed`.
 */
  type: WebhookResponseFailedType;
}

/**
 * Event data payload.

 */
export type WebhookResponseIncompleteData = {
  /** The unique ID of the model response.
 */
  id: string;
};

/**
 * The object of the event. Always `event`.

 */
export type WebhookResponseIncompleteObject = typeof WebhookResponseIncompleteObject[keyof typeof WebhookResponseIncompleteObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseIncompleteObject = {
  event: 'event',
} as const;

/**
 * The type of the event. Always `response.incomplete`.

 */
export type WebhookResponseIncompleteType = typeof WebhookResponseIncompleteType[keyof typeof WebhookResponseIncompleteType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebhookResponseIncompleteType = {
  responseincomplete: 'response.incomplete',
} as const;

/**
 * Sent when a background response has been interrupted.

 */
export interface WebhookResponseIncomplete {
  /** The Unix timestamp (in seconds) of when the model response was interrupted.
 */
  created_at: number;
  /** The unique ID of the event.
 */
  id: string;
  /** Event data payload.
 */
  data: WebhookResponseIncompleteData;
  /** The object of the event. Always `event`.
 */
  object?: WebhookResponseIncompleteObject;
  /** The type of the event. Always `response.incomplete`.
 */
  type: WebhookResponseIncompleteType;
}

/**
 * Specify additional output data to include in the model response. Currently supported values are:
- `web_search_call.action.sources`: Include the sources of the web search tool call.
- `code_interpreter_call.outputs`: Includes the outputs of python code execution in code interpreter tool call items.
- `computer_call_output.output.image_url`: Include image urls from the computer call output.
- `file_search_call.results`: Include the search results of the file search tool call.
- `message.input_image.image_url`: Include image urls from the input message.
- `message.output_text.logprobs`: Include logprobs with assistant messages.
- `reasoning.encrypted_content`: Includes an encrypted version of reasoning tokens in reasoning item outputs. This enables reasoning items to be used in multi-turn conversations when using the Responses API statelessly (like when the `store` parameter is set to `false`, or when an organization is enrolled in the zero data retention program).
 */
export type IncludeEnum = typeof IncludeEnum[keyof typeof IncludeEnum];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const IncludeEnum = {
  file_search_callresults: 'file_search_call.results',
  web_search_callresults: 'web_search_call.results',
  web_search_callactionsources: 'web_search_call.action.sources',
  messageinput_imageimage_url: 'message.input_image.image_url',
  computer_call_outputoutputimage_url: 'computer_call_output.output.image_url',
  code_interpreter_calloutputs: 'code_interpreter_call.outputs',
  reasoningencrypted_content: 'reasoning.encrypted_content',
  messageoutput_textlogprobs: 'message.output_text.logprobs',
} as const;

export type MessageStatus = typeof MessageStatus[keyof typeof MessageStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

export type MessageRole = typeof MessageRole[keyof typeof MessageRole];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageRole = {
  unknown: 'unknown',
  user: 'user',
  assistant: 'assistant',
  system: 'system',
  critic: 'critic',
  discriminator: 'discriminator',
  developer: 'developer',
  tool: 'tool',
} as const;

/**
 * The type of the input item. Always `input_text`.
 */
export type InputTextContentType = typeof InputTextContentType[keyof typeof InputTextContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputTextContentType = {
  input_text: 'input_text',
} as const;

/**
 * A text input to the model.
 */
export interface InputTextContent {
  /** The type of the input item. Always `input_text`. */
  type: InputTextContentType;
  /** The text input to the model. */
  text: string;
}

/**
 * The type of the file citation. Always `file_citation`.
 */
export type FileCitationBodyType = typeof FileCitationBodyType[keyof typeof FileCitationBodyType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileCitationBodyType = {
  file_citation: 'file_citation',
} as const;

/**
 * A citation to a file.
 */
export interface FileCitationBody {
  /** The type of the file citation. Always `file_citation`. */
  type: FileCitationBodyType;
  /** The ID of the file. */
  file_id: string;
  /** The index of the file in the list of files. */
  index: number;
  /** The filename of the file cited. */
  filename: string;
}

/**
 * The type of the URL citation. Always `url_citation`.
 */
export type UrlCitationBodyType = typeof UrlCitationBodyType[keyof typeof UrlCitationBodyType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UrlCitationBodyType = {
  url_citation: 'url_citation',
} as const;

/**
 * A citation for a web resource used to generate a model response.
 */
export interface UrlCitationBody {
  /** The type of the URL citation. Always `url_citation`. */
  type: UrlCitationBodyType;
  /** The URL of the web resource. */
  url: string;
  /** The index of the first character of the URL citation in the message. */
  start_index: number;
  /** The index of the last character of the URL citation in the message. */
  end_index: number;
  /** The title of the web resource. */
  title: string;
}

/**
 * The type of the container file citation. Always `container_file_citation`.
 */
export type ContainerFileCitationBodyType = typeof ContainerFileCitationBodyType[keyof typeof ContainerFileCitationBodyType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ContainerFileCitationBodyType = {
  container_file_citation: 'container_file_citation',
} as const;

/**
 * A citation for a container file used to generate a model response.
 */
export interface ContainerFileCitationBody {
  /** The type of the container file citation. Always `container_file_citation`. */
  type: ContainerFileCitationBodyType;
  /** The ID of the container file. */
  container_id: string;
  /** The ID of the file. */
  file_id: string;
  /** The index of the first character of the container file citation in the message. */
  start_index: number;
  /** The index of the last character of the container file citation in the message. */
  end_index: number;
  /** The filename of the container file cited. */
  filename: string;
}

export type Annotation = FileCitationBody | UrlCitationBody | ContainerFileCitationBody | FilePath;

/**
 * The top log probability of a token.
 */
export interface TopLogProb {
  token: string;
  logprob: number;
  bytes: number[];
}

/**
 * The log probability of a token.
 */
export interface LogProb {
  token: string;
  logprob: number;
  bytes: number[];
  top_logprobs: TopLogProb[];
}

/**
 * The type of the output text. Always `output_text`.
 */
export type OutputTextContentType = typeof OutputTextContentType[keyof typeof OutputTextContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OutputTextContentType = {
  output_text: 'output_text',
} as const;

/**
 * A text output from the model.
 */
export interface OutputTextContent {
  /** The type of the output text. Always `output_text`. */
  type: OutputTextContentType;
  /** The text output from the model. */
  text: string;
  /** The annotations of the text output. */
  annotations: Annotation[];
  logprobs?: LogProb[];
}

export type TextContentType = typeof TextContentType[keyof typeof TextContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TextContentType = {
  text: 'text',
} as const;

/**
 * A text content.
 */
export interface TextContent {
  type: TextContentType;
  text: string;
}

/**
 * The type of the object. Always `summary_text`.
 */
export type SummaryTextContentType = typeof SummaryTextContentType[keyof typeof SummaryTextContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SummaryTextContentType = {
  summary_text: 'summary_text',
} as const;

/**
 * A summary text from the model.
 */
export interface SummaryTextContent {
  /** The type of the object. Always `summary_text`. */
  type: SummaryTextContentType;
  /** A summary of the reasoning output from the model so far. */
  text: string;
}

/**
 * The type of the reasoning text. Always `reasoning_text`.
 */
export type ReasoningTextContentType = typeof ReasoningTextContentType[keyof typeof ReasoningTextContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReasoningTextContentType = {
  reasoning_text: 'reasoning_text',
} as const;

/**
 * Reasoning text from the model.
 */
export interface ReasoningTextContent {
  /** The type of the reasoning text. Always `reasoning_text`. */
  type: ReasoningTextContentType;
  /** The reasoning text from the model. */
  text: string;
}

/**
 * The type of the refusal. Always `refusal`.
 */
export type RefusalContentType = typeof RefusalContentType[keyof typeof RefusalContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefusalContentType = {
  refusal: 'refusal',
} as const;

/**
 * A refusal from the model.
 */
export interface RefusalContent {
  /** The type of the refusal. Always `refusal`. */
  type: RefusalContentType;
  /** The refusal explanation from the model. */
  refusal: string;
}

export type ImageDetail = typeof ImageDetail[keyof typeof ImageDetail];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ImageDetail = {
  low: 'low',
  high: 'high',
  auto: 'auto',
} as const;

/**
 * The type of the input item. Always `input_image`.
 */
export type InputImageContentType = typeof InputImageContentType[keyof typeof InputImageContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputImageContentType = {
  input_image: 'input_image',
} as const;

export type InputImageContentImageUrl = string | null;

export type InputImageContentFileId = string | null;

/**
 * An image input to the model. Learn about [image inputs](https://platform.openai.com/docs/guides/vision).
 */
export interface InputImageContent {
  /** The type of the input item. Always `input_image`. */
  type: InputImageContentType;
  image_url?: InputImageContentImageUrl;
  file_id?: InputImageContentFileId;
  /** The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`. */
  detail: ImageDetail;
}

/**
 * Specifies the event type. For a computer screenshot, this property is always set to `computer_screenshot`.
 */
export type ComputerScreenshotContentType = typeof ComputerScreenshotContentType[keyof typeof ComputerScreenshotContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerScreenshotContentType = {
  computer_screenshot: 'computer_screenshot',
} as const;

export type ComputerScreenshotContentImageUrl = string | null;

export type ComputerScreenshotContentFileId = string | null;

/**
 * A screenshot of a computer.
 */
export interface ComputerScreenshotContent {
  /** Specifies the event type. For a computer screenshot, this property is always set to `computer_screenshot`. */
  type: ComputerScreenshotContentType;
  image_url: ComputerScreenshotContentImageUrl;
  file_id: ComputerScreenshotContentFileId;
}

/**
 * The type of the input item. Always `input_file`.
 */
export type InputFileContentType = typeof InputFileContentType[keyof typeof InputFileContentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputFileContentType = {
  input_file: 'input_file',
} as const;

export type InputFileContentFileId = string | null;

/**
 * A file input to the model.
 */
export interface InputFileContent {
  /** The type of the input item. Always `input_file`. */
  type: InputFileContentType;
  file_id?: InputFileContentFileId;
  /** The name of the file to be sent to the model. */
  filename?: string;
  /** The URL of the file to be sent to the model. */
  file_url?: string;
  /** The content of the file to be sent to the model.
 */
  file_data?: string;
}

/**
 * The type of the message. Always set to `message`.
 */
export type MessageType = typeof MessageType[keyof typeof MessageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MessageType = {
  message: 'message',
} as const;

export type MessageContentPropertyItem = InputTextContent | OutputTextContent | TextContent | SummaryTextContent | ReasoningTextContent | RefusalContent | InputImageContent | ComputerScreenshotContent | InputFileContent;

/**
 * A message to or from the model.
 */
export interface Message {
  /** The type of the message. Always set to `message`. */
  type: MessageType;
  /** The unique ID of the message. */
  id: string;
  /** The status of item. One of `in_progress`, `completed`, or `incomplete`. Populated when items are returned via API. */
  status: MessageStatus;
  /** The role of the message. One of `unknown`, `user`, `assistant`, `system`, `critic`, `discriminator`, `developer`, or `tool`. */
  role: MessageRole;
  /** The content of the message */
  content: MessageContentPropertyItem[];
}

export type ClickButtonType = typeof ClickButtonType[keyof typeof ClickButtonType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClickButtonType = {
  left: 'left',
  right: 'right',
  wheel: 'wheel',
  back: 'back',
  forward: 'forward',
} as const;

/**
 * Specifies the event type. For a click action, this property is always `click`.
 */
export type ClickParamType = typeof ClickParamType[keyof typeof ClickParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClickParamType = {
  click: 'click',
} as const;

/**
 * A click action.
 */
export interface ClickParam {
  /** Specifies the event type. For a click action, this property is always `click`. */
  type: ClickParamType;
  /** Indicates which mouse button was pressed during the click. One of `left`, `right`, `wheel`, `back`, or `forward`. */
  button: ClickButtonType;
  /** The x-coordinate where the click occurred. */
  x: number;
  /** The y-coordinate where the click occurred. */
  y: number;
}

/**
 * Specifies the event type. For a double click action, this property is always set to `double_click`.
 */
export type DoubleClickActionType = typeof DoubleClickActionType[keyof typeof DoubleClickActionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DoubleClickActionType = {
  double_click: 'double_click',
} as const;

/**
 * A double click action.
 */
export interface DoubleClickAction {
  /** Specifies the event type. For a double click action, this property is always set to `double_click`. */
  type: DoubleClickActionType;
  /** The x-coordinate where the double click occurred. */
  x: number;
  /** The y-coordinate where the double click occurred. */
  y: number;
}

/**
 * An x/y coordinate pair, e.g. `{ x: 100, y: 200 }`.
 */
export interface DragPoint {
  /** The x-coordinate. */
  x: number;
  /** The y-coordinate. */
  y: number;
}

/**
 * Specifies the event type. For a keypress action, this property is always set to `keypress`.
 */
export type KeyPressActionType = typeof KeyPressActionType[keyof typeof KeyPressActionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const KeyPressActionType = {
  keypress: 'keypress',
} as const;

/**
 * A collection of keypresses the model would like to perform.
 */
export interface KeyPressAction {
  /** Specifies the event type. For a keypress action, this property is always set to `keypress`. */
  type: KeyPressActionType;
  /** The combination of keys the model is requesting to be pressed. This is an array of strings, each representing a key. */
  keys: string[];
}

export type ComputerCallSafetyCheckParamCode = string | null;

export type ComputerCallSafetyCheckParamMessage = string | null;

/**
 * A pending safety check for the computer call.
 */
export interface ComputerCallSafetyCheckParam {
  /** The ID of the pending safety check. */
  id: string;
  code?: ComputerCallSafetyCheckParamCode;
  message?: ComputerCallSafetyCheckParamMessage;
}

/**
 * The type of the output. Always `logs`.
 */
export type CodeInterpreterOutputLogsType = typeof CodeInterpreterOutputLogsType[keyof typeof CodeInterpreterOutputLogsType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterOutputLogsType = {
  logs: 'logs',
} as const;

/**
 * The logs output from the code interpreter.
 */
export interface CodeInterpreterOutputLogs {
  /** The type of the output. Always `logs`. */
  type: CodeInterpreterOutputLogsType;
  /** The logs output from the code interpreter. */
  logs: string;
}

/**
 * The type of the output. Always `image`.
 */
export type CodeInterpreterOutputImageType = typeof CodeInterpreterOutputImageType[keyof typeof CodeInterpreterOutputImageType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterOutputImageType = {
  image: 'image',
} as const;

/**
 * The image output from the code interpreter.
 */
export interface CodeInterpreterOutputImage {
  /** The type of the output. Always `image`. */
  type: CodeInterpreterOutputImageType;
  /** The URL of the image output from the code interpreter. */
  url: string;
}

/**
 * The type of the local shell action. Always `exec`.
 */
export type LocalShellExecActionType = typeof LocalShellExecActionType[keyof typeof LocalShellExecActionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LocalShellExecActionType = {
  exec: 'exec',
} as const;

export type LocalShellExecActionTimeoutMs = number | null;

export type LocalShellExecActionWorkingDirectory = string | null;

/**
 * Environment variables to set for the command.
 */
export type LocalShellExecActionEnv = {[key: string]: string};

export type LocalShellExecActionUser = string | null;

/**
 * Execute a shell command on the server.
 */
export interface LocalShellExecAction {
  /** The type of the local shell action. Always `exec`. */
  type: LocalShellExecActionType;
  /** The command to run. */
  command: string[];
  timeout_ms?: LocalShellExecActionTimeoutMs;
  working_directory?: LocalShellExecActionWorkingDirectory;
  /** Environment variables to set for the command. */
  env: LocalShellExecActionEnv;
  user?: LocalShellExecActionUser;
}

export type MCPToolCallStatus = typeof MCPToolCallStatus[keyof typeof MCPToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MCPToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
  calling: 'calling',
  failed: 'failed',
} as const;

export type DetailEnum = typeof DetailEnum[keyof typeof DetailEnum];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DetailEnum = {
  low: 'low',
  high: 'high',
  auto: 'auto',
} as const;

export type FunctionCallItemStatus = typeof FunctionCallItemStatus[keyof typeof FunctionCallItemStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionCallItemStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
  incomplete: 'incomplete',
} as const;

export type ComputerCallOutputItemParamId = string | null;

/**
 * The type of the computer tool call output. Always `computer_call_output`.
 */
export type ComputerCallOutputItemParamType = typeof ComputerCallOutputItemParamType[keyof typeof ComputerCallOutputItemParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerCallOutputItemParamType = {
  computer_call_output: 'computer_call_output',
} as const;

export type ComputerCallOutputItemParamAcknowledgedSafetyChecks = ComputerCallSafetyCheckParam[] | null;

export type ComputerCallOutputItemParamStatus = FunctionCallItemStatus | null;

/**
 * The output of a computer tool call.
 */
export interface ComputerCallOutputItemParam {
  id?: ComputerCallOutputItemParamId;
  /**
   * The ID of the computer tool call that produced the output.
   * @minLength 1
   * @maxLength 64
   */
  call_id: string;
  /** The type of the computer tool call output. Always `computer_call_output`. */
  type: ComputerCallOutputItemParamType;
  output: ComputerScreenshotImage;
  acknowledged_safety_checks?: ComputerCallOutputItemParamAcknowledgedSafetyChecks;
  status?: ComputerCallOutputItemParamStatus;
}

/**
 * The type of the input item. Always `input_text`.
 */
export type InputTextContentParamType = typeof InputTextContentParamType[keyof typeof InputTextContentParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputTextContentParamType = {
  input_text: 'input_text',
} as const;

/**
 * A text input to the model.
 */
export interface InputTextContentParam {
  /** The type of the input item. Always `input_text`. */
  type: InputTextContentParamType;
  /**
   * The text input to the model.
   * @maxLength 10485760
   */
  text: string;
}

/**
 * The type of the input item. Always `input_image`.
 */
export type InputImageContentParamAutoParamType = typeof InputImageContentParamAutoParamType[keyof typeof InputImageContentParamAutoParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputImageContentParamAutoParamType = {
  input_image: 'input_image',
} as const;

export type InputImageContentParamAutoParamImageUrl = string | null;

export type InputImageContentParamAutoParamFileId = string | null;

export type InputImageContentParamAutoParamDetail = DetailEnum | null;

/**
 * An image input to the model. Learn about [image inputs](https://platform.openai.com/docs/guides/vision)
 */
export interface InputImageContentParamAutoParam {
  /** The type of the input item. Always `input_image`. */
  type: InputImageContentParamAutoParamType;
  image_url?: InputImageContentParamAutoParamImageUrl;
  file_id?: InputImageContentParamAutoParamFileId;
  detail?: InputImageContentParamAutoParamDetail;
}

/**
 * The type of the input item. Always `input_file`.
 */
export type InputFileContentParamType = typeof InputFileContentParamType[keyof typeof InputFileContentParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputFileContentParamType = {
  input_file: 'input_file',
} as const;

export type InputFileContentParamFileId = string | null;

export type InputFileContentParamFilename = string | null;

export type InputFileContentParamFileData = string | null;

export type InputFileContentParamFileUrl = string | null;

/**
 * A file input to the model.
 */
export interface InputFileContentParam {
  /** The type of the input item. Always `input_file`. */
  type: InputFileContentParamType;
  file_id?: InputFileContentParamFileId;
  filename?: InputFileContentParamFilename;
  file_data?: InputFileContentParamFileData;
  file_url?: InputFileContentParamFileUrl;
}

export type FunctionCallOutputItemParamId = string | null;

/**
 * The type of the function tool call output. Always `function_call_output`.
 */
export type FunctionCallOutputItemParamType = typeof FunctionCallOutputItemParamType[keyof typeof FunctionCallOutputItemParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionCallOutputItemParamType = {
  function_call_output: 'function_call_output',
} as const;

export type FunctionCallOutputItemParamOutputAnyOfItem = InputTextContentParam | InputImageContentParamAutoParam | InputFileContentParam;

/**
 * Text, image, or file output of the function tool call.
 */
export type FunctionCallOutputItemParamOutput = string | FunctionCallOutputItemParamOutputAnyOfItem[];

export type FunctionCallOutputItemParamStatus = FunctionCallItemStatus | null;

/**
 * The output of a function tool call.
 */
export interface FunctionCallOutputItemParam {
  id?: FunctionCallOutputItemParamId;
  /**
   * The unique ID of the function tool call generated by the model.
   * @minLength 1
   * @maxLength 64
   */
  call_id: string;
  /** The type of the function tool call output. Always `function_call_output`. */
  type: FunctionCallOutputItemParamType;
  /** Text, image, or file output of the function tool call. */
  output: FunctionCallOutputItemParamOutput;
  status?: FunctionCallOutputItemParamStatus;
}

export type ItemReferenceParamType = 'item_reference' | null;

/**
 * An internal identifier for an item to reference.
 */
export interface ItemReferenceParam {
  type?: ItemReferenceParamType;
  /** The ID of the item to reference. */
  id: string;
}

/**
 * The object type, which is always `conversation`.
 */
export type ConversationResourceObject = typeof ConversationResourceObject[keyof typeof ConversationResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConversationResourceObject = {
  conversation: 'conversation',
} as const;

export interface ConversationResource {
  /** The unique ID of the conversation. */
  id: string;
  /** The object type, which is always `conversation`. */
  object: ConversationResourceObject;
  /** Set of 16 key-value pairs that can be attached to an object. This can be         useful for storing additional information about the object in a structured         format, and querying for objects via API or the dashboard.
        Keys are strings with a maximum length of 64 characters. Values are strings         with a maximum length of 512 characters. */
  metadata: unknown;
  /** The time at which the conversation was created, measured in seconds since the Unix epoch. */
  created_at: number;
}

/**
 * The type of the function tool. Always `function`.
 */
export type FunctionToolType = typeof FunctionToolType[keyof typeof FunctionToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FunctionToolType = {
  function: 'function',
} as const;

export type FunctionToolDescription = string | null;

/**
 * A JSON schema object describing the parameters of the function.
 */
export type FunctionToolParametersAnyOf = {[key: string]: unknown};

export type FunctionToolParameters = FunctionToolParametersAnyOf | null;

export type FunctionToolStrict = boolean | null;

/**
 * Defines a function in your own code the model can choose to call. Learn more about [function calling](https://platform.openai.com/docs/guides/function-calling).
 */
export interface FunctionTool {
  /** The type of the function tool. Always `function`. */
  type: FunctionToolType;
  /** The name of the function to call. */
  name: string;
  description?: FunctionToolDescription;
  parameters: FunctionToolParameters;
  strict: FunctionToolStrict;
}

export type RankerVersionType = typeof RankerVersionType[keyof typeof RankerVersionType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RankerVersionType = {
  auto: 'auto',
  'default-2024-11-15': 'default-2024-11-15',
} as const;

export interface RankingOptions {
  /** The ranker to use for the file search. */
  ranker?: RankerVersionType;
  /** The score threshold for the file search, a number between 0 and 1. Numbers closer to 1 will attempt to return only the most relevant results, but may return fewer results. */
  score_threshold?: number;
}

export type Filters = ComparisonFilter | CompoundFilter;

/**
 * The type of the file search tool. Always `file_search`.
 */
export type FileSearchToolType = typeof FileSearchToolType[keyof typeof FileSearchToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileSearchToolType = {
  file_search: 'file_search',
} as const;

export type FileSearchToolFilters = Filters | null;

/**
 * A tool that searches for relevant content from uploaded files. Learn more about the [file search tool](https://platform.openai.com/docs/guides/tools-file-search).
 */
export interface FileSearchTool {
  /** The type of the file search tool. Always `file_search`. */
  type: FileSearchToolType;
  /** The IDs of the vector stores to search. */
  vector_store_ids: string[];
  /** The maximum number of results to return. This number should be between 1 and 50 inclusive. */
  max_num_results?: number;
  /** Ranking options for search. */
  ranking_options?: RankingOptions;
  filters?: FileSearchToolFilters;
}

export type ComputerEnvironment = typeof ComputerEnvironment[keyof typeof ComputerEnvironment];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerEnvironment = {
  windows: 'windows',
  mac: 'mac',
  linux: 'linux',
  ubuntu: 'ubuntu',
  browser: 'browser',
} as const;

/**
 * The type of the computer use tool. Always `computer_use_preview`.
 */
export type ComputerUsePreviewToolType = typeof ComputerUsePreviewToolType[keyof typeof ComputerUsePreviewToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComputerUsePreviewToolType = {
  computer_use_preview: 'computer_use_preview',
} as const;

/**
 * A tool that controls a virtual computer. Learn more about the [computer tool](https://platform.openai.com/docs/guides/tools-computer-use).
 */
export interface ComputerUsePreviewTool {
  /** The type of the computer use tool. Always `computer_use_preview`. */
  type: ComputerUsePreviewToolType;
  /** The type of computer environment to control. */
  environment: ComputerEnvironment;
  /** The width of the computer display. */
  display_width: number;
  /** The height of the computer display. */
  display_height: number;
}

/**
 * 
            Control how much effort the model will exert to match the style and features, especially facial features, of input images. This parameter is only supported for `gpt-image-1`. Unsupported for `gpt-image-1-mini`. Supports `high` and `low`. Defaults to `low`.
 */
export type InputFidelity = typeof InputFidelity[keyof typeof InputFidelity];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InputFidelity = {
  high: 'high',
  low: 'low',
} as const;

/**
 * The type of the local shell tool. Always `local_shell`.
 */
export type LocalShellToolParamType = typeof LocalShellToolParamType[keyof typeof LocalShellToolParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LocalShellToolParamType = {
  local_shell: 'local_shell',
} as const;

export interface LocalShellToolParam {
  /** The type of the local shell tool. Always `local_shell`. */
  type: LocalShellToolParamType;
}

/**
 * Unconstrained text format. Always `text`.
 */
export type CustomTextFormatParamType = typeof CustomTextFormatParamType[keyof typeof CustomTextFormatParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomTextFormatParamType = {
  text: 'text',
} as const;

export interface CustomTextFormatParam {
  /** Unconstrained text format. Always `text`. */
  type: CustomTextFormatParamType;
}

export type GrammarSyntax1 = typeof GrammarSyntax1[keyof typeof GrammarSyntax1];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GrammarSyntax1 = {
  lark: 'lark',
  regex: 'regex',
} as const;

/**
 * Grammar format. Always `grammar`.
 */
export type CustomGrammarFormatParamType = typeof CustomGrammarFormatParamType[keyof typeof CustomGrammarFormatParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomGrammarFormatParamType = {
  grammar: 'grammar',
} as const;

export interface CustomGrammarFormatParam {
  /** Grammar format. Always `grammar`. */
  type: CustomGrammarFormatParamType;
  /** The syntax of the grammar definition. One of `lark` or `regex`. */
  syntax: GrammarSyntax1;
  /** The grammar definition. */
  definition: string;
}

/**
 * The type of the custom tool. Always `custom`.
 */
export type CustomToolParamType = typeof CustomToolParamType[keyof typeof CustomToolParamType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomToolParamType = {
  custom: 'custom',
} as const;

/**
 * The input format for the custom tool. Default is unconstrained text.
 */
export type CustomToolParamFormat = CustomTextFormatParam | CustomGrammarFormatParam;

export interface CustomToolParam {
  /** The type of the custom tool. Always `custom`. */
  type: CustomToolParamType;
  /** The name of the custom tool, used to identify it in tool calls. */
  name: string;
  /** Optional description of the custom tool, used to provide more context. */
  description?: string;
  /** The input format for the custom tool. Default is unconstrained text. */
  format?: CustomToolParamFormat;
}

/**
 * The type of location approximation. Always `approximate`.
 */
export type ApproximateLocationType = typeof ApproximateLocationType[keyof typeof ApproximateLocationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ApproximateLocationType = {
  approximate: 'approximate',
} as const;

export type ApproximateLocationCountry = string | null;

export type ApproximateLocationRegion = string | null;

export type ApproximateLocationCity = string | null;

export type ApproximateLocationTimezone = string | null;

export interface ApproximateLocation {
  /** The type of location approximation. Always `approximate`. */
  type: ApproximateLocationType;
  country?: ApproximateLocationCountry;
  region?: ApproximateLocationRegion;
  city?: ApproximateLocationCity;
  timezone?: ApproximateLocationTimezone;
}

export type SearchContextSize = typeof SearchContextSize[keyof typeof SearchContextSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SearchContextSize = {
  low: 'low',
  medium: 'medium',
  high: 'high',
} as const;

/**
 * The type of the web search tool. One of `web_search_preview` or `web_search_preview_2025_03_11`.
 */
export type WebSearchPreviewToolType = typeof WebSearchPreviewToolType[keyof typeof WebSearchPreviewToolType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WebSearchPreviewToolType = {
  web_search_preview: 'web_search_preview',
  web_search_preview_2025_03_11: 'web_search_preview_2025_03_11',
} as const;

export type WebSearchPreviewToolUserLocation = ApproximateLocation | null;

/**
 * This tool searches the web for relevant results to use in a response. Learn more about the [web search tool](https://platform.openai.com/docs/guides/tools-web-search).
 */
export interface WebSearchPreviewTool {
  /** The type of the web search tool. One of `web_search_preview` or `web_search_preview_2025_03_11`. */
  type: WebSearchPreviewToolType;
  user_location?: WebSearchPreviewToolUserLocation;
  /** High level guidance for the amount of context window space to use for the search. One of `low`, `medium`, or `high`. `medium` is the default. */
  search_context_size?: SearchContextSize;
}

/**
 * The input tokens detailed information for the image generation.
 */
export interface ImageGenInputUsageDetails {
  /** The number of text tokens in the input prompt. */
  text_tokens: number;
  /** The number of image tokens in the input prompt. */
  image_tokens: number;
}

/**
 * For `gpt-image-1` only, the token usage information for the image generation.
 */
export interface ImageGenUsage {
  /** The number of tokens (images and text) in the input prompt. */
  input_tokens: number;
  /** The total number of tokens (images and text) used for the image generation. */
  total_tokens: number;
  /** The number of output tokens generated by the model. */
  output_tokens: number;
  input_tokens_details: ImageGenInputUsageDetails;
}

/**
 * The conversation that this response belongs to.
 */
export interface ConversationParam2 {
  /** The unique ID of the conversation. */
  id: string;
}

/**
 * The conversation that this response belongs to. Input items and output items from this response are automatically added to this conversation.
 */
export interface Conversation2 {
  /** The unique ID of the conversation. */
  id: string;
}

export type CreateConversationBodyMetadata = Metadata | null;

export type CreateConversationBodyItems = InputItem[] | null;

export interface CreateConversationBody {
  metadata?: CreateConversationBodyMetadata;
  items?: CreateConversationBodyItems;
}

export interface UpdateConversationBody {
  /** Set of 16 key-value pairs that can be attached to an object. This can be         useful for storing additional information about the object in a structured         format, and querying for objects via API or the dashboard.
        Keys are strings with a maximum length of 64 characters. Values are strings         with a maximum length of 512 characters. */
  metadata: Metadata;
}

export type DeletedConversationResourceObject = typeof DeletedConversationResourceObject[keyof typeof DeletedConversationResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeletedConversationResourceObject = {
  conversationdeleted: 'conversation.deleted',
} as const;

export interface DeletedConversationResource {
  object: DeletedConversationResourceObject;
  deleted: boolean;
  id: string;
}

export type OrderEnum = typeof OrderEnum[keyof typeof OrderEnum];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OrderEnum = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type VideoModel = typeof VideoModel[keyof typeof VideoModel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoModel = {
  'sora-2': 'sora-2',
  'sora-2-pro': 'sora-2-pro',
} as const;

export type VideoStatus = typeof VideoStatus[keyof typeof VideoStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoStatus = {
  queued: 'queued',
  in_progress: 'in_progress',
  completed: 'completed',
  failed: 'failed',
} as const;

export type VideoSize = typeof VideoSize[keyof typeof VideoSize];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoSize = {
  '720x1280': '720x1280',
  '1280x720': '1280x720',
  '1024x1792': '1024x1792',
  '1792x1024': '1792x1024',
} as const;

export type VideoSeconds = typeof VideoSeconds[keyof typeof VideoSeconds];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoSeconds = {
  NUMBER_4: '4',
  NUMBER_8: '8',
  NUMBER_12: '12',
} as const;

export interface Error2 {
  code: string;
  message: string;
}

/**
 * The object type, which is always `video`.
 */
export type VideoResourceObject = typeof VideoResourceObject[keyof typeof VideoResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoResourceObject = {
  video: 'video',
} as const;

export type VideoResourceCompletedAt = number | null;

export type VideoResourceExpiresAt = number | null;

export type VideoResourceRemixedFromVideoId = string | null;

export type VideoResourceError = Error2 | null;

/**
 * Structured information describing a generated video job.
 */
export interface VideoResource {
  /** Unique identifier for the video job. */
  id: string;
  /** The object type, which is always `video`. */
  object: VideoResourceObject;
  /** The video generation model that produced the job. */
  model: VideoModel;
  /** Current lifecycle status of the video job. */
  status: VideoStatus;
  /** Approximate completion percentage for the generation task. */
  progress: number;
  /** Unix timestamp (seconds) for when the job was created. */
  created_at: number;
  completed_at: VideoResourceCompletedAt;
  expires_at: VideoResourceExpiresAt;
  /** The resolution of the generated video. */
  size: VideoSize;
  /** Duration of the generated clip in seconds. */
  seconds: VideoSeconds;
  remixed_from_video_id: VideoResourceRemixedFromVideoId;
  error: VideoResourceError;
}

export type VideoListResourceFirstId = string | null;

export type VideoListResourceLastId = string | null;

export interface VideoListResource {
  /** The type of object returned, must be `list`. */
  object: 'list';
  /** A list of items */
  data: VideoResource[];
  first_id: VideoListResourceFirstId;
  last_id: VideoListResourceLastId;
  /** Whether there are more items available. */
  has_more: boolean;
}

/**
 * Parameters for creating a new video generation job.
 */
export interface CreateVideoBody {
  /** The video generation model to use. Defaults to `sora-2`. */
  model?: VideoModel;
  /**
   * Text prompt that describes the video to generate.
   * @minLength 1
   * @maxLength 32000
   */
  prompt: string;
  /** Optional image reference that guides generation. */
  input_reference?: Blob;
  /** Clip duration in seconds. Defaults to 4 seconds. */
  seconds?: VideoSeconds;
  /** Output resolution formatted as width x height. Defaults to 720x1280. */
  size?: VideoSize;
}

/**
 * The object type that signals the deletion response.
 */
export type DeletedVideoResourceObject = typeof DeletedVideoResourceObject[keyof typeof DeletedVideoResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeletedVideoResourceObject = {
  videodeleted: 'video.deleted',
} as const;

/**
 * Confirmation payload returned after deleting a video.
 */
export interface DeletedVideoResource {
  /** The object type that signals the deletion response. */
  object: DeletedVideoResourceObject;
  /** Indicates that the video resource was deleted. */
  deleted: boolean;
  /** Identifier of the deleted video. */
  id: string;
}

export type VideoContentVariant = typeof VideoContentVariant[keyof typeof VideoContentVariant];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const VideoContentVariant = {
  video: 'video',
  thumbnail: 'thumbnail',
  spritesheet: 'spritesheet',
} as const;

/**
 * Parameters for remixing an existing generated video.
 */
export interface CreateVideoRemixBody {
  /**
   * Updated text prompt that directs the remix generation.
   * @minLength 1
   * @maxLength 32000
   */
  prompt: string;
}

export type TruncationEnum = typeof TruncationEnum[keyof typeof TruncationEnum];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TruncationEnum = {
  auto: 'auto',
  disabled: 'disabled',
} as const;

export type TokenCountsBodyModel = string | null;

/**
 * Text, image, or file inputs to the model, used to generate a response
 */
export type TokenCountsBodyInputAnyOf = string | InputItem[];

export type TokenCountsBodyInput = TokenCountsBodyInputAnyOf | null;

export type TokenCountsBodyPreviousResponseId = string | null;

export type TokenCountsBodyTools = Tool[] | null;

export type TokenCountsBodyText = ResponseTextParam | null;

export type TokenCountsBodyReasoning = Reasoning | null;

export type TokenCountsBodyInstructions = string | null;

export type TokenCountsBodyConversation = ConversationParam | null;

export type TokenCountsBodyToolChoice = ToolChoiceParam | null;

export type TokenCountsBodyParallelToolCalls = boolean | null;

export interface TokenCountsBody {
  model?: TokenCountsBodyModel;
  input?: TokenCountsBodyInput;
  previous_response_id?: TokenCountsBodyPreviousResponseId;
  tools?: TokenCountsBodyTools;
  text?: TokenCountsBodyText;
  reasoning?: TokenCountsBodyReasoning;
  /** The truncation strategy to use for the model response. - `auto`: If the input to this Response exceeds the model's context window size, the model will truncate the response to fit the context window by dropping items from the beginning of the conversation. - `disabled` (default): If the input size will exceed the context window size for a model, the request will fail with a 400 error. */
  truncation?: TruncationEnum;
  instructions?: TokenCountsBodyInstructions;
  conversation?: TokenCountsBodyConversation;
  tool_choice?: TokenCountsBodyToolChoice;
  parallel_tool_calls?: TokenCountsBodyParallelToolCalls;
}

export type TokenCountsResourceObject = typeof TokenCountsResourceObject[keyof typeof TokenCountsResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TokenCountsResourceObject = {
  responseinput_tokens: 'response.input_tokens',
} as const;

export interface TokenCountsResource {
  object: TokenCountsResourceObject;
  input_tokens: number;
}

/**
 * Controls diagnostic tracing during the session.
 */
export interface ChatkitWorkflowTracing {
  /** Indicates whether tracing is enabled. */
  enabled: boolean;
}

export type ChatkitWorkflowVersion = string | null;

/**
 * State variable key-value pairs applied when invoking the workflow. Defaults to null when no overrides were provided.
 */
export type ChatkitWorkflowStateVariablesAnyOf = {[key: string]: string | number | boolean | number};

export type ChatkitWorkflowStateVariables = ChatkitWorkflowStateVariablesAnyOf | null;

/**
 * Workflow metadata and state returned for the session.
 */
export interface ChatkitWorkflow {
  /** Identifier of the workflow backing the session. */
  id: string;
  version: ChatkitWorkflowVersion;
  state_variables: ChatkitWorkflowStateVariables;
  /** Tracing settings applied to the workflow. */
  tracing: ChatkitWorkflowTracing;
}

/**
 * Active per-minute request limit for the session.
 */
export interface ChatSessionRateLimits {
  /** Maximum allowed requests per one-minute window. */
  max_requests_per_1_minute: number;
}

export type ChatSessionStatus = typeof ChatSessionStatus[keyof typeof ChatSessionStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatSessionStatus = {
  active: 'active',
  expired: 'expired',
  cancelled: 'cancelled',
} as const;

/**
 * Automatic thread title preferences for the session.
 */
export interface ChatSessionAutomaticThreadTitling {
  /** Whether automatic thread titling is enabled. */
  enabled: boolean;
}

export type ChatSessionFileUploadMaxFileSize = number | null;

export type ChatSessionFileUploadMaxFiles = number | null;

/**
 * Upload permissions and limits applied to the session.
 */
export interface ChatSessionFileUpload {
  /** Indicates if uploads are enabled for the session. */
  enabled: boolean;
  max_file_size: ChatSessionFileUploadMaxFileSize;
  max_files: ChatSessionFileUploadMaxFiles;
}

export type ChatSessionHistoryRecentThreads = number | null;

/**
 * History retention preferences returned for the session.
 */
export interface ChatSessionHistory {
  /** Indicates if chat history is persisted for the session. */
  enabled: boolean;
  recent_threads: ChatSessionHistoryRecentThreads;
}

/**
 * ChatKit configuration for the session.
 */
export interface ChatSessionChatkitConfiguration {
  /** Automatic thread titling preferences. */
  automatic_thread_titling: ChatSessionAutomaticThreadTitling;
  /** Upload settings for the session. */
  file_upload: ChatSessionFileUpload;
  /** History retention configuration. */
  history: ChatSessionHistory;
}

/**
 * Type discriminator that is always `chatkit.session`.
 */
export type ChatSessionResourceObject = typeof ChatSessionResourceObject[keyof typeof ChatSessionResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatSessionResourceObject = {
  chatkitsession: 'chatkit.session',
} as const;

/**
 * Represents a ChatKit session and its resolved configuration.
 */
export interface ChatSessionResource {
  /** Identifier for the ChatKit session. */
  id: string;
  /** Type discriminator that is always `chatkit.session`. */
  object: ChatSessionResourceObject;
  /** Unix timestamp (in seconds) for when the session expires. */
  expires_at: number;
  /** Ephemeral client secret that authenticates session requests. */
  client_secret: string;
  /** Workflow metadata for the session. */
  workflow: ChatkitWorkflow;
  /** User identifier associated with the session. */
  user: string;
  /** Resolved rate limit values. */
  rate_limits: ChatSessionRateLimits;
  /** Convenience copy of the per-minute request limit. */
  max_requests_per_1_minute: number;
  /** Current lifecycle state of the session. */
  status: ChatSessionStatus;
  /** Resolved ChatKit feature configuration for the session. */
  chatkit_configuration: ChatSessionChatkitConfiguration;
}

/**
 * Controls diagnostic tracing during the session.
 */
export interface WorkflowTracingParam {
  /** Whether tracing is enabled during the session. Defaults to true. */
  enabled?: boolean;
}

/**
 * State variables forwarded to the workflow. Keys may be up to 64 characters, values must be primitive types, and the map defaults to an empty object.
 */
export type WorkflowParamStateVariables = {[key: string]: string | number | boolean | number};

/**
 * Workflow reference and overrides applied to the chat session.
 */
export interface WorkflowParam {
  /** Identifier for the workflow invoked by the session. */
  id: string;
  /** Specific workflow version to run. Defaults to the latest deployed version. */
  version?: string;
  /** State variables forwarded to the workflow. Keys may be up to 64 characters, values must be primitive types, and the map defaults to an empty object. */
  state_variables?: WorkflowParamStateVariables;
  /** Optional tracing overrides for the workflow invocation. When omitted, tracing is enabled by default. */
  tracing?: WorkflowTracingParam;
}

/**
 * Base timestamp used to calculate expiration. Currently fixed to `created_at`.
 */
export type ExpiresAfterParamAnchor = typeof ExpiresAfterParamAnchor[keyof typeof ExpiresAfterParamAnchor];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ExpiresAfterParamAnchor = {
  created_at: 'created_at',
} as const;

/**
 * Controls when the session expires relative to an anchor timestamp.
 */
export interface ExpiresAfterParam {
  /** Base timestamp used to calculate expiration. Currently fixed to `created_at`. */
  anchor: ExpiresAfterParamAnchor;
  /**
   * Number of seconds after the anchor when the session expires.
   * @minimum 1
   * @maximum 600
   */
  seconds: number;
}

/**
 * Controls request rate limits for the session.
 */
export interface RateLimitsParam {
  /**
   * Maximum number of requests allowed per minute for the session. Defaults to 10.
   * @minimum 1
   */
  max_requests_per_1_minute?: number;
}

/**
 * Controls whether ChatKit automatically generates thread titles.
 */
export interface AutomaticThreadTitlingParam {
  /** Enable automatic thread title generation. Defaults to true. */
  enabled?: boolean;
}

/**
 * Controls whether users can upload files.
 */
export interface FileUploadParam {
  /** Enable uploads for this session. Defaults to false. */
  enabled?: boolean;
  /**
   * Maximum size in megabytes for each uploaded file. Defaults to 512 MB, which is the maximum allowable size.
   * @minimum 1
   * @maximum 512
   */
  max_file_size?: number;
  /**
   * Maximum number of files that can be uploaded to the session. Defaults to 10.
   * @minimum 1
   */
  max_files?: number;
}

/**
 * Controls how much historical context is retained for the session.
 */
export interface HistoryParam {
  /** Enables chat users to access previous ChatKit threads. Defaults to true. */
  enabled?: boolean;
  /**
   * Number of recent ChatKit threads users have access to. Defaults to unlimited when unset.
   * @minimum 1
   */
  recent_threads?: number;
}

/**
 * Optional per-session configuration settings for ChatKit behavior.
 */
export interface ChatkitConfigurationParam {
  /** Configuration for automatic thread titling. When omitted, automatic thread titling is enabled by default. */
  automatic_thread_titling?: AutomaticThreadTitlingParam;
  /** Configuration for upload enablement and limits. When omitted, uploads are disabled by default (max_files 10, max_file_size 512 MB). */
  file_upload?: FileUploadParam;
  /** Configuration for chat history retention. When omitted, history is enabled by default with no limit on recent_threads (null). */
  history?: HistoryParam;
}

/**
 * Parameters for provisioning a new ChatKit session.
 */
export interface CreateChatSessionBody {
  /** Workflow that powers the session. */
  workflow: WorkflowParam;
  /**
   * A free-form string that identifies your end user; ensures this Session can access other objects that have the same `user` scope.
   * @minLength 1
   */
  user: string;
  /** Optional override for session expiration timing in seconds from creation. Defaults to 10 minutes. */
  expires_after?: ExpiresAfterParam;
  /** Optional override for per-minute request limits. When omitted, defaults to 10. */
  rate_limits?: RateLimitsParam;
  /** Optional overrides for ChatKit runtime configuration features */
  chatkit_configuration?: ChatkitConfigurationParam;
}

/**
 * Type discriminator that is always `input_text`.
 */
export type UserMessageInputTextType = typeof UserMessageInputTextType[keyof typeof UserMessageInputTextType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMessageInputTextType = {
  input_text: 'input_text',
} as const;

/**
 * Text block that a user contributed to the thread.
 */
export interface UserMessageInputText {
  /** Type discriminator that is always `input_text`. */
  type: UserMessageInputTextType;
  /** Plain-text content supplied by the user. */
  text: string;
}

/**
 * Type discriminator that is always `quoted_text`.
 */
export type UserMessageQuotedTextType = typeof UserMessageQuotedTextType[keyof typeof UserMessageQuotedTextType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMessageQuotedTextType = {
  quoted_text: 'quoted_text',
} as const;

/**
 * Quoted snippet that the user referenced in their message.
 */
export interface UserMessageQuotedText {
  /** Type discriminator that is always `quoted_text`. */
  type: UserMessageQuotedTextType;
  /** Quoted text content. */
  text: string;
}

export type AttachmentType = typeof AttachmentType[keyof typeof AttachmentType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AttachmentType = {
  image: 'image',
  file: 'file',
} as const;

export type AttachmentPreviewUrl = string | null;

/**
 * Attachment metadata included on thread items.
 */
export interface Attachment {
  /** Attachment discriminator. */
  type: AttachmentType;
  /** Identifier for the attachment. */
  id: string;
  /** Original display name for the attachment. */
  name: string;
  /** MIME type of the attachment. */
  mime_type: string;
  preview_url: AttachmentPreviewUrl;
}

/**
 * Tool selection that the assistant should honor when executing the item.
 */
export interface ToolChoice {
  /** Identifier of the requested tool. */
  id: string;
}

export type InferenceOptionsToolChoice = ToolChoice | null;

export type InferenceOptionsModel = string | null;

/**
 * Model and tool overrides applied when generating the assistant response.
 */
export interface InferenceOptions {
  tool_choice: InferenceOptionsToolChoice;
  model: InferenceOptionsModel;
}

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type UserMessageItemObject = typeof UserMessageItemObject[keyof typeof UserMessageItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMessageItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

export type UserMessageItemType = typeof UserMessageItemType[keyof typeof UserMessageItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMessageItemType = {
  chatkituser_message: 'chatkit.user_message',
} as const;

/**
 * Content blocks that comprise a user message.
 */
export type UserMessageItemContentItem = UserMessageInputText | UserMessageQuotedText;

export type UserMessageItemInferenceOptions = InferenceOptions | null;

/**
 * User-authored messages within a thread.
 */
export interface UserMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: UserMessageItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  type: UserMessageItemType;
  /** Ordered content elements supplied by the user. */
  content: UserMessageItemContentItem[];
  /** Attachments associated with the user message. Defaults to an empty list. */
  attachments: Attachment[];
  inference_options: UserMessageItemInferenceOptions;
}

/**
 * Type discriminator that is always `file`.
 */
export type FileAnnotationSourceType = typeof FileAnnotationSourceType[keyof typeof FileAnnotationSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileAnnotationSourceType = {
  file: 'file',
} as const;

/**
 * Attachment source referenced by an annotation.
 */
export interface FileAnnotationSource {
  /** Type discriminator that is always `file`. */
  type: FileAnnotationSourceType;
  /** Filename referenced by the annotation. */
  filename: string;
}

/**
 * Type discriminator that is always `file` for this annotation.
 */
export type FileAnnotationType = typeof FileAnnotationType[keyof typeof FileAnnotationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FileAnnotationType = {
  file: 'file',
} as const;

/**
 * Annotation that references an uploaded file.
 */
export interface FileAnnotation {
  /** Type discriminator that is always `file` for this annotation. */
  type: FileAnnotationType;
  /** File attachment referenced by the annotation. */
  source: FileAnnotationSource;
}

/**
 * Type discriminator that is always `url`.
 */
export type UrlAnnotationSourceType = typeof UrlAnnotationSourceType[keyof typeof UrlAnnotationSourceType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UrlAnnotationSourceType = {
  url: 'url',
} as const;

/**
 * URL backing an annotation entry.
 */
export interface UrlAnnotationSource {
  /** Type discriminator that is always `url`. */
  type: UrlAnnotationSourceType;
  /** URL referenced by the annotation. */
  url: string;
}

/**
 * Type discriminator that is always `url` for this annotation.
 */
export type UrlAnnotationType = typeof UrlAnnotationType[keyof typeof UrlAnnotationType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UrlAnnotationType = {
  url: 'url',
} as const;

/**
 * Annotation that references a URL.
 */
export interface UrlAnnotation {
  /** Type discriminator that is always `url` for this annotation. */
  type: UrlAnnotationType;
  /** URL referenced by the annotation. */
  source: UrlAnnotationSource;
}

/**
 * Type discriminator that is always `output_text`.
 */
export type ResponseOutputTextType = typeof ResponseOutputTextType[keyof typeof ResponseOutputTextType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseOutputTextType = {
  output_text: 'output_text',
} as const;

/**
 * Annotation object describing a cited source.
 */
export type ResponseOutputTextAnnotationsItem = FileAnnotation | UrlAnnotation;

/**
 * Assistant response text accompanied by optional annotations.
 */
export interface ResponseOutputText {
  /** Type discriminator that is always `output_text`. */
  type: ResponseOutputTextType;
  /** Assistant generated text. */
  text: string;
  /** Ordered list of annotations attached to the response text. */
  annotations: ResponseOutputTextAnnotationsItem[];
}

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type AssistantMessageItemObject = typeof AssistantMessageItemObject[keyof typeof AssistantMessageItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantMessageItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

/**
 * Type discriminator that is always `chatkit.assistant_message`.
 */
export type AssistantMessageItemType = typeof AssistantMessageItemType[keyof typeof AssistantMessageItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AssistantMessageItemType = {
  chatkitassistant_message: 'chatkit.assistant_message',
} as const;

/**
 * Assistant-authored message within a thread.
 */
export interface AssistantMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: AssistantMessageItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.assistant_message`. */
  type: AssistantMessageItemType;
  /** Ordered assistant response segments. */
  content: ResponseOutputText[];
}

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type WidgetMessageItemObject = typeof WidgetMessageItemObject[keyof typeof WidgetMessageItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WidgetMessageItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

/**
 * Type discriminator that is always `chatkit.widget`.
 */
export type WidgetMessageItemType = typeof WidgetMessageItemType[keyof typeof WidgetMessageItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WidgetMessageItemType = {
  chatkitwidget: 'chatkit.widget',
} as const;

/**
 * Thread item that renders a widget payload.
 */
export interface WidgetMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: WidgetMessageItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.widget`. */
  type: WidgetMessageItemType;
  /** Serialized widget payload rendered in the UI. */
  widget: string;
}

export type ClientToolCallStatus = typeof ClientToolCallStatus[keyof typeof ClientToolCallStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClientToolCallStatus = {
  in_progress: 'in_progress',
  completed: 'completed',
} as const;

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type ClientToolCallItemObject = typeof ClientToolCallItemObject[keyof typeof ClientToolCallItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClientToolCallItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

/**
 * Type discriminator that is always `chatkit.client_tool_call`.
 */
export type ClientToolCallItemType = typeof ClientToolCallItemType[keyof typeof ClientToolCallItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClientToolCallItemType = {
  chatkitclient_tool_call: 'chatkit.client_tool_call',
} as const;

export type ClientToolCallItemOutput = string | null;

/**
 * Record of a client side tool invocation initiated by the assistant.
 */
export interface ClientToolCallItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: ClientToolCallItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.client_tool_call`. */
  type: ClientToolCallItemType;
  /** Execution status for the tool call. */
  status: ClientToolCallStatus;
  /** Identifier for the client tool call. */
  call_id: string;
  /** Tool name that was invoked. */
  name: string;
  /** JSON-encoded arguments that were sent to the tool. */
  arguments: string;
  output: ClientToolCallItemOutput;
}

export type TaskType = typeof TaskType[keyof typeof TaskType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TaskType = {
  custom: 'custom',
  thought: 'thought',
} as const;

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type TaskItemObject = typeof TaskItemObject[keyof typeof TaskItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TaskItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

/**
 * Type discriminator that is always `chatkit.task`.
 */
export type TaskItemType = typeof TaskItemType[keyof typeof TaskItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TaskItemType = {
  chatkittask: 'chatkit.task',
} as const;

export type TaskItemHeading = string | null;

export type TaskItemSummary = string | null;

/**
 * Task emitted by the workflow to show progress and status updates.
 */
export interface TaskItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: TaskItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.task`. */
  type: TaskItemType;
  /** Subtype for the task. */
  task_type: TaskType;
  heading: TaskItemHeading;
  summary: TaskItemSummary;
}

export type TaskGroupTaskHeading = string | null;

export type TaskGroupTaskSummary = string | null;

/**
 * Task entry that appears within a TaskGroup.
 */
export interface TaskGroupTask {
  /** Subtype for the grouped task. */
  type: TaskType;
  heading: TaskGroupTaskHeading;
  summary: TaskGroupTaskSummary;
}

/**
 * Type discriminator that is always `chatkit.thread_item`.
 */
export type TaskGroupItemObject = typeof TaskGroupItemObject[keyof typeof TaskGroupItemObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TaskGroupItemObject = {
  chatkitthread_item: 'chatkit.thread_item',
} as const;

/**
 * Type discriminator that is always `chatkit.task_group`.
 */
export type TaskGroupItemType = typeof TaskGroupItemType[keyof typeof TaskGroupItemType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const TaskGroupItemType = {
  chatkittask_group: 'chatkit.task_group',
} as const;

/**
 * Collection of workflow tasks grouped together in the thread.
 */
export interface TaskGroupItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: TaskGroupItemObject;
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.task_group`. */
  type: TaskGroupItemType;
  /** Tasks included in the group. */
  tasks: TaskGroupTask[];
}

export type ThreadItem = UserMessageItem | AssistantMessageItem | WidgetMessageItem | ClientToolCallItem | TaskItem | TaskGroupItem;

export type ThreadItemListResourceFirstId = string | null;

export type ThreadItemListResourceLastId = string | null;

/**
 * A paginated list of thread items rendered for the ChatKit API.
 */
export interface ThreadItemListResource {
  /** The type of object returned, must be `list`. */
  object: 'list';
  /** A list of items */
  data: ThreadItem[];
  first_id: ThreadItemListResourceFirstId;
  last_id: ThreadItemListResourceLastId;
  /** Whether there are more items available. */
  has_more: boolean;
}

/**
 * Status discriminator that is always `active`.
 */
export type ActiveStatusType = typeof ActiveStatusType[keyof typeof ActiveStatusType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ActiveStatusType = {
  active: 'active',
} as const;

/**
 * Indicates that a thread is active.
 */
export interface ActiveStatus {
  /** Status discriminator that is always `active`. */
  type: ActiveStatusType;
}

/**
 * Status discriminator that is always `locked`.
 */
export type LockedStatusType = typeof LockedStatusType[keyof typeof LockedStatusType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LockedStatusType = {
  locked: 'locked',
} as const;

export type LockedStatusReason = string | null;

/**
 * Indicates that a thread is locked and cannot accept new input.
 */
export interface LockedStatus {
  /** Status discriminator that is always `locked`. */
  type: LockedStatusType;
  reason: LockedStatusReason;
}

/**
 * Status discriminator that is always `closed`.
 */
export type ClosedStatusType = typeof ClosedStatusType[keyof typeof ClosedStatusType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ClosedStatusType = {
  closed: 'closed',
} as const;

export type ClosedStatusReason = string | null;

/**
 * Indicates that a thread has been closed.
 */
export interface ClosedStatus {
  /** Status discriminator that is always `closed`. */
  type: ClosedStatusType;
  reason: ClosedStatusReason;
}

/**
 * Type discriminator that is always `chatkit.thread`.
 */
export type ThreadResourceObject = typeof ThreadResourceObject[keyof typeof ThreadResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ThreadResourceObject = {
  chatkitthread: 'chatkit.thread',
} as const;

export type ThreadResourceTitle = string | null;

/**
 * Current status for the thread. Defaults to `active` for newly created threads.
 */
export type ThreadResourceStatus = ActiveStatus | LockedStatus | ClosedStatus;

/**
 * Represents a ChatKit thread and its current status.
 */
export interface ThreadResource {
  /** Identifier of the thread. */
  id: string;
  /** Type discriminator that is always `chatkit.thread`. */
  object: ThreadResourceObject;
  /** Unix timestamp (in seconds) for when the thread was created. */
  created_at: number;
  title: ThreadResourceTitle;
  /** Current status for the thread. Defaults to `active` for newly created threads. */
  status: ThreadResourceStatus;
  /** Free-form string that identifies your end user who owns the thread. */
  user: string;
}

/**
 * Type discriminator that is always `chatkit.thread.deleted`.
 */
export type DeletedThreadResourceObject = typeof DeletedThreadResourceObject[keyof typeof DeletedThreadResourceObject];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeletedThreadResourceObject = {
  chatkitthreaddeleted: 'chatkit.thread.deleted',
} as const;

/**
 * Confirmation payload returned after deleting a thread.
 */
export interface DeletedThreadResource {
  /** Identifier of the deleted thread. */
  id: string;
  /** Type discriminator that is always `chatkit.thread.deleted`. */
  object: DeletedThreadResourceObject;
  /** Indicates that the thread has been deleted. */
  deleted: boolean;
}

export type ThreadListResourceFirstId = string | null;

export type ThreadListResourceLastId = string | null;

/**
 * A paginated list of ChatKit threads.
 */
export interface ThreadListResource {
  /** The type of object returned, must be `list`. */
  object: 'list';
  /** A list of items */
  data: ThreadResource[];
  first_id: ThreadListResourceFirstId;
  last_id: ThreadListResourceLastId;
  /** Whether there are more items available. */
  has_more: boolean;
}

export interface RealtimeConnectParams {
  model?: string;
  call_id?: string;
}

/**
 * Always `image_url`.
 */
export type ModerationImageURLInputType = typeof ModerationImageURLInputType[keyof typeof ModerationImageURLInputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ModerationImageURLInputType = {
  image_url: 'image_url',
} as const;

/**
 * Contains either an image URL or a data URL for a base64 encoded image.
 */
export type ModerationImageURLInputImageUrl = {
  /** Either a URL of the image or the base64 encoded image data. */
  url: string;
};

/**
 * An object describing an image to classify.
 */
export interface ModerationImageURLInput {
  /** Always `image_url`. */
  type: ModerationImageURLInputType;
  /** Contains either an image URL or a data URL for a base64 encoded image. */
  image_url: ModerationImageURLInputImageUrl;
}

/**
 * Always `text`.
 */
export type ModerationTextInputType = typeof ModerationTextInputType[keyof typeof ModerationTextInputType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ModerationTextInputType = {
  text: 'text',
} as const;

/**
 * An object describing text to classify.
 */
export interface ModerationTextInput {
  /** Always `text`. */
  type: ModerationTextInputType;
  /** A string of text to classify. */
  text: string;
}

export type ComparisonFilterValueItems = string | number;

/**
 * The strategy used to chunk the file.
 */
export type ChunkingStrategyResponse = StaticChunkingStrategyResponseParam | OtherChunkingStrategyResponseParam;

/**
 * The intended purpose of the uploaded file. One of: - `assistants`: Used in the Assistants API - `batch`: Used in the Batch API - `fine-tune`: Used for fine-tuning - `vision`: Images used for vision fine-tuning - `user_data`: Flexible file type for any purpose - `evals`: Used for eval data sets

 */
export type FilePurpose = typeof FilePurpose[keyof typeof FilePurpose];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FilePurpose = {
  assistants: 'assistants',
  batch: 'batch',
  'fine-tune': 'fine-tune',
  vision: 'vision',
  user_data: 'user_data',
  evals: 'evals',
} as const;

export type BatchErrorParam = string | null;

export type BatchErrorLine = number | null;

export interface BatchError {
  /** An error code identifying the error type. */
  code?: string;
  /** A human-readable message providing more details about the error. */
  message?: string;
  param?: BatchErrorParam;
  line?: BatchErrorLine;
}

/**
 * The request counts for different statuses within the batch.
 */
export interface BatchRequestCounts {
  /** Total number of requests in the batch. */
  total: number;
  /** Number of requests that have been completed successfully. */
  completed: number;
  /** Number of requests that have failed. */
  failed: number;
}

export type AssistantTool = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

export type TextAnnotationDelta = MessageDeltaContentTextAnnotationsFileCitationObject | MessageDeltaContentTextAnnotationsFilePathObject;

export type TextAnnotation = MessageContentTextAnnotationsFileCitationObject | MessageContentTextAnnotationsFilePathObject;

export type RunStepDetailsToolCall = RunStepDetailsToolCallsCodeObject | RunStepDetailsToolCallsFileSearchObject | RunStepDetailsToolCallsFunctionObject;

export type RunStepDeltaStepDetailsToolCall = RunStepDeltaStepDetailsToolCallsCodeObject | RunStepDeltaStepDetailsToolCallsFileSearchObject | RunStepDeltaStepDetailsToolCallsFunctionObject;

export type MessageContent = MessageContentImageFileObject | MessageContentImageUrlObject | MessageContentTextObject | MessageContentRefusalObject;

export type MessageContentDelta = MessageDeltaContentImageFileObject | MessageDeltaContentTextObject | MessageDeltaContentRefusalObject | MessageDeltaContentImageUrlObject;

export type ChatModel = typeof ChatModel[keyof typeof ChatModel];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ChatModel = {
  'gpt-5': 'gpt-5',
  'gpt-5-mini': 'gpt-5-mini',
  'gpt-5-nano': 'gpt-5-nano',
  'gpt-5-2025-08-07': 'gpt-5-2025-08-07',
  'gpt-5-mini-2025-08-07': 'gpt-5-mini-2025-08-07',
  'gpt-5-nano-2025-08-07': 'gpt-5-nano-2025-08-07',
  'gpt-5-chat-latest': 'gpt-5-chat-latest',
  'gpt-41': 'gpt-4.1',
  'gpt-41-mini': 'gpt-4.1-mini',
  'gpt-41-nano': 'gpt-4.1-nano',
  'gpt-41-2025-04-14': 'gpt-4.1-2025-04-14',
  'gpt-41-mini-2025-04-14': 'gpt-4.1-mini-2025-04-14',
  'gpt-41-nano-2025-04-14': 'gpt-4.1-nano-2025-04-14',
  'o4-mini': 'o4-mini',
  'o4-mini-2025-04-16': 'o4-mini-2025-04-16',
  o3: 'o3',
  'o3-2025-04-16': 'o3-2025-04-16',
  'o3-mini': 'o3-mini',
  'o3-mini-2025-01-31': 'o3-mini-2025-01-31',
  o1: 'o1',
  'o1-2024-12-17': 'o1-2024-12-17',
  'o1-preview': 'o1-preview',
  'o1-preview-2024-09-12': 'o1-preview-2024-09-12',
  'o1-mini': 'o1-mini',
  'o1-mini-2024-09-12': 'o1-mini-2024-09-12',
  'gpt-4o': 'gpt-4o',
  'gpt-4o-2024-11-20': 'gpt-4o-2024-11-20',
  'gpt-4o-2024-08-06': 'gpt-4o-2024-08-06',
  'gpt-4o-2024-05-13': 'gpt-4o-2024-05-13',
  'gpt-4o-audio-preview': 'gpt-4o-audio-preview',
  'gpt-4o-audio-preview-2024-10-01': 'gpt-4o-audio-preview-2024-10-01',
  'gpt-4o-audio-preview-2024-12-17': 'gpt-4o-audio-preview-2024-12-17',
  'gpt-4o-audio-preview-2025-06-03': 'gpt-4o-audio-preview-2025-06-03',
  'gpt-4o-mini-audio-preview': 'gpt-4o-mini-audio-preview',
  'gpt-4o-mini-audio-preview-2024-12-17': 'gpt-4o-mini-audio-preview-2024-12-17',
  'gpt-4o-search-preview': 'gpt-4o-search-preview',
  'gpt-4o-mini-search-preview': 'gpt-4o-mini-search-preview',
  'gpt-4o-search-preview-2025-03-11': 'gpt-4o-search-preview-2025-03-11',
  'gpt-4o-mini-search-preview-2025-03-11': 'gpt-4o-mini-search-preview-2025-03-11',
  'chatgpt-4o-latest': 'chatgpt-4o-latest',
  'codex-mini-latest': 'codex-mini-latest',
  'gpt-4o-mini': 'gpt-4o-mini',
  'gpt-4o-mini-2024-07-18': 'gpt-4o-mini-2024-07-18',
  'gpt-4-turbo': 'gpt-4-turbo',
  'gpt-4-turbo-2024-04-09': 'gpt-4-turbo-2024-04-09',
  'gpt-4-0125-preview': 'gpt-4-0125-preview',
  'gpt-4-turbo-preview': 'gpt-4-turbo-preview',
  'gpt-4-1106-preview': 'gpt-4-1106-preview',
  'gpt-4-vision-preview': 'gpt-4-vision-preview',
  'gpt-4': 'gpt-4',
  'gpt-4-0314': 'gpt-4-0314',
  'gpt-4-0613': 'gpt-4-0613',
  'gpt-4-32k': 'gpt-4-32k',
  'gpt-4-32k-0314': 'gpt-4-32k-0314',
  'gpt-4-32k-0613': 'gpt-4-32k-0613',
  'gpt-35-turbo': 'gpt-3.5-turbo',
  'gpt-35-turbo-16k': 'gpt-3.5-turbo-16k',
  'gpt-35-turbo-0301': 'gpt-3.5-turbo-0301',
  'gpt-35-turbo-0613': 'gpt-3.5-turbo-0613',
  'gpt-35-turbo-1106': 'gpt-3.5-turbo-1106',
  'gpt-35-turbo-0125': 'gpt-3.5-turbo-0125',
  'gpt-35-turbo-16k-0613': 'gpt-3.5-turbo-16k-0613',
} as const;

/**
 * The type of the object. Always `summary_text`.
 */
export type SummaryType = typeof SummaryType[keyof typeof SummaryType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SummaryType = {
  summary_text: 'summary_text',
} as const;

/**
 * A summary text from the model.
 */
export interface Summary {
  /** The type of the object. Always `summary_text`. */
  type: SummaryType;
  /** A summary of the reasoning output from the model so far. */
  text: string;
}

/**
 * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
 * @nullable
 */
export type CreateThreadAndRunRequestWithoutStreamModel = string | 'gpt-5' | 'gpt-5-mini' | 'gpt-5-nano' | 'gpt-5-2025-08-07' | 'gpt-5-mini-2025-08-07' | 'gpt-5-nano-2025-08-07' | 'gpt-4.1' | 'gpt-4.1-mini' | 'gpt-4.1-nano' | 'gpt-4.1-2025-04-14' | 'gpt-4.1-mini-2025-04-14' | 'gpt-4.1-nano-2025-04-14' | 'gpt-4o' | 'gpt-4o-2024-11-20' | 'gpt-4o-2024-08-06' | 'gpt-4o-2024-05-13' | 'gpt-4o-mini' | 'gpt-4o-mini-2024-07-18' | 'gpt-4.5-preview' | 'gpt-4.5-preview-2025-02-27' | 'gpt-4-turbo' | 'gpt-4-turbo-2024-04-09' | 'gpt-4-0125-preview' | 'gpt-4-turbo-preview' | 'gpt-4-1106-preview' | 'gpt-4-vision-preview' | 'gpt-4' | 'gpt-4-0314' | 'gpt-4-0613' | 'gpt-4-32k' | 'gpt-4-32k-0314' | 'gpt-4-32k-0613' | 'gpt-3.5-turbo' | 'gpt-3.5-turbo-16k' | 'gpt-3.5-turbo-0613' | 'gpt-3.5-turbo-1106' | 'gpt-3.5-turbo-0125' | 'gpt-3.5-turbo-16k-0613' | null;

export type CreateThreadAndRunRequestWithoutStreamToolResourcesCodeInterpreter = {
  /**
   * A list of [file](https://platform.openai.com/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool.

   * @maxItems 20
   */
  file_ids?: string[];
};

export type CreateThreadAndRunRequestWithoutStreamToolResourcesFileSearch = {
  /**
   * The ID of the [vector store](https://platform.openai.com/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant.

   * @maxItems 1
   */
  vector_store_ids?: string[];
};

/**
 * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

 * @nullable
 */
export type CreateThreadAndRunRequestWithoutStreamToolResources = {
  code_interpreter?: CreateThreadAndRunRequestWithoutStreamToolResourcesCodeInterpreter;
  file_search?: CreateThreadAndRunRequestWithoutStreamToolResourcesFileSearch;
} | null;

/**
 * @nullable
 */
export type CreateThreadAndRunRequestWithoutStreamTruncationStrategyAllOf = unknown | null;

export type CreateThreadAndRunRequestWithoutStreamTruncationStrategy = TruncationObject & CreateThreadAndRunRequestWithoutStreamTruncationStrategyAllOf;

/**
 * @nullable
 */
export type CreateThreadAndRunRequestWithoutStreamToolChoiceAllOf = unknown | null;

export type CreateThreadAndRunRequestWithoutStreamToolChoice = AssistantsApiToolChoiceOption & CreateThreadAndRunRequestWithoutStreamToolChoiceAllOf;

export interface CreateThreadAndRunRequestWithoutStream {
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  thread?: CreateThreadRequest;
  /**
   * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
   * @nullable
   */
  model?: CreateThreadAndRunRequestWithoutStreamModel;
  /**
   * Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis.
   * @nullable
   */
  instructions?: string | null;
  /**
   * Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
   * @maxItems 20
   * @nullable
   */
  tools?: AssistantTool[] | null;
  /**
   * A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.

   * @nullable
   */
  tool_resources?: CreateThreadAndRunRequestWithoutStreamToolResources;
  metadata?: Metadata;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

   * @minimum 0
   * @maximum 2
   * @nullable
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or temperature but not both.

   * @minimum 0
   * @maximum 1
   * @nullable
   */
  top_p?: number | null;
  /**
   * The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_prompt_tokens?: number | null;
  /**
   * The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_completion_tokens?: number | null;
  truncation_strategy?: CreateThreadAndRunRequestWithoutStreamTruncationStrategy;
  tool_choice?: CreateThreadAndRunRequestWithoutStreamToolChoice;
  parallel_tool_calls?: ParallelToolCalls;
  /** @nullable */
  response_format?: AssistantsApiResponseFormatOption;
}

/**
 * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
 * @nullable
 */
export type CreateRunRequestWithoutStreamModel = string | AssistantSupportedModels | null;

/**
 * @nullable
 */
export type CreateRunRequestWithoutStreamTruncationStrategyAllOf = unknown | null;

export type CreateRunRequestWithoutStreamTruncationStrategy = TruncationObject & CreateRunRequestWithoutStreamTruncationStrategyAllOf;

/**
 * @nullable
 */
export type CreateRunRequestWithoutStreamToolChoiceAllOf = unknown | null;

export type CreateRunRequestWithoutStreamToolChoice = AssistantsApiToolChoiceOption & CreateRunRequestWithoutStreamToolChoiceAllOf;

export interface CreateRunRequestWithoutStream {
  /** The ID of the [assistant](https://platform.openai.com/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  /**
   * The ID of the [Model](https://platform.openai.com/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
   * @nullable
   */
  model?: CreateRunRequestWithoutStreamModel;
  reasoning_effort?: ReasoningEffort;
  /**
   * Overrides the [instructions](https://platform.openai.com/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for modifying the behavior on a per-run basis.
   * @nullable
   */
  instructions?: string | null;
  /**
   * Appends additional instructions at the end of the instructions for the run. This is useful for modifying the behavior on a per-run basis without overriding other instructions.
   * @nullable
   */
  additional_instructions?: string | null;
  /**
   * Adds additional messages to the thread before creating the run.
   * @nullable
   */
  additional_messages?: CreateMessageRequest[] | null;
  /**
   * Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
   * @maxItems 20
   * @nullable
   */
  tools?: AssistantTool[] | null;
  metadata?: Metadata;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.

   * @minimum 0
   * @maximum 2
   * @nullable
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.

We generally recommend altering this or temperature but not both.

   * @minimum 0
   * @maximum 1
   * @nullable
   */
  top_p?: number | null;
  /**
   * The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_prompt_tokens?: number | null;
  /**
   * The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.

   * @minimum 256
   * @nullable
   */
  max_completion_tokens?: number | null;
  truncation_strategy?: CreateRunRequestWithoutStreamTruncationStrategy;
  tool_choice?: CreateRunRequestWithoutStreamToolChoice;
  parallel_tool_calls?: ParallelToolCalls;
  /** @nullable */
  response_format?: AssistantsApiResponseFormatOption;
}

export type SubmitToolOutputsRunRequestWithoutStreamToolOutputsItem = {
  /** The ID of the tool call in the `required_action` object within the run object the output is being submitted for. */
  tool_call_id?: string;
  /** The output of the tool call to be submitted to continue the run. */
  output?: string;
};

export interface SubmitToolOutputsRunRequestWithoutStream {
  /** A list of tools for which the outputs are being submitted. */
  tool_outputs: SubmitToolOutputsRunRequestWithoutStreamToolOutputsItem[];
}

/**
 * The status of the run, which can be either `queued`, `in_progress`, `requires_action`, `cancelling`, `cancelled`, `failed`, `completed`, `incomplete`, or `expired`.
 */
export type RunStatus = typeof RunStatus[keyof typeof RunStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RunStatus = {
  queued: 'queued',
  in_progress: 'in_progress',
  requires_action: 'requires_action',
  cancelling: 'cancelling',
  cancelled: 'cancelled',
  failed: 'failed',
  completed: 'completed',
  incomplete: 'incomplete',
  expired: 'expired',
} as const;

/**
 * The details of the run step.
 */
export type RunStepDeltaObjectDeltaStepDetails = RunStepDeltaStepDetailsMessageCreationObject | RunStepDeltaStepDetailsToolCallsObject;

/**
 * The delta containing the fields that have changed on the run step.
 */
export interface RunStepDeltaObjectDelta {
  /** The details of the run step. */
  step_details?: RunStepDeltaObjectDeltaStepDetails;
}

/**
 * Always `auto`.
 */
export type CodeInterpreterContainerAutoType = typeof CodeInterpreterContainerAutoType[keyof typeof CodeInterpreterContainerAutoType];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CodeInterpreterContainerAutoType = {
  auto: 'auto',
} as const;

/**
 * Configuration for a code interpreter container. Optionally specify the IDs of the files to run the code on.
 */
export interface CodeInterpreterContainerAuto {
  /** Always `auto`. */
  type: CodeInterpreterContainerAutoType;
  /**
   * An optional list of uploaded files to make available to your code.
   * @maxItems 50
   */
  file_ids?: string[];
}

export type ListAssistantsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListAssistantsOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
};

export type ListAssistantsOrder = typeof ListAssistantsOrder[keyof typeof ListAssistantsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListAssistantsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type CreateTranscription200One = CreateTranscriptionResponseJson | CreateTranscriptionResponseDiarizedJson | CreateTranscriptionResponseVerboseJson;

export type CreateTranslation200 = CreateTranslationResponseJson | CreateTranslationResponseVerboseJson;

/**
 * The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`, `/v1/embeddings`, and `/v1/completions` are supported. Note that `/v1/embeddings` batches are also restricted to a maximum of 50,000 embedding inputs across all requests in the batch.
 */
export type CreateBatchBodyEndpoint = typeof CreateBatchBodyEndpoint[keyof typeof CreateBatchBodyEndpoint];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateBatchBodyEndpoint = {
  '/v1/responses': '/v1/responses',
  '/v1/chat/completions': '/v1/chat/completions',
  '/v1/embeddings': '/v1/embeddings',
  '/v1/completions': '/v1/completions',
} as const;

/**
 * The time frame within which the batch should be processed. Currently only `24h` is supported.
 */
export type CreateBatchBodyCompletionWindow = typeof CreateBatchBodyCompletionWindow[keyof typeof CreateBatchBodyCompletionWindow];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateBatchBodyCompletionWindow = {
  '24h': '24h',
} as const;

export type CreateBatchBody = {
  /** The ID of an uploaded file that contains requests for the new batch.

See [upload file](https://platform.openai.com/docs/api-reference/files/create) for how to upload a file.

Your input file must be formatted as a [JSONL file](https://platform.openai.com/docs/api-reference/batch/request-input), and must be uploaded with the purpose `batch`. The file can contain up to 50,000 requests, and can be up to 200 MB in size.
 */
  input_file_id: string;
  /** The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`, `/v1/embeddings`, and `/v1/completions` are supported. Note that `/v1/embeddings` batches are also restricted to a maximum of 50,000 embedding inputs across all requests in the batch. */
  endpoint: CreateBatchBodyEndpoint;
  /** The time frame within which the batch should be processed. Currently only `24h` is supported. */
  completion_window: CreateBatchBodyCompletionWindow;
  metadata?: Metadata;
  output_expires_after?: BatchFileExpirationAfter;
};

export type ListBatchesParams = {
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
};

export type ListChatCompletionsParams = {
/**
 * The model used to generate the Chat Completions.
 */
model?: string;
/**
 * A list of metadata keys to filter the Chat Completions by. Example:

`metadata[key1]=value1&metadata[key2]=value2`

 */
metadata?: Metadata;
/**
 * Identifier for the last chat completion from the previous pagination request.
 */
after?: string;
/**
 * Number of Chat Completions to retrieve.
 */
limit?: number;
/**
 * Sort order for Chat Completions by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
 */
order?: ListChatCompletionsOrder;
};

export type ListChatCompletionsOrder = typeof ListChatCompletionsOrder[keyof typeof ListChatCompletionsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListChatCompletionsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type UpdateChatCompletionBody = {
  metadata: Metadata;
};

export type GetChatCompletionMessagesParams = {
/**
 * Identifier for the last message from the previous pagination request.
 */
after?: string;
/**
 * Number of messages to retrieve.
 */
limit?: number;
/**
 * Sort order for messages by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
 */
order?: GetChatCompletionMessagesOrder;
};

export type GetChatCompletionMessagesOrder = typeof GetChatCompletionMessagesOrder[keyof typeof GetChatCompletionMessagesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetChatCompletionMessagesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListContainersParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListContainersOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListContainersOrder = typeof ListContainersOrder[keyof typeof ListContainersOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListContainersOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListContainerFilesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListContainerFilesOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListContainerFilesOrder = typeof ListContainerFilesOrder[keyof typeof ListContainerFilesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListContainerFilesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type CreateConversationItemsParams = {
/**
 * Additional fields to include in the response. See the `include`
parameter for [listing Conversation items above](https://platform.openai.com/docs/api-reference/conversations/list-items#conversations_list_items-include) for more information.

 */
include?: IncludeEnum[];
};

export type CreateConversationItemsBody = {
  /**
   * The items to add to the conversation. You may add up to 20 items at a time.

   * @maxItems 20
   */
  items: InputItem[];
};

export type ListConversationItemsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between
1 and 100, and the default is 20.

 */
limit?: number;
/**
 * The order to return the input items in. Default is `desc`.
- `asc`: Return the input items in ascending order.
- `desc`: Return the input items in descending order.

 */
order?: ListConversationItemsOrder;
/**
 * An item ID to list items after, used in pagination.

 */
after?: string;
/**
 * Specify additional output data to include in the model response. Currently supported values are:
- `web_search_call.action.sources`: Include the sources of the web search tool call.
- `code_interpreter_call.outputs`: Includes the outputs of python code execution in code interpreter tool call items.
- `computer_call_output.output.image_url`: Include image urls from the computer call output.
- `file_search_call.results`: Include the search results of the file search tool call.
- `message.input_image.image_url`: Include image urls from the input message.
- `message.output_text.logprobs`: Include logprobs with assistant messages.
- `reasoning.encrypted_content`: Includes an encrypted version of reasoning tokens in reasoning item outputs. This enables reasoning items to be used in multi-turn conversations when using the Responses API statelessly (like when the `store` parameter is set to `false`, or when an organization is enrolled in the zero data retention program).
 */
include?: IncludeEnum[];
};

export type ListConversationItemsOrder = typeof ListConversationItemsOrder[keyof typeof ListConversationItemsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListConversationItemsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type GetConversationItemParams = {
/**
 * Additional fields to include in the response. See the `include`
parameter for [listing Conversation items above](https://platform.openai.com/docs/api-reference/conversations/list-items#conversations_list_items-include) for more information.

 */
include?: IncludeEnum[];
};

export type ListEvalsParams = {
/**
 * Identifier for the last eval from the previous pagination request.
 */
after?: string;
/**
 * Number of evals to retrieve.
 */
limit?: number;
/**
 * Sort order for evals by timestamp. Use `asc` for ascending order or `desc` for descending order.
 */
order?: ListEvalsOrder;
/**
 * Evals can be ordered by creation time or last updated time. Use
`created_at` for creation time or `updated_at` for last updated time.

 */
order_by?: ListEvalsOrderBy;
};

export type ListEvalsOrder = typeof ListEvalsOrder[keyof typeof ListEvalsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListEvalsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListEvalsOrderBy = typeof ListEvalsOrderBy[keyof typeof ListEvalsOrderBy];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListEvalsOrderBy = {
  created_at: 'created_at',
  updated_at: 'updated_at',
} as const;

export type UpdateEvalBody = {
  /** Rename the evaluation. */
  name?: string;
  metadata?: Metadata;
};

export type DeleteEval200 = {
  object: string;
  deleted: boolean;
  eval_id: string;
};

export type GetEvalRunsParams = {
/**
 * Identifier for the last run from the previous pagination request.
 */
after?: string;
/**
 * Number of runs to retrieve.
 */
limit?: number;
/**
 * Sort order for runs by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
 */
order?: GetEvalRunsOrder;
/**
 * Filter runs by status. One of `queued` | `in_progress` | `failed` | `completed` | `canceled`.
 */
status?: GetEvalRunsStatus;
};

export type GetEvalRunsOrder = typeof GetEvalRunsOrder[keyof typeof GetEvalRunsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetEvalRunsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type GetEvalRunsStatus = typeof GetEvalRunsStatus[keyof typeof GetEvalRunsStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetEvalRunsStatus = {
  queued: 'queued',
  in_progress: 'in_progress',
  completed: 'completed',
  canceled: 'canceled',
  failed: 'failed',
} as const;

export type DeleteEvalRun200 = {
  object?: string;
  deleted?: boolean;
  run_id?: string;
};

export type GetEvalRunOutputItemsParams = {
/**
 * Identifier for the last output item from the previous pagination request.
 */
after?: string;
/**
 * Number of output items to retrieve.
 */
limit?: number;
/**
 * Filter output items by status. Use `failed` to filter by failed output
items or `pass` to filter by passed output items.

 */
status?: GetEvalRunOutputItemsStatus;
/**
 * Sort order for output items by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
 */
order?: GetEvalRunOutputItemsOrder;
};

export type GetEvalRunOutputItemsStatus = typeof GetEvalRunOutputItemsStatus[keyof typeof GetEvalRunOutputItemsStatus];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetEvalRunOutputItemsStatus = {
  fail: 'fail',
  pass: 'pass',
} as const;

export type GetEvalRunOutputItemsOrder = typeof GetEvalRunOutputItemsOrder[keyof typeof GetEvalRunOutputItemsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetEvalRunOutputItemsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListFilesParams = {
/**
 * Only return files with the given purpose.
 */
purpose?: string;
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 10,000, and the default is 10,000.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListFilesOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListFilesOrder = typeof ListFilesOrder[keyof typeof ListFilesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFilesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListFineTuningCheckpointPermissionsParams = {
/**
 * The ID of the project to get permissions for.
 */
project_id?: string;
/**
 * Identifier for the last permission ID from the previous pagination request.
 */
after?: string;
/**
 * Number of permissions to retrieve.
 */
limit?: number;
/**
 * The order in which to retrieve permissions.
 */
order?: ListFineTuningCheckpointPermissionsOrder;
};

export type ListFineTuningCheckpointPermissionsOrder = typeof ListFineTuningCheckpointPermissionsOrder[keyof typeof ListFineTuningCheckpointPermissionsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFineTuningCheckpointPermissionsOrder = {
  ascending: 'ascending',
  descending: 'descending',
} as const;

export type ListPaginatedFineTuningJobsParams = {
/**
 * Identifier for the last job from the previous pagination request.
 */
after?: string;
/**
 * Number of fine-tuning jobs to retrieve.
 */
limit?: number;
/**
 * Optional metadata filter. To filter, use the syntax `metadata[k]=v`. Alternatively, set `metadata=null` to indicate no metadata.

 */
metadata?: {[key: string]: string} | null;
};

export type ListFineTuningJobCheckpointsParams = {
/**
 * Identifier for the last checkpoint ID from the previous pagination request.
 */
after?: string;
/**
 * Number of checkpoints to retrieve.
 */
limit?: number;
};

export type ListFineTuningEventsParams = {
/**
 * Identifier for the last event from the previous pagination request.
 */
after?: string;
/**
 * Number of events to retrieve.
 */
limit?: number;
};

export type AdminApiKeysListParams = {
after?: string | null;
order?: AdminApiKeysListOrder;
limit?: number;
};

export type AdminApiKeysListOrder = typeof AdminApiKeysListOrder[keyof typeof AdminApiKeysListOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AdminApiKeysListOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type AdminApiKeysCreateBody = {
  name: string;
};

export type AdminApiKeysDelete200 = {
  id?: string;
  object?: string;
  deleted?: boolean;
};

export type ListAuditLogsParams = {
/**
 * Return only events whose `effective_at` (Unix seconds) is in this range.
 */
effective_at?: {
  /** Return only events whose `effective_at` (Unix seconds) is greater than this value. */
  gt?: number;
  /** Return only events whose `effective_at` (Unix seconds) is greater than or equal to this value. */
  gte?: number;
  /** Return only events whose `effective_at` (Unix seconds) is less than this value. */
  lt?: number;
  /** Return only events whose `effective_at` (Unix seconds) is less than or equal to this value. */
  lte?: number;
};
/**
 * Return only events for these projects.
 */
'project_ids[]'?: string[];
/**
 * Return only events with a `type` in one of these values. For example, `project.created`. For all options, see the documentation for the [audit log object](https://platform.openai.com/docs/api-reference/audit-logs/object).
 */
'event_types[]'?: AuditLogEventType[];
/**
 * Return only events performed by these actors. Can be a user ID, a service account ID, or an api key tracking ID.
 */
'actor_ids[]'?: string[];
/**
 * Return only events performed by users with these emails.
 */
'actor_emails[]'?: string[];
/**
 * Return only events performed on these targets. For example, a project ID updated.
 */
'resource_ids[]'?: string[];
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
};

export type ListOrganizationCertificatesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListOrganizationCertificatesOrder;
};

export type ListOrganizationCertificatesOrder = typeof ListOrganizationCertificatesOrder[keyof typeof ListOrganizationCertificatesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListOrganizationCertificatesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type GetCertificateParams = {
/**
 * A list of additional fields to include in the response. Currently the only supported value is `content` to fetch the PEM content of the certificate.
 */
include?: GetCertificateIncludeItem[];
};

export type GetCertificateIncludeItem = typeof GetCertificateIncludeItem[keyof typeof GetCertificateIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetCertificateIncludeItem = {
  content: 'content',
} as const;

export type UsageCostsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently only `1d` is supported, default to `1d`.
 */
bucket_width?: UsageCostsBucketWidth;
/**
 * Return only costs for these projects.
 */
project_ids?: string[];
/**
 * Group the costs by the specified fields. Support fields include `project_id`, `line_item` and any combination of them.
 */
group_by?: UsageCostsGroupByItem[];
/**
 * A limit on the number of buckets to be returned. Limit can range between 1 and 180, and the default is 7.

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageCostsBucketWidth = typeof UsageCostsBucketWidth[keyof typeof UsageCostsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCostsBucketWidth = {
  '1d': '1d',
} as const;

export type UsageCostsGroupByItem = typeof UsageCostsGroupByItem[keyof typeof UsageCostsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCostsGroupByItem = {
  project_id: 'project_id',
  line_item: 'line_item',
} as const;

export type ListInvitesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListProjectsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * If `true` returns all projects including those that have been `archived`. Archived projects are not included by default.
 */
include_archived?: boolean;
};

export type ListProjectApiKeysParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListProjectCertificatesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListProjectCertificatesOrder;
};

export type ListProjectCertificatesOrder = typeof ListProjectCertificatesOrder[keyof typeof ListProjectCertificatesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListProjectCertificatesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListProjectRateLimitsParams = {
/**
 * A limit on the number of objects to be returned. The default is 100.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
};

export type ListProjectServiceAccountsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type ListProjectUsersParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
};

export type UsageAudioSpeechesParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageAudioSpeechesBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
 */
group_by?: UsageAudioSpeechesGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageAudioSpeechesBucketWidth = typeof UsageAudioSpeechesBucketWidth[keyof typeof UsageAudioSpeechesBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioSpeechesBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageAudioSpeechesGroupByItem = typeof UsageAudioSpeechesGroupByItem[keyof typeof UsageAudioSpeechesGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioSpeechesGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
} as const;

export type UsageAudioTranscriptionsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageAudioTranscriptionsBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
 */
group_by?: UsageAudioTranscriptionsGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageAudioTranscriptionsBucketWidth = typeof UsageAudioTranscriptionsBucketWidth[keyof typeof UsageAudioTranscriptionsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioTranscriptionsBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageAudioTranscriptionsGroupByItem = typeof UsageAudioTranscriptionsGroupByItem[keyof typeof UsageAudioTranscriptionsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageAudioTranscriptionsGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
} as const;

export type UsageCodeInterpreterSessionsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageCodeInterpreterSessionsBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`.
 */
group_by?: UsageCodeInterpreterSessionsGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageCodeInterpreterSessionsBucketWidth = typeof UsageCodeInterpreterSessionsBucketWidth[keyof typeof UsageCodeInterpreterSessionsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCodeInterpreterSessionsBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageCodeInterpreterSessionsGroupByItem = typeof UsageCodeInterpreterSessionsGroupByItem[keyof typeof UsageCodeInterpreterSessionsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCodeInterpreterSessionsGroupByItem = {
  project_id: 'project_id',
} as const;

export type UsageCompletionsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageCompletionsBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * If `true`, return batch jobs only. If `false`, return non-batch jobs only. By default, return both.

 */
batch?: boolean;
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `batch`, `service_tier` or any combination of them.
 */
group_by?: UsageCompletionsGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageCompletionsBucketWidth = typeof UsageCompletionsBucketWidth[keyof typeof UsageCompletionsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCompletionsBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageCompletionsGroupByItem = typeof UsageCompletionsGroupByItem[keyof typeof UsageCompletionsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageCompletionsGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
  batch: 'batch',
  service_tier: 'service_tier',
} as const;

export type UsageEmbeddingsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageEmbeddingsBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
 */
group_by?: UsageEmbeddingsGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageEmbeddingsBucketWidth = typeof UsageEmbeddingsBucketWidth[keyof typeof UsageEmbeddingsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageEmbeddingsBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageEmbeddingsGroupByItem = typeof UsageEmbeddingsGroupByItem[keyof typeof UsageEmbeddingsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageEmbeddingsGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
} as const;

export type UsageImagesParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageImagesBucketWidth;
/**
 * Return only usages for these sources. Possible values are `image.generation`, `image.edit`, `image.variation` or any combination of them.
 */
sources?: UsageImagesSourcesItem[];
/**
 * Return only usages for these image sizes. Possible values are `256x256`, `512x512`, `1024x1024`, `1792x1792`, `1024x1792` or any combination of them.
 */
sizes?: UsageImagesSizesItem[];
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `size`, `source` or any combination of them.
 */
group_by?: UsageImagesGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageImagesBucketWidth = typeof UsageImagesBucketWidth[keyof typeof UsageImagesBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageImagesBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageImagesSourcesItem = typeof UsageImagesSourcesItem[keyof typeof UsageImagesSourcesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageImagesSourcesItem = {
  imagegeneration: 'image.generation',
  imageedit: 'image.edit',
  imagevariation: 'image.variation',
} as const;

export type UsageImagesSizesItem = typeof UsageImagesSizesItem[keyof typeof UsageImagesSizesItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageImagesSizesItem = {
  '256x256': '256x256',
  '512x512': '512x512',
  '1024x1024': '1024x1024',
  '1792x1792': '1792x1792',
  '1024x1792': '1024x1792',
} as const;

export type UsageImagesGroupByItem = typeof UsageImagesGroupByItem[keyof typeof UsageImagesGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageImagesGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
  size: 'size',
  source: 'source',
} as const;

export type UsageModerationsParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageModerationsBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Return only usage for these users.
 */
user_ids?: string[];
/**
 * Return only usage for these API keys.
 */
api_key_ids?: string[];
/**
 * Return only usage for these models.
 */
models?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
 */
group_by?: UsageModerationsGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageModerationsBucketWidth = typeof UsageModerationsBucketWidth[keyof typeof UsageModerationsBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageModerationsBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageModerationsGroupByItem = typeof UsageModerationsGroupByItem[keyof typeof UsageModerationsGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageModerationsGroupByItem = {
  project_id: 'project_id',
  user_id: 'user_id',
  api_key_id: 'api_key_id',
  model: 'model',
} as const;

export type UsageVectorStoresParams = {
/**
 * Start time (Unix seconds) of the query time range, inclusive.
 */
start_time: number;
/**
 * End time (Unix seconds) of the query time range, exclusive.
 */
end_time?: number;
/**
 * Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
 */
bucket_width?: UsageVectorStoresBucketWidth;
/**
 * Return only usage for these projects.
 */
project_ids?: string[];
/**
 * Group the usage data by the specified fields. Support fields include `project_id`.
 */
group_by?: UsageVectorStoresGroupByItem[];
/**
 * Specifies the number of buckets to return.
- `bucket_width=1d`: default: 7, max: 31
- `bucket_width=1h`: default: 24, max: 168
- `bucket_width=1m`: default: 60, max: 1440

 */
limit?: number;
/**
 * A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
 */
page?: string;
};

export type UsageVectorStoresBucketWidth = typeof UsageVectorStoresBucketWidth[keyof typeof UsageVectorStoresBucketWidth];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageVectorStoresBucketWidth = {
  '1m': '1m',
  '1h': '1h',
  '1d': '1d',
} as const;

export type UsageVectorStoresGroupByItem = typeof UsageVectorStoresGroupByItem[keyof typeof UsageVectorStoresGroupByItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UsageVectorStoresGroupByItem = {
  project_id: 'project_id',
} as const;

export type ListUsersParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * Filter by the email address of users.
 */
emails?: string[];
};

export type GetResponseParams = {
/**
 * Additional fields to include in the response. See the `include`
parameter for Response creation above for more information.

 */
include?: IncludeEnum[];
/**
 * If set to true, the model response data will be streamed to the client
as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).
See the [Streaming section below](https://platform.openai.com/docs/api-reference/responses-streaming)
for more information.

 */
stream?: boolean;
/**
 * The sequence number of the event after which to start streaming.

 */
starting_after?: number;
/**
 * When true, stream obfuscation will be enabled. Stream obfuscation adds
random characters to an `obfuscation` field on streaming delta events
to normalize payload sizes as a mitigation to certain side-channel
attacks. These obfuscation fields are included by default, but add a
small amount of overhead to the data stream. You can set
`include_obfuscation` to false to optimize for bandwidth if you trust
the network links between your application and the OpenAI API.

 */
include_obfuscation?: boolean;
};

export type ListInputItemsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between
1 and 100, and the default is 20.

 */
limit?: number;
/**
 * The order to return the input items in. Default is `desc`.
- `asc`: Return the input items in ascending order.
- `desc`: Return the input items in descending order.

 */
order?: ListInputItemsOrder;
/**
 * An item ID to list items after, used in pagination.

 */
after?: string;
/**
 * Additional fields to include in the response. See the `include`
parameter for Response creation above for more information.

 */
include?: IncludeEnum[];
};

export type ListInputItemsOrder = typeof ListInputItemsOrder[keyof typeof ListInputItemsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListInputItemsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListMessagesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListMessagesOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
/**
 * Filter messages by the run ID that generated them.

 */
run_id?: string;
};

export type ListMessagesOrder = typeof ListMessagesOrder[keyof typeof ListMessagesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListMessagesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListRunsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListRunsOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
};

export type ListRunsOrder = typeof ListRunsOrder[keyof typeof ListRunsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRunsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type CreateRunParams = {
/**
 * A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.

See the [file search tool documentation](https://platform.openai.com/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.

 */
'include[]'?: CreateRunIncludeItem[];
};

export type CreateRunIncludeItem = typeof CreateRunIncludeItem[keyof typeof CreateRunIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRunIncludeItem = {
  'step_detailstool_calls[*]file_searchresults[*]content': 'step_details.tool_calls[*].file_search.results[*].content',
} as const;

export type ListRunStepsParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListRunStepsOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
/**
 * A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.

See the [file search tool documentation](https://platform.openai.com/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.

 */
'include[]'?: ListRunStepsIncludeItem[];
};

export type ListRunStepsOrder = typeof ListRunStepsOrder[keyof typeof ListRunStepsOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRunStepsOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListRunStepsIncludeItem = typeof ListRunStepsIncludeItem[keyof typeof ListRunStepsIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRunStepsIncludeItem = {
  'step_detailstool_calls[*]file_searchresults[*]content': 'step_details.tool_calls[*].file_search.results[*].content',
} as const;

export type GetRunStepParams = {
/**
 * A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.

See the [file search tool documentation](https://platform.openai.com/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.

 */
'include[]'?: GetRunStepIncludeItem[];
};

export type GetRunStepIncludeItem = typeof GetRunStepIncludeItem[keyof typeof GetRunStepIncludeItem];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRunStepIncludeItem = {
  'step_detailstool_calls[*]file_searchresults[*]content': 'step_details.tool_calls[*].file_search.results[*].content',
} as const;

export type ListVectorStoresParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListVectorStoresOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
};

export type ListVectorStoresOrder = typeof ListVectorStoresOrder[keyof typeof ListVectorStoresOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListVectorStoresOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListFilesInVectorStoreBatchParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListFilesInVectorStoreBatchOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
/**
 * Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
 */
filter?: ListFilesInVectorStoreBatchFilter;
};

export type ListFilesInVectorStoreBatchOrder = typeof ListFilesInVectorStoreBatchOrder[keyof typeof ListFilesInVectorStoreBatchOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFilesInVectorStoreBatchOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListFilesInVectorStoreBatchFilter = typeof ListFilesInVectorStoreBatchFilter[keyof typeof ListFilesInVectorStoreBatchFilter];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListFilesInVectorStoreBatchFilter = {
  in_progress: 'in_progress',
  completed: 'completed',
  failed: 'failed',
  cancelled: 'cancelled',
} as const;

export type ListVectorStoreFilesParams = {
/**
 * A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.

 */
limit?: number;
/**
 * Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.

 */
order?: ListVectorStoreFilesOrder;
/**
 * A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.

 */
after?: string;
/**
 * A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.

 */
before?: string;
/**
 * Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.
 */
filter?: ListVectorStoreFilesFilter;
};

export type ListVectorStoreFilesOrder = typeof ListVectorStoreFilesOrder[keyof typeof ListVectorStoreFilesOrder];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListVectorStoreFilesOrder = {
  asc: 'asc',
  desc: 'desc',
} as const;

export type ListVectorStoreFilesFilter = typeof ListVectorStoreFilesFilter[keyof typeof ListVectorStoreFilesFilter];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListVectorStoreFilesFilter = {
  in_progress: 'in_progress',
  completed: 'completed',
  failed: 'failed',
  cancelled: 'cancelled',
} as const;

export type ListVideosParams = {
/**
 * Number of items to retrieve
 */
limit?: number;
/**
 * Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order.
 */
order?: OrderEnum;
/**
 * Identifier for the last item from the previous pagination request
 */
after?: string;
};

export type RetrieveVideoContentParams = {
/**
 * Which downloadable asset to return. Defaults to the MP4 video.
 */
variant?: VideoContentVariant;
};

export type ListThreadItemsMethodParams = {
/**
 * Maximum number of thread items to return. Defaults to 20.
 */
limit?: number;
/**
 * Sort order for results by creation time. Defaults to `desc`.
 */
order?: OrderEnum;
/**
 * List items created after this thread item ID. Defaults to null for the first page.
 */
after?: string;
/**
 * List items created before this thread item ID. Defaults to null for the newest results.
 */
before?: string;
};

export type ListThreadsMethodParams = {
/**
 * Maximum number of thread items to return. Defaults to 20.
 */
limit?: number;
/**
 * Sort order for results by creation time. Defaults to `desc`.
 */
order?: OrderEnum;
/**
 * List items created after this thread item ID. Defaults to null for the first page.
 */
after?: string;
/**
 * List items created before this thread item ID. Defaults to null for the newest results.
 */
before?: string;
/**
 * Filter threads that belong to this user identifier. Defaults to null to return all users.
 */
user?: string;
};

