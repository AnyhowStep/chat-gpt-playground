/**
 * Generated by orval v7.7.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import {
  z as zod
} from 'zod';


/**
 * Returns a list of projects.
 * @summary List projects
 */
export const listProjectsQueryLimitDefault = 20;export const listProjectsQueryIncludeArchivedDefault = false;

export const listProjectsQueryParams = zod.object({
  "limit": zod.number().optional().describe('A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n'),
  "after": zod.string().optional().describe('A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n'),
  "include_archived": zod.boolean().optional().describe('If `true` returns all projects including those that have been `archived`. Archived projects are not included by default.')
})

export const listProjectsResponse = zod.object({
  "object": zod.enum(['list']),
  "data": zod.array(zod.object({
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "object": zod.enum(['organization.project']).describe('The object type, which is always `organization.project`'),
  "name": zod.string().describe('The name of the project. This appears in reporting.'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was created.'),
  "archived_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was archived or `null`.').or(zod.null()).optional(),
  "status": zod.enum(['active', 'archived']).describe('`active` or `archived`')
}).describe('Represents an individual project.')),
  "first_id": zod.string(),
  "last_id": zod.string(),
  "has_more": zod.boolean()
})

/**
 * Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
 * @summary Create project
 */
export const createProjectBody = zod.object({
  "name": zod.string().describe('The friendly name of the project, this name appears in reports.'),
  "geography": zod.enum(['US', 'EU', 'JP', 'IN', 'KR', 'CA', 'AU', 'SG']).optional().describe('Create the project with the specified data residency region. Your organization must have access to Data residency functionality in order to use. See [data residency controls](https://platform.openai.com/docs/guides/your-data#data-residency-controls) to review the functionality and limitations of setting this field.')
})

export const createProjectResponse = zod.object({
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "object": zod.enum(['organization.project']).describe('The object type, which is always `organization.project`'),
  "name": zod.string().describe('The name of the project. This appears in reporting.'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was created.'),
  "archived_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was archived or `null`.').or(zod.null()).optional(),
  "status": zod.enum(['active', 'archived']).describe('`active` or `archived`')
}).describe('Represents an individual project.')

/**
 * Retrieves a project.
 * @summary Retrieve project
 */
export const retrieveProjectParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const retrieveProjectResponse = zod.object({
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "object": zod.enum(['organization.project']).describe('The object type, which is always `organization.project`'),
  "name": zod.string().describe('The name of the project. This appears in reporting.'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was created.'),
  "archived_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was archived or `null`.').or(zod.null()).optional(),
  "status": zod.enum(['active', 'archived']).describe('`active` or `archived`')
}).describe('Represents an individual project.')

/**
 * Modifies a project in the organization.
 * @summary Modify project
 */
export const modifyProjectParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const modifyProjectBody = zod.object({
  "name": zod.string().describe('The updated name of the project, this name appears in reports.')
})

export const modifyProjectResponse = zod.object({
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "object": zod.enum(['organization.project']).describe('The object type, which is always `organization.project`'),
  "name": zod.string().describe('The name of the project. This appears in reporting.'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was created.'),
  "archived_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was archived or `null`.').or(zod.null()).optional(),
  "status": zod.enum(['active', 'archived']).describe('`active` or `archived`')
}).describe('Represents an individual project.')

/**
 * Returns a list of API keys in the project.
 * @summary List project API keys
 */
export const listProjectApiKeysParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const listProjectApiKeysQueryLimitDefault = 20;

export const listProjectApiKeysQueryParams = zod.object({
  "limit": zod.number().optional().describe('A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n'),
  "after": zod.string().optional().describe('A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n')
})

export const listProjectApiKeysResponse = zod.object({
  "object": zod.enum(['list']),
  "data": zod.array(zod.object({
  "object": zod.enum(['organization.project.api_key']).describe('The object type, which is always `organization.project.api_key`'),
  "redacted_value": zod.string().describe('The redacted value of the API key'),
  "name": zod.string().describe('The name of the API key'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the API key was created'),
  "last_used_at": zod.number().describe('The Unix timestamp (in seconds) of when the API key was last used.'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "owner": zod.object({
  "type": zod.enum(['user', 'service_account']).optional().describe('`user` or `service_account`'),
  "user": zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).optional().describe('Represents an individual user in a project.'),
  "service_account": zod.object({
  "object": zod.enum(['organization.project.service_account']).describe('The object type, which is always `organization.project.service_account`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the service account'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the service account was created')
}).optional().describe('Represents an individual service account in a project.')
})
}).describe('Represents an individual API key in a project.')),
  "first_id": zod.string(),
  "last_id": zod.string(),
  "has_more": zod.boolean()
})

/**
 * Retrieves an API key in the project.
 * @summary Retrieve project API key
 */
export const retrieveProjectApiKeyParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "key_id": zod.string().describe('The ID of the API key.')
})

export const retrieveProjectApiKeyResponse = zod.object({
  "object": zod.enum(['organization.project.api_key']).describe('The object type, which is always `organization.project.api_key`'),
  "redacted_value": zod.string().describe('The redacted value of the API key'),
  "name": zod.string().describe('The name of the API key'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the API key was created'),
  "last_used_at": zod.number().describe('The Unix timestamp (in seconds) of when the API key was last used.'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "owner": zod.object({
  "type": zod.enum(['user', 'service_account']).optional().describe('`user` or `service_account`'),
  "user": zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).optional().describe('Represents an individual user in a project.'),
  "service_account": zod.object({
  "object": zod.enum(['organization.project.service_account']).describe('The object type, which is always `organization.project.service_account`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the service account'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the service account was created')
}).optional().describe('Represents an individual service account in a project.')
})
}).describe('Represents an individual API key in a project.')

/**
 * Deletes an API key from the project.
 * @summary Delete project API key
 */
export const deleteProjectApiKeyParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "key_id": zod.string().describe('The ID of the API key.')
})

export const deleteProjectApiKeyResponse = zod.object({
  "object": zod.enum(['organization.project.api_key.deleted']),
  "id": zod.string(),
  "deleted": zod.boolean()
})

/**
 * Archives a project in the organization. Archived projects cannot be used or updated.
 * @summary Archive project
 */
export const archiveProjectParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const archiveProjectResponse = zod.object({
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "object": zod.enum(['organization.project']).describe('The object type, which is always `organization.project`'),
  "name": zod.string().describe('The name of the project. This appears in reporting.'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was created.'),
  "archived_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was archived or `null`.').or(zod.null()).optional(),
  "status": zod.enum(['active', 'archived']).describe('`active` or `archived`')
}).describe('Represents an individual project.')

/**
 * Returns the rate limits per model for a project.
 * @summary List project rate limits
 */
export const listProjectRateLimitsParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const listProjectRateLimitsQueryLimitDefault = 100;

export const listProjectRateLimitsQueryParams = zod.object({
  "limit": zod.number().optional().describe('A limit on the number of objects to be returned. The default is 100.\n'),
  "after": zod.string().optional().describe('A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n'),
  "before": zod.string().optional().describe('A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.\n')
})

export const listProjectRateLimitsResponse = zod.object({
  "object": zod.enum(['list']),
  "data": zod.array(zod.object({
  "object": zod.enum(['project.rate_limit']).describe('The object type, which is always `project.rate_limit`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints.'),
  "model": zod.string().describe('The model this rate limit applies to.'),
  "max_requests_per_1_minute": zod.number().describe('The maximum requests per minute.'),
  "max_tokens_per_1_minute": zod.number().describe('The maximum tokens per minute.'),
  "max_images_per_1_minute": zod.number().optional().describe('The maximum images per minute. Only present for relevant models.'),
  "max_audio_megabytes_per_1_minute": zod.number().optional().describe('The maximum audio megabytes per minute. Only present for relevant models.'),
  "max_requests_per_1_day": zod.number().optional().describe('The maximum requests per day. Only present for relevant models.'),
  "batch_1_day_max_input_tokens": zod.number().optional().describe('The maximum batch input tokens per day. Only present for relevant models.')
}).describe('Represents a project rate limit config.')),
  "first_id": zod.string(),
  "last_id": zod.string(),
  "has_more": zod.boolean()
})

/**
 * Updates a project rate limit.
 * @summary Modify project rate limit
 */
export const updateProjectRateLimitsParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "rate_limit_id": zod.string().describe('The ID of the rate limit.')
})

export const updateProjectRateLimitsBody = zod.object({
  "max_requests_per_1_minute": zod.number().optional().describe('The maximum requests per minute.'),
  "max_tokens_per_1_minute": zod.number().optional().describe('The maximum tokens per minute.'),
  "max_images_per_1_minute": zod.number().optional().describe('The maximum images per minute. Only relevant for certain models.'),
  "max_audio_megabytes_per_1_minute": zod.number().optional().describe('The maximum audio megabytes per minute. Only relevant for certain models.'),
  "max_requests_per_1_day": zod.number().optional().describe('The maximum requests per day. Only relevant for certain models.'),
  "batch_1_day_max_input_tokens": zod.number().optional().describe('The maximum batch input tokens per day. Only relevant for certain models.')
})

export const updateProjectRateLimitsResponse = zod.object({
  "object": zod.enum(['project.rate_limit']).describe('The object type, which is always `project.rate_limit`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints.'),
  "model": zod.string().describe('The model this rate limit applies to.'),
  "max_requests_per_1_minute": zod.number().describe('The maximum requests per minute.'),
  "max_tokens_per_1_minute": zod.number().describe('The maximum tokens per minute.'),
  "max_images_per_1_minute": zod.number().optional().describe('The maximum images per minute. Only present for relevant models.'),
  "max_audio_megabytes_per_1_minute": zod.number().optional().describe('The maximum audio megabytes per minute. Only present for relevant models.'),
  "max_requests_per_1_day": zod.number().optional().describe('The maximum requests per day. Only present for relevant models.'),
  "batch_1_day_max_input_tokens": zod.number().optional().describe('The maximum batch input tokens per day. Only present for relevant models.')
}).describe('Represents a project rate limit config.')

/**
 * Returns a list of service accounts in the project.
 * @summary List project service accounts
 */
export const listProjectServiceAccountsParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const listProjectServiceAccountsQueryLimitDefault = 20;

export const listProjectServiceAccountsQueryParams = zod.object({
  "limit": zod.number().optional().describe('A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n'),
  "after": zod.string().optional().describe('A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n')
})

export const listProjectServiceAccountsResponse = zod.object({
  "object": zod.enum(['list']),
  "data": zod.array(zod.object({
  "object": zod.enum(['organization.project.service_account']).describe('The object type, which is always `organization.project.service_account`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the service account'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the service account was created')
}).describe('Represents an individual service account in a project.')),
  "first_id": zod.string(),
  "last_id": zod.string(),
  "has_more": zod.boolean()
})

/**
 * Creates a new service account in the project. This also returns an unredacted API key for the service account.
 * @summary Create project service account
 */
export const createProjectServiceAccountParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const createProjectServiceAccountBody = zod.object({
  "name": zod.string().describe('The name of the service account being created.')
})

export const createProjectServiceAccountResponse = zod.object({
  "object": zod.enum(['organization.project.service_account']),
  "id": zod.string(),
  "name": zod.string(),
  "role": zod.enum(['member']).describe('Service accounts can only have one role of type `member`'),
  "created_at": zod.number(),
  "api_key": zod.object({
  "object": zod.enum(['organization.project.service_account.api_key']).describe('The object type, which is always `organization.project.service_account.api_key`'),
  "value": zod.string(),
  "name": zod.string(),
  "created_at": zod.number(),
  "id": zod.string()
})
})

/**
 * Retrieves a service account in the project.
 * @summary Retrieve project service account
 */
export const retrieveProjectServiceAccountParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "service_account_id": zod.string().describe('The ID of the service account.')
})

export const retrieveProjectServiceAccountResponse = zod.object({
  "object": zod.enum(['organization.project.service_account']).describe('The object type, which is always `organization.project.service_account`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the service account'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "created_at": zod.number().describe('The Unix timestamp (in seconds) of when the service account was created')
}).describe('Represents an individual service account in a project.')

/**
 * Deletes a service account from the project.
 * @summary Delete project service account
 */
export const deleteProjectServiceAccountParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "service_account_id": zod.string().describe('The ID of the service account.')
})

export const deleteProjectServiceAccountResponse = zod.object({
  "object": zod.enum(['organization.project.service_account.deleted']),
  "id": zod.string(),
  "deleted": zod.boolean()
})

/**
 * Returns a list of users in the project.
 * @summary List project users
 */
export const listProjectUsersParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const listProjectUsersQueryLimitDefault = 20;

export const listProjectUsersQueryParams = zod.object({
  "limit": zod.number().optional().describe('A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n'),
  "after": zod.string().optional().describe('A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n')
})

export const listProjectUsersResponse = zod.object({
  "object": zod.string(),
  "data": zod.array(zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).describe('Represents an individual user in a project.')),
  "first_id": zod.string(),
  "last_id": zod.string(),
  "has_more": zod.boolean()
})

/**
 * Adds a user to the project. Users must already be members of the organization to be added to a project.
 * @summary Create project user
 */
export const createProjectUserParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.')
})

export const createProjectUserBody = zod.object({
  "user_id": zod.string().describe('The ID of the user.'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`')
})

export const createProjectUserResponse = zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).describe('Represents an individual user in a project.')

/**
 * Retrieves a user in the project.
 * @summary Retrieve project user
 */
export const retrieveProjectUserParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "user_id": zod.string().describe('The ID of the user.')
})

export const retrieveProjectUserResponse = zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).describe('Represents an individual user in a project.')

/**
 * Modifies a user's role in the project.
 * @summary Modify project user
 */
export const modifyProjectUserParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "user_id": zod.string().describe('The ID of the user.')
})

export const modifyProjectUserBody = zod.object({
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`')
})

export const modifyProjectUserResponse = zod.object({
  "object": zod.enum(['organization.project.user']).describe('The object type, which is always `organization.project.user`'),
  "id": zod.string().describe('The identifier, which can be referenced in API endpoints'),
  "name": zod.string().describe('The name of the user'),
  "email": zod.string().describe('The email address of the user'),
  "role": zod.enum(['owner', 'member']).describe('`owner` or `member`'),
  "added_at": zod.number().describe('The Unix timestamp (in seconds) of when the project was added.')
}).describe('Represents an individual user in a project.')

/**
 * Deletes a user from the project.
 * @summary Delete project user
 */
export const deleteProjectUserParams = zod.object({
  "project_id": zod.string().describe('The ID of the project.'),
  "user_id": zod.string().describe('The ID of the user.')
})

export const deleteProjectUserResponse = zod.object({
  "object": zod.enum(['organization.project.user.deleted']),
  "id": zod.string(),
  "deleted": zod.boolean()
})

