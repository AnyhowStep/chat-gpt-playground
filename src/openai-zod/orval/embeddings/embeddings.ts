/**
 * Generated by orval v7.7.0 🍺
 * Do not edit manually.
 * OpenAI API
 * The OpenAI REST API. Please see https://platform.openai.com/docs/api-reference for more details.
 * OpenAPI spec version: 2.3.0
 */
import {
  z as zod
} from 'zod';


/**
 * Creates an embedding vector representing the input text.
 * @summary Create embeddings
 */
export const createEmbeddingBodyInputDefaultOne = "";export const createEmbeddingBodyInputItemDefault = "";export const createEmbeddingBodyInputMaxTwo = 2048;
export const createEmbeddingBodyInputMaxThree = 2048;
export const createEmbeddingBodyInputMaxFour = 2048;
export const createEmbeddingBodyEncodingFormatDefault = "float";

export const createEmbeddingBody = zod.object({
  "input": zod.string().optional().describe('The string that will be turned into an embedding.').or(zod.array(zod.string().optional()).min(1).max(createEmbeddingBodyInputMaxTwo).describe('The array of strings that will be turned into an embedding.')).or(zod.array(zod.number()).min(1).max(createEmbeddingBodyInputMaxThree).describe('The array of integers that will be turned into an embedding.')).or(zod.array(zod.array(zod.number()).min(1)).min(1).max(createEmbeddingBodyInputMaxFour).describe('The array of arrays containing integers that will be turned into an embedding.')).describe('Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request.\n'),
  "model": zod.string().or(zod.enum(['text-embedding-ada-002', 'text-embedding-3-small', 'text-embedding-3-large'])).describe('ID of the model to use. You can use the [List models](https://platform.openai.com/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](https://platform.openai.com/docs/models) for descriptions of them.\n'),
  "encoding_format": zod.enum(['float', 'base64']).optional().describe('The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/).'),
  "dimensions": zod.number().min(1).optional().describe('The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models.\n'),
  "user": zod.string().optional().describe('A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](https://platform.openai.com/docs/guides/safety-best-practices#end-user-ids).\n')
})

export const createEmbeddingResponse = zod.object({
  "data": zod.array(zod.object({
  "index": zod.number().describe('The index of the embedding in the list of embeddings.'),
  "embedding": zod.array(zod.number()).describe('The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](https://platform.openai.com/docs/guides/embeddings).\n'),
  "object": zod.enum(['embedding']).describe('The object type, which is always \"embedding\".')
}).describe('Represents an embedding vector returned by embedding endpoint.\n')).describe('The list of embeddings generated by the model.'),
  "model": zod.string().describe('The name of the model used to generate the embedding.'),
  "object": zod.enum(['list']).describe('The object type, which is always \"list\".'),
  "usage": zod.object({
  "prompt_tokens": zod.number().describe('The number of tokens used by the prompt.'),
  "total_tokens": zod.number().describe('The total number of tokens used by the request.')
}).describe('The usage information for the request.')
})

